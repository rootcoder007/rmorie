% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/signal.R
\name{sgolay}
\alias{sgolay}
\title{Savitzky-Golay smoothing (direct alias)}
\usage{
sgolay(x, window = 11L, polyorder = 3L)
}
\arguments{
\item{x}{Numeric vector.}

\item{window}{Window length (odd, default 11).}

\item{polyorder}{Polynomial order (default 3).}
}
\value{
List with \code{filtered}, \code{name}, \code{fs}, \code{n_samples}, \code{extra}
(\code{window}, \code{polyorder}).
}
\description{
Direct short-name export of the Savitzky-Golay smoother (matches the
Python \code{morie.signal.sgolay} name). For the long-form, see
\code{\link[=morie_sgolay_smooth]{morie_sgolay_smooth()}}, which this function delegates to.
}
\details{
Reference: Savitzky, A. & Golay, M.J.E. (1964) "Smoothing and
differentiation of data by simplified least-squares procedures",
\emph{Anal. Chem.} 36(8):1627--1639.
}
\examples{
\donttest{
if (requireNamespace("signal", quietly = TRUE)) {
  set.seed(1)
  x <- sin(seq(0, 2 * pi, length.out = 200)) + 0.1 * rnorm(200)
  res <- sgolay(x)
  length(res$filtered)
}
}
}

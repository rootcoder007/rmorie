% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/drpfw.R
\name{morie_drpfw_dropout_forward}
\alias{morie_drpfw_dropout_forward}
\alias{morie_dropout_forward}
\title{Dropout forward pass (inverted)}
\usage{
morie_drpfw_dropout_forward(
  x,
  p = 0.5,
  seed = 0L,
  training = TRUE,
  deterministic_seed = NULL
)

morie_dropout_forward(
  x,
  p = 0.5,
  seed = 0L,
  training = TRUE,
  deterministic_seed = NULL
)
}
\arguments{
\item{x}{Numeric array.}

\item{p}{Drop probability in \code{[0, 1)}.}

\item{seed}{RNG seed.}

\item{training}{If FALSE, returns input unchanged.}

\item{deterministic_seed}{Optional integer; if non-NULL, a SHA-keyed
seed from \code{\link{morie_det_rng}("drpfw", deterministic_seed)} is
installed before sampling so Py<->R streams agree.  Overrides
\code{seed} when set.}
}
\value{
Named list \code{(y, estimate, mask, p, kept_fraction, method)}.
}
\description{
R parity for \code{morie.fn.drpfw.dropout_forward}.
}
\details{
\deqn{y = x \odot m / (1-p), \quad m \sim \mathrm{Bernoulli}(1-p)}{y = x odot m / (1-p), m ~ Bernoulli(1-p)}
}
\examples{
morie_drpfw_dropout_forward(x = rnorm(50))
}
\references{
Srivastava et al. (2014), JMLR 15:1929-1958.
}
\keyword{internal}

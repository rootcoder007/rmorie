# SPDX-License-Identifier: AGPL-3.0-or-later

# `morie_grm_vanraden()` was moved to R/_helpers_montesinos.R so that gblpf.R,
# mtgbl.R, and mrkvr.R can rely on it being defined regardless of R's
# alphabetical source order. See DESCRIPTION Collate: for the explicit
# load order.

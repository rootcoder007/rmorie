% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ml_pipeline.R
\name{morie_ml_model}
\alias{morie_ml_model}
\title{Specify a supervised model without fitting it}
\usage{
morie_ml_model(
  type = c("logistic", "linear"),
  optimizer = c("adam", "gd", "sgd"),
  learning_rate = 0.1,
  epochs = 200L,
  loss = NULL,
  batch_size = 32L,
  l2 = 0,
  tol = 1e-08,
  seed = 42L,
  nofit = FALSE
)
}
\arguments{
\item{type}{One of "logistic" (classification) or "linear"
(regression). Distinguishes the default loss.}

\item{optimizer}{One of "gd" (full-batch gradient descent), "sgd"
(stochastic minibatch), or "adam".}

\item{learning_rate}{Step size (default 0.1). Lower values give longer
but more stable descent (ML7.4).}

\item{epochs}{Number of passes over the data (default 200).}

\item{loss}{Loss name; defaults to "logloss" for logistic and "mse"
for linear. Custom losses may be supplied by name in \code{.ml_loss}.}

\item{batch_size}{Minibatch size for "sgd" (default 32).}

\item{l2}{Ridge (L2) penalty (default 0).}

\item{tol}{Convergence tolerance on the loss (default 1e-8).}

\item{seed}{RNG seed.}

\item{nofit}{If TRUE, \code{morie_ml_train} returns the untrained spec.}
}
\value{
A \code{morie_ml_model} specification.
}
\description{
Defines the learner, its optimizer, learning rate, number of epochs,
loss function, and regularisation, as a distinct stage prior to
training. With \code{nofit = TRUE} (the default here is FALSE) the object is
returned without being fitted, enabling batch control of expensive fits.
}
\examples{
morie_ml_model("logistic", optimizer = "adam", epochs = 50)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/frns_predpol.R
\name{morie_predpol_calibration_audit}
\alias{morie_predpol_calibration_audit}
\title{Audit whether an algorithm's area risk ranking matches realised outcomes}
\usage{
morie_predpol_calibration_audit(areas, mean_risk, outcome_rate, group)
}
\arguments{
\item{areas}{Area identifiers (one per area).}

\item{mean_risk}{Mean predicted risk per area.}

\item{outcome_rate}{Realised outcome rate per area.}

\item{group}{Majority protected-attribute label per area.}
}
\value{
A named list: \code{value} (worst per-group mean gap), \code{spearman},
\code{spearman_pvalue}, \code{group_rank_gap}, \code{worst_group}, \code{rank_gap},
\code{warnings}, \code{interpretation}.
}
\description{
Ranks areas by predicted risk and by realised outcome rate (rank 1 =
highest), forms \code{rank_gap = outcome_rank - risk_rank} per area
(positive = over-predicted), and averages the gap within each group.
A Spearman correlation summarises overall calibration.
}
\examples{
res <- morie_predpol_calibration_audit(
  areas = c("d1", "d2", "d3", "d4", "d5", "d6"),
  mean_risk = c(90, 80, 70, 30, 20, 10),
  outcome_rate = c(10, 20, 30, 70, 80, 90),
  group = c("X", "X", "X", "Y", "Y", "Y")
)
res$group_rank_gap$X # 3  (group X over-predicted)
res$spearman # -1 (perfectly miscalibrated)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/otis_causal.R
\name{morie_otis_aipw_ate}
\alias{morie_otis_aipw_ate}
\title{Doubly-robust (AIPW) ATE on OTIS data via cross-fitted nuisances.}
\usage{
morie_otis_aipw_ate(
  df,
  treatment,
  outcome,
  covariates,
  n_folds = 5L,
  seed = 123L,
  eps = 0.02
)
}
\arguments{
\item{df}{A data frame containing \code{treatment}, \code{outcome},
and all \code{covariates}.}

\item{treatment}{Name of the binary treatment column.}

\item{outcome}{Name of the (numeric) outcome column.}

\item{covariates}{Character vector of covariate names.}

\item{n_folds}{Number of cross-fitting folds (default 5).}

\item{seed}{Integer seed for the fold partition (default 123).}

\item{eps}{Propensity clip bound (default 0.02).}
}
\value{
A \code{morie_causal_estimate} list.
}
\description{
Uses \code{n_folds} cross-fitting: propensity (logistic ridge) and
outcome regression (OLS separately for D=1 and D=0) are fit on K-1
folds and predicted on the held-out fold. The doubly-robust
influence function (Robins-Rotnitzky-Zhao 1994) is averaged to
yield the ATE.
}
\examples{
set.seed(1)
n <- 300L
x <- rnorm(n)
d <- rbinom(n, 1, plogis(0.4 * x))
y <- 0.5 * d + x + rnorm(n)
df <- data.frame(d = d, y = y, x = x)
morie_otis_aipw_ate(df, treatment = "d", outcome = "y",
                    covariates = "x", n_folds = 3L)
}
\references{
Robins, J. M., Rotnitzky, A., & Zhao, L. P. (1994). \emph{JASA}
89(427), 846-866.
}

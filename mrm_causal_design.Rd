% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mrm_design.R
\name{mrm_causal_design}
\alias{mrm_causal_design}
\title{Designed-experiment convenience wrapper around the morie causal
estimator family}
\usage{
mrm_causal_design(
  data,
  treatment_col,
  outcome_col,
  covariates = character(0),
  estimator = c("ipw", "diff_in_means")
)
}
\arguments{
\item{data}{data.frame with treatment, outcome, optional
covariates.}

\item{treatment_col}{Binary 0/1 treatment column.}

\item{outcome_col}{Continuous outcome column.}

\item{covariates}{Optional character vector of covariate columns.}

\item{estimator}{One of \code{"ipw"} (Hajek IPW with logistic
propensity), \code{"diff_in_means"} (no adjustment).}
}
\value{
Named list with estimator, estimate, se, ci_lower,
ci_upper, p_value, n, n_treated, interpretation.
}
\description{
Designed-experiment convenience wrapper around the morie causal
estimator family
}
\examples{
set.seed(2026)
n <- 200L
x <- rnorm(n)
D <- rbinom(n, 1, plogis(0.5 * x))
y <- 0.7 * D + 0.3 * x + rnorm(n, 0, 0.5)
df <- data.frame(D = D, y = y, age = x)
# IPW-adjusted ATE
ipw <- mrm_causal_design(df,
  treatment_col = "D",
  outcome_col = "y",
  covariates = "age",
  estimator = "ipw"
)
# Naive difference in means for comparison
raw <- mrm_causal_design(df,
  treatment_col = "D",
  outcome_col = "y",
  estimator = "diff_in_means"
)
c(ipw = ipw$estimate, raw = raw$estimate)
}

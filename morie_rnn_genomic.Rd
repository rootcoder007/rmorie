% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rnnge.R
\name{morie_rnn_genomic}
\alias{morie_rnn_genomic}
\title{Vanilla RNN genomic predictor (BPTT, base R)}
\usage{
morie_rnn_genomic(
  x,
  y,
  markers,
  hidden = 8,
  n_epochs = 150,
  lr = 0.01,
  l2 = 0.001,
  seed = 0,
  deterministic_seed = NULL
)
}
\arguments{
\item{x}{Optional fixed-effect design.}

\item{y}{Numeric response.}

\item{markers}{(n x L) marker sequence.}

\item{hidden, n_epochs, lr, l2, seed}{Hyperparameters.}

\item{deterministic_seed}{Optional integer; if supplied, RNG state is
derived via \code{\link[=morie_det_rng]{morie_det_rng()}} keyed on ("rnnge", deterministic_seed)
so Py<->R streams agree on the canonical fixture.  When \code{NULL}
(default) behaviour is unchanged.}
}
\value{
list(estimate, y_hat, W_h, W_x, b_h, w_o, b_o, se, n, method).
}
\description{
Vanilla RNN genomic predictor (BPTT, base R)
}
\examples{
morie_rnn_genomic(x = rnorm(50), y = rnorm(50), markers = matrix(sample(0:2, 200, TRUE), 50, 4))
}
\references{
Montesinos Lopez Ch 14.
}

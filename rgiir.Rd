% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rgiir.R
\name{rgiir}
\alias{rgiir}
\alias{morie_rangayyan_iir_filter}
\title{Butterworth IIR filter -- Rangayyan Ch 3}
\usage{
rgiir(x, cutoff, order = 4L, fs = 1, btype = c("low", "high", "pass", "stop"))

morie_rangayyan_iir_filter(
  x,
  cutoff,
  order = 4L,
  fs = 1,
  btype = c("low", "high", "pass", "stop")
)
}
\arguments{
\item{x}{Numeric vector.}

\item{cutoff}{Cutoff frequency (Hz). For \code{"pass"}/\code{"stop"}, a length-2
numeric vector \code{c(low, high)}.}

\item{order}{Filter order (default 4).}

\item{fs}{Sampling rate (Hz).}

\item{btype}{One of \code{"low"}, \code{"high"}, \code{"pass"} (bandpass), \code{"stop"}
(bandstop).}
}
\value{
Named list \code{signal}, \code{order}, \code{cutoff}, \code{fs}, \code{btype}.
}
\description{
Zero-phase Butterworth IIR filter via the \code{signal} package (\code{butter} +
\code{filtfilt}). Same transfer function as SciPy's \code{butter} so the Python
and R outputs match to machine precision for identical inputs.
}
\examples{
\donttest{
if (requireNamespace("signal", quietly = TRUE)) {
  t <- seq(0, 1, length.out = 500)
  x <- sin(2 * pi * 5 * t) + 0.5 * sin(2 * pi * 40 * t)
  r <- rgiir(x, cutoff = 10, order = 4, fs = 500, btype = "low")
  length(r$signal)
}
}
}
\references{
Rangayyan Ch 3.
}
\keyword{internal}

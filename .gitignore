.Rproj.user/
.Rhistory
.RData
.Ruserdata
*.Rcheck/
*.tar.gz
*.o
*.so
*.dll

# renv keeps lockfile + activate.R but excludes the library
renv/library/
renv/local/
renv/cellar/
renv/staging/
renv/python/
renv/sandbox/
.renv-token
.clangd
*.o.tmp
*.o

# Papers / preprints / Zenodo deposits -- managed in the morie repo,
# not rmorie. Stops accidental commits of generated PDFs or LaTeX
# artefacts here.
papers/
paper.md
paper.bib
*.pdf
*.aux
*.log
*.bbl
*.blg
*.out
*.synctex.gz

# Phase 1 wrapper-as-extender playbook (working notes; lives outside the repo)
REPLACE_INVENTORY.md

# Hex sticker artwork drafts (kept locally; not shipped with the R package)
inst/figures/

# local Serena scope — never commit
.serena/

# never ship private wrangled data
data/private/

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mrm_tps.R
\name{mrm_tps_neighbourhood_recurrence_km}
\alias{mrm_tps_neighbourhood_recurrence_km}
\title{Kaplan-Meier inter-event recurrence on TPS by neighbourhood}
\usage{
mrm_tps_neighbourhood_recurrence_km(
  data,
  date_col = "OCC_DATE",
  hood_col = "HOOD_158",
  min_gap_days = 0
)
}
\arguments{
\item{data}{A data.frame with \code{date_col} and \code{hood_col}.}

\item{date_col}{Column name of the date column
(default \code{"OCC_DATE"}).}

\item{hood_col}{Column name of the neighbourhood ID
(default \code{"HOOD_158"}).}

\item{min_gap_days}{Smallest gap to include (default \code{0}).}
}
\value{
A data.frame with one row per neighbourhood, columns
\code{hood}, \code{n_events}, \code{n_gaps}, \code{mean_gap_days}, \code{median_gap_days},
\code{p25_gap_days}, \code{p75_gap_days}.
}
\description{
For each HOOD_158 neighbourhood, sorts events chronologically and
computes the gap (in days) between consecutive events. Returns the
per-neighbourhood mean, median, and total gaps. No censoring is
applied (every gap is observed).
}
\examples{
if (FALSE) {
  tps <- read.csv("Assault_Open_Data.csv")
  mrm_tps_neighbourhood_recurrence_km(tps)
}
}

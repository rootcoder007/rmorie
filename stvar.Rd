% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stvar.R
\name{stvar}
\alias{stvar}
\alias{morie_spatiotemporal_variogram}
\title{Empirical spatiotemporal semivariogram gamma(h, u).}
\usage{
stvar(
  x,
  coords,
  times,
  n_spatial_bins = 6,
  n_temporal_bins = 6,
  max_spatial = NULL,
  max_temporal = NULL
)

morie_spatiotemporal_variogram(
  x,
  coords,
  times,
  n_spatial_bins = 6,
  n_temporal_bins = 6,
  max_spatial = NULL,
  max_temporal = NULL
)
}
\arguments{
\item{x}{Numeric vector.}

\item{coords}{Coord matrix.}

\item{times}{Numeric times.}

\item{n_spatial_bins, n_temporal_bins}{Integer bin counts.}

\item{max_spatial, max_temporal}{Upper cutoffs.}
}
\value{
Named list: estimate (gamma, spatial_bins, temporal_bins,
counts), n, method.
}
\description{
\deqn{\hat\gamma(h, u) = \frac{1}{2|N(h, u)|}
      \sum_{(i,j)\in N(h,u)} (Z(s_i, t_i) - Z(s_j, t_j))^2}{hatgamma(h, u) = (1)/(2|N(h, u)|) sum_(i,j)in N(h,u) (Z(s_i, t_i) - Z(s_j, t_j))^2}.
}
\examples{
stvar(x = rnorm(50), coords = matrix(runif(100), 50, 2), times = sort(cumsum(rexp(50))))
}
\references{
Cressie & Huang (1999); Schabenberger & Gotway (2005), Ch 8.
}
\keyword{internal}

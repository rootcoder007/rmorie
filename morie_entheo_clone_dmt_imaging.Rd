% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/entheo_dmt.R
\name{morie_entheo_clone_dmt_imaging}
\alias{morie_entheo_clone_dmt_imaging}
\title{Clone the DMT_Imaging dataset (Timmerman et al.) into a local cache.}
\usage{
morie_entheo_clone_dmt_imaging(root = NULL, overwrite = FALSE, branch = NULL)
}
\arguments{
\item{root}{Optional destination directory. Defaults to
\verb{$MORIE_DMT_IMAGING_ROOT}, else \code{file.path(morie_cache_dir(), "DMT_Imaging")}.}

\item{overwrite}{Logical; if \code{TRUE} and \code{root} already exists,
wipe it first (rare; defaults \code{FALSE}).}

\item{branch}{Optional branch / tag / SHA to check out after
clone. \code{NULL} uses the default upstream branch.}
}
\value{
Invisibly returns the destination path.
}
\description{
\code{morie_entheo_clone_dmt_imaging()} shells out to \verb{git clone} to
fetch the open-source DMT_Imaging dataset published by Christopher
Timmerman's group at \url{https://github.com/timmer500/DMT_Imaging}.
After the clone completes, \code{load_dmt_imaging()} and
\verb{morie_entheo_load_*()} will pick the real fixture up
automatically (they probe \verb{$MORIE_DMT_IMAGING_ROOT} and the cache
dir).
}
\details{
This is opt-in -- the package never auto-clones at load time. We
clone from a specific commit by default to make tests
reproducible; pass \code{branch = NULL} to track main.

Related upstream resources Vee surfaced 2026-05-25:
\itemize{
\item \url{https://github.com/timmer500/DMT_Imaging.git}  (the actual EEG+fMRI dataset)
\item \url{https://github.com/pnk314/psychedelics.git}     (analysis pipeline)
\item \url{https://github.com/lisagirard/Psychedelics.git} (review repository)
\item \url{https://github.com/kianenigma/awesome-psychedelics.git} (curated index)
}
}

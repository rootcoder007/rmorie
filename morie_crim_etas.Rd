% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crim_native.R
\name{morie_crim_etas}
\alias{morie_crim_etas}
\title{Temporal ETAS model (Epidemic-Type Aftershock Sequence)}
\usage{
morie_crim_etas(times, magnitudes = NULL, m0 = NULL, t_max = NULL)
}
\arguments{
\item{times}{Numeric event times (sorted or sortable).}

\item{magnitudes}{Numeric marks (same length); constant marks give
a plain Omori-Hawkes process.}

\item{m0}{Reference (cutoff) magnitude. Default \code{min(magnitudes)}.}

\item{t_max}{Observation horizon. Default \code{max(times)}.}
}
\value{
List of class \code{"morie_etas"}: par (mu, K, alpha, c, p),
loglik, branching_ratio, n, converged, call.
}
\description{
Ogata (1988) ETAS with the modified-Omori (Lomax) triggering kernel
and exponential magnitude productivity: conditional intensity
\deqn{\lambda(t) = \mu + K \sum_{t_i < t} e^{\alpha (m_i - m_0)}
  (t - t_i + c)^{-p}.}
MLE by L-BFGS-B on the exact log-likelihood (direct sum plus the
closed-form kernel integral).
}
\examples{
set.seed(1)
tt <- sort(runif(120, 0, 100))
mm <- rexp(120, 1.5) + 2
morie_crim_etas(tt, mm)
}
\references{
Ogata (1988) JASA 83(401).
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/signal.R
\name{welch}
\alias{welch}
\title{Welch power spectral density}
\usage{
welch(x, fs, nperseg = 256L)
}
\arguments{
\item{x}{Numeric vector (1-D signal).}

\item{fs}{Sampling frequency in Hz.}

\item{nperseg}{Segment length (default 256).}
}
\value{
List with \code{filtered} (PSD), \code{name}, \code{fs}, \code{n_samples}, and
\code{extra} (\code{freqs}).
}
\description{
Welch's averaged periodogram PSD: split into segments, window (Hanning),
periodogram each, average. Delegates to \code{oce::pwelch()} if available,
otherwise computes a Hanning-windowed, 50\%-overlap implementation in
base R.
}
\details{
Reference: Welch, P.D. (1967) "The use of fast Fourier transform for
the estimation of power spectra", \emph{IEEE Trans. Audio Electroacoust.}
AU-15(2):70--73.
}
\examples{
\donttest{
set.seed(1)
t <- seq(0, 1, length.out = 1024)
x <- sin(2 * pi * 50 * t) + 0.3 * rnorm(length(t))
res <- welch(x, fs = 1024)
length(res$filtered)
}
}

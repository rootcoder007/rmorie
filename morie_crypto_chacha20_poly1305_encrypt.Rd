% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crypto_sym.R
\name{morie_crypto_chacha20_poly1305_encrypt}
\alias{morie_crypto_chacha20_poly1305_encrypt}
\title{ChaCha20-Poly1305 IETF authenticated encryption}
\usage{
morie_crypto_chacha20_poly1305_encrypt(key, nonce, plaintext, aad = raw(0))
}
\arguments{
\item{key}{32-byte raw vector.}

\item{nonce}{12-byte raw vector (single-use per key; reuse is
catastrophic).}

\item{plaintext}{Raw vector to encrypt (may be empty).}

\item{aad}{Optional raw vector of additional authenticated data
(default empty).}
}
\value{
List with \code{ct} (raw vector, length = \code{length(plaintext)})
and \code{tag} (raw vector, 16 bytes).
}
\description{
Phase 3JJJ1. Wraps libsodium's
\code{crypto_aead_chacha20poly1305_ietf_encrypt} (RFC 8439 IETF
variant: 32-byte key, 12-byte nonce, 16-byte authentication tag).
}
\details{
Byte-compatible with the Python morie
\code{chacha20_poly1305_encrypt(key, nonce, plaintext, aad)}. The C
transport returns ciphertext || tag as a single buffer;
this R wrapper splits it into \code{list(ct = ..., tag = ...)} to
match the Python tuple return shape.
}
\examples{
if (morie_crypto_sodium_available()) {
  k <- morie_crypto_random_bytes(32)
  n <- morie_crypto_random_bytes(12)
  r <- morie_crypto_chacha20_poly1305_encrypt(k, n, charToRaw("hello"))
  p <- morie_crypto_chacha20_poly1305_decrypt(k, n, c(r$ct, r$tag))
  rawToChar(p)
}
}

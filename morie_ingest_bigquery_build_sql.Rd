% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ingest_bigquery.R
\name{morie_ingest_bigquery_build_sql}
\alias{morie_ingest_bigquery_build_sql}
\title{Build a parameter-safe BigQuery SELECT}
\usage{
morie_ingest_bigquery_build_sql(
  project,
  dataset,
  table,
  where = NULL,
  limit = NULL,
  select = "*"
)
}
\arguments{
\item{project, dataset, table}{Fully-qualified BigQuery table
reference, e.g. project \code{"bigquery-public-data"}, dataset
\code{"chicago_crime"}, table \code{"crime"}.}

\item{where}{Optional raw SQL \code{WHERE} clause (no leading
\code{WHERE}).}

\item{limit}{Optional \code{LIMIT}.}

\item{select}{Projection list (default \code{"*"}).}
}
\value{
A SQL string.
}
\description{
Builds a \code{SELECT ... FROM `project`.`dataset`.`table`}
string with identifier validation and backtick-quoting.
\code{where} is passed through unchanged; callers compose SQL
fragments themselves and are responsible for not injecting hostile
clauses (same contract as
\code{\link{morie_ingest_bigquery_query}}).
}
\examples{
morie_ingest_bigquery_build_sql(
  project = "bigquery-public-data",
  dataset = "chicago_crime",
  table   = "crime",
  where   = "year = 2024",
  limit   = 10000L
)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/okrig.R
\name{okrig}
\alias{okrig}
\alias{morie_ordinary_kriging}
\title{Ordinary kriging prediction (exponential / gaussian / spherical).}
\usage{
okrig(
  x,
  coords,
  target,
  model = "exponential",
  nugget = 0,
  sill = 1,
  range_ = 1
)

morie_ordinary_kriging(
  x,
  coords,
  target,
  model = "exponential",
  nugget = 0,
  sill = 1,
  range_ = 1
)
}
\arguments{
\item{x}{Numeric vector.}

\item{coords}{Numeric coordinate matrix (n by d).}

\item{target}{Numeric (m by d) or (d,) target coords.}

\item{model}{"exponential", "gaussian", or "spherical".}

\item{nugget, sill, range_}{Variogram parameters.}
}
\value{
Named list: estimate, se, n, method.
}
\description{
\deqn{\hat Z(s_0) = \lambda^\top Z,
  \begin{bmatrix}C & 1 \\ 1^\top & 0\end{bmatrix}
  \begin{bmatrix}\lambda \\ \mu\end{bmatrix} =
  \begin{bmatrix}c_0 \\ 1\end{bmatrix}}{hat Z(s_0) = lambda^top Z, begin{bmatrix}C & 1 1^top & 0end{bmatrix} begin{bmatrix}lambda muend{bmatrix} = begin{bmatrix}c_0 1end{bmatrix}}
}
\examples{
okrig(x = rnorm(50), coords = matrix(runif(100), 50, 2), target = rnorm(50))
}
\references{
Schabenberger & Gotway (2005), Ch 4.
}
\keyword{internal}

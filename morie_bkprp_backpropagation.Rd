% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bkprp.R
\name{morie_bkprp_backpropagation}
\alias{morie_bkprp_backpropagation}
\alias{morie_backpropagation}
\title{Backpropagation gradient computation (1-layer MSE)}
\usage{
morie_bkprp_backpropagation(x, y, w = NULL, b = NULL, activation = "sigmoid")

morie_backpropagation(x, y, w = NULL, b = NULL, activation = "sigmoid")
}
\arguments{
\item{x}{Numeric matrix \code{(batch, n_in)} or vector.}

\item{y}{Numeric matrix \code{(batch, n_out)} or vector.}

\item{w}{Weight matrix; defaults to \code{diag(n_out, n_in)}.}

\item{b}{Bias vector; defaults to zeros.}

\item{activation}{One of \code{"sigmoid"}, \code{"tanh"},
\code{"relu"}, \code{"identity"}.}
}
\value{
Named list \code{(loss, estimate, dW, db, dx, a, z, method)}.
}
\description{
R parity for \code{morie.fn.bkprp.backpropagation}.
}
\details{
For \eqn{L = \tfrac{1}{2n}\sum (a - y)^2}{L = (1)/(2n)sum (a - y)^2} with
\eqn{a = \sigma(W x + b)}{a = sigma(W x + b)}:
\deqn{\partial L / \partial W = \delta x^\top, \quad
      \delta = (a - y) \odot \sigma'(z)}{d L / d W = delta x^top, delta = (a - y) odot sigma'(z)}
}
\examples{
morie_bkprp_backpropagation(x = rnorm(50), y = rnorm(50))
}
\references{
Rumelhart, Hinton & Williams (1986); Goodfellow et al. (2016).
}
\keyword{internal}

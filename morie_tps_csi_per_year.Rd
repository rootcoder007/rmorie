% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tps_csi.R
\name{morie_tps_csi_per_year}
\alias{morie_tps_csi_per_year}
\title{Toronto CSI per fiscal year from per-category counts}
\usage{
morie_tps_csi_per_year(
  counts_per_year,
  variant = c("total", "violent"),
  weights = NULL,
  population = NULL,
  per_capita_unit = 100000L,
  rebase_to_year = NULL,
  rebase_to_value = 100
)
}
\arguments{
\item{counts_per_year}{Long data.frame or nested list (see above).}

\item{variant}{One of "total" or "violent".}

\item{weights}{Optional override vector of weights.}

\item{population}{Optional named integer vector (year -> pop);
defaults to \code{MORIE_TPS_TORONTO_POPULATION_BY_YEAR()}.}

\item{per_capita_unit}{Rate denominator (default 100000).}

\item{rebase_to_year}{Optional anchor year for the index.}

\item{rebase_to_value}{Index value at the anchor year (default 100).}
}
\value{
A data.frame with one row per year.
}
\description{
Accepts either a long-format data.frame (columns \code{year},
\code{category}, \code{count}) or a nested list keyed
\code{[[year]][[category]] = count}.
}
\details{
Returns a data.frame indexed by year with columns:
\itemize{
\item \code{raw_weighted_sum} -- sum_c w_c * n_(c,year)
\item \code{total_count} -- sum_c n_(c,year)
\item \code{population} -- Toronto population that year
\item \code{csi_per_capita} -- raw_weighted_sum / population *
per_capita_unit
\item \code{simple_count_rate} -- total_count / population *
per_capita_unit
}
When \code{rebase_to_year} is supplied, an additional
\code{csi_index} column is added, anchored so that that year's value
equals \code{rebase_to_value}.
}

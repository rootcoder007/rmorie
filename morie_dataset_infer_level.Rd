% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dataset.R
\name{morie_dataset_infer_level}
\alias{morie_dataset_infer_level}
\title{Infer the Stevens NOIR measurement level for a single vector.}
\usage{
morie_dataset_infer_level(x, name = NULL, ordinal_threshold = 10L)
}
\arguments{
\item{x}{A vector (any atomic type or factor).}

\item{name}{Optional column name to drive the name-based heuristics.
Defaults to \code{NULL} (no name-based promotion).}

\item{ordinal_threshold}{Integer; max unique values for a categorical
column to be considered ordinal (default 10).}
}
\value{
Character scalar; one of \code{"nominal"}, \code{"ordinal"}, \code{"interval"},
\code{"ratio"}.
}
\description{
Decision rules, in order:
\enumerate{
\item Character / factor with \code{n_unique <= ordinal_threshold} and an ordinal
name hit (likert/grade/scale/...): \code{"ordinal"}.
\item Character / factor otherwise: \code{"nominal"}.
\item Logical: \code{"nominal"}.
\item Numeric with \code{n_unique <= 2} (binary): \code{"nominal"}.
\item Numeric with \code{n_unique <= 20} + ordinal name hit: \code{"ordinal"}.
\item Double with interval name hit (year/index/date/...): \code{"interval"}.
\item Double otherwise: \code{"ratio"}.
\item Integer with non-negative range: \code{"ratio"}; else \code{"interval"}.
\item Date / POSIXct: \code{"interval"}.
}
}

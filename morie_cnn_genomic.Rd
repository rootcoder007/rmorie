% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cnnge.R
\name{morie_cnn_genomic}
\alias{morie_cnn_genomic}
\title{CNN genomic predictor (Conv1D + GAP + dense, base R)}
\usage{
morie_cnn_genomic(
  x,
  y,
  markers,
  n_filters = 8,
  kernel = 3,
  hidden = 8,
  n_epochs = 150,
  lr = 0.01,
  l2 = 0.001,
  seed = 0,
  deterministic_seed = NULL
)
}
\arguments{
\item{x}{Optional fixed-effect design.}

\item{y}{Numeric response.}

\item{markers}{(n x m) genotype matrix.}

\item{n_filters, kernel, hidden, n_epochs, lr, l2, seed}{Hyperparameters.}

\item{deterministic_seed}{Optional integer; if supplied, RNG state is
derived via \code{\link[=morie_det_rng]{morie_det_rng()}} keyed on ("cnnge", deterministic_seed)
so Py<->R streams agree on the canonical fixture.  When \code{NULL}
(default) behaviour is unchanged.}
}
\value{
list(estimate, y_hat, W_conv, b_conv, W1, b1, w2, b2, se, n, method).
}
\description{
CNN genomic predictor (Conv1D + GAP + dense, base R)
}
\examples{
morie_cnn_genomic(x = rnorm(50), y = rnorm(50), markers = matrix(sample(0:2, 200, TRUE), 50, 4))
}
\references{
Montesinos Lopez Ch 13.
}

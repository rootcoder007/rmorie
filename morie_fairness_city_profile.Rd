% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fairness_cityprofile.R
\name{morie_fairness_city_profile}
\alias{morie_fairness_city_profile}
\title{Construct a city profile (column map onto the canonical audit schema)}
\usage{
morie_fairness_city_profile(
  name,
  area_col,
  risk_col = NULL,
  outcome_col = NULL,
  population_col = NULL,
  group_col = NULL,
  notes = ""
)
}
\arguments{
\item{name}{Character identifier used with
\code{\link{morie_fairness_get_city}}.}

\item{area_col}{Column holding the area / district / precinct
identifier. Required.}

\item{risk_col, outcome_col, population_col, group_col}{Optional
columns for predicted risk, realised-outcome count, area
population, and protected attribute.}

\item{notes}{Free-text provenance or caveats, surfaced to the user.}
}
\value{
A list of class \code{morie_city_profile}.
}
\description{
Each \code{*_col} argument names the column, in that city's own
export, that carries the corresponding canonical field.
\code{risk_col} and \code{outcome_col} may be \code{NULL} when a
city only supplies one side (e.g. risk scores but no realised-
outcome feed); the missing side must then be supplied separately
to the audit.
}
\examples{
p <- morie_fairness_city_profile(
  "chicago", area_col = "community_area",
  risk_col = "rti", group_col = "majority_race"
)
p$name
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap_methods.R
\name{bootstrap}
\alias{bootstrap}
\title{Nonparametric bootstrap inference}
\usage{
bootstrap(
  data,
  statistic,
  n_boot = 2000L,
  ci_level = 0.95,
  ci_method = "bca",
  seed = 42L,
  stratify = NULL,
  cluster = NULL
)
}
\arguments{
\item{data}{A numeric vector or matrix of observations.}

\item{statistic}{Function of one argument that returns a scalar.}

\item{n_boot}{Number of bootstrap replicates (default 2000).}

\item{ci_level}{Confidence level (default 0.95).}

\item{ci_method}{One of \code{"percentile"}, \code{"normal"},
\code{"basic"}, \code{"bca"}, \code{"studentized"}.}

\item{seed}{Random seed.}

\item{stratify}{Optional vector of stratum labels (length n).}

\item{cluster}{Optional vector of cluster labels (length n).}
}
\value{
A \code{morie_bootstrap_result} list.
}
\description{
Resamples observations with replacement and computes a confidence
interval via the percentile, normal, basic, BCa, or studentized
method. Delegates the resampling loop and CI computation to
\code{boot::boot} and \code{boot::boot.ci} when the \pkg{boot}
package is installed. Falls back to an inline implementation
otherwise so the wrapper keeps working on minimal installs.
Stratified and cluster resamples are supported in both arms.
}
\seealso{
\code{boot::boot}, \code{boot::boot.ci},
\code{\link[=morie_boot_run]{morie_boot_run()}}, \code{\link[=morie_boot_basic_ci]{morie_boot_basic_ci()}}.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/signal.R
\name{hcepst}
\alias{hcepst}
\title{Complex cepstrum with phase unwrapping}
\usage{
hcepst(x, n_fft = NULL)
}
\arguments{
\item{x}{Numeric vector (1-D signal).}

\item{n_fft}{FFT length (default: next power of 2 \eqn{\geq}{\>=} \code{length(x)}).}
}
\value{
List with \code{filtered} (complex cepstrum, real-valued),
\code{name}, \code{fs}, \code{n_samples}, and \code{extra} (\code{quefrency}, \code{n_fft},
\code{original_length}).
}
\description{
Complex cepstrum: inverse FFT of \eqn{\log X(\omega)}{log X(omega)} using the unwrapped
phase. Unlike the real cepstrum, it preserves enough information to
invert the operation, which is what enables homomorphic deconvolution.
}
\details{
Reference: Oppenheim, A.V. & Schafer, R.W. (2009) \emph{Discrete-Time Signal
Processing}, 3rd ed., Pearson, chapter on cepstral analysis.
}
\examples{
\donttest{
set.seed(1)
x <- sin(2 * pi * 5 * seq(0, 1, length.out = 512))
res <- hcepst(x)
length(res$filtered)
}
}

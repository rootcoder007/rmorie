% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stacv.R
\name{stacv}
\alias{stacv}
\alias{morie_spatiotemporal_autocovariance}
\title{Empirical spatiotemporal autocovariance.}
\usage{
stacv(
  x,
  coords,
  times,
  n_spatial_bins = 6,
  n_temporal_bins = 6,
  max_spatial = NULL,
  max_temporal = NULL
)

morie_spatiotemporal_autocovariance(
  x,
  coords,
  times,
  n_spatial_bins = 6,
  n_temporal_bins = 6,
  max_spatial = NULL,
  max_temporal = NULL
)
}
\arguments{
\item{x}{Numeric vector.}

\item{coords}{Coord matrix (n by d_s).}

\item{times}{Numeric times vector.}

\item{n_spatial_bins, n_temporal_bins}{Integer bin counts.}

\item{max_spatial, max_temporal}{Upper cutoffs (default: max/2).}
}
\value{
Named list: estimate (C, spatial_bins, temporal_bins, counts),
n, method.
}
\description{
\deqn{\hat C(h, u) = \frac{1}{|N(h, u)|}
      \sum_{(i,j) \in N(h,u)} (Z_i - \bar Z)(Z_j - \bar Z)}{hat C(h, u) = (1)/(|N(h, u)|) sum_(i,j) in N(h,u) (Z_i - bar Z)(Z_j - bar Z)}.
}
\examples{
stacv(x = rnorm(50), coords = matrix(runif(100), 50, 2), times = sort(cumsum(rexp(50))))
}
\references{
Cressie & Huang (1999); Schabenberger & Gotway (2005), Ch 8.
}
\keyword{internal}

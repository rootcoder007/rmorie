% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dataset_browse.R
\name{morie_datasets_browse}
\alias{morie_datasets_browse}
\title{Browse + filter the morie cross-portal dataset catalog}
\usage{
morie_datasets_browse(
  keyword = NULL,
  portal = NULL,
  api_mode = NULL,
  loader_pattern = NULL,
  keyword_includes_url = FALSE,
  sort_by = c("dataset_key", "source", "n_rows_bundled", "id")
)
}
\arguments{
\item{keyword}{Optional case-insensitive substring to grep against
\code{dataset_key}/\code{id}/\code{loader}. \code{NULL} (default) skips this filter.}

\item{portal}{Optional portal name (see
\code{\link[=morie_dataset_portal_catalog]{morie_dataset_portal_catalog()}} for the canonical list).
Accepts a character vector for multi-portal queries.}

\item{api_mode}{Optional API mode substring to match against the
\code{api_modes} column (e.g., \code{"soda3"}, \code{"arcgis"},
\code{"opendatasoft"}, \code{"statcan_wds"}, \code{"manual_download"}).
Accepts a character vector.}

\item{loader_pattern}{Optional perl-style regex against the
\code{loader} column (e.g., \code{"^morie_datasets_tps"}).}

\item{keyword_includes_url}{If \code{TRUE}, also greps the \code{dict_url}.}

\item{sort_by}{Sort order: \code{"dataset_key"} (default), \code{"source"},
\code{"n_rows_bundled"} (descending), or \code{"id"}.}
}
\value{
A \code{data.frame} -- the filtered subset of the catalog
with the same 7-column schema.
}
\description{
Phase 3DDD4. Convenience wrapper over
\code{\link[=morie_dataset_portal_catalog]{morie_dataset_portal_catalog()}} that lets callers filter +
search by keyword, portal, api_mode, or loader pattern without
writing subset expressions by hand.
}
\details{
Filters compose with AND semantics. A \code{keyword} matches against
\code{dataset_key} + \code{id} + \code{loader} (case-insensitive). To match
anywhere including the dict URL, pass \code{keyword_includes_url = TRUE}.
}
\examples{
# All TPS datasets, alphabetical (offline; reads the cached
# cross-portal catalog -- no network).
tps <- morie_datasets_browse(portal = "tps_arcgis_hub")
nrow(tps)

# Anything mentioning "homicide"
h <- morie_datasets_browse(keyword = "homicide")
head(h$dataset_key)
}

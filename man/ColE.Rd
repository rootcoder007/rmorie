% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/colE.R
\name{ColE}
\alias{ColE}
\title{Cold-start user fallback}
\usage{
ColE(
  user,
  mode = "popular",
  R = NULL,
  item_features = NULL,
  user_features = NULL,
  min_ratings = 3,
  topn = 3
)
}
\arguments{
\item{user}{Zero-based index of the target user.}

\item{mode}{One of popular, content, metadata.}

\item{R}{An n_users x n_items rating matrix; 0 means unobserved.}

\item{item_features}{An n_items x f matrix for the content fallback.}

\item{user_features}{An n_users x g matrix for the metadata fallback.}

\item{min_ratings}{Threshold below which the user counts as cold.}

\item{topn}{Length of the returned recommendation list.}
}
\value{
List with \code{estimate}, \code{is_cold}, \code{n_rated},
\code{scores}, \code{recommended}, \code{mode}, \code{n_users},
\code{n_items}, \code{method}.
}
\description{
Formula: fallback to popular / content / metadata
}
\details{
A user with fewer than min_ratings observed interactions cannot be
served by collaborative filtering at all, because their row of the
rating matrix carries no signal.  The three fallbacks are: rank by
global popularity, rank by similarity between the item features and
whatever the user has rated, and rank by similarity to users with
matching metadata.  The cold/warm decision is reported so it can be
audited separately from the ranking.
}
\examples{
R <- matrix(c(5, 3, 0, 4, 0, 2, 0, 5, 3, 4, 4, 5), 4, 3, byrow = TRUE)
ColE(user = 1, mode = "popular", R = R, min_ratings = 1)
}
\references{
Schein, Popescul, Ungar & Pennock (2002), Methods and
Metrics for Cold-Start Recommendations, SIGIR 2002:253-260.
}

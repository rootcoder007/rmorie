% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geron_ml2_native.R
\name{morie_geron_cross_attention_report}
\alias{morie_geron_cross_attention_report}
\title{Cross-attention with per-query entropy (Geron Ch 15, morie.fn hmcatt)}
\usage{
morie_geron_cross_attention_report(dec_h, enc_h, W_Q, W_K, W_V, mask = NULL)
}
\arguments{
\item{dec_h}{Decoder states (T_dec, d).}

\item{enc_h}{Encoder states.}

\item{W_Q, W_K, W_V}{Projection matrices.}

\item{mask}{Optional mask.}
}
\value{
List with \code{context}, \code{output}, \code{attention_weights},
\code{logits}, \code{scale}, \code{d_k}, \code{entropy}, \code{max_entropy}, \code{argmax}
(0-based), \code{estimate}, \code{n}.
}
\description{
The attention itself is DELEGATED to \code{morie_geron_cross_attention}
(the grca core); this adds the per-query entropy in bits, its
log2(T_enc) ceiling and the 0-based argmax. Queries come from the
decoder and keys/values from the encoder, so no causal mask is
needed.
}
\examples{
a <- 0.5
morie_geron_resnet(c(1, 2, 3), function(a) a * 0.5)
morie_geron_cross_attention_report(dec_h = a, enc_h = a, W_Q = a, W_K = a, W_V = a)
}

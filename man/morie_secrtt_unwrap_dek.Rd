% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/secrtt_native.R
\name{morie_secrtt_unwrap_dek}
\alias{morie_secrtt_unwrap_dek}
\title{Unwrap a DEK and record the call}
\usage{
morie_secrtt_unwrap_dek(wrapped, kek, audit_log = NULL)
}
\arguments{
\item{wrapped}{List with \code{wrapped}, \code{nonce}, \code{tag},
\code{kek_id}, optional \code{aad}.}

\item{kek}{Raw KEK.}

\item{audit_log}{Optional list to append to.}
}
\value{
List with \code{dek}, \code{kek_id}, \code{audited} and
\code{audit_log}, the log extended by this call (R does not modify
its argument in place).
}
\description{
The envelope bounds offline compromise; an attacker who can call
the KEK is bounded only by what the audit trail catches, so the
call is logged rather than left silent.
}
\examples{
if (morie_crypto_sodium_available()) {
  seed <- as.raw(1:32)
  dek <- morie_secrtt_generate_dek(seed, record_id = 7L)$dek
  kek <- as.raw(rev(1:32))
  w <- morie_secrtt_wrap_dek(dek, kek, nonce = as.raw(1:12))
  u <- morie_secrtt_unwrap_dek(w, kek)
  identical(u$dek, dek)
}
}
\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geron_ml_native.R
\name{morie_geron_auxiliary_task_pretraining}
\alias{morie_geron_auxiliary_task_pretraining}
\title{Auxiliary-task pretraining then fine-tuning (Geron Ch 11, hmauxpt)}
\usage{
morie_geron_auxiliary_task_pretraining(
  model,
  aux_data,
  target_data,
  aux_epochs = 200,
  epochs = 20,
  lr = 0.05
)
}
\arguments{
\item{model}{Initial parameters or NULL for zeros.}

\item{aux_data, target_data}{Lists of (X, y).}

\item{aux_epochs, epochs}{Gradient steps. @param lr Step size.}
}
\value{
List with \code{theta}, \code{theta_pretrained}, \code{theta_scratch},
\code{target_loss}, \code{scratch_loss}, \code{transfer_gain}, \code{aux_losses},
\code{finetune_losses}, \code{scratch_losses}.
}
\description{
Gradient descent on the auxiliary task, then the SAME parameter vector
is fine-tuned on the target; the from-scratch control gets an
identical budget, so \code{transfer_gain} is measured, not asserted.
}

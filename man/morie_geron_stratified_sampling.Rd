% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geron_w4d_native.R
\name{morie_geron_stratified_sampling}
\alias{morie_geron_stratified_sampling}
\title{Stratified sampling preserves class/strata proportions in each split}
\usage{
morie_geron_stratified_sampling(X, y = NULL, stratum = NULL, n_total, seed = 0)
}
\arguments{
\item{X}{Data rows (n, ...).}

\item{y}{Optional labels used as strata.}

\item{stratum}{Optional explicit stratum key per row.}

\item{n_total}{Sample size (required).}

\item{seed}{LCG seed.}
}
\value{
list with indices (0-based), X_sample, y_sample, allocation, strata,
population_share, sample_share, max_share_error, estimate, n, method.
}
\description{
Largest-remainder (Hamilton) allocation: floor(n_total * N_h / N) per
stratum, leftover units to largest fractional remainders (ties to
larger stratum); within-stratum draw via Fisher-Yates on an LCG stream.
}
\examples{
set.seed(6)
X <- matrix(rnorm(60), 30, 2)
y <- rep(c(0L, 1L, 2L), each = 10)
morie_geron_stratified_sampling(X, y, n_total = 12, seed = 1)
}

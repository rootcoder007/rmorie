% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viterb.R
\name{Viterb}
\alias{Viterb}
\title{Most likely hidden state sequence, in one forward pass}
\usage{
Viterb(obs, trans, emit, init = NULL)
}
\arguments{
\item{obs}{Zero-based observation symbols.}

\item{trans}{Transition probabilities.}

\item{emit}{Emission probabilities.}

\item{init}{Initial state distribution; uniform by default.}
}
\value{
List with \code{path}, \code{estimate}, \code{T}, \code{K}.
}
\description{
The greedy per-step choice is not the best path: a state that looks
good now can be unreachable from what follows. Viterbi carries, for
every state, the best path ending there, collapsing an exponential
search to O(T K^2). Log space, because a product of a thousand
probabilities underflows before the path is decided.
}
\details{
Formula:
\code{delta_t(j) = max_i \[delta_{t-1}(i) + log A_ij\] + log B_j(o_t)}.
}
\references{
Viterbi, A. J. (1967). IEEE Trans Inform Theory
13:260-269.
}

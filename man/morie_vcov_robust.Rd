% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vcov_native.R
\name{morie_vcov_robust}
\alias{morie_vcov_robust}
\title{Unified robust-covariance dispatcher (native)}
\usage{
morie_vcov_robust(model, type = "HC3", cluster = NULL, ...)
}
\arguments{
\item{model}{A fitted \code{lm} / \code{glm}.}

\item{type}{\code{"HC0"}-\code{"HC5"}, \code{"HC4m"},
\code{"const"}, \code{"HAC"}, or \code{"CL"}.}

\item{cluster}{Cluster vector (required for \code{"CL"}).}

\item{...}{Passed to the underlying estimator (e.g. \code{lag}).}
}
\value{
The coefficient covariance matrix.
}
\description{
A single entry point over the native HC / HAC / CL estimators,
mirroring the \code{type} vocabulary used by
\code{morie_causal_robust_se()}.
}
\examples{
set.seed(1)
df <- data.frame(y = rnorm(60), x = rnorm(60),
                 cl = rep(1:6, each = 10))
fit <- stats::lm(y ~ x, data = df)
morie_vcov_robust(fit)
}

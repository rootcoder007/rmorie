% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mrm_tps.R
\name{mrm_tps_levy_scaling}
\alias{mrm_tps_levy_scaling}
\title{Levy-flight Hill-MLE exponent on TPS inter-incident step lengths}
\usage{
mrm_tps_levy_scaling(
  data,
  date_col = "OCC_DATE",
  lat_col = "LAT_WGS84",
  lon_col = "LONG_WGS84",
  min_step_km = 0.5,
  x_min = NULL
)
}
\arguments{
\item{data}{A data.frame with at least the columns named in
\code{date_col}, \code{lat_col}, \code{lon_col}.}

\item{date_col}{Column name of the date / timestamp
(default \code{"OCC_DATE"}).}

\item{lat_col}{Column name of WGS84 latitude
(default \code{"LAT_WGS84"}).}

\item{lon_col}{Column name of WGS84 longitude
(default \code{"LONG_WGS84"}).}

\item{min_step_km}{Lower-tail cutoff in km (default \code{0.5}).}

\item{x_min}{Hill-MLE cutoff (default = \code{min_step_km}).}
}
\value{
A list with \code{n_events}, \code{n_steps_tail}, \code{min_step_km},
\code{hill_alpha}.
}
\description{
Treats consecutive events in chronological order as a single stream
and computes the inter-event step length (km) via haversine on
WGS84 latitude/longitude. Returns the Hill-MLE exponent restricted
to steps above \code{min_step_km}.
}
\examples{
if (FALSE) {
  tps <- read.csv("Assault_Open_Data.csv")
  mrm_tps_levy_scaling(tps)
}
}

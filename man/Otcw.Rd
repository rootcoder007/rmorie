% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/otcw.R
\name{Otcw}
\alias{Otcw}
\title{Test whether a matching can be improved by re-shuffling a cycle}
\usage{
Otcw(X, Y, Cost, perm)
}
\arguments{
\item{X, Y}{Point sets, kept for interface symmetry.}

\item{Cost}{Cost matrix with \code{Cost\[i, j\] = c(x_i, y_j)}.}

\item{perm}{Zero-based assignment: \code{y_i} matched to \code{x_perm\[i\]}.}

}
\value{
List with \code{is_cm}, \code{slack}, \code{estimate}, \code{n}.
}
\description{
Cyclical monotonicity is the combinatorial fingerprint of optimality:
a coupling is optimal exactly when no finite cycle of reassignments
lowers the cost. Checking every permutation is factorial, so the test
here is over all transpositions, the two-cycle case.
}
\details{
Formula: for all \code{(i, j)},
\code{c(x_s(i), y_i) + c(x_s(j), y_j) <= c(x_s(j), y_i) + c(x_s(i), y_j)}.
}
\references{
Villani, C. (2003). AMS GSM 58, theorem 2.12.
}

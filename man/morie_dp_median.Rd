% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dp_native.R
\name{morie_dp_median}
\alias{morie_dp_median}
\title{Differentially private median}
\usage{
morie_dp_median(x, epsilon = 1, a = NULL, b = NULL, seed = NULL)
}
\arguments{
\item{x}{values.}

\item{epsilon}{privacy budget.}

\item{a, b}{bounds. Taken from the data when NULL, which is itself a
non-private query -- the function warns.}

\item{seed}{optional integer seed.}
}
\value{
list with \code{release}, \code{true_median}, \code{interval},
\code{bounds}.
}
\description{
\code{\link{morie_dp_quantile}} at q = 0.5. Kept separate because the
median is the case where the naive approach fails most clearly: its
global sensitivity is the entire domain, so noise addition is not an
option and rank selection is the only route.
}
\examples{
set.seed(1)
round(morie_dp_median(rnorm(500), epsilon = 2, a = -4, b = 4)$release, 2)
}
\references{
Smith, A. (2011). \emph{STOC}, 813-822.
}

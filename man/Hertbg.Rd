% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Hertbg.R
\name{Hertbg}
\alias{Hertbg}
\title{Genomic heritability from a relationship matrix}
\usage{
Hertbg(y, K, grid = 41, refine = 40)
}
\arguments{
\item{y}{Phenotypes.}

\item{K}{Genomic relationship matrix, n x n.}

\item{grid}{Number of grid points on (0, 1).}

\item{refine}{Golden-section refinement steps.}
}
\value{
List with \code{estimate}, \code{h2}, \code{var_g},
\code{var_e}, \code{total_var}, \code{loglik}, \code{grid_h2},
\code{grid_loglik}, \code{n}, \code{method}.
}
\description{
The restricted log-likelihood profiled over the total variance
depends only on h^2, so the estimate is found by evaluating that
profile on a deterministic grid and refining the grid maximum by
golden-section search.  sigma_e^2 = 0 forces h^2 = 1 exactly, which
is the degenerate case the tests use.
}
\details{
Formula: h^2 = sigma_g^2 / (sigma_g^2 + sigma_e^2); profile
-0.5 \link{log|V| + log(1'V^{-1}1) + (n - 1) log(y' P y)} with
V = h^2 K + (1 - h^2) I.
}
\references{
VanRaden (2008), Journal of Dairy Science
91(11):4414-4423. \doi{10.3168/jds.2007-0980}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/asmnvr_native.R
\name{de_bruijn_graph}
\alias{de_bruijn_graph}
\title{Build G(S, l) with l = k. Vertices are (k-1)-mers; each k-mer is}
\usage{
de_bruijn_graph(reads, k, multiplicity = "set")
}
\arguments{
\item{reads}{Coerced to character by the body, with \code{as.character}.}

\item{k}{Numeric; combined arithmetically in the body.}

\item{multiplicity}{One of \code{"count"}, \code{"set"}. Defaults to \code{"set"}.}
}
\value{
A list with \code{edges}, \code{indeg}, \code{outdeg}.
}
\description{
an edge from its prefix to its suffix. multiplicity="set" gives one
edge per distinct k-mer (the Idury-Waterman construction); "count"
gives one per occurrence for callers who already know their
multiplicities. Returns a list (edges, indeg, outdeg);
edges/indeg/outdeg are environments keyed by vertex (string),
paralleling the Python dict-of-lists + counter-dicts.
}
\examples{
de_bruijn_graph(reads = data.frame(x = c(1, 2, 3, 4), y = c(2, 4, 5, 9)), k = 5L)
}
\keyword{internal}

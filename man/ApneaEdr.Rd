% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aaa_rangayyan_qrs.R
\name{ApneaEdr}
\alias{ApneaEdr}
\title{Section 10.2.5 frames the problem and the AHI but gives NO detection}
\usage{
ApneaEdr(edr, spo2, fs, hours = NULL, mindur = 10, desat = 3)
}
\arguments{
\item{edr}{A vector; its length is taken.}

\item{spo2}{A vector; its length is taken and its elements indexed.}

\item{fs}{Numeric; combined arithmetically in the body.}

\item{hours}{Optional; may be \code{NULL}. Numeric; combined arithmetically in the body.}

\item{mindur}{Numeric; combined arithmetically in the body. Defaults to \code{10}.}

\item{desat}{Coerced to numeric by the body, with \code{as.numeric}. Defaults to \code{3}.}
}
\value{
A list with \code{events}, \code{nevents}, \code{ahi}, \code{desatdepth},
\code{hours}, \code{mindur}, \code{desat}, \code{fs}, \code{method}.
}
\description{
algorithm, so none is attributed to it.  Requiring BOTH a respiratory
pause and an SpO2 desaturation is what keeps a motion-induced flat
stretch of EDR from being scored as an apnea.
}
\examples{
set.seed(4)
fs <- 4
n <- fs * 600
edr <- sin(2 * pi * 0.25 * (1:n) / fs)
edr[800:1000] <- 0.02 * rnorm(201)
spo2 <- rep(97, n)
spo2[850:1100] <- 92
r <- ApneaEdr(edr, spo2, fs = fs)
str(r, max.level = 1)
}
\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hrzadm.R
\name{Npaddreg}
\alias{Npaddreg}
\alias{morie_horowitz_additive_model}
\title{Nonparametric additive model by marginal integration}
\usage{
Npaddreg(x, y, h1 = NULL, h2 = NULL, ngrid = 25L, grids = NULL)

morie_horowitz_additive_model(
  x,
  y,
  h1 = NULL,
  h2 = NULL,
  ngrid = 25L,
  grids = NULL
)
}
\arguments{
\item{x}{Numeric matrix of covariates, n by d.}

\item{y}{Numeric outcome vector.}

\item{h1}{Numeric bandwidth for the direction being estimated;
default n^(-1/5).}

\item{h2}{Numeric bandwidth for the remaining directions; default
n^(-1/(d+3)).}

\item{ngrid}{Integer points per component grid.}

\item{grids}{Optional list of explicit evaluation grids.}
}
\value{
Named list with mu, grids, components, fitted, resid, rss,
h1, h2, d, n, method.
}
\description{
Horowitz (2009), Semiparametric and Nonparametric Methods in
Econometrics, Section 3.1.1, equations (3.6) to (3.9) (pages 55-57).
For E(Y|X=x) = mu + m_1(x^1) + ... + m_d(x^d) the decomposition is
unique only after the location normalisation E\link{m_j(X^j)} = 0 (3.6);
then E(Y) = mu (3.7) and
}
\details{
m_1(x^1) = integral E(Y|X=x) p_{-1}(x^{-1}) dx^{-1} - mu   (3.8)

estimated by averaging the product-kernel estimator (3.9) over the
sample.  Bandwidths are explicit with fixed defaults; nothing is
cross-validated and nothing is random.  The additive structure is
what buys the escape from the curse of dimensionality.
}
\examples{
n <- 150
x1 <- seq(-2, 2, length.out = n)
x2 <- cos(seq_len(n) * 0.9) * 2
Npaddreg(cbind(x1, x2), 1 + x1 + 0.5 * x2^2, h1 = 0.4, h2 = 0.5)$mu
}
\keyword{internal}

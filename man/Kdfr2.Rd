% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fzr2.R
\name{Kdfr2}
\alias{Kdfr2}
\alias{morie_fauzi_r2_integral}
\title{The kernel constant r_2 of the bias-reduced KDFE variance}
\usage{
Kdfr2(a, kernel = "gaussian", lo = -8, hi = 8, ngrid = 4001L)

morie_fauzi_r2_integral(a, kernel = "gaussian", lo = -8, hi = 8, ngrid = 4001L)
}
\arguments{
\item{a}{The second smoothing parameter of (2.5); \code{a > 0}, \code{a != 1}.}

\item{kernel}{\code{"gaussian"} or a function \code{K(y)}.}

\item{lo, hi}{Quadrature limits.}

\item{ngrid}{Number of nodes; fixed, never adapted.}

}
\value{
Named list with \code{estimate}, \code{a}, \code{method}.
}
\description{
Eq. (2.10):
\deqn{r_2 = \int y K(y)\[W(y/a) + a^{-1}W(y)K(y/a)\]\,dy.}{r2 = int y \[K(y) W(y/a) + (1/a) W(y) K(y/a)\] dy.}
}
\details{
Read the bracket carefully: the two terms differ in WHICH factor is
rescaled by \code{a}. It is the cross term from expanding the variance of the
linear combination \verb{a^2/(a^2-1) Fhat_h - 1/(a^2-1) Fhat_ah} in Theorem 2.3,
so it is not symmetric in the two bandwidths and must not be "simplified"
into \verb{2 r1}.

At \code{a = 1} the two terms coincide and \verb{r2 = 2 r1}, but \code{a = 1} is excluded
by Theorem 2.1 anyway (\code{a^2 - 1} is a denominator).

Integrated on a fixed trapezoid grid for every kernel including the
Gaussian: the book states no closed form, and inventing one would be worse
than integrating.
}
\examples{
Kdfr2(a = 2)
}
\references{
Fauzi and Maesono (2023), Eq. (2.10).
}
\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fzb2x.R
\name{Kdfb2}
\alias{Kdfb2}
\alias{morie_fauzi_b2_coefficient_kdfe}
\title{The b_2 bias coefficient of the kernel distribution function estimator}
\usage{
Kdfb2(fp, mu2 = 1)

morie_fauzi_b2_coefficient_kdfe(fp, mu2 = 1)
}
\arguments{
\item{fp}{\verb{f'(x)}.}

\item{mu2}{\verb{int w^2 K(w) dw}; 1 for the Gaussian kernel.}
}
\value{
Named list with \code{estimate}, \code{mu2}, \code{method}.
}
\description{
Eq. (2.7): \deqn{b_2(x) = \frac{f_X'(x)}{2}\int w^2 K(w)\,dw.}{b2(x) = (f'(x)/2) int w^2 K(w) dw.}
}
\details{
The leading bias coefficient of the KDFE:
\verb{Bias\[Fhat_h(x)\] = h^2 b2(x) + o(h^2)}.

Note which derivative appears. A kernel DENSITY estimator's leading bias
carries \verb{f''}; the distribution-function estimator carries \verb{f'}, one order
lower, because it smooths with the INTEGRATED kernel \code{W}. That single fact
is why the book's bandwidth rate is \code{n^(-1/3)} and not \code{n^(-1/5)}.

\code{mu2} defaults to 1, the Gaussian value; pass \code{0.2} for Epanechnikov. It is
an explicit argument, never estimated from data -- the kernel is a
modelling choice, not a random quantity.

Naming note. The backlog assigns this row the public name
\code{fauzi_b2_coefficient}, but \code{morie_fauzi_b2_coefficient} is already exported
from \code{R/fauzi_native.R} for the Chapter 4 coefficient \code{b_2(t)} of Eq.
(4.15) -- a different quantity that merely shares the book's symbol.
Defining it again here would silently rebind that export, since
\code{fauzi_native.R} collates first. The legacy spelling kept here is therefore
\code{morie_fauzi_b2_coefficient_kdfe}.
}
\examples{
Kdfb2(fp = 0.4)
}
\references{
Fauzi and Maesono (2023), Eq. (2.7).
}
\keyword{internal}

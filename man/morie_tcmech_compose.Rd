% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tcmech_native.R
\name{morie_tcmech_compose}
\alias{morie_tcmech_compose}
\title{Composition: the rhos add and the truncation is the tightest one}
\usage{
morie_tcmech_compose(rhos, omegas = NULL)
}
\arguments{
\item{rhos}{The per-release budgets.}

\item{omegas}{The per-release truncations, or NULL.}
}
\value{
A list with the total rho and the composed truncation.
}
\description{
Renyi divergence is additive over independent releases at each order,
so the budgets add. A composed mechanism is only bounded at orders
where EVERY part is bounded, which is why the truncation takes the
minimum -- taking the maximum would claim a guarantee at orders one
of the parts never had.
}
\examples{
V <- c(1, 2, 3, 4, 5, 6, 7, 8)
morie_tcmech_compose(V)
}
\keyword{internal}

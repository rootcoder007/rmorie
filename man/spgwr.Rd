% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spgwr.R
\name{spgwr}
\alias{spgwr}
\title{Geographically weighted regression: locally varying coefficients}
\usage{
spgwr(x, y, coords, bandwidth = NULL, kernel = "gaussian")
}
\arguments{
\item{x}{Covariates (n by p).}

\item{y}{Response, length n.}

\item{coords}{Coordinates (n by 2).}

\item{bandwidth}{Kernel bandwidth.}

\item{kernel}{Kernel family.}
}
\value{
The result of \code{gwreg()}.
}
\description{
Same estimator as \code{\link[=gwreg]{gwreg()}}; this delegates rather than carrying a
second implementation. The model is fitted locally in the spatial
sense while allowing general covariates -- the weights determine how
far each observation influences the coefficients estimated at a given
point, and the locality is based on spatial position, not on
covariate values.
}
\examples{
n <- 30
spgwr(cbind(1, runif(n)), rnorm(n), matrix(runif(2 * n), n, 2))
}
\references{
Schabenberger & Gotway (2005), Sec 6.1.3 "Spatially
Explicit Models", eq (6.9), pp. 316-317, citing Fotheringham et al. (2002).
}

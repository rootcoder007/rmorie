% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fzber.R
\name{Qbebnd}
\alias{Qbebnd}
\alias{morie_fauzi_berry_esseen_quantile}
\title{Berry-Esseen bound for the kernel quantile estimator}
\usage{
Qbebnd(x, n, m = 4L, improved = TRUE)

morie_fauzi_berry_esseen_quantile(x, n, m = 4L, improved = TRUE)
}
\arguments{
\item{x}{Argument, in units of \code{sigma_n}.}

\item{n}{Sample size.}

\item{m}{Kernel order; 2, 3 or 4.}

\item{improved}{For \code{m = 4}, use Remark 3.1's \code{r = 1/2} instead of the
literature's \code{7/17}.}
}
\value{
Named list with \code{estimate}, \code{bound}, \code{rate}, \code{m}, \code{n}, \code{method}.
}
\description{
Eq. (3.5) and Remark 3.1:
\deqn{P(\sqrt{n}|\hat Q_{p,h} - Q(p)| \le x\sigma_n) = 2\Phi(x) - 1 + O(n^{-r}).}{P(sqrt(n)|Qhat - Q(p)| <= x sigma_n) = 2 Phi(x) - 1 + O(n^-r).}
}
\details{
The rate \code{r} depends on the ORDER of the kernel, not on the sample:
\code{m = 2} gives \code{r = 1/3}; \code{m = 3} gives \code{5/13}; \code{m = 4} gives \code{7/17} by the
earlier literature, improved to \code{1/2} by Remark 3.1 of this book. Those five
numbers are quoted verbatim from the text.

The book also says plainly that \code{o(n^-1/2)} is unreachable for ANY kernel
order without adding the next Edgeworth term, so \code{r = 1/2} is the ceiling of
this approach, not a stepping stone.

The two-sided normal probability and the bound term \code{n^(-r)} come back
separately: the second is an order symbol with no constant, and adding it to
the first would be arithmetic on something with no numerical value.
}
\examples{
Qbebnd(x = 1.96, n = 100)
}
\references{
Fauzi and Maesono (2023), Eq. (3.5), Remark 3.1.
}
\keyword{internal}

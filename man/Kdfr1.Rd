% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fzr1.R
\name{Kdfr1}
\alias{Kdfr1}
\alias{morie_fauzi_r1_integral}
\title{The kernel constant r_1 of the KDFE variance}
\usage{
Kdfr1(kernel = "gaussian", lo = -8, hi = 8, ngrid = 4001L)

morie_fauzi_r1_integral(kernel = "gaussian", lo = -8, hi = 8, ngrid = 4001L)
}
\arguments{
\item{kernel}{\code{"gaussian"} for the closed form, or a function \code{K(y)}.}

\item{lo, hi}{Quadrature limits, used only for a callable kernel.}

\item{ngrid}{Number of nodes; fixed, never adapted.}

}
\value{
Named list with \code{estimate}, \code{kernel}, \code{method}.
}
\description{
Eq. (2.9): \deqn{r_1 = \int y K(y) W(y)\,dy,}{r1 = int y K(y) W(y) dy,}
with \verb{W(y) = int_\{-inf\}^\{y\} K(v) dv}.
}
\details{
This is the constant that makes the book worth writing. The KDFE variance
(2.4) is \verb{F(1-F)/n - 2 h r1 f(x) / n}: the bandwidth enters with a NEGATIVE
sign, so smoothing REDUCES variance -- the opposite of a density estimator,
where it enters at \code{1/(nh)}. Sec. 2.1 concludes that any kernel with
\code{r1 > 0} beats the empirical df for every \code{F_X}, and proves \code{r1 >= 0} for
symmetric kernels by splitting the integral at 0.

For the Gaussian kernel the value is exactly \verb{1/(2 sqrt(pi))}, returned in
closed form. Any other kernel is integrated on a fixed trapezoid grid --
fixed node count, no adaptive refinement, so two calls agree bitwise.
}
\examples{
Kdfr1()
}
\references{
Fauzi and Maesono (2023), Eqs. (2.4) and (2.9).
}
\keyword{internal}

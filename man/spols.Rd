% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spols.R
\name{spols}
\alias{spols}
\title{Ordinary least squares fitting of a semivariogram model}
\usage{
spols(empirical_variogram, variogram_model = "exponential")
}
\arguments{
\item{empirical_variogram}{A list with \code{lags}, \code{gamma} and optionally
\code{counts}, or a matrix of those columns.}

\item{variogram_model}{One of "exponential", "gaussian", "spherical".}
}
\value{
A list with \code{nugget}, \code{partial_sill}, \code{sill}, \code{range},
\code{objective}, \code{converged}, \code{n_lags} and \code{fitted}.
}
\description{
Minimises \eqn{\sum_m \{\hat\gamma(h_m) - \gamma(h_m; \theta)\}^2}, which is
the generalized sum of squares (4.31) under the simplification
\eqn{R = \phi I} named in the text immediately after eq (4.34). OLS
therefore ignores both the correlation among the empirical semivariogram
ordinates and their unequal dispersion. Schabenberger & Gotway report
Zimmerman and Zimmerman's (1991) finding that OLS and WLS perform "more or
less equally well", and that the greater loss of efficiency comes from
ignoring the correlations rather than from preferring OLS to WLS.
}
\references{
Schabenberger Ch 4, Sec 4.5.1
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bnppvl_native.R
\name{morie_bnppvl}
\alias{morie_bnppvl}
\title{Fit a Beta quantile pyramid and report the predictive for a new draw}
\usage{
morie_bnppvl(
  x,
  m = 4L,
  c = 2.5,
  schedule = "cubic",
  centring = "uniform",
  nullq = NULL,
  likelihood = "exact",
  lo = 0,
  hi = 1,
  sweeps = 400L,
  burn = 100L,
  thin = 2L,
  seed = 0,
  init = "prior",
  grid = NULL,
  probs = c(0.05, 0.25, 0.5, 0.75, 0.95)
)
}
\arguments{
\item{x}{The observations, which must lie inside the support.}

\item{m}{Pyramid depth; the histogram has k = 2^m cells.}

\item{c}{Concentration scale. Larger holds the pyramid closer to its
centring.}

\item{schedule}{A member of the schedule list.}

\item{centring}{A member of the centring list; "null" centres on the
given quantile function.}

\item{nullq}{The centring quantile function on the unit interval,
strictly increasing, or NULL.}

\item{likelihood}{A member of the likelihood list.}

\item{lo}{Lower end of the support.}

\item{hi}{Upper end of the support.}

\item{sweeps}{Metropolis-Hastings sweeps in total.}

\item{burn}{Sweeps discarded.}

\item{thin}{Keep-every rate.}

\item{seed}{The random stream.}

\item{init}{A member of the init list. "prior" starts the chain at a
draw from the prior, which is what the paper describes;
"empirical" starts it near the data, which reaches the same
posterior in far fewer sweeps of a single-site sampler.}

\item{grid}{Points at which the predictive density and distribution
function are reported, or NULL.}

\item{probs}{Predictive quantile levels.}
}
\value{
A list with the posterior mean quantile function, the
predictive mean and standard deviation of a new observation, the
predictive density and distribution function on the grid, the
predictive quantiles, and the acceptance rate.
}
\description{
Fit a Beta quantile pyramid and report the predictive for a new draw
}
\examples{
X <- vapply(0:39, function(i) {
    v <- ((i * 37)\%\%101)/100
    v * v
}, numeric(1))
morie_bnppvl(X, m = 3L, c = 2.5, likelihood = "exact", sweeps = 400L,
    burn = 100L, thin = 4L, seed = 7, init = "empirical")
}
\keyword{internal}

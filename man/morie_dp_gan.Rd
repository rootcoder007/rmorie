% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dp_native3.R
\name{morie_dp_gan}
\alias{morie_dp_gan}
\title{Differentially private GAN discriminator step}
\usage{
morie_dp_gan(
  disc_grads,
  C = 1,
  sigma = 1,
  lr = 0.1,
  n_disc_steps = 1,
  seed = NULL
)
}
\arguments{
\item{disc_grads}{per-example discriminator gradients, (B, p).}

\item{C, sigma, lr}{as for \code{\link{morie_dp_sgd}}.}

\item{n_disc_steps}{discriminator steps per generator step, recorded
for accounting.}

\item{seed}{optional integer seed.}


}
\value{
list with \code{disc_update}, \code{private_gradient},
\code{generator_is_free}, \code{steps_to_account}.
}
\description{
Only the DISCRIMINATOR touches real data, so only the discriminator
needs privatising. The generator sees the discriminator's output and
nothing else, which makes it post-processing and therefore free --
adding noise to it spends budget for nothing.
}
\details{
Account over discriminator steps, which typically outnumber generator
steps several to one; counting generator steps instead understates
the spend.
}
\examples{
set.seed(1)
morie_dp_gan(matrix(rnorm(80), ncol = 4))$generator_is_free
}
\references{
Xie, L. et al. (2018). Differentially private generative
adversarial network. \emph{arXiv:1802.06739}.
}

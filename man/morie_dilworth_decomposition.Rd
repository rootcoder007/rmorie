% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extremal_design_native.R
\name{morie_dilworth_decomposition}
\alias{morie_dilworth_decomposition}
\title{Dilworth's theorem, with both sides computed}
\usage{
morie_dilworth_decomposition(leq)
}
\arguments{
\item{leq}{Square logical matrix; \verb{leq\[i, j\]} when \eqn{i \le j}.}
}
\value{
A list with \code{antichain_size}, \code{chain_cover_size},
\code{dilworth_holds}, \code{antichain}, \code{warnings}.
}
\description{
The largest antichain in a finite poset equals the fewest chains
needed to cover it. Both are computed -- the antichain exhaustively,
the chain cover by a bipartite matching -- and the equality is then
CHECKED rather than assumed, since equality is the content.
}
\examples{
divisibility_poset <- function(n) {
    outer(seq_len(n), seq_len(n), function(i, j) j\%\%i == 0)
}
n <- 6L
morie_dilworth_decomposition(divisibility_poset(n))
}
\references{
Dilworth RP (1950) \emph{Annals of Mathematics}
51(1):161-166.
}

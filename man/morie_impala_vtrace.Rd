% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/misc_native.R
\name{morie_impala_vtrace}
\alias{morie_impala_vtrace}
\title{IMPALA V-trace off-policy targets}
\usage{
morie_impala_vtrace(
  rewards,
  values,
  behavior_logp,
  target_logp,
  gamma = 0.99,
  rho_bar = 1,
  c_bar = 1,
  bootstrap_value = 0
)
}
\arguments{
\item{rewards, values}{per-step rewards and value estimates.}

\item{behavior_logp, target_logp}{log-probabilities under each policy.}

\item{gamma}{discount.}

\item{rho_bar, c_bar}{truncation levels.}

\item{bootstrap_value}{value past the end of the segment.}
}
\value{
list with \code{vs}, \code{advantage}, \code{rho}, \code{c},
\code{delta}, \code{ratio}, \code{n_truncated_rho}.
}
\description{
Truncated importance weights \eqn{\rho = \min(\bar\rho, \pi/\mu)} and
\eqn{c = \min(\bar c, \pi/\mu)} applied to an n-step return.
}
\details{
The two clips are not interchangeable and confusing them is the
standard mistake. \code{rho_bar} determines WHAT is learned -- the
fixed point lies somewhere between the behaviour and target policies,
at the behaviour policy when \code{rho_bar} is small and the target
policy as it grows. \code{c_bar} determines HOW FAST the trace decays
and does not move the fixed point at all.
}
\examples{
morie_impala_vtrace(c(1, 0, 1), c(0.5, 0.4, 0.3), c(-1, -1, -1),
                    c(-0.9, -1.2, -1))$vs
}
\references{
Espeholt, L. et al. (2018). IMPALA: scalable distributed
deep-RL with importance weighted actor-learner architectures.
\emph{ICML}, 1407-1416.
}

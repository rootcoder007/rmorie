% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Alfplddt.R
\name{Alfplddt}
\alias{Alfplddt}
\title{Per-residue model confidence (pLDDT) head of AlphaFold}
\usage{
Alfplddt(s, w1, w2, w3, bins = NULL, rtrue = NULL)
}
\arguments{
\item{s}{Single representation, an \code{n x cs} matrix.}

\item{w1, w2}{The two projections of line 1, each followed by a relu.}

\item{w3}{Projection to the bin logits (line 2).}

\item{bins}{Bin centres; defaults to the spec's 1, 3, ..., 99.}

\item{rtrue}{Optional ground-truth lDDT per residue, giving the loss.}

}
\value{
A list with \code{plddt}, the bin distributions \code{p}, the
\code{loss}, \code{estimate}, \code{n} and \code{method}.
}
\description{
Algorithm 29 of the Supplementary Information to Jumper et al. (2021),
p. 37.  A small MLP on the single representation produces a distribution
over lDDT bins; the reported confidence is that distribution's mean.
}
\details{
Line 4 of the published pseudocode writes the confidence loss without a
leading minus sign, which would make it something to maximise while
equation (7) adds it to a total that is minimised.  The reference
implementation uses the negative log likelihood, so that is what is
computed here; the sign in the supplement is a typo.
}
\references{
Jumper et al (2021) Nature 596:583-589, Suppl. Algorithm 29
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap_methods.R
\name{morie_simpleboot_two}
\alias{morie_simpleboot_two}
\title{Direct bridge to \code{simpleboot::two.boot}}
\usage{
morie_simpleboot_two(x, y, statistic = mean, R = 1000L, ...)
}
\arguments{
\item{x, y}{Numeric vectors.}

\item{statistic}{A scalar function applied to one sample at a
time (e.g. \code{mean}, \code{median}); two.boot's contract.}

\item{R}{Number of bootstrap replicates (default 1000).}

\item{...}{Forwarded to the per-sample statistic.}

}
\value{
A \code{morie_boot} object for the difference statistic.
}
\description{
Thin pass-through to \code{simpleboot::two.boot} for the
common two-sample bootstrap-of-a-statistic case (e.g. difference
of means, ratio of medians). Returns a \code{boot} object the
user can hand to \code{boot::boot.ci} or \code{\link[=morie_boot_basic_ci]{morie_boot_basic_ci()}}.
}
\examples{
if (requireNamespace("simpleboot", quietly = TRUE)) {
  set.seed(1)
  b <- morie_simpleboot_two(rnorm(25), rnorm(25, 0.5), R = 200L)
  class(b)
}
}
\seealso{
\code{simpleboot::two.boot}, \code{simpleboot::one.boot},
\code{simpleboot::lm.boot}.
}

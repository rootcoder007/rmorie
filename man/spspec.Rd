% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spspec.R
\name{spspec}
\alias{spspec}
\title{Sum-of-sinusoids (spectral) representation, eqs (2.26)-(2.27).}
\usage{
spspec(h, sigma2 = NULL, omega = NULL, mu = 0, seed = NULL)
}
\arguments{
\item{h}{Lags at which to evaluate C.}

\item{sigma2}{Variance at each frequency; a decaying set by default.}

\item{omega}{Frequencies, same length as \code{sigma2}.}

\item{mu}{Process mean, carried into the realisation.}

\item{seed}{Optional seed for amplitudes and phases.}
}
\value{
Named list: h, covariance, variance, sum_sigma2, omega,
sigma2, realisation.
}
\description{
Z(s) = mu + sum_j A_j cos(w_j s + phi_j) with random amplitudes A_j
and phases phi_j uniform on (0, 2pi), all mutually independent. The
Y_j have zero mean because the integral of cos(a + phi) over a full
period vanishes, and the covariance collapses to a cosine sum
C(h) = sum_j sigma_j^2 cos(w_j h) with sigma_j^2 = E[A_j^2] / 2. So
Var[Z(s)] = C(0) = sum_j sigma_j^2: the spectrum is a budget for the
variance, which is the content of the representation.
}
\examples{
spspec(c(0, 1, 2))$variance
}
\references{
Schabenberger & Gotway (2005), Sec 2.5, eqs (2.26)-(2.27).
}

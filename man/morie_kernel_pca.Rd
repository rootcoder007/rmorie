% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extenders_spatial.R
\name{morie_kernel_pca}
\alias{morie_kernel_pca}
\title{Kernel principal components analysis via \pkg{kernlab}}
\usage{
morie_kernel_pca(x, ...)
}
\arguments{
\item{x}{Numeric matrix or data frame of features (rows =
observations).}

\item{...}{Further arguments forwarded to \code{kernlab::kpca}
(e.g. \code{kernel}, \code{kpar}, \code{features},
\code{th}).}
}
\value{
A list with \code{$method = "kernlab::kpca"} and
\code{$raw} (a \code{kpca} S4 object with eigenvalues,
eigenvectors and the projected rotated data).
}
\description{
Thin extender over \code{kernlab::kpca} that performs PCA in a
feature space induced by a reproducing-kernel.
}
\examples{
\dontrun{
  if (requireNamespace("kernlab", quietly = TRUE)) {
    set.seed(1)
    x <- matrix(stats::rnorm(200), ncol = 4)
    morie_kernel_pca(x, kernel = "rbfdot", features = 2)
  }
}
}

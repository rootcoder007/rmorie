% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geron_ml2_native.R
\name{morie_geron_conv_autoencoder_trained}
\alias{morie_geron_conv_autoencoder_trained}
\title{Trained patch convolutional autoencoder (Geron Ch 18, morie.fn hmcae)}
\usage{
morie_geron_conv_autoencoder_trained(
  X,
  filters = 2,
  epochs = 100,
  lr = 0.05,
  seed = 0,
  patch = 2
)
}
\arguments{
\item{X}{Image (H, W) or a list of images.}

\item{filters}{Bottleneck width. @param epochs Training epochs.}

\item{lr}{Learning rate. @param seed LCG seed. @param patch Patch size.}
}
\value{
List with \code{loss_history}, \code{final_loss}, \code{encoder}, \code{decoder},
\code{codes} (list of images, each a list of F patch-grid matrices),
\code{code_shape}, \code{reconstruction} (list of images),
\code{compression_ratio}, \code{n_patches}, \code{patch}, \code{grcae_check},
\code{estimate}, \code{n}.
}
\description{
Splits every image into non-overlapping p x p patches (row-major
over patch rows then patch columns, each patch flattened row-major),
normalises by the RMS pixel value, and trains a linear
encoder/decoder pair by gradient descent on the reconstruction MSE.
Weights come from one continuing LCG stream: W_enc (p^2 x F) then
V_dec (F x p^2), both reshaped row-major.
}
\details{
A bottleneck narrower than the patch cannot reach zero loss; the
residual IS the compression cost, which the parity test checks by
comparing \code{compression_ratio} against \code{final_loss}.

\code{grcae_check} re-runs the single forward pass of
\code{morie_geron_convolutional_autoencoder} on the first image, exactly
as the Python module cross-checks itself against grcae.
}

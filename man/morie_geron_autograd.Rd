% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geron_ml_native.R
\name{morie_geron_autograd}
\alias{morie_geron_autograd}
\title{Reverse-mode autograd over a scalar tape (Geron Ch 10, morie.fn hmagrd)}
\usage{
morie_geron_autograd(loss, params)
}
\arguments{
\item{loss}{Function(list of leaves) -> tape node.}

\item{params}{Leaf values (numeric vector).}
}
\value{
List with \code{grad}, \code{value}, \code{tape_size}, \code{params}.
}
\description{
\code{loss} receives a list of tape leaves and must return a tape node --
returning a plain number means the tape was bypassed and no gradient
exists, which is an error rather than a silent zero. Leaves support
\verb{+ - * / ^} plus \verb{exp log sqrt tanh sin cos} and
\code{\link[=morie_gvar_relu]{morie_gvar_relu()}} / \code{\link[=morie_gvar_sigmoid]{morie_gvar_sigmoid()}}. One backward sweep fills
every leaf's gradient at the cost of one forward pass.
}
\examples{
loss <- function(p) p[[1]] * p[[1]] + 3 * p[[2]]
r <- morie_geron_autograd(loss, c(2, 1))
str(r, max.level = 1)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/esl_native2.R
\name{morie_esl_partial_dependence}
\alias{morie_esl_partial_dependence}
\title{Partial dependence}
\usage{
morie_esl_partial_dependence(model, X, S, grid = NULL, n_grid = 20L)
}
\arguments{
\item{model}{Function taking an \verb{(m, p)} matrix and returning predictions.}

\item{X}{Data supplying the complement distribution, n by p.}

\item{S}{Column indices (1-based) to vary.}

\item{grid}{Values of x_S to evaluate; defaults to a quantile grid.}

\item{n_grid}{Grid size when \code{grid} is not given.}
}
\value{
List with \code{grid}, \code{pd}, \code{centered}, \code{extrapolation_warning}.
}
\description{
The prediction averaged over the observed joint distribution of the
complement variables, with the variables in \code{S} held fixed. This is a
marginal of the FITTED surface, not a conditional expectation: it answers
"what does the model do if I move x_S", not "what is E of y given x_S".
}
\details{
The average runs over the marginal of the complement, so when x_S and the
complement are correlated the model is evaluated at combinations that never
occur, and the curve there is extrapolation. \code{extrapolation_warning} flags
grid points whose synthetic rows sit far from the observed cloud --
measured in the JOINT, since every quantile grid point is by construction
close to observed x_S values.
}
\examples{
set.seed(1)
X <- matrix(runif(800, -2, 2), ncol = 2)
f <- function(Z) 3 * Z[, 1] + Z[, 2]^2
r <- morie_esl_partial_dependence(f, X, S = 1, n_grid = 9)
abs(stats::coef(stats::lm(r$pd ~ r$grid[, 1]))[[2]] - 3) < 1e-8
}
\references{
Friedman, J. H. (2001). Greedy function approximation.
Annals of Statistics 29(5), 1189-1232.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datasets.R
\name{morie_datasets_chicago_crime_map}
\alias{morie_datasets_chicago_crime_map}
\title{City of Chicago "Crimes -- 2001 to Present -- Map" view (\code{ahwe-kpsy})}
\usage{
morie_datasets_chicago_crime_map(
  date_from = NULL,
  date_to = NULL,
  where = NULL,
  max_features = NULL,
  offline = TRUE,
  resource_id = NULL,
  paginate = FALSE,
  page_size = 1000L,
  max_pages = 200L,
  app_token = NULL
)
}
\arguments{
\item{date_from}{Lower bound on \code{date} (inclusive). Accepts a
\code{Date}, \code{POSIXct}, or ISO-8601 string. \code{NULL} defaults to one
year before \code{date_to} (matches the upstream Map view's rolling
1-year window).}

\item{date_to}{Upper bound on \code{date} (exclusive). \code{NULL} defaults
to today.}

\item{where}{Optional additional SoQL \code{WHERE} fragment ANDed onto
the date window. e.g. \code{"primary_type='HOMICIDE'"}.}

\item{max_features}{Optional total row cap.}

\item{offline}{Logical; if \code{TRUE} (default), read the bundled
39-col fixture.}

\item{resource_id}{Optional view id override (default
\code{"ahwe-kpsy"}).}

\item{paginate}{Logical; opt-in pagination via baked-in
\verb{LIMIT n OFFSET m}.}

\item{page_size}{Per-page row count when paginating.}

\item{max_pages}{Safety net on paginated walks.}

\item{app_token}{Optional Socrata app token (sent as
\code{X-App-Token}).}
}
\value{
A \code{data.frame} with the 39-col schema.
}
\description{
Wraps the Socrata MAP VIEW derived from the main Crimes feed
(parent_fxf = \code{ijzp-q8t2}). Verified live as
\verb{type: map, parent_fxf: [ijzp-q8t2]} via the Socrata catalog API;
landing page at
\url{https://data.cityofchicago.org/Public-Safety/Crimes-2001-to-Present-Map/ahwe-kpsy}.
}
\details{
\strong{SODA3-only}. The SODA2 endpoint \verb{/resource/ahwe-kpsy.json} does
technically return HTTP 200 but ships rows as empty objects
(\verb{[\{\}]}) -- column resolution doesn't fire on map/filtered views.
This loader uses the SODA3 endpoint
\verb{/api/v3/views/ahwe-kpsy/query.json?query=SELECT ... WHERE ...}
via \code{\link[=.morie_dataset_soda3_query]{.morie_dataset_soda3_query()}}.

The live ahwe-kpsy view returns a \strong{39-column} schema:
\itemize{
\item 22 base ijzp-q8t2 columns (id, case_number, date, ..., location)
\item 4 reverse-geocoded extras (\code{location_address}, \code{location_city},
\code{location_state}, \code{location_zip})
\item 4 Socrata-internal metadata cols (\verb{:id}, \verb{:version},
\verb{:created_at}, \verb{:updated_at})
\item 9 \verb{:@computed_region_*} spatial-overlay columns mapping each
row to other Chicago boundary layers (wards, community areas,
etc.) via Socrata's automatic point-in-polygon computation
}

Offline mode reads a bundled 5-row 39-col fixture
(\code{inst/extdata/chicago_crime_map_ahwe_kpsy_sample.csv}).
}
\examples{
df <- morie_datasets_chicago_crime_map(offline = TRUE)
df$primary_type
}
\references{
City of Chicago Data Portal, "Crimes - 2001 to
Present - Map" (\code{ahwe-kpsy}), derived from \code{ijzp-q8t2}.
}

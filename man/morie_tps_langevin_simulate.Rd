% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tps_stochastic.R
\name{morie_tps_langevin_simulate}
\alias{morie_tps_langevin_simulate}
\title{Euler-Maruyama Ornstein-Uhlenbeck simulation}
\usage{
morie_tps_langevin_simulate(
  df,
  ds_name = "?",
  n_paths = 100L,
  T_days = 365L,
  dt = 1,
  seed = 42L
)
}
\arguments{
\item{df}{A \code{data.frame}.}

\item{ds_name}{Character label.}

\item{n_paths}{Number of forward paths to simulate.}

\item{T_days}{Forward horizon in days.}

\item{dt}{Time step (days).}

\item{seed}{RNG seed.}
}
\value{
A \code{morie_rich_result} list with \code{theta},
\code{mu}, \code{sigma}, \code{paths} (matrix of n_paths x
n_steps), and final-day quantiles.
}
\description{
Fits an OU process \eqn{dX_t = theta(mu - X_t) dt + sigma dW_t}
to daily incident counts via OLS on first-differences, then runs
\code{n_paths} forward simulations of length \code{T_days}.
}
\examples{
set.seed(1)
df <- data.frame(OCC_YEAR = rep(2014:2023, each = 30),
                 OCC_MONTH = sample(month.name, 300, TRUE),
                 HOOD_158 = sample(sprintf("\%03d", 1:20), 300, TRUE),
                 LAT_WGS84 = runif(300, 43.6, 43.8),
                 LONG_WGS84 = runif(300, -79.5, -79.2))
res <- try(morie_tps_langevin_simulate(df, ds_name = "synthetic", n_paths = 20L))
if (!inherits(res, "try-error")) str(res, max.level = 1)
}

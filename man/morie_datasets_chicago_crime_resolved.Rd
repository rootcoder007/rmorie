% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datasets.R
\name{morie_datasets_chicago_crime_resolved}
\alias{morie_datasets_chicago_crime_resolved}
\title{One-call Chicago crime + boundary + dictionary join}
\usage{
morie_datasets_chicago_crime_resolved(
  year = NULL,
  max_features = NULL,
  offline = TRUE,
  mode = c("soda2", "soda3"),
  paginate = FALSE,
  page_size = 1000L,
  max_pages = 200L,
  app_token = NULL,
  resolvers = c("ward", "community_area", "beat", "district", "iucr")
)
}
\arguments{
\item{year}{Integer or \code{NULL}; server-side year filter.}

\item{max_features}{Integer or \code{NULL}; cap on returned rows. When
\code{paginate = TRUE} this is the total cap across all walked pages.}

\item{offline}{Logical; if \code{TRUE}, return the bundled synthetic frame.}

\item{mode}{One of \code{"soda2"} (default) or \code{"soda3"}. Selects the
API path for live mode:
\itemize{
\item \code{"soda2"} -> \verb{/resource/<id>.json?$where=...} via
\code{\link[=.morie_dataset_socrata_fetch]{.morie_dataset_socrata_fetch()}} (URL-param SoQL grammar).
\item \code{"soda3"} -> \verb{/api/v3/views/<id>/query.json?query=SELECT ...}
via \code{\link[=.morie_dataset_soda3_query]{.morie_dataset_soda3_query()}} (full SoQL passthrough).
Both modes return the same 22-column schema; SODA3 is required
when a derived/map view is involved (none here, but available
for parity with \code{\link[=morie_datasets_chicago_crime_map]{morie_datasets_chicago_crime_map()}}) and for
the canonical "single SoQL string" experience.
}}

\item{paginate}{Logical; if \code{TRUE} and \code{offline = FALSE}, walk
pagination in \code{page_size} chunks. SODA2 uses \verb{$offset}; SODA3
uses \verb{LIMIT page_size OFFSET m} baked into the SoQL.}

\item{page_size}{Integer; per-page row count when \code{paginate = TRUE}.
Default 1,000 (the unauthenticated SODA2 ceiling).}

\item{max_pages}{Integer; safety net on \code{paginate = TRUE} walks
(default 200 -> up to 200,000 rows without an app_token).}

\item{app_token}{Optional Socrata app token (SODA3 only -- sent
as the \code{X-App-Token} header; ignored under \code{mode = "soda2"}).}

\item{resolvers}{Character subset of the 5 resolver names to
join. Default joins all 5. Pass a shorter vector to skip
specific joins (e.g. \code{"iucr"} only).}
}
\value{
A wide \code{data.frame}: crime columns first, then the
joined resolver columns with their canonical prefixes.
}
\description{
Phase 3VV+. Pulls a slice of \code{\link[=morie_datasets_chicago_crime]{morie_datasets_chicago_crime()}}
and left-joins each of its five canonical foreign keys against
the matching resolver dataset shipped in morie:
}
\details{
\tabular{lll}{
\strong{crime field}     \tab \strong{resolver}                       \tab \strong{join key}        \cr
\code{beat}                   \tab \code{\link[=morie_datasets_chicago_police_beats]{morie_datasets_chicago_police_beats()}} \tab \code{beat == beat_num}       \cr
\code{district}               \tab \code{\link[=morie_datasets_chicago_police_districts]{morie_datasets_chicago_police_districts()}} \tab \code{district == dist_num} \cr
\code{ward}                   \tab \code{\link[=morie_datasets_chicago_wards]{morie_datasets_chicago_wards()}}        \tab \code{ward == ward}           \cr
\code{community_area}         \tab \code{\link[=morie_datasets_chicago_community_areas]{morie_datasets_chicago_community_areas()}} \tab \code{community_area == area_numbe} \cr
\code{iucr}                   \tab \code{\link[=morie_datasets_chicago_iucr_codes]{morie_datasets_chicago_iucr_codes()}}   \tab \code{iucr == iucr}           \cr
}

The resolvers are loaded in offline mode (they're all bundled +
small), so this analyzer only touches the network for the crime
pull itself. Resolver columns are prefixed with the source name
(\verb{ward_*}, \verb{community_*}, \verb{beat_*}, \verb{district_*}, \verb{iucr_*}) to
avoid collisions with the crime schema.

Both \code{mode = "soda2"} and \code{mode = "soda3"} are honoured for the
crime fetch, matching the dual-API design from 3VV+.
}
\examples{
df <- morie_datasets_chicago_crime_resolved(
  offline = TRUE,
  max_features = 5L,
  resolvers = c("ward", "iucr"))
names(df)
}

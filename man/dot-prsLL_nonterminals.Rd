% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prsLL_native.R
\name{.prsLL_nonterminals}
\alias{.prsLL_nonterminals}
\title{.prsLL_nonterminals}
\usage{
.prsLL_nonterminals(g)
}
\arguments{
\item{g}{A list; the body reads \code{$rules} from it.}
}
\value{
The value of \code{out}, as built in the body.
}
\description{
A step of the prsLL_native implementation. Called by \code{.prsLL_first_of}, \code{.prsLL_first_sets}, \code{.prsLL_follow_sets} and 8 others in the module.
See the file header for the source the module follows.
source it follows.
}
\examples{
# The expression grammar of Aho, Lam, Sethi & Ullman (2nd ed., 4.4.2):
#   E -> T E';  E' -> + T E' | eps;  T -> F T';  T' -> * F T' | eps;
#   F -> ( E ) | id
rules <- list(
  list("E",  c("T", "Ep")),
  list("Ep", c("+", "T", "Ep")),
  list("Ep", character(0)),
  list("T",  c("F", "Tp")),
  list("Tp", c("*", "F", "Tp")),
  list("Tp", character(0)),
  list("F",  c("(", "E", ")")),
  list("F",  "id")
)
g <- .prsLL_grammar(rules, start = "E")
.prsLL_nonterminals(g)
}

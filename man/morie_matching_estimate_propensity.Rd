% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/matching.R
\name{morie_matching_estimate_propensity}
\alias{morie_matching_estimate_propensity}
\title{Estimate propensity scores}
\usage{
morie_matching_estimate_propensity(
  data,
  treatment,
  covariates,
  model = "logistic",
  max_iter = 1000
)
}
\arguments{
\item{data}{Data frame.}

\item{treatment}{Name of the binary treatment column (0/1).}

\item{covariates}{Character vector of covariate names.}

\item{model}{One of \code{"logistic"} (default) or \code{"gbm"}.
\code{"gbm"} requires the \pkg{gbm} package.}

\item{max_iter}{Maximum iterations for logistic regression.}
}
\value{
A numeric vector of propensity scores aligned to the rows of
\code{data} (after dropping NAs in \code{treatment} or
\code{covariates}); the \code{names} of the vector are the row names
of the retained rows.
}
\description{
Estimates the probability of treatment via logistic regression or
gradient boosting on a set of covariates.  Mirrors Python
\code{morie.matching.estimate_propensity_score}.
}
\examples{
\dontrun{
df <- data.frame(d = rbinom(200, 1, 0.4),
                 x1 = rnorm(200), x2 = rnorm(200))
ps <- morie_matching_estimate_propensity(df, "d", c("x1", "x2"))
}
}
\references{
Rosenbaum, P. R., & Rubin, D. B. (1983). The central role of
the propensity score in observational studies for causal effects.
\emph{Biometrika}, 70(1), 41--55.
}

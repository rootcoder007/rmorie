% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/esl_native.R
\name{morie_esl_em_gmm}
\alias{morie_esl_em_gmm}
\title{EM for a Gaussian mixture}
\usage{
morie_esl_em_gmm(
  X,
  k = 2,
  max_iter = 200L,
  tol = 1e-06,
  reg = 1e-06,
  seed = 0L
)
}
\arguments{
\item{X}{Data matrix, n by p (a vector is treated as one column).}

\item{k}{Number of components.}

\item{max_iter}{Maximum EM iterations.}

\item{tol}{Stop when the log-likelihood improves by less than this.}

\item{reg}{Ridge added to each covariance diagonal.}

\item{seed}{Seed for the k-means++ style initialisation.}
}
\value{
List with \code{pi}, \code{mu}, \code{sigma}, \code{resp}, \code{labels}, \code{loglik},
\code{loglik_path}, \code{n_iter}, \code{converged}, \code{aic}, \code{bic}.
}
\description{
Fits a k-component Gaussian mixture by expectation-maximisation. The
observed-data log-likelihood is checked to be non-decreasing at run time --
a decrease is a bug, not slow convergence. \code{reg} ridges each covariance
diagonal, which is what stops a component collapsing onto a single point
and driving the likelihood to infinity.
}
\examples{
set.seed(1)
x <- c(rnorm(200, -4, 0.5), rnorm(200, 4, 0.5))
fit <- morie_esl_em_gmm(x, k = 2)
sort(round(as.numeric(fit$mu), 1))
}
\references{
Hastie, T., Tibshirani, R., & Friedman, J. (2009). The Elements
of Statistical Learning (2nd ed.), Sec 8.5. Springer.
}

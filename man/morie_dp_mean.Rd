% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bounds_native.R
\name{morie_dp_mean}
\alias{morie_dp_mean}
\title{Differentially private mean of a bounded sample}
\usage{
morie_dp_mean(
  y,
  C = NULL,
  epsilon = 1,
  n = NULL,
  mechanism = c("laplace", "gaussian"),
  delta = 1e-06,
  alpha = 0.05,
  lower = NULL,
  noise = NULL
)
}
\arguments{
\item{y}{Numeric sample.}

\item{C}{Width of the clipping interval.}

\item{epsilon}{Privacy parameter, positive.}

\item{n}{Denominator for the sensitivity; defaults to \code{length(y)}.}

\item{mechanism}{"laplace" or "gaussian".}

\item{delta}{Used by the Gaussian mechanism only.}

\item{alpha}{Two-sided level.}

\item{lower}{Lower clipping bound.}

\item{noise}{Optional fixed perturbation, bypassing the draw.}
}
\value{
A list with \code{estimate}, \code{clipped_mean}, \code{clipping_bias},
\code{sensitivity}, \code{noise_scale}, \code{noise_sd}, \code{ci_lower}, \code{ci_upper},
\code{ci_naive_lower}, \code{ci_naive_upper}, \code{n_clipped}, \code{warnings}.
}
\description{
Clip to \eqn{\[lower, lower + C\]}, average, then perturb. Under
replace-one adjacency the sensitivity of the mean of \eqn{n} clipped
values is \eqn{\Delta = C/n}; Laplace noise \eqn{Lap(\Delta/\epsilon)}
gives \eqn{\epsilon}-differential privacy and Gaussian noise with
\eqn{\sigma = \Delta\sqrt{2\ln(1.25/\delta)}/\epsilon} gives
\eqn{(\epsilon, \delta)}-differential privacy.
}
\details{
Clipping is the only stage that introduces bias, and nothing
downstream removes it, so the bias is returned rather than folded
into the estimate. Supplying \code{noise} directly makes every returned
quantity deterministic, which is what the cross-language parity test
anchors against -- R and Python cannot be made to draw the same
Laplace variate, but everything else agrees exactly.

The width \code{C} must come from prior knowledge of the domain. Taking it
from the range of \code{y} makes it a function of the private data and
voids the guarantee; that case is flagged in \code{warnings}.
}
\references{
Dwork C, Roth A (2014) \emph{Found Trends Theor Comput
Sci} 9(3-4):211-487, Sec 3.3 and Thm A.1. Karwa V, Vadhan S (2017)
arXiv:1711.03908.
}
\examples{
V <- c(1, 2, 3, 4, 5, 6, 7, 8)
morie_dp_mean(V)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geron_ml_native.R
\name{morie_geron_beam_search}
\alias{morie_geron_beam_search}
\title{Beam search over a next-token scorer (Geron Ch 14, morie.fn hmbms)}
\usage{
morie_geron_beam_search(
  model,
  src,
  beam_width = 3,
  max_len = 10,
  eos = NULL,
  length_penalty = 0
)
}
\arguments{
\item{model}{Scorer function.}

\item{src}{Source, passed through.}

\item{beam_width}{Hypotheses kept per step.}

\item{max_len}{Token cap.}

\item{eos}{Optional 0-based end-of-sequence token.}

\item{length_penalty}{Exponent alpha in score / len^alpha for the final rank.}
}
\value{
List with \code{sequence}, \code{score}, \code{beams}, \code{scores}, \code{finished}.
}
\description{
\code{model(src, prefix)} returns log-probabilities over the vocabulary for
the token after \code{prefix} (a 0-based integer vector). The scorer is
checked for normalisation, because an unnormalised one turns beam
search into an arbitrary greedy walk. Emitted tokens are 0-based.
}
\examples{
lp <- log(c(0.5, 0.3, 0.2))
morie_geron_beam_search(function(s, prefix) lp, NULL, beam_width = 2, 
    max_len = 3)
}

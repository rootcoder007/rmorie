% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fzwm.R
\name{Swilmom}
\alias{Swilmom}
\alias{morie_fauzi_wilcoxon_moments}
\title{Mean and variance of the smoothed Wilcoxon signed rank statistic}
\usage{
Swilmom(n, gtheta = 0.5, projint = 1/3)

morie_fauzi_wilcoxon_moments(n, gtheta = 0.5, projint = 1/3)
}
\arguments{
\item{n}{Sample size.}

\item{gtheta}{\code{G(theta)}; 1/2 under the null.}

\item{projint}{\verb{int F^2(u + 2 theta) f(u) du}; 1/3 under the null.}
}
\value{
Named list with \code{mean}, \code{variance}, \code{se}, \code{projvar}, \code{n}, \code{method}.
}
\description{
Sec. 5.3.1. With \verb{Wtilde = n(n+1)/2 - sum_\{i<=j\} K(-(X_i+X_j)/(2h))},
\deqn{E_\theta(\tilde W) = \frac{n(n+1)}{2}\{G(\theta) + O(h^2)\},\quad V_\theta(\tilde W) = n(n+1)^2\Big\{\int F^2(u+2\theta)f(u)du - G^2(\theta) + O(h^2)\Big\},}{E(Wtilde) = n(n+1)/2 {G(theta) + O(h^2)}, V(Wtilde) = n(n+1)^2 {int F^2(u + 2 theta) f(u) du - G(theta)^2 + O(h^2)},}
with \code{G} the half-sum distribution function.
}
\details{
\code{Wtilde} is a U-statistic, which is why its variance has the \code{n(n+1)^2}
shape rather than the sign test's \code{n}: the leading term is \code{n} times the
variance of the FIRST PROJECTION, and each of the \code{choose(n+1, 2)} pairs
contributes through it.

Under \code{H0} with symmetric \code{F}, \code{G(0) = 1/2} and the projection integral is
\code{1/3}, giving the familiar \code{n(n+1)^2/12}. Those null values are the defaults.

Unlike the sign test, \code{Wtilde} is NOT distribution-free -- the book says so
plainly -- but its asymptotic moments under \code{H0} do not depend on \code{F}, which
is all Theorem 5.9 needs.

This module previously carried a copy of a Kolmogorov-Smirnov
implementation. It now computes the moments.

Verified against Maesono, Moriyama and Lu (2018), AISM 70(5):969-982
(arXiv:1610.02145), Sec. 3.
}
\examples{
Swilmom(n = 10)
}
\references{
Fauzi and Maesono (2023), Sec. 5.3.1; Maesono, Moriyama and Lu (2018), AISM 70:969-982.
}
\keyword{internal}

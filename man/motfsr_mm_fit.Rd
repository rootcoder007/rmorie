% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/motfsr_native.R
\name{motfsr_mm_fit}
\alias{motfsr_mm_fit}
\title{motfsr_mm_fit}
\usage{
motfsr_mm_fit(
  sequences,
  w,
  alphabet = NULL,
  theta0 = NULL,
  lambda0 = NULL,
  beta = 0.01,
  erasing = NULL,
  max_iter = 1000,
  tol = 1e-06,
  normalize_overlaps = TRUE,
  erase_by = "letter"
)
}
\arguments{
\item{sequences}{Passed to \code{motfsr_prepare}.}

\item{w}{A count; the body uses it as \code{seq_len(...)}.}

\item{alphabet}{Passed to \code{motfsr_prepare}.}

\item{theta0}{Optional; may be \code{NULL}. Iterated over elementwise, with \code{lapply}.}

\item{lambda0}{Optional; may be \code{NULL}. Coerced to numeric by the body, with
\code{as.numeric}.}

\item{beta}{Numeric; combined arithmetically in the body. Defaults to \code{0.01}.}

\item{erasing}{Optional; may be \code{NULL}. Iterated over elementwise, with \code{lapply}.}

\item{max_iter}{A count; the body uses it as \code{seq_len(...)}. Defaults to \code{1000}.}

\item{tol}{Coerced to numeric by the body, with \code{as.numeric}. Defaults to \code{1e-06}.}

\item{normalize_overlaps}{A flag; the body branches on it. Defaults to \code{TRUE}.}

\item{erase_by}{One of \code{"letter"}, \code{"start"}. Defaults to \code{"letter"}.}
}
\value{
A list with \code{theta}, \code{motif}, \code{background}, \code{lambda1},
\code{z}, \code{log_likelihood}, \code{log_likelihood_trace}, \code{n_iter},
\code{converged}, \code{alphabet}, \code{w}.
}
\description{
A step of the motfsr_native implementation. Called by \code{motfsr_run}.
See the file header for the source the module follows.
source it follows.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aaa_rangayyan_ar.R
\name{PzResp}
\alias{PzResp}
\title{Eqs (3.71)-(3.73): on the unit circle the magnitude is the product}
\usage{
PzResp(zeros, poles, omega, gain = 1)
}
\arguments{
\item{zeros}{Coerced to complex by the body, with \code{as.complex}.}

\item{poles}{Coerced to complex by the body, with \code{as.complex}.}

\item{omega}{Coerced to numeric by the body, with \code{as.numeric}.}

\item{gain}{Numeric; passed to \code{abs}. Defaults to \code{1}.}
}
\value{
A list with \code{H}, \code{magnitude}, \code{phase}, \code{zero_distances},
\code{pole_distances}, \code{omega}, \code{magnitude_matches_product}, \code{method}.
}
\description{
of distances to the zeros over the product of distances to the poles,
and the phase is (M-N) angle(z0) plus the zero angles minus the pole
angles.  A zero ON the circle sends one distance to zero -- a
spectral null; a pole near it makes one distance small -- a
resonance.  The distances are returned so that reading can be made.
}
\examples{
PzResp(zeros = c(1, 2, 3, 4, 5, 6, 7, 8), poles = c(1, 2, 3, 4, 5, 6, 7, 8),
  omega = c(1, 2, 3, 4, 5, 6, 7, 8))
}
\keyword{internal}

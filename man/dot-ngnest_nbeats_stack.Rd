% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ngnest_native.R
\name{.ngnest_nbeats_stack}
\alias{.ngnest_nbeats_stack}
\title{Delegate to the shared implementation, which is what the Python arm}
\usage{
.ngnest_nbeats_stack(window, H, blocks, ridge = 1e-08)
}
\arguments{
\item{window}{A vector; its length is taken.}

\item{H}{Passed to \code{nbeats_stack}.}

\item{blocks}{Passed to \code{nbeats_stack}.}

\item{ridge}{Passed to \code{nbeats_stack}. Defaults to \code{1e-08}.}
}
\value{
The value of \code{list}.
}
\description{
does (morie.fn.ngnest imports nbeats_stack from morie.fn.nbeats). The
local copy that used to live here normalised trend time by (lb - 1)
rather than lb and built the Fourier basis on max(H, lb) with 1-based
time, so its forecasts drifted from the spec.
}

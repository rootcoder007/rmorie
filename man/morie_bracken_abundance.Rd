% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/abundance_text_voting_native.R
\name{morie_bracken_abundance}
\alias{morie_bracken_abundance}
\title{Bracken Bayesian abundance re-estimation}
\usage{
morie_bracken_abundance(
  kraken_output,
  kmer_distribution,
  max_iter = 1000L,
  tol = 1e-12
)
}
\arguments{
\item{kraken_output}{Reads assigned to each classification node.}

\item{kmer_distribution}{P(node | species); columns sum to 1.}

\item{max_iter, tol}{Expectation-maximisation controls.}
}
\value{
A list with \code{estimate}, \code{fractions}, \code{reads_reassigned},
\code{iterations}, \code{converged}, \code{identifiable}, \code{log_likelihood}.
}
\description{
A k-mer classifier assigns each read to the lowest node whose k-mers
identify it uniquely, so reads from species sharing genome stop at a
common ancestor. Reading species counts straight off the classifier
therefore understates every species by a DIFFERENT amount, which is a
distorted ranking rather than a noisy one.
}
\details{
Bracken redistributes them by expectation-maximisation over
\eqn{P(j \mid i) \propto P(i \mid j)\theta_j}. What it cannot do is
invent information: species with identical k-mer columns leave the
likelihood flat along the direction trading one for the other, and
\code{identifiable} reports that rather than returning an arbitrary point
on a ridge.
}
\examples{
P <- rbind(c(0.6, 0, 0), c(0, 0.5, 0), c(0, 0, 0.4), c(0.4, 0.5, 
    0), c(0, 0, 0.6))
reads <- c(1200, 900, 700, 1500, 1100)
morie_bracken_abundance(reads, P)
}
\references{
Lu J, Breitwieser FP, Thielen P, Salzberg SL (2017)
\emph{PeerJ Comput Sci} 3:e104, \doi{10.7717/peerj-cs.104}.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/arsau_datasets.R
\name{morie_arsau_download}
\alias{morie_arsau_download}
\title{Bulk-download ARSAU CSVs from the Ontario Data Catalogue.}
\usage{
morie_arsau_download(
  target_dir,
  kinds = NULL,
  years = NULL,
  timeout_sec = 60L,
  quiet = FALSE
)
}
\arguments{
\item{target_dir}{Destination directory (created if missing).}

\item{kinds}{Optional character vector of \code{ARSAU_KINDS()} to
restrict the download.}

\item{years}{Optional vector of \code{ARSAU_YEARS()} entries.}

\item{timeout_sec}{Per-file download timeout (default 60).}

\item{quiet}{Suppress the progress message and per-file warnings.}
}
\value{
Invisibly, a list with \code{downloaded} (paths),
\code{skipped} (registry keys), and \code{target_dir}.
}
\description{
Native R port of the maintainer's \code{scripts/refresh_arsau.py}
mirror: walks the bundled \code{ARSAU_REGISTRY()} and pulls each
entry's datastore dump CSV from data.ontario.ca into
\code{target_dir}.  Entries without a published datastore resource
are reported in \code{$skipped}.  Filter with \code{kinds} /
\code{years} to fetch a subset (recommended; the full release is
large).
}
\examples{
\dontrun{
res <- try(morie_arsau_download(tempdir(), kinds = ARSAU_KINDS()[1],
                                years = ARSAU_YEARS()[1], quiet = TRUE))
if (!inherits(res, "try-error")) length(res$downloaded)
}
}

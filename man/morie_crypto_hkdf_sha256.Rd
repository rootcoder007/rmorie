% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crypto_sym.R
\name{morie_crypto_hkdf_sha256}
\alias{morie_crypto_hkdf_sha256}
\title{HKDF-SHA256 (RFC 5869) key derivation}
\usage{
morie_crypto_hkdf_sha256(ikm, length = 32L, salt = raw(0), info = raw(0))
}
\arguments{
\item{ikm}{Input keying material (raw vector).}

\item{length}{Output length in bytes (1..8160).}

\item{salt}{Optional salt raw vector. Empty -> zero-fill.}

\item{info}{Optional context/application info raw vector.}
}
\value{
Derived key material as raw vector of length \code{length}.
}
\description{
Phase 3JJJ1. Mirrors the Python
\verb{morie.crypto.hkdf_sha256(ikm, length=32, salt=b"", info=b"")}
byte-for-byte. Empty \code{salt} defaults to a 32-byte zero-filled
salt per RFC 5869 §2.2 (matches Python).
}
\examples{
if (morie_crypto_sodium_available()) {
  out <- morie_crypto_hkdf_sha256("seed", len = 32L, salt = "salt",
                                  info = "ctx")
  length(out)
}
}

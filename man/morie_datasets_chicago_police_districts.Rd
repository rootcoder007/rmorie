% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datasets.R
\name{morie_datasets_chicago_police_districts}
\alias{morie_datasets_chicago_police_districts}
\title{Chicago Police Districts (current) boundaries (\verb{24zt-jpfn})}
\usage{
morie_datasets_chicago_police_districts(
  offline = TRUE,
  geometry = FALSE,
  max_features = NULL,
  resource_id = NULL,
  mode = c("soda2", "soda3"),
  paginate = FALSE,
  page_size = 1000L,
  max_pages = 200L,
  app_token = NULL
)
}
\arguments{
\item{offline}{If \code{TRUE} (default), read the bundled fixture.}

\item{geometry}{If \code{TRUE} and \code{offline = FALSE}, include
\code{the_geom} (MultiPolygon).}

\item{max_features}{Optional row cap.}

\item{resource_id}{Optional Socrata resource id override.}

\item{mode}{One of \code{"soda2"} (default JSON resource endpoint) or
\code{"soda3"} (SoQL \code{query} endpoint). 3VV+ dual-mode dispatch.}

\item{paginate}{Logical; 3OO opt-in pagination.}

\item{page_size}{Per-page row count when paginating.}

\item{max_pages}{Safety net on paginated walks.}

\item{app_token}{Optional Socrata API app token for higher rate
limits; passed as the \code{X-App-Token} header.}
}
\value{
A \code{data.frame} with 2 attribute cols (offline) or 3
including \code{the_geom} (live, \code{geometry = TRUE}).
}
\description{
Wraps the City of Chicago "Boundaries - Police Districts (current)"
open dataset (Socrata resource id \verb{24zt-jpfn}; portal landing
\url{https://data.cityofchicago.org/Public-Safety/Boundaries-Police-Districts-current-/fthy-xz3r}).
Returns 22 active districts (1-12, 14-20, 22, 24, 25) plus the
special "31" headquarters polygon. Attribute schema:
}
\details{
\describe{
\item{dist_num}{District number (string, "1"-"31").}
\item{dist_label}{Display label (e.g. \code{"1ST"}, \code{"22ND"}).}
}

Offline mode reads a bundled attribute-only fixture
(\code{inst/extdata/chicago_police_districts.csv}). Live mode hits
SODA2 JSON; pass \code{geometry = TRUE} for \code{the_geom}.

Socrata exposes this dataset in all four format permutations
(SODA2 + SODA3, JSON + GeoJSON + CSV):
\itemize{
\item SODA2 JSON: \verb{/resource/24zt-jpfn.json}
\item SODA2 GeoJSON: \verb{/resource/24zt-jpfn.geojson}
\item SODA2 CSV: \verb{/resource/24zt-jpfn.csv}
\item SODA3 JSON: \verb{/api/v3/views/24zt-jpfn/query.json}
\item SODA3 GeoJSON: \verb{/api/v3/views/24zt-jpfn/query.geojson}
}

morie defaults to SODA2 JSON; pass an explicit URL via
\code{resource_id} to exercise the others (e.g. for direct sf reads
you'd typically hit the GeoJSON variant via \code{sf::st_read()}
yourself rather than going through this loader).
}
\examples{
df <- morie_datasets_chicago_police_districts(offline = TRUE)
head(df)
}
\references{
City of Chicago Data Portal, "Boundaries - Police
Districts (current)" (\verb{24zt-jpfn}).
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/esl_native.R
\name{morie_esl_cv_score}
\alias{morie_esl_cv_score}
\title{K-fold cross-validation}
\usage{
morie_esl_cv_score(
  X,
  y,
  model = NULL,
  k = 5L,
  loss = "mse",
  stratify = FALSE,
  seed = 0L
)
}
\arguments{
\item{X}{Predictors, n by p.}

\item{y}{Response.}

\item{model}{Function \verb{(X_tr, y_tr, X_te) -> predictions}; defaults to OLS.}

\item{k}{Number of folds, 2 to n.}

\item{loss}{"mse", "mae" or "01".}

\item{stratify}{Preserve class proportions per fold.}

\item{seed}{Seed for the fold shuffle.}
}
\value{
List with \code{cv}, \code{se}, \code{fold_scores}, \code{predictions}, \code{fold_id}.
}
\description{
\code{model} is any function \code{model(X_tr, y_tr, X_te)} returning predictions, so
this is independent of any particular estimator; the default is OLS with an
intercept. The reported \code{se} is across folds, which is what the
one-standard-error rule uses -- the folds share training data and so are
positively correlated, biasing it downward.
}
\examples{
set.seed(1)
X <- matrix(rnorm(300), ncol = 3)
y <- X \%*\% c(1, -2, 0.5) + rnorm(100, sd = 0.3)
morie_esl_cv_score(X, y, k = 5)$cv < stats::var(y)
}
\references{
Hastie, T., et al. (2009). ESL (2nd ed.), Sec 7.10. Springer.
}

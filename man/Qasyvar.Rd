% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fzavar.R
\name{Qasyvar}
\alias{Qasyvar}
\alias{morie_fauzi_quantile_asymp_var}
\title{Asymptotic variance of the quantile estimator}
\usage{
Qasyvar(p, n, density = NULL, qp = NULL)

morie_fauzi_quantile_asymp_var(p, n, density = NULL, qp = NULL)
}
\arguments{
\item{p}{Probability in \verb{(0, 1)}.}

\item{n}{Sample size.}

\item{density}{\code{f(F^-1(p))}.}

\item{qp}{\verb{Q'(p)}; equals \code{1 / density}.}
}
\value{
Named list with \code{variance}, \code{se}, \code{amse}, \code{sigma2}, \code{qp}, \code{n}, \code{method}.
}
\description{
Eqs. (3.2)-(3.3):
\deqn{V\[\hat Q(p)\] = \frac{p(1-p)}{n f^2(F^{-1}(p))} + O(n^{-2}),\qquad \mathrm{AMSE} = \frac{\[Q'(p)\]^2 p(1-p)}{n},}{V\[Qhat(p)\] = p(1-p)/(n f^2(F^-1(p))) + O(n^-2), AMSE = Q'(p)^2 p(1-p)/n,}
the second form using \verb{Q'(p) = 1 / f(Q(p))}.
}
\details{
Both spellings come back, because the equality between them is the thing
worth checking: a quantile's variance is the binomial variance \code{p(1-p)}
transported through the inverse-cdf map, and the density at the quantile is
the Jacobian. Where the density is small the quantile is badly determined --
the entire reason tail quantiles are hard, stated as arithmetic.

Remark 3.3 is blunt that the KERNEL quantile estimator has this same
first-order variance. Chapter 3's improvement is second-order only.

Supply \code{qp} (the quantile-function derivative) or \code{density} (the density at
the quantile), not both.
}
\examples{
Qasyvar(p = 0.5, n = 100, density = 0.4)
}
\references{
Fauzi and Maesono (2023), Eqs. (3.2)-(3.3).
}
\keyword{internal}

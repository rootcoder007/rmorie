% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aaa_rangayyan_tf.R
\name{Spectrogram}
\alias{Spectrogram}
\title{STFT spectrogram, eq (8.8); |STFT|^2 per the text after eq (8.9)}
\usage{
Spectrogram(x, fs = 1, nperseg = 64, noverlap = NULL, window = "hann")
}
\arguments{
\item{x}{Passed to \code{.tf_need}.}

\item{fs}{Numeric; combined arithmetically in the body. Defaults to \code{1}.}

\item{nperseg}{Coerced to integer by the body, with \code{as.integer}. Defaults to \code{64}.}

\item{noverlap}{Optional; may be \code{NULL}. Coerced to integer by the body, with
\code{as.integer}.}

\item{window}{Passed to \code{.tf_win}. Defaults to \code{"hann"}.}
}
\value{
A list with \code{spectrogram}, \code{stft}, \code{times}, \code{freqs},
\code{nperseg}, \code{hop}, \code{window}, \code{n_frames}, \code{total_energy},
\code{peak_freq}, \code{method}.
}
\description{
A step of the rangayyan_tf implementation. Called by \code{HrvTv}.
See the file header for the source the module follows.
source it follows.
}
\examples{
set.seed(5)
fs <- 200
x <- sin(2 * pi * 20 * seq(0, 2, by = 1 / fs)) + 0.1 * rnorm(401)
str(Spectrogram(x, fs = fs, nperseg = 64), max.level = 1)
}
\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causalw.R
\name{Snmlin}
\alias{Snmlin}
\title{G-estimation of a linear structural nested mean model}
\usage{
Snmlin(
  y,
  treatment_history,
  covariate_history = NULL,
  time = NULL,
  propensity = NULL
)
}
\arguments{
\item{y}{Outcomes.}

\item{treatment_history}{Subjects by time.}

\item{covariate_history, time}{Accepted for signature compatibility.}

\item{propensity}{\eqn{E[A|H]} for cumulative treatment; sample mean
if omitted (correct only under randomization).}
}
\value{
Named list with \code{estimate}, \code{psi}, \code{se}, \code{ci_lower},
\code{ci_upper}, \code{ols_slope}, \code{residual_treatment}, \code{n}, \code{method}.
}
\description{
The blipped-down outcome \eqn{Y - \psi \sum_t A_t} must be
conditionally independent of treatment given history, giving
\deqn{\psi = \frac{\sum_i (A_i - \bar A_i) Y_i}{\sum_i (A_i - \bar A_i) A_i}}{psi = sum (A-Abar)Y / sum (A-Abar)A}
Solved in one step -- the linear case needs no grid search over psi.
Under randomization with the sample mean as the propensity, psi
equals the OLS slope of Y on cumulative treatment exactly, which is
why \code{ols_slope} is returned next to it.
}
\examples{
Snmlin(c(2, 1, 4, 2.5), matrix(c(1, 0, 1, 0, 0, 0, 1, 1), nrow = 4))
}
\references{
Robins (1994) Commun Stat 23:2379-2412.
}

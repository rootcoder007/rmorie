% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gh_c10_5.R
\name{Ghosalwnadapt}
\alias{Ghosalwnadapt}
\title{Spike-and-slab white-noise adaptation}
\usage{
Ghosalwnadapt(y = NULL, n = 400, pi_incl = 0.2, tau2 = 1, seed = 42)
}
\arguments{
\item{y}{Numeric vector of observed coordinates; simulated when NULL.}

\item{n}{Precision (sample size) of each coordinate.}

\item{pi_incl}{Prior inclusion probability, in (0, 1).}

\item{tau2}{Slab variance.}

\item{seed}{Seed for the deterministic draw of \code{y}.}
}
\value{
List with \code{estimate} (first inclusion probability),
\code{inclusion_probs}, \code{method}.
}
\description{
theta_jk ~ pi N(0, tau^2) + (1 - pi) delta_0.  Because the slab is
conjugate the posterior inclusion probability of each coordinate is
available exactly, and it recovers a sparse truth: coordinates whose
observed value is large relative to the noise level 1/sqrt(n) get
inclusion probabilities near one, the rest near zero.
}
\details{
Formula: p_k = 1 / (1 + exp(l_0 - l_1)) with
l_1 = log(pi) + log phi(y_k; 0, 1/n + tau2),
l_0 = log(1 - pi) + log phi(y_k; 0, 1/n).
}
\references{
Ghosal & van der Vaart (2017), Fundamentals of
Nonparametric Bayesian Inference, CUP, section 10.3.2.
}
\examples{
Ghosalwnadapt()
}

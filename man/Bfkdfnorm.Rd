% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fzt53.R
\name{Bfkdfnorm}
\alias{Bfkdfnorm}
\alias{morie_fauzi_thm5_3_bdfree_normality}
\title{Asymptotic normality of the boundary-free KDFE (Theorem 5.3)}
\usage{
Bfkdfnorm(estimate, variance, null = NULL, bias = 0, level = 0.95)

morie_fauzi_thm5_3_bdfree_normality(
  estimate,
  variance,
  null = NULL,
  bias = 0,
  level = 0.95
)
}
\arguments{
\item{estimate}{\code{Ftilde(x)}.}

\item{variance}{\verb{Var\[Ftilde(x)\]}, e.g. from \code{Bfkdfbv}.}

\item{null}{The value of \code{F_X(x)} to test against.}

\item{bias}{Bias to subtract before standardising.}

\item{level}{Confidence level for the interval.}
}
\value{
Named list with \code{statistic}, \code{p_value}, \code{lower}, \code{upper}, \code{se}, \code{level}, \code{method}.
}
\description{
Theorem 5.3: under D1-D5,
\deqn{\frac{\tilde F_X(x) - F_X(x)}{\sqrt{\mathrm{Var}\[\tilde F_X(x)\]}} \to_D N(0,1).}{(Ftilde(x) - F(x)) / sqrt(Var\[Ftilde(x)\]) ->_D N(0,1).}
}
\details{
The proof is a Lyapunov argument, easy for a reason worth keeping: because
\verb{0 <= W(v) <= 1} for every \code{v}, the \code{(2+delta)} moment of each summand is
bounded by \code{2^(2+delta) < Inf} with NO assumption on \code{F_X}. The estimator
averages bounded variables, so Lyapunov's condition is automatic.

Returns the standardised statistic and a Wald interval for \code{F_X(x)}, clipped
to \verb{\[0,1\]}: the estimand is a probability, and an unclipped Wald interval
routinely leaves the unit interval near the boundary -- precisely the region
this construction exists to handle.

The CENTRING is \code{F_X(x)}, not \verb{E\[Ftilde(x)\]}. The theorem standardises by
the variance alone, so the interval inherits the \code{O(h^2)} bias of Theorem
5.2 and is not bias-corrected; supply \code{bias} to subtract it.
}
\examples{
Bfkdfnorm(estimate = 0.5, variance = 0.0025, null = 0.5)
}
\references{
Fauzi and Maesono (2023), Theorem 5.3.
}
\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mass_native.R
\name{morie_mvrnorm}
\alias{morie_mvrnorm}
\title{Native multivariate normal sampling (reproduces MASS::mvrnorm)}
\usage{
morie_mvrnorm(n = 1, mu, Sigma, tol = 1e-06, empirical = FALSE)
}
\arguments{
\item{n}{Number of samples.}

\item{mu}{Mean vector (length p).}

\item{Sigma}{p x p covariance matrix.}

\item{tol}{Tolerance for the positive-definiteness check.}

\item{empirical}{If TRUE, force the sample mean/covariance to match
\code{mu}/\code{Sigma} exactly (as in MASS).}
}
\value{
A length-p vector when \code{n == 1}, else an \code{n x p}
matrix.
}
\description{
Draws from \eqn{N(\mu, \Sigma)} using the symmetric eigen
decomposition, matching \code{MASS::mvrnorm} exactly -- including its
RNG consumption (\code{rnorm(p * n)} in the same order), so under a
common seed the draws are identical.
}
\examples{
if (requireNamespace("MASS", quietly = TRUE)) {
  set.seed(1)
  morie_mvrnorm(3, mu = c(0, 0), Sigma = diag(2))
}
}
\references{
Venables, W. N., & Ripley, B. D. (2002). \emph{Modern
Applied Statistics with S}. Springer.
}

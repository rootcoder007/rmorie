% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causdidwd_native.R
\name{morie_imputation}
\alias{morie_imputation}
\title{The two-step imputation estimator of Sec. 4}
\usage{
morie_imputation(Y, unit, period, first_treated, X = NULL)
}
\arguments{
\item{Y}{N-vector of outcomes.}

\item{unit, period}{N-vectors of panel identifiers.}

\item{first_treated}{N-vector of adoption periods (the period in
which each unit is first treated; \code{NULL} or \code{NA} for
never-treated).}

\item{X}{Optional N-by-p matrix of covariates.}
}
\value{
A named list mirroring the RichResult payload: \code{estimate},
\code{att}, \code{n_cells}, \code{n_untreated_used}, \code{coef},
\code{method}, \code{identical_to}.
}
\description{
Fit the two-way model on \emph{*untreated*} observations only, impute
the untreated potential outcome everywhere, and average the
residual within each \eqn{(g, t)}{(g, t)} cell.  Sec. 5 proves
this is numerically identical to \code{morie_etwfe}.
}
\examples{
set.seed(1)
nu <- 4; T <- 5
unit <- rep(1:nu, each = T)
period <- rep(1:T, nu)
ft_unit <- c(NA, NA, 4, 4)
first_treated <- rep(ft_unit, each = T)
treated_now <- !is.na(first_treated) & period >= first_treated
Y <- rnorm(nu * T) + treated_now * 2
morie_imputation(Y, unit, period, first_treated)
}
\references{
Wooldridge (2025) Sec. 4.
}
\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datasets_tps_arcgis_hub.R
\name{morie_datasets_toronto_zoning_per_neighbourhood}
\alias{morie_datasets_toronto_zoning_per_neighbourhood}
\title{Toronto Zoning per Neighbourhood (EsriCanadaEducation)}
\usage{
morie_datasets_toronto_zoning_per_neighbourhood(
  layer = c("neighbourhoods", "zoning_stats"),
  format = "json",
  where = "1=1",
  max_features = NULL,
  offline = TRUE
)
}
\arguments{
\item{layer}{One of \code{"neighbourhoods"} (default, polygon
demographics) or \code{"zoning_stats"} (per-zone area table).}

\item{format}{One of \code{"json"} (default), \code{"geojson"}, \code{"csv"},
\code{"shapefile"}, \code{"fgdb"}. Only honoured when \code{offline = FALSE}.}

\item{where}{Optional FeatureServer WHERE filter (live mode).}

\item{max_features}{Optional row cap.}

\item{offline}{Logical; if \code{TRUE} (default), read the bundled
synthetic fixture.}
}
\value{
A \code{data.frame} (json / csv / offline), parsed GeoJSON
list, or file path (binary).
}
\description{
Wraps EsriCanadaEducation's ArcGIS Online Feature Service
\code{ZonesofToronto_Neighbourhoods}
(item id \code{af06159170914808983959df6163fc86}; FeatureServer at
\code{services.arcgis.com/As5CFN3ThbQpy8Ph/.../ZonesofToronto_Neighbourhoods/FeatureServer}).
Two layers in the service:
}
\details{
\describe{
\item{\code{layer = "neighbourhoods"} (FeatureServer layer 0)}{Polygon
boundaries for Toronto neighbourhoods with a 39-column
demographic schema -- total population, sex split, 18 age
brackets (0-4 through 85+), senior + youth + child aggregates,
and 10 specific language counts (Chinese, Italian, Korean,
Persian, Portuguese, Russian, Spanish, Tagalog, Tamil, Urdu)
plus a \code{HomeLanguageCategory} total.}
\item{\code{layer = "zoning_stats"} (FeatureServer table 1)}{Per-
neighbourhood zoning-area stats -- 4 columns (\code{OBJECTID},
\code{ZoneDesc}, \code{Neighbourhood_Name}, \code{SUM_Area}). Many rows per
neighbourhood, one per \code{ZoneDesc} (Commercial, Residential,
Industrial, etc.).}
}

Offline mode reads bundled 5-row synthetic fixtures
(\code{toronto_zoning_neighbourhoods_sample.csv} /
\code{toronto_zoning_stats_sample.csv}) -- SYNTH-stamped, not
attributable to actual Toronto neighbourhoods. Live mode hits
the FeatureServer via the 3SS+ generic
\code{\link[=morie_datasets_arcgis_item_by_id]{morie_datasets_arcgis_item_by_id()}} resolver.
}
\examples{
df <- morie_datasets_toronto_zoning_per_neighbourhood(offline = TRUE)
head(df[, c("Neighbourhood", "Total_Population", "Seniors65andover")])
}
\references{
Esri Canada Education -- ArcGIS Online item
\code{af06159170914808983959df6163fc86}.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lstmc.R
\name{morie_lstmc_lstm_cell}
\alias{morie_lstmc_lstm_cell}
\alias{morie_lstm_cell}
\title{LSTM cell forward pass}
\usage{
morie_lstmc_lstm_cell(
  x,
  h_prev = NULL,
  c_prev = NULL,
  W = NULL,
  U = NULL,
  b = NULL,
  hidden_size = NULL,
  seed = 0L,
  deterministic_seed = NULL
)

morie_lstm_cell(
  x,
  h_prev = NULL,
  c_prev = NULL,
  W = NULL,
  U = NULL,
  b = NULL,
  hidden_size = NULL,
  seed = 0L,
  deterministic_seed = NULL
)
}
\arguments{
\item{x}{Numeric vector (input).}

\item{h_prev, c_prev}{Previous hidden / cell states (default zeros).}

\item{W, U, b}{Optional weight matrices and bias.}

\item{hidden_size}{Hidden size \code{H}.}

\item{seed}{RNG seed for default weights.}

\item{deterministic_seed}{Optional integer; if non-NULL, a SHA-keyed
seed from \code{\link{morie_det_rng}("lstmc", deterministic_seed)} is
installed before sampling so Py<->R streams agree.  Overrides
\code{seed} when set.}
}
\value{
Named list \code{(h, c, estimate, i, f, g, o, method)}.
}
\description{
R parity for \code{morie.fn.lstmc.lstm_cell}.  Gates stacked as
\code{[W_i; W_f; W_g; W_o]}.
}
\details{
\deqn{f, i, o = \sigma(\ldots); \quad g = \tanh(\ldots); \quad
      c = f \odot c_\text{prev} + i \odot g; \quad
      h = o \odot \tanh(c)}{f, i, o = sigma(...); g = tanh(...); c = f odot c_prev + i odot g; h = o odot tanh(c)}
}
\examples{
morie_lstmc_lstm_cell(x = rnorm(50))
}
\references{
Hochreiter & Schmidhuber (1997), Neural Computation 9(8).
}
\keyword{internal}

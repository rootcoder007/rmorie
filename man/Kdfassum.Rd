% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fzb1b5.R
\name{Kdfassum}
\alias{Kdfassum}
\alias{morie_fauzi_assumptions_b1_b5}
\title{Check assumptions B1-B5 of the bias-reduced KDFE}
\usage{
Kdfassum(
  kernel = NULL,
  h = NULL,
  n = NULL,
  smooth = NA,
  tol = 1e-06,
  lo = -8,
  hi = 8,
  ngrid = 4001L
)

morie_fauzi_assumptions_b1_b5(
  kernel = NULL,
  h = NULL,
  n = NULL,
  smooth = NA,
  tol = 1e-06,
  lo = -8,
  hi = 8,
  ngrid = 4001L
)
}
\arguments{
\item{kernel}{\code{K(w)}; defaults to the Gaussian density.}

\item{h}{Bandwidth, for B3.}

\item{n}{Sample size, for B3.}

\item{smooth}{The caller's assertion of B4; there is no way to check it.}

\item{tol}{Tolerance for the B1 symmetry and unit-mass checks.}

\item{lo, hi, ngrid}{Fixed quadrature window and node count.}
}
\value{
Named list with \code{b1}, \code{b2}, \code{b3}, \code{b4}, \code{b5}, \code{mu4}, \code{mass}, \code{method}.
}
\description{
The five standing assumptions of Sec. 2.2, checked numerically rather than
asserted:
}
\details{
\itemize{
\item B1: \code{K} non-negative, continuous, symmetric about 0, integrating to 1.
\item B2: \verb{int w^4 K(w) dw} finite.
\item B3: \code{h > 0}, \code{h -> 0}, \verb{n h -> inf}; checked in its usable finite-sample
form, \verb{0 < h < 1} and \verb{n h > 1}.
\item B4: \code{f_X} three times continuously differentiable with \code{f^(4)} existing --
a smoothness claim about the unknown density that NO routine can verify
from data. Reported as \code{NA}; assert it via \code{smooth}.
\item B5: \verb{int [f'(x)]^2 / F(x) dx} and \verb{int f(x) dx} finite.
}

The book is explicit about the division of labour: B1 and B3 are the usual
kernel conditions, B2 and B4 exist only to make the exponential and
logarithmic expansions legitimate, and B5 only so the MISE of Theorem 2.4
is finite. A failure of B2 or B4 invalidates the bias RATE; a failure of B5
invalidates the MISE but leaves the pointwise results standing. Hence they
are reported separately, not as one boolean.
}
\examples{
Kdfassum(h = 0.1, n = 100)
}
\references{
Fauzi and Maesono (2023), assumptions B1-B5 of Sec. 2.2.
}
\keyword{internal}

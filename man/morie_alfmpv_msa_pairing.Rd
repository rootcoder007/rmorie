% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alfmpv_native.R
\name{morie_alfmpv_msa_pairing}
\alias{morie_alfmpv_msa_pairing}
\title{MSA pairing for a protein complex}
\usage{
morie_alfmpv_msa_pairing(
  msas,
  mode = "multimer",
  min_coverage = 0.5,
  max_gap = 0.9,
  copies = NULL,
  max_pairs = NULL
)
}
\arguments{
\item{msas}{one entry per chain: either a plain vector of species
identifiers, or a list with a species element and any of evalue,
identity, gaps and coverage as parallel vectors.}

\item{mode}{multimer, colabfold or folddock.}

\item{min_coverage}{colabfold only: least fraction of the query the
alignment must cover. 0.5 is the published value.}

\item{max_gap}{folddock only: most gaps a hit may be, as a fraction.
0.9 is the published value.}

\item{copies}{copy count per chain for a homo-oligomer. ColabFold
copies the alignment per component rather than searching again, so
the copies share one chain's hits.}

\item{max_pairs}{stop after this many paired rows.}
}
\value{
a list with paired, species_paired, unpaired, n_paired,
n_unpaired, n_rows, n_chains, chain_source, n_filtered, mode,
pairing_rule and method.
}
\description{
MSA pairing for a protein complex
}
\examples{
V <- c(1, 2, 3, 4, 5, 6, 7, 8)
morie_alfmpv_msa_pairing(V)
}
\keyword{internal}

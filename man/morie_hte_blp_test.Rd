% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causal_forest_honest.R
\name{morie_hte_blp_test}
\alias{morie_hte_blp_test}
\title{Best-linear-predictor test for treatment-effect heterogeneity}
\usage{
morie_hte_blp_test(y, d, cate_predictions, propensity = NULL)
}
\arguments{
\item{y}{Outcome.}

\item{d}{Binary 0/1 treatment.}

\item{cate_predictions}{Out-of-bag CATE estimates.}

\item{propensity}{Treatment probabilities; default \code{mean(d)}.}
}
\value{
List with \code{alpha}, \code{beta}, \code{se_beta}, \code{p_value},
\code{heterogeneous}, \code{n}.
}
\description{
Regress the demeaned outcome on the residualised treatment and its
interaction with the centred CATE predictions. \code{alpha} near 1 says
the forest's average effect is calibrated; \code{beta} is the
heterogeneity coefficient, and \code{beta = 0} means the predicted
variation carries no signal. Pass out-of-bag predictions -- in-bag
ones inflate \code{beta} mechanically. Mirrors \code{morie.fn.crfhte}.
}
\examples{
set.seed(2)
n <- 100
X <- matrix(rnorm(n * 2), n, 2)
d <- rbinom(n, 1, 0.5)
y <- 1 + X[, 1] + d * (0.5 + X[, 2]) + rnorm(n, 0, 0.3)
tau <- 0.5 + X[, 2] + rnorm(n, 0, 0.2)
morie_hte_blp_test(y, d, tau)
}
\references{
Chernozhukov V, Demirer M, Duflo E, Fernandez-Val I
(2018). Generic machine learning inference on heterogenous
treatment effects in randomized experiments. arXiv:1712.04802.
}

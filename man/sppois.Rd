% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sppois.R
\name{sppois}
\alias{sppois}
\title{Homogeneous Poisson process (HPP), the CSR reference model.}
\usage{
sppois(lam = 1, region = NULL, seed = NULL)
}
\arguments{
\item{lam}{Intensity, events per unit area; must be positive.}

\item{region}{c(xmin, ymin, xmax, ymax) or a matrix of vertices; the
unit square when NULL.}

\item{seed}{Optional seed for the realisation.}
}
\value{
Named list: points, n, lambda, area, expected_n, var_n
(equal to expected_n, as the Poisson requires), region.
}
\description{
Two defining properties (Sec 3.2.2): N(A) ~ Poisson(lambda nu(A)) for
any region A, and counts in DISJOINT regions are independent.
Conditional on N(A) = n the events are independently uniform over A,
which is exactly the binomial process of Sec 3.2.1 and is how an HPP
is simulated.
}
\examples{
sppois(lam = 2, region = c(0, 0, 10, 10), seed = 1)$expected_n
}
\references{
Schabenberger & Gotway (2005), Sec 3.2.2.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forwsr_native.R
\name{morie_forwsr}
\alias{morie_forwsr}
\title{Forward search regression: run the search and report where it jumps}
\usage{
morie_forwsr(
  X,
  y,
  start = NULL,
  n_draw = 500L,
  seed = 1L,
  threshold = 3,
  min_df = 5L
)
}
\arguments{
\item{X}{Design matrix; supply your own intercept column.}

\item{y}{Response.}

\item{start}{Starting subset (0-based) or NULL for the LMS start.}

\item{n_draw}{Draws for the LMS start.}

\item{seed}{SplitMix64 seed.}

\item{threshold}{Deletion-residual threshold.}

\item{min_df}{Residual degrees of freedom before the rule applies.}
}
\value{
List with \code{estimate}, \code{coefficients}, \code{steps},
\code{n}, \code{n_flagged}, \code{flagged}, \code{jump_at},
\code{method}.
}
\description{
SPDX-License-Identifier: AGPL-3.0-or-later morie.fn -- function file (rootcoder007/morie) Sources:   Analysis, Springer, ISBN 978-0-387-95017-5,   least-median-of-squares start, the residual ordering that defines   each subset, and the monitoring of the deletion residuals.   unknown number of multivariate outliers", Journal of the Royal   Statistical Society Series B 71(2), 447-454 -- equation (12), the   consistency factor for a scale estimated from a truncated sample, Native implementation mirroring Python morie.fn.forwsr exactly: the same Gauss-Jordan solve with partial pivoting, the same SplitMix64 draws for the LMS start, the same residual ordering, and the same consistency correction. Row indices are 0-based here, as they are in the Python arm; R subscripts add one at the point of use.
}
\details{
\code{threshold} is on the minimum deletion residual; the units
flagged are those entering after the first exceedance -- candidates,
not verdicts. \code{min_df} holds the rule back until the subset has
that many residual degrees of freedom.
}
\examples{
V <- c(1, 2, 3, 4, 5, 6, 7, 8)
morie_forwsr(V, V)
}
\references{
Atkinson and Riani (2000); Riani, Atkinson and Cerioli
(2009) doi:10.1111/j.1467-9868.2008.00692.x.
}
\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aaa_rangayyan_class.R
\name{KFoldCv}
\alias{KFoldCv}
\title{Section 10.10.3.  The book\'s point is that the training and test}
\usage{
KFoldCv(X, y, k = 5, classifier = NULL, stratified = TRUE)
}
\arguments{
\item{X}{A matrix; passed to \code{as.matrix}.}

\item{y}{A vector; its length is taken and its elements indexed.}

\item{k}{Coerced to integer by the body, with \code{as.integer}. Defaults to \code{5}.}

\item{classifier}{Optional; may be \code{NULL}. Passed to \code{is.null}.}

\item{stratified}{A flag; the body branches on it. Defaults to \code{TRUE}.}
}
\value{
A list with \code{error_rate}, \code{accuracy}, \code{errors}, \code{n},
\code{k}, \code{per_fold}, \code{stratified}, \code{train_and_test_must_be_separate},
\code{method}.
}
\description{
steps must use SEPARATE data: an error rate measured on the samples
that trained the classifier is optimistic, and with enough free
parameters it reaches zero while the classifier generalizes not at
all.  Folds are stratified by default; unstratified folds on
unbalanced data can leave a class absent from a training fold, which
measures luck rather than generalization.
}
\examples{
V <- c(1, 2, 3, 4, 5, 6, 7, 8)
KFoldCv(V, V)
}
\keyword{internal}

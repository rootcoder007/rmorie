% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lsa_native.R
\name{cosine_ranking}
\alias{cosine_ranking}
\title{cosine_ranking}
\usage{
cosine_ranking(q_hat, model, top_k = 5)
}
\arguments{
\item{q_hat}{A vector; indexed elementwise.}

\item{model}{A list; the body reads \code{$D}, \code{$S} from it.}

\item{top_k}{Coerced to integer by the body, with \code{as.integer}. Defaults to \code{5}.}
}
\value{
A list with \code{ranking}, \code{scores}, \code{n_documents}.
}
\description{
A step of the lsa_native implementation. Called by \code{morie_lsa}.
See the file header for the source the module follows.
source it follows.
}
\examples{
X <- matrix(c(2, 0, 1, 0, 3, 1, 1, 1, 0, 0, 2, 1), nrow = 3, byrow = TRUE)
model <- lsa_decompose(X, k_dim = 2)
q_hat <- fold_in(c(1, 0, 2), model)
r <- cosine_ranking(q_hat, model, top_k = 3)
r$n_documents
}
\keyword{internal}

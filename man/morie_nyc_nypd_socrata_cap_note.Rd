% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datasets_nyc_nypd.R
\name{morie_nyc_nypd_socrata_cap_note}
\alias{morie_nyc_nypd_socrata_cap_note}
\title{Socrata default-API-cap note + pagination wiring}
\description{
All NYC OpenData SODA2 endpoints apply a default cap of 1,000 rows
per request unless an explicit \verb{$limit} (or \verb{$$app_token} for
authenticated requests) is supplied. For the NYPD CJ datasets
wrapped here that means:
}
\details{
\itemize{
\item \code{morie_datasets_nyc_nypd_arrests_ytd(offline = FALSE)} returns
\strong{only 1,000 rows} by default, even though the live feed
carries ~69,300 rows.
\item Pass \code{max_features = N} to lift the single-request cap to N
rows (Socrata enforces a hard server-side cap of 50,000 rows
per request).
\item \strong{Pagination (wired in 3OO).} For full pulls over the cap,
pass \code{paginate = TRUE}. morie walks SODA2 \verb{$offset} in
\code{page_size}-row chunks until the server returns a short page
(exhausted) or \code{max_features} is reached. Without an app_token
the per-request ceiling is 1,000 rows so \code{page_size = 1000} is
the default; with \code{page_size = 50000} + \code{app_token} you can
pull the full ~69K-row arrests_ytd feed in two requests.
\code{max_pages} (default 200) is a safety net against runaway pulls.
}

Worked example:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# Full live pull of the YTD arrests feed (~69K rows over ~70 pages).
df <- morie_datasets_nyc_nypd_arrests_ytd(
  offline = FALSE, paginate = TRUE)

# First 5,000 rows only (5 paged requests of 1,000 each).
df <- morie_datasets_nyc_nypd_arrests_ytd(
  offline = FALSE, paginate = TRUE, max_features = 5000L)
}\if{html}{\out{</div>}}

The bundled fixtures (offline mode) are unaffected -- they ship 5
rows each as deterministic sample data, and \code{max_features} simply
truncates the fixture.
}

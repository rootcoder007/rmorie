% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Wvar.R
\name{Wvar}
\alias{Wvar}
\title{Weighted variance and weighted mean}
\usage{
Wvar(y, weights = NULL)
}
\arguments{
\item{y}{Numeric observations.}

\item{weights}{Optional non-negative weights; equal weights if NULL.}
}
\value{
List with \code{estimate} (weighted variance), \code{mean},
\code{sd}, \code{se}, \code{sumw}, \code{n}, \code{method}.
}
\description{
The weights are reliability weights: an observation of weight w counts
as w observations, so the denominator is sum(w) - 1 rather than n - 1.
With every weight equal to one this is exactly the unbiased sample
variance.
}
\details{
Formula: ybar_w = sum(w y) / sum(w),
s2_w = sum(w (y - ybar_w)^2) / (sum(w) - 1).
}
\examples{
Wvar(c(1, 2, 3, 10))
}
\references{
Lohr, S. L. (2010). Sampling: Design and Analysis, 2nd ed.
Brooks/Cole, section 7.2.
}

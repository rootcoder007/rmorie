% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/desigeff.R
\name{Desigeff}
\alias{Desigeff}
\title{Design effect of unequal weighting and clustering}
\usage{
Desigeff(y, weights = NULL, cluster = NULL)
}
\arguments{
\item{y}{Survey variable, length n.}

\item{weights}{Sampling weights, or NULL for equal weights.}

\item{cluster}{Cluster (PSU) identifier per unit, or NULL.}
}
\value{
List with \code{estimate} (DEFF), \code{deff_w},
\code{deff_c}, \code{rho}, \code{m0}, \code{n_eff}, \code{n},
\code{method}.
}
\description{
Formula: DEFF = Var_complex / Var_SRS
}
\details{
Decomposed the way Kish does it, into the unequal-weighting effect
DEFF_w = n sum w_i^2 / (sum w_i)^2 and the clustering effect
DEFF_c = 1 + (m0 - 1) rho, where rho is the one-way ANOVA intraclass
correlation and m0 the Kish average cluster size.  The reported DEFF
is their product.  Equal weights give DEFF_w = 1 exactly, and
singleton clusters give DEFF_c = 1 exactly.
}
\references{
Kish (1965), Survey Sampling, Wiley, sections 8.2 and 5.4.
}
\examples{
V <- c(1, 2, 3, 4, 5, 6, 7, 8)
Desigeff(V)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/contse.R
\name{Contse}
\alias{Contse}
\title{SimCSE contrastive sentence embedding}
\usage{
Contse(sentences, tau = 0.05, dropout = 0.1, seed = 42)
}
\arguments{
\item{sentences}{An n x d matrix of sentence embeddings.}

\item{tau}{Temperature, strictly positive.}

\item{dropout}{Dropout rate in [0, 1) used to build the two views.}

\item{seed}{Seed of the deterministic stream.}
}
\value{
List with \code{estimate}, \code{loss}, \code{per_item},
\code{alignment}, \code{uniformity}, \code{n}, \code{d},
\code{method}.
}
\description{
Formula: InfoNCE on dropout-augmented pairs
}
\details{
l_i = -log exp(sim(h_i, h_i+)/tau) / sum_j exp(sim(h_i, h_j+)/tau).
The positive is the SAME sentence encoded twice under different
dropout masks, so the objective needs no labelled pairs at all.  When
every similarity is identical the loss is exactly log N, the value it
must return on a degenerate batch.
}
\references{
Gao, Yao & Chen (2021), SimCSE, EMNLP 2021:6894-6910.
}
\examples{
M <- matrix(c(1, 2, 3, 4, 5, 6), nrow = 2)
Contse(M)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ml.R
\name{morie_ml_apply_smote}
\alias{morie_ml_apply_smote}
\title{Apply SMOTE oversampling to balance a binary outcome}
\usage{
morie_ml_apply_smote(X, y, random_state = 42L, k_neighbors = NULL)
}
\arguments{
\item{X}{Feature data.frame.}

\item{y}{Binary outcome vector (numeric or factor).}

\item{random_state}{Integer seed for the random fallback. Default 42.}

\item{k_neighbors}{Integer or NULL. SMOTE neighbour count; auto-picked
to \code{min(5, minority - 1)} when NULL.}
}
\value{
list(X, y, status) where \code{status} mirrors the Python dict
keys (\code{method}, \code{minority_before}, \code{majority_before},
\code{imbalance_ratio_before}, \code{total_before}, \code{total_after}, plus
\verb{class_<label>_before} / \verb{class_<label>_after}).
}
\description{
R port of \code{morie.ml.apply_smote}. Uses \code{smotefamily::SMOTE} when
installed and feasible; falls back to random oversampling (duplicate
minority rows) otherwise. Returns the resampled \verb{(X, y)} together
with a status list of before/after counts and the method used.
}

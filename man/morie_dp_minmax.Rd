% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dp_native2.R
\name{morie_dp_minmax}
\alias{morie_dp_minmax}
\title{Private substitute for a range}
\usage{
morie_dp_minmax(x, epsilon = 1, a = NULL, b = NULL, alpha = 0.01, seed = NULL)
}
\arguments{
\item{x}{values.}

\item{epsilon}{total budget, split evenly between the two quantiles.}

\item{a, b}{bounds, as for \code{\link{morie_dp_quantile}}.}

\item{alpha}{tail fraction, in (0, 0.5).}

\item{seed}{optional integer seed.}

}
\value{
list with \code{lower}, \code{upper}, \code{epsilon_each},
\code{true_min}, \code{true_max}.
}
\description{
The alpha and 1-alpha quantiles, each spending half the budget
through \code{\link{morie_dp_quantile}}.
}
\details{
There is no private minimum or maximum. Each extreme is determined by
exactly one record, so releasing it releases that record. This
function returns inner quantiles and says so in \code{warnings};
treating them as the range is the error it exists to prevent.
}
\examples{
set.seed(1)
r <- morie_dp_minmax(rnorm(500), epsilon = 2, a = -4, b = 4)
r$lower < r$upper
}
\references{
Smith, A. (2011). \emph{STOC}, 813-822.
}

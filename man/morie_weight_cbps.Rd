% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/weight_native.R
\name{morie_weight_cbps}
\alias{morie_weight_cbps}
\title{Covariate-balancing propensity score (Imai & Ratkovic 2014)}
\usage{
morie_weight_cbps(
  data,
  treatment,
  covariates,
  estimand = "ATE",
  max_iter = 100L,
  tol = 1e-10
)
}
\arguments{
\item{data}{Data frame.}

\item{treatment}{Binary treatment column (0/1).}

\item{covariates}{Covariate column names.}

\item{estimand}{"ATE" (default), "ATT", or "ATC".}

\item{max_iter}{Maximum damped-Newton iterations. Default 100.}

\item{tol}{Convergence tolerance on the balance-moment norm.
Default 1e-10.}
}
\value{
A \code{morie_weight} object.
}
\description{
Native just-identified CBPS: solves the covariate-balance moment
conditions \eqn{E[(T - p(X)) X / (p(X)(1-p(X)))] = 0} directly
(the exactly-identified estimator), via Newton iterations on the
logistic index. Balance is thus built into the score rather than
checked after the fact.
}
\examples{
d <- data.frame(t = rbinom(120, 1, 0.5), x = rnorm(120))
morie_weight_cbps(d, "t", "x")
}
\references{
Imai & Ratkovic (2014) JRSS-B 76(1).
}

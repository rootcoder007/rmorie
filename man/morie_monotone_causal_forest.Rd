% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causal_forest_honest.R
\name{morie_monotone_causal_forest}
\alias{morie_monotone_causal_forest}
\title{Causal forest with an isotonic monotonicity constraint on the CATE}
\usage{
morie_monotone_causal_forest(
  y,
  d,
  x,
  monotone_feature = NULL,
  direction = 1L,
  n_trees = 200L,
  min_leaf = 10L,
  seed = 0L
)
}
\arguments{
\item{y, d, x}{As in \code{morie_causal_forest()}.}

\item{monotone_feature}{Column index the effect must be monotone in;
\code{NULL} returns the unconstrained forest.}

\item{direction}{1 for nondecreasing, -1 for nonincreasing.}

\item{n_trees, min_leaf, seed}{Forest hyperparameters.}




}
\value{
List with \code{cate}, \code{cate_raw}, \code{monotone_feature}, \code{direction},
\code{violations_before}, \code{violations_after}, \code{n}.
}
\description{
When theory says the effect can only move one way in a covariate, the
unconstrained forest's wiggles are noise. The fitted CATE is projected
onto the monotone functions of that covariate by pool-adjacent-
violators, the exact L2 isotonic projection. Mirrors \code{morie.fn.htgcrf}.
}
\examples{
set.seed(4)
n <- 120
X <- matrix(rnorm(n * 2), n, 2)
d <- rbinom(n, 1, 0.5)
y <- 1 + X[, 1] + d * (0.5 + 0.5 * X[, 1]) + rnorm(n, 0, 0.3)
r <- morie_monotone_causal_forest(y, d, X, monotone_feature = 1L,
                                  n_trees = 20L)
str(r, max.level = 1)
}
\references{
Robertson T, Wright FT, Dykstra RL (1988). \emph{Order
Restricted Statistical Inference}. Wiley.
}

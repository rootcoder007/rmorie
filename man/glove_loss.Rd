% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/glove_native.R
\name{glove_loss}
\alias{glove_loss}
\title{Eq. (8), evaluated over the nonzero entries only}
\usage{
glove_loss(X, W, Wt, b, bt, x_max = 100, alpha = 0.75)
}
\arguments{
\item{X}{A list; the body reads \code{$count}, \code{$i}, \code{$j} from it.}

\item{W}{A matrix; indexed by row and column.}

\item{Wt}{A matrix; indexed by row and column.}

\item{b}{A vector; indexed elementwise.}

\item{bt}{A vector; indexed elementwise.}

\item{x_max}{Passed to \code{glove_weight}. Defaults to \code{100}.}

\item{alpha}{Passed to \code{glove_weight}. Defaults to \code{0.75}.}
}
\value{
The value of \code{total}, as built in the body.
}
\description{
A step of the glove_native implementation. Called by \code{morie_glove}.
See the file header for the source the module follows.
source it follows.
}
\examples{
set.seed(1)
X <- data.frame(i = c(1, 2, 1), j = c(2, 1, 3), count = c(5, 3, 2))
W <- matrix(rnorm(6), 3, 2)
Wt <- matrix(rnorm(6), 3, 2)
b <- rnorm(3)
bt <- rnorm(3)
glove_loss(X, W, Wt, b, bt)
}
\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/caus_native.R
\name{morie_caus_iv_liml}
\alias{morie_caus_iv_liml}
\title{Limited-information maximum likelihood}
\usage{
morie_caus_iv_liml(y, X, Z, fuller = NULL, endog = NULL)
}
\arguments{
\item{y}{outcome.}

\item{X}{regressors.}

\item{Z}{full instrument set.}

\item{fuller}{optional Fuller \code{a}, subtracting \code{a/(n - m)} from
\code{kappa} to restore finite moments.}

\item{endog}{optional column indices of \code{X} that are endogenous;
detected by matching against \code{Z} when \code{NULL}.}
}
\value{
list: beta, se, kappa, kappa_minus_one, just_identified,
equals_2sls, endogenous_columns, fuller_a,
n_overid_restrictions, n, k, m, method.
}
\description{
The k-class estimator whose \code{k} is the Anderson-Rubin variance
ratio: \code{kappa} is the smallest eigenvalue of
\verb{(Ybar' M_\[W,Z\] Ybar)^-1 (Ybar' M_W Ybar)}, with \code{Ybar} holding
\code{y} and the ENDOGENOUS regressors only.
}
\details{
Two facts make this checkable. \code{kappa >= 1} always, and when the
model is EXACTLY identified \code{kappa = 1} and LIML coincides with
2SLS to machine precision. An implementation that does not
reproduce 2SLS in the just-identified case is wrong, and nothing
else about it needs checking first.

\code{Ybar} must exclude the exogenous columns. An intercept there is
annihilated exactly by \code{M_W}, which makes the matrix singular,
drives \code{kappa} to zero, and silently turns the k-class estimator
back into least squares -- a finite, plausible, entirely wrong
number.

Where LIML and 2SLS differ is with many or weak instruments: 2SLS
is biased toward least squares and the bias grows with the
instrument count, while LIML is approximately median-unbiased
there at the cost of heavier tails.
}
\examples{
Z <- matrix(stats::rnorm(100), 100)
D <- Z \%*\% 1.2 + stats::rnorm(100)
morie_caus_iv_liml(2 * D + stats::rnorm(100), D, Z)$kappa
}
\references{
Anderson and Rubin (1949), \emph{Annals of Mathematical
Statistics} 20:46-63; Fuller (1977), \emph{Econometrica} 45:939-953.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dp_native.R
\name{morie_dp_gaussian_mechanism}
\alias{morie_dp_gaussian_mechanism}
\title{Gaussian mechanism for (epsilon, delta)-differential privacy}
\usage{
morie_dp_gaussian_mechanism(
  y,
  sensitivity = 1,
  epsilon = 1,
  delta = 1e-05,
  seed = NULL
)
}
\arguments{
\item{y}{the true query answer; scalar or vector.}

\item{sensitivity}{L2 sensitivity.}

\item{epsilon}{privacy budget, positive.}

\item{delta}{failure probability, in (0, 1).}

\item{seed}{optional integer seed.}
}
\value{
list with \code{release}, \code{sigma}, \code{epsilon},
\code{delta}, \code{sensitivity}, and \code{warnings} when the
classical bound does not apply.
}
\description{
Adds N(0, sigma^2) noise with
\eqn{\sigma = \Delta_2\sqrt{2\log(1.25/\delta)}/\epsilon}.
}
\details{
Calibrated to L2 sensitivity, not L1, which is why it beats Laplace
for high-dimensional queries. The classical bound above is only valid
for \eqn{\epsilon \le 1}; past that it is not a proof of anything, and
this function says so rather than returning a number that looks fine.
}
\examples{
round(morie_dp_gaussian_mechanism(10, 1, 0.5, 1e-5, seed = 1)$sigma, 4)
}
\references{
Dwork, C. and Roth, A. (2014). The algorithmic foundations
of differential privacy. \emph{FnTTCS}, 9(3-4), Appendix A.
}

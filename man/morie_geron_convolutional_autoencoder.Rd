% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geron_ml_native.R
\name{morie_geron_convolutional_autoencoder}
\alias{morie_geron_convolutional_autoencoder}
\title{Convolutional autoencoder forward pass (Geron Ch 18, morie.fn grcae)}
\usage{
morie_geron_convolutional_autoencoder(
  x,
  encoder_weights,
  decoder_weights,
  stride = 2,
  output_activation = "identity"
)
}
\arguments{
\item{x}{Image (H, W). @param encoder_weights,decoder_weights Lists of kernels.}

\item{stride}{Shared stride. @param output_activation "identity", "tanh", "sigmoid".}
}
\value{
List with \code{x_hat}, \code{code}, \code{code_shape}, \code{loss}, \code{mse_per_pixel},
\code{compression_ratio}.
}
\description{
Strided-conv encoder with ReLU after every layer; transposed-conv
decoder with ReLU after every layer but the last. Transposed conv
gives (n-1)s + k per axis, so a decoder that does not land back on
the input size is an error rather than a silent crop.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ksr07.R
\name{Bootemp}
\alias{Bootemp}
\alias{morie_ksr07_kosorok_bootstrap_empirical}
\alias{morie_kosorok_bootstrap_empirical}
\title{Nonparametric (multinomial) bootstrap of the empirical mean process}
\usage{
Bootemp(x, B = 200, seed = 1, deterministic_seed = NULL)

morie_ksr07_kosorok_bootstrap_empirical(x, B = 200, seed = 1, deterministic_seed = NULL)

morie_kosorok_bootstrap_empirical(x, B = 200, seed = 1, deterministic_seed = NULL)
}
\arguments{
\item{x}{The sample.}

\item{B}{Number of bootstrap replicates (fixed budget).}

\item{seed}{Seed for the pinned generator.}

\item{deterministic_seed}{Optional integer; if supplied, the pinned
LCG is seeded from SHA-256("ksr07_bootstrap:<deterministic_seed>")
via \code{morie_det_rng} instead of \code{seed}, which is the
cross-arm reproducible path.}

}
\value{
List with \code{estimate}, \code{boot_mean}, \code{boot_sd},
\code{process_sd}, \code{ci_lower}, \code{ci_upper}, \code{B},
\code{n}.
}
\description{
The bootstrap process is sqrt(n)(Phat_n - P_n), centred at the
EMPIRICAL measure. Resampling uses a pinned Lehmer generator with a
FIXED budget of B replicates, so the two language arms agree exactly.
}
\details{
Formula: Phat_n f = n^-1 sum_i W_i f(X_i),
(W_1..W_n) ~ Multinomial(n, 1/n); Ghat_n = sqrt(n) (Phat_n - P_n)
}
\examples{
V <- c(1, 2, 3, 4, 5, 6, 7, 8)
Bootemp(V)
}
\references{
Kosorok (2008), Introduction to Empirical Processes and
Semiparametric Inference, Section 2.2.3 and Theorem 2.6. Fetched as
the full text of the book.
}
\keyword{internal}

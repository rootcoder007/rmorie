% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tmlcat.R
\name{Tmlecat}
\alias{Tmlecat}
\title{Target E[Y_a] at every level of a categorical treatment}
\usage{
Tmlecat(Y, A, Q, G, ref = 1, gbound = 0.025, level = 0.95)
}
\arguments{
\item{Y}{Outcome in [0, 1].}

\item{A}{One-based treatment level of each observation.}

\item{Q}{Initial E[Y | A = a, W], one column per level.}

\item{G}{Initial P(A = a | W), rows summing to 1.}

\item{ref}{One-based reference level for the contrasts.}

\item{gbound}{Truncation applied to each g_a.}

\item{level}{Confidence level.}
}
\value{
List with \code{psi}, \code{se}, \code{contrast},
\code{contrast_se}, \code{ci_lower}, \code{ci_upper}, \code{min_g},
\code{ref}, \code{n}, \code{L}.
}
\description{
Each level gets its OWN clever covariate 1\{A = a\}/g_a(W) and its own
fluctuation, because one logistic update cannot solve L score
equations at once. \code{min_g} exposes levels with almost no support.
\code{A} holds one-based level labels.
}
\details{
Formula: for each level a, fluctuate logit Q(a,W) on
H_a = 1\{A = a\}/g_a(W); psi_a = mean(Q*(a,W))
}
\references{
Verified against the CRAN package tmle 2.1.1 (Gruber & van
der Laan); the multi-level extension applies the same fluctuation per
level, as in van der Laan & Rose (2011), Targeted Learning, Chapter 4.
}

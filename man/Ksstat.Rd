% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fzksst.R
\name{Ksstat}
\alias{Ksstat}
\alias{morie_fauzi_ks_statistic}
\title{Kolmogorov-Smirnov statistic against a fully specified distribution}
\usage{
Ksstat(x, cdf)

morie_fauzi_ks_statistic(x, cdf)
}
\arguments{
\item{x}{Sample.}

\item{cdf}{The fully specified null distribution \code{F(t)}.}
}
\value{
Named list with \code{statistic}, \code{dplus}, \code{dminus}, \code{p_value}, \code{n}, \code{method}.
}
\description{
The classical statistic the chapter's smoothed versions are compared
against: \deqn{KS_n = \sup_x |F_n(x) - F(x)|,}{KS_n = sup_x |F_n(x) - F(x)|,}
with \code{F_n} the empirical distribution function.
}
\details{
Computed as \verb{max(D+, D-)} over the order statistics, which is exact: the
supremum of a step function against a continuous one is always attained at a
jump, so no grid search is needed and none is done.

Sec. 5.1 gives the motivation for replacing \code{F_n} here: its lack of
smoothness makes the test over-sensitive near the centre of the distribution
and inflates the type-I error above the nominal \code{alpha} at small \code{n}.
Theorems 5.1 and 5.6 then show the smoothed replacements have the SAME
limit, so the same critical values apply.

Uses the exact one-sample Kolmogorov distribution for \code{n <= 40} and the
standard asymptotic series otherwise.
}
\examples{
Ksstat(c(0.1, 0.3, 0.5, 0.7, 0.9), cdf = function(t) pmin(pmax(t, 0), 1))
}
\references{
Fauzi and Maesono (2023), Sec. 5.1, the display preceding (5.3).
}
\keyword{internal}

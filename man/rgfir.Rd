% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rgfir.R
\name{rgfir}
\alias{rgfir}
\alias{morie_rangayyan_fir_filter}
\title{FIR lowpass filter (windowed sinc) -- Rangayyan Ch 3}
\usage{
rgfir(x, cutoff, order = 51L, fs = 1, window = "hamming")

morie_rangayyan_fir_filter(x, cutoff, order = 51L, fs = 1, window = "hamming")
}
\arguments{
\item{x}{Numeric vector. Input signal.}

\item{cutoff}{Cutoff frequency in Hz (if \code{fs} given) or normalised to
Nyquist=0.5.}

\item{order}{Number of taps (odd recommended; coerced to odd).}

\item{fs}{Sampling frequency (Hz). Default 1.}

\item{window}{Window name: \code{"hamming"}, \code{"hann"}, \code{"blackman"}, \code{"rectangular"}.}
}
\value{
Named list \code{signal}, \code{taps}, \code{order}, \code{cutoff}, \code{fs}, \code{window}.
}
\description{
Designs a linear-phase windowed-sinc FIR lowpass filter and applies it
zero-phase via \code{\link[signal:filtfilt]{signal::filtfilt()}} (or single-pass \code{\link[signal:filter]{signal::filter()}}
when the signal is too short to allow forward-backward padding).
}
\details{
\deqn{h[n] = w[n] \, 2 f_c \, \mathrm{sinc}(2 f_c (n - M/2))}{h[n] = w[n] 2 f_c sinc(2 f_c (n - M/2))}
}
\examples{
\donttest{
if (requireNamespace("signal", quietly = TRUE)) {
  t <- seq(0, 1, length.out = 200)
  x <- sin(2 * pi * 5 * t) + 0.5 * sin(2 * pi * 60 * t)
  r <- rgfir(x, cutoff = 10, order = 51, fs = 200)
  length(r$signal)
}
}
}
\references{
Rangayyan, R.M. \emph{Biomedical Signal Analysis} (Wiley/IEEE, 2015), Ch. 3.
}
\keyword{internal}

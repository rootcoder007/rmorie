% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aaa_rangayyan_class.R
\name{LinDisc}
\alias{LinDisc}
\title{Section 10.4.1: d_i(x) = w_i^T x + w_i0, assign to the largest.  The}
\usage{
LinDisc(x, weights, w0 = NULL)
}
\arguments{
\item{x}{Coerced to numeric by the body, with \code{as.numeric}.}

\item{weights}{A matrix; passed to \code{as.matrix}.}

\item{w0}{Optional; may be \code{NULL}. Coerced to numeric by the body, with \code{as.numeric}.}
}
\value{
A list with \code{d}, \code{assigned}, \code{margin}, \code{n_classes},
\code{regions_are_convex}, \code{decision_surfaces_are_hyperplanes}, \code{method}.
}
\description{
surfaces between classes are hyperplanes, so a linear machine carves
the space into CONVEX regions -- which is why it cannot separate
classes whose regions are not convex, however many features are
added.
}
\examples{
M <- matrix(c(1, 2, 3, 4, 5, 6), nrow = 2)
S <- c("a", "b", "c")
LinDisc(S, M)
}
\keyword{internal}

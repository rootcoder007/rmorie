% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Gpmlt.R
\name{Gpmlt}
\alias{Gpmlt}
\title{Multi-task Gaussian process}
\usage{
Gpmlt(
  X,
  y_tasks,
  X_test = NULL,
  task_cov = NULL,
  lengthscale = 1,
  variance = 1,
  noise = 0.01
)
}
\arguments{
\item{X}{Inputs shared by every task, one row per point.}

\item{y_tasks}{Matrix of observations, one row per task.}

\item{X_test}{Test inputs; the training inputs by default.}

\item{task_cov}{Inter-task covariance; the identity by default.}

\item{lengthscale, variance, noise}{GP hyperparameters.}
}
\value{
List with \code{estimate}, \code{mean}, \code{loglik},
\code{tasks}, \code{n}, \code{method}.
}
\description{
The covariance between the values of two tasks factorises into an
inter-task matrix times an input kernel, so the joint covariance is a
Kronecker product.  A diagonal task covariance means the tasks share
nothing and the model collapses to independent single-task GPs -- the
tests check that collapse exactly, because it is the only way to be
sure the Kronecker indexing is the right way round.
}
\details{
Formula: cov(f_l(x), f_m(x')) = K^f_{lm} k^x(x, x').
}
\references{
Bonilla, Chai and Williams (2008), Multi-task Gaussian
process prediction, NIPS 20, eqs. (1)-(2).
}
\examples{
Gpmlt(X = 5L, y_tasks = c(1, 2, 3, 4, 5, 6, 7, 8))
}

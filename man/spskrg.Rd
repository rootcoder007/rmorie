% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spskrg.R
\name{spskrg}
\alias{spskrg}
\title{Simple kriging: the mean is known}
\usage{
spskrg(coords, z, target, cov_model = NULL, mu = NULL)
}
\arguments{
\item{coords}{Observation coordinates (n by d).}

\item{z}{Observed values, length n.}

\item{target}{Prediction locations (m by d).}

\item{cov_model}{List with \code{model}, \code{nugget}, \code{sill}, \code{range}.}

\item{mu}{Known mean. Defaults to the sample mean of \code{z}, which makes
this simple kriging with an estimated rather than known mean.}
}
\value{
Named list: prediction, variance, weights, mu.
}
\description{
With Z(s) = mu(s) + e(s), e ~ (0, Sigma), the linear predictor
minimising the mean-squared prediction error is
p(Z; s0) = mu(s0) + sigma' Sigma^-1 (Z(s) - mu(s)), with kriging
variance sigma^2 - sigma' Sigma^-1 sigma.
}
\details{
Simple kriging is an EXACT interpolator: predicting at an observed
location returns that observation with zero variance, because sigma
becomes a column of Sigma. The book calls this honouring the data.
}
\examples{
co <- matrix(runif(40), 20, 2) * 5
spskrg(co, rnorm(20), matrix(c(2, 2), 1, 2),
       list(model = "exponential", sill = 1, range = 2))
}
\references{
Schabenberger & Gotway (2005), Sec 5.2.1, eqs (5.10)-(5.11),
pp. 223-224.
}

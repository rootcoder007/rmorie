% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dsp_filters.R
\name{morie_dsp_hann_filter}
\alias{morie_dsp_hann_filter}
\title{Hann-windowed smoothing filter}
\usage{
morie_dsp_hann_filter(x, window = 5L)
}
\arguments{
\item{x}{Numeric vector.}

\item{window}{Window length. Default 5.}
}
\value{
Smoothed vector, length(x).
}
\description{
Convolves \code{x} with a normalised Hann (raised-cosine) window of
length \code{window}. Less ringing than the boxcar.
}
\examples{
set.seed(1)
t <- seq.int(0, 199) / 200
x <- sin(2 * pi * 5 * t) + rnorm(200, sd = 0.4)
y <- morie_dsp_hann_filter(x, window = 7L)
sd(y) < sd(x)  # smoothing reduces variance
}
\references{
Rangayyan & Krishnan (2015), Ch. 3.
}

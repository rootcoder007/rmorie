% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aaa_rangayyan_class.R
\name{Roc}
\alias{Roc}
\title{Section 10.9.1.  The area the book calls A_z, by the trapezoidal}
\usage{
Roc(scores, labels, positive = 1)
}
\arguments{
\item{scores}{Coerced to numeric by the body, with \code{as.numeric}.}

\item{labels}{A vector; its length is taken.}

\item{positive}{Passed to \code{==}. Defaults to \code{1}.}
}
\value{
A list with \code{fpf}, \code{tpf}, \code{sensitivity},
\code{one_minus_specificity}, \code{thresholds}, \code{auc}, \code{az},
\code{mann_whitney}, \code{trapezoidal_equals_mann_whitney}, \code{n_positive},
\code{n_negative}, \code{best_index}, \code{best_operating_point},
\code{ties_counted_as_half}, \code{method}.
}
\description{
rule, which for tied scores is exactly the Mann-Whitney statistic --
the probability a random diseased subject outscores a random healthy
one, ties counted as half.  Summing rectangles instead biases the
area whenever scores tie, which they do whenever a classifier emits a
class rather than a probability.
}
\examples{
V <- c(1, 2, 3, 4, 5, 6, 7, 8)
Roc(V, V)
}
\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gbens.R
\name{morie_gradient_boosting_ensemble}
\alias{morie_gradient_boosting_ensemble}
\title{Gradient boosting ensemble (R parity)}
\usage{
morie_gradient_boosting_ensemble(
  x,
  y,
  n_estimators = 100L,
  learning_rate = 0.1,
  max_depth = 3L,
  task = "auto",
  seed = 0L,
  deterministic_seed = NULL
)
}
\arguments{
\item{x}{Numeric predictor matrix.}

\item{y}{Response.}

\item{n_estimators}{Number of boosting iterations.}

\item{learning_rate}{Shrinkage nu.}

\item{max_depth}{Depth of each tree.}

\item{task}{"auto", "classification", or "regression".}

\item{seed}{RNG seed.}

\item{deterministic_seed}{Integer or NULL.  If supplied, the RNG state
is derived from the SHA-keyed \code{\link[=morie_det_rng]{morie_det_rng()}} so Py<->R streams
agree on the canonical fixture.  When \code{NULL} (default), behaviour
is unchanged: \code{seed} drives \code{set.seed()} directly.}
}
\value{
Named list: estimate, train_score, feature_importances,
n_estimators, learning_rate, max_depth, task, n, method.
}
\description{
Wraps \code{gbm::gbm} when available, otherwise falls back to
\code{xgboost} as a portable boosted-trees backend.
}
\examples{
morie_gradient_boosting_ensemble(x = rnorm(50), y = rnorm(50))
}

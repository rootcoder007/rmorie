% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/esl_native2.R
\name{morie_esl_svc}
\alias{morie_esl_svc}
\title{Linear support vector classifier}
\usage{
morie_esl_svc(
  X,
  y,
  C = 1,
  newdata = NULL,
  tol = 0.001,
  max_passes = 50L,
  seed = 0L
)
}
\arguments{
\item{X}{Predictors, n by p.}

\item{y}{Labels; any two distinct values.}

\item{C}{Cost of margin violations, positive.}

\item{newdata}{Points to classify; defaults to \code{X}.}

\item{tol, max_passes, seed}{SMO controls.}


}
\value{
List with \code{w}, \code{b}, \code{margin}, \code{alpha}, \code{support}, \code{decision},
\code{class}, \code{accuracy}, \code{slack}, \code{n_violations}.
}
\description{
Soft-margin linear SVC solved through its dual, with the primal weights
recovered as \code{w = sum(alpha * y * x)}. Unlike the kernel form the weight
vector is explicit, so the rule is interpretable coefficient by
coefficient. The margin width \verb{2 / ||w||} is reported, and widens as \code{C}
falls.
}
\examples{
set.seed(1)
X <- rbind(
  matrix(rnorm(100, -3, 0.5), ncol = 2),
  matrix(rnorm(100, 3, 0.5), ncol = 2)
)
y <- rep(c(-1, 1), each = 50)
morie_esl_svc(X, y, C = 1, seed = 1L)$accuracy
}
\references{
Hastie, T., et al. (2009). ESL (2nd ed.), Sec 12.2. Springer.
}

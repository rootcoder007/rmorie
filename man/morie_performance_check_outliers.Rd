% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extenders_stats.R
\name{morie_performance_check_outliers}
\alias{morie_performance_check_outliers}
\title{Outlier check via \pkg{performance}}
\usage{
morie_performance_check_outliers(x, ...)
}
\arguments{
\item{x}{A fitted model object or numeric data frame supported
by \code{performance::check_outliers}.}

\item{...}{Further arguments forwarded to
\code{performance::check_outliers} (e.g. \code{method},
\code{threshold}).}
}
\value{
A list with \code{$method =
  "performance::check_outliers"} and \code{$raw} (the upstream
outlier-check object).
}
\description{
Thin extender over \code{performance::check_outliers} for
composite outlier detection on a fitted model or numeric data.
}
\examples{
if (requireNamespace("performance", quietly = TRUE)) {
  set.seed(1)
  df <- data.frame(y = c(rnorm(49), 8), x = rnorm(50))
  morie_performance_check_outliers(stats::lm(y ~ x, df))
}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/toronto_neighbourhoods.R
\name{morie_tps_resolve_hood_col}
\alias{morie_tps_resolve_hood_col}
\title{Resolve which HOOD_* column to use on a TPS crime data.frame}
\usage{
morie_tps_resolve_hood_col(df, prefer = c("158", "140"), fallback = TRUE)
}
\arguments{
\item{df}{A TPS crime \code{data.frame}.}

\item{prefer}{Either \code{"158"} (current) or \code{"140"} (historical).}

\item{fallback}{If \code{TRUE} (default), accept the other version when
the preferred one is absent (with a warning). If \code{FALSE}, return
\code{NULL} when the preferred version is missing.}
}
\value{
Character scalar (the chosen column name), or \code{NULL} if no
suitable column is present.
}
\description{
Many TPS PSDP crime layers carry BOTH \code{HOOD_158} (current) and
\code{HOOD_140} (historical 2014--2021) columns. Pick the version your
analysis needs explicitly so the two schemes are not silently mixed
across years.
}
\examples{
df <- data.frame(OCC_YEAR = 2024L, HOOD_158 = "82", HOOD_140 = "82")
morie_tps_resolve_hood_col(df, prefer = "158")
}

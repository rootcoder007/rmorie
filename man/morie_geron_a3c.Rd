% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geron_ml_native.R
\name{morie_geron_a3c}
\alias{morie_geron_a3c}
\title{Asynchronous advantage actor-critic, A3C (Geron Ch 19, morie.fn hma3c)}
\usage{
morie_geron_a3c(
  env,
  actor,
  critic,
  n_workers = 4,
  lr = 0.1,
  epochs = 50,
  gamma = 0.99,
  critic_lr = NULL,
  max_steps = 200,
  seed = 0
)
}
\arguments{
\item{env}{List with reset/step, or a function(worker_id) returning one.}

\item{actor, critic}{Initial parameters. @param n_workers Workers.}

\item{lr, critic_lr}{Step sizes. @param epochs Episodes per worker.}

\item{gamma}{Discount. @param max_steps Episode cap. @param seed Base seed.}
}
\value{
List with \code{actor}, \code{critic}, \code{returns}, \code{worker_returns},
\code{policy}, \code{updates}.
}
\description{
Workers are stepped in deterministic round-robin: each pulls the
shared parameters, rolls out one episode, and pushes its gradient into
parameters other workers may have already moved -- the staleness that
makes A3C asynchronous. Worker w uses LCG seed + 7919 w.
}

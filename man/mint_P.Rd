% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hierF_native.R
\name{mint_P}
\alias{mint_P}
\title{mint_P}
\usage{
mint_P(S, W = NULL, method = "shrink", residuals = NULL, ridge = 1e-10)
}
\arguments{
\item{S}{A matrix; indexed by row and column.}

\item{W}{Optional; may be \code{NULL}. A matrix; passed to \code{as.matrix}.}

\item{method}{One of \code{"custom"}, \code{"ols"}, \code{"shrink"}, \code{"wls"}.
Defaults to \code{"shrink"}.}

\item{residuals}{Optional; may be \code{NULL}. A matrix; passed to \code{nrow}.}

\item{ridge}{A matrix; passed to \code{diag}. Defaults to \code{1e-10}.}
}
\value{
A list with \code{P}, \code{lambda}.
}
\description{
A step of the hierF_native implementation. Called by \code{mint_reconcile}.
See the file header for the source the module follows.
source it follows.
}
\examples{
set.seed(1)
S <- summing_matrix(list(c(0L, 1L)), 2L)
res <- matrix(rnorm(30), 10, 3)
str(mint_P(S, method = "shrink", residuals = res), max.level = 1)
}
\keyword{internal}

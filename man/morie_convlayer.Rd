% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ml_geron.R
\name{morie_convlayer}
\alias{morie_convlayer}
\title{Output of a convolutional layer, Equation 12-1 (p. 423)}
\usage{
morie_convlayer(x, kernel, bias = NULL, stride = c(1, 1), padding = c(0, 0))
}
\arguments{
\item{x}{height by width by in-channels array}

\item{kernel}{fh by fw by in-channels by out-channels array}

\item{bias}{one entry per output feature map}

\item{stride}{c(sh, sw)}

\item{padding}{c(ph, pw) zero padding}
}
\value{
list(z, height, width, channels, total, maxz, nparams)
}
\description{
z\link{i, j, k} = b\link{k} + sum_u sum_v sum_kp x\link{ip, jp, kp} w\link{u, v, kp, k}
with ip = i * sh + u and jp = j * sw + v. This is a
cross-correlation, as the book's own footnote 6 on p. 419 points
out. Exported as morie_convlayer because morie_conv2d was already
taken by R/gp_ch13_15.R.
}
\examples{
set.seed(1)
x <- array(rnorm(5 * 5 * 2), dim = c(5, 5, 2))
kernel <- array(rnorm(3 * 3 * 2 * 4), dim = c(3, 3, 2, 4))
z <- morie_convlayer(x, kernel)
dim(z)
}

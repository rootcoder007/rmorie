% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geostat_native.R
\name{morie_spatial_variogram_fit}
\alias{morie_spatial_variogram_fit}
\title{Fit a variogram model by Gaussian maximum likelihood}
\usage{
morie_spatial_variogram_fit(coords, values, model = "exponential")
}
\arguments{
\item{coords, values}{As in \code{\link{morie_spatial_variogram}}.}

\item{model}{\code{"exponential"} (default), \code{"spherical"},
or \code{"gaussian"}.}

}
\value{
A list with \code{model}, \code{nugget}, \code{psill},
\code{range}, \code{loglik}, \code{converged}, \code{method}.
}
\description{
Fits (nugget, partial sill, range) of a spherical / exponential /
Gaussian covariance model by maximizing the Gaussian log-likelihood
of the (constant-mean) data, via \code{stats::optim} on a log
parameterization.
}
\examples{
set.seed(1)
coords <- cbind(runif(60, 0, 10), runif(60, 0, 10))
values <- coords[, 1] * 0.5 + rnorm(60, 0, 0.3)
str(morie_spatial_variogram_fit(coords, values), max.level = 1)
}

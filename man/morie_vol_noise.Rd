% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/btvol_native.R
\name{morie_vol_noise}
\alias{morie_vol_noise}
\title{Microstructure noise variance from high-frequency returns}
\usage{
morie_vol_noise(r_intraday, K = NULL)
}
\arguments{
\item{r_intraday}{intraday log-returns at the finest sampling.}

\item{K}{subsampling factor; \code{round(n^(2/3))} (the ZMA rate) when
\code{NULL}.}
}
\value{
list: noise_variance, noise_sd, rv_all, rv_subsampled,
iv_two_scale, K, noise_share_of_rv, n, method.
}
\description{
Zhang-Mykland-Ait-Sahalia (2005), Ait-Sahalia-Mykland-Zhang
(2005): with observed price = efficient price + iid noise,
\verb{E\[RV_all\] = IV + 2n E\[eps^2\]}, so at the finest grid the noise
dominates and \verb{E\[eps^2\] = RV_all/(2n)}. That divergence is the
volatility signature plot -- naive RV gets WORSE as data get
finer. The two-scale estimator
\verb{IV_TS = RV_subsampled - (nbar/n) RV_all} recovers the integrated
variance, and both are returned so the size of the correction is
visible.
}
\examples{
morie_vol_noise(stats::rnorm(100, sd = 1e-3), K = 5)$noise_variance
}
\references{
Zhang, Mykland and Ait-Sahalia (2005), \emph{JASA}
100:1394-1411; Ait-Sahalia, Mykland and Zhang (2005), \emph{RFS}
18:351-416.
}

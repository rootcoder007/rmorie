% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geron_w4d_native.R
\name{morie_geron_xgboost}
\alias{morie_geron_xgboost}
\title{XGBoost: regularized gradient boosting with second-order Taylor approximation}
\usage{
morie_geron_xgboost(
  X,
  y,
  n_estimators = 10,
  learning_rate = 0.3,
  max_depth = 3,
  reg_lambda = 1,
  gamma = 0,
  min_child_weight = 1,
  objective = "squared"
)
}
\arguments{
\item{X}{Design matrix (n, d).}

\item{y}{Targets \verb{(\{0,1\}} for logistic).}

\item{n_estimators, learning_rate, max_depth, reg_lambda, gamma, min_child_weight, objective}{As in Python original.}
}
\value{
list with predicted, raw_score, trees, base_score, loss_curve,
feature_importance, objective, estimate, n, method.
}
\description{
w_j = -G_j/(H_j+lambda); gain = 0.5*(GL^2/(HL+lam) + GR^2/(HR+lam) -
G^2/(H+lam)) - gamma. Exact greedy split search over sorted feature
values.
}
\examples{
V <- c(1, 2, 3, 4, 5, 6, 7, 8)
morie_geron_xgboost(V, V)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aaa_rangayyan_ar.R
\name{ArmaFit}
\alias{ArmaFit}
\title{Section 7.7: H(z) = B(z)/A(z), needed when the signal has spectral}
\usage{
ArmaFit(x, p, q, fs = 1)
}
\arguments{
\item{x}{Coerced to numeric by the body, with \code{as.numeric}.}

\item{p}{Coerced to integer by the body, with \code{as.integer}.}

\item{q}{Coerced to integer by the body, with \code{as.integer}.}

\item{fs}{Accepted by the signature and not used anywhere in the body. Defaults to \code{1}.}
}
\value{
A list with \code{a}, \code{b}, \code{p}, \code{q}, \code{gain}, \code{poles},
\code{zeros}, \code{stable}, \code{ar_error}, \code{two_stage}, \code{method}.
}
\description{
nulls as well as resonances -- an all-pole model can only make peaks.
Fitted in two stages (AR from the ACF, MA from the residual ACF),
which is NOT joint maximum likelihood and is biased when the zeros
sit close to the poles.  The stage structure is stated, not presented
as an optimal fit.
}
\examples{
ArmaFit(x = c(1, 2, 3, 4, 5, 6, 7, 8), p = 1, q = 0.5)
}
\keyword{internal}

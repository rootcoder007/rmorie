% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bnp_ghosal.R
\name{morie_gh_kl}
\alias{morie_gh_kl}
\title{Kullback-Leibler divergence between two discrete distributions}
\usage{
morie_gh_kl(p, q)
}
\arguments{
\item{p}{numeric vector of non-negative weights (the reference
measure); renormalised internally}

\item{q}{numeric vector of non-negative weights on the same support;
renormalised internally}
}
\value{
numeric scalar divergence in nats
}
\description{
Renormalises both inputs to sum to one and returns
\eqn{\sum_i p_i \log(p_i / q_i)} in nats, restricted to the support
of \code{p}. Zeros in \code{q} are floored at \code{1e-300} rather
than returning \code{Inf}, so the result is always finite.
}
\examples{
V <- c(1, 2, 3, 4, 5, 6, 7, 8)
morie_gh_kl(V, V)
}

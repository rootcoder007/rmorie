% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/did.R
\name{morie_did_wild_cluster_bootstrap}
\alias{morie_did_wild_cluster_bootstrap}
\title{DiD with wild cluster bootstrap p-values (Cameron-Gelbach-Miller, 2008)}
\usage{
morie_did_wild_cluster_bootstrap(
  data,
  outcome,
  treatment,
  post,
  cluster,
  covariates = NULL,
  n_bootstrap = 999L,
  weight_type = "rademacher",
  seed = 42L,
  alpha = 0.05
)
}
\arguments{
\item{data}{A data frame containing the outcome, treatment, post and
any covariate columns.}

\item{outcome}{Name of the outcome column.}

\item{treatment}{Name of the binary (0/1) treatment-group column.}

\item{post}{Name of the binary (0/1) post-period column.}

\item{cluster}{Optional cluster ID column for CR1 standard errors.}

\item{covariates}{Optional character vector of covariate column names.}

\item{n_bootstrap}{Number of bootstrap replications.}

\item{weight_type}{\code{"rademacher"} (default) or \code{"webb"}.}

\item{seed}{RNG seed.}

\item{alpha}{Significance level for confidence intervals (default 0.05).}
}
\value{
A result list; see \code{\link{morie_did_2x2}}.  \code{p_value}
is the bootstrap p-value.
}
\description{
Recommended when the number of clusters is small (< 50). Uses a
base-R Rademacher / Webb wild-cluster-bootstrap implementation
that mirrors the Python module. Earlier morie versions also
delegated to \code{fwildclusterboot::boottest} when installed; we
dropped that branch because fwildclusterboot is GitHub-only and
transitively requires summclust, also GitHub-only, which made the
CI dependency resolver unreliable.
}

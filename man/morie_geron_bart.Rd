% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geron_ml_native.R
\name{morie_geron_bart}
\alias{morie_geron_bart}
\title{BART text-infilling corruption and reconstruction score (Geron Ch 15, hmbart)}
\usage{
morie_geron_bart(
  src,
  tgt,
  mask_ratio = 0.3,
  mean_span = 3,
  permute = FALSE,
  model = NULL,
  seed = 0
)
}
\arguments{
\item{src, tgt}{Token vectors. @param mask_ratio Fraction in (0, 1).}

\item{mean_span}{Mean geometric span length, >= 1.}

\item{permute}{Also rotate the corrupted token list.}

\item{model}{Optional function(corrupted, tgt) -> per-token log-probs.}

\item{seed}{LCG seed.}
}
\value{
List with \code{corrupted}, \code{spans}, \code{n_masked}, \code{n_spans}, \code{loss},
\code{perplexity}, \code{token_logprobs}.
}
\description{
Contiguous spans collapse to a SINGLE \verb{<mask>} token, so the decoder
must recover both content and length. With no \code{model} the scorer is an
add-one-smoothed bigram LM estimated on the target, giving a genuine
cross-entropy in nats per token. \code{spans} entries are 0-based
c(start, length).
}

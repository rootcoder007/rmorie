% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geometric_combinatorics_native.R
\name{morie_pick_theorem}
\alias{morie_pick_theorem}
\title{Pick's theorem, checked by counting rather than assumed}
\usage{
morie_pick_theorem(
  vertices,
  verify_by_enumeration = TRUE,
  enumeration_cap = 1e+06
)
}
\arguments{
\item{vertices}{Two-column matrix of integer (x, y) in traversal
order.}

\item{verify_by_enumeration}{Count interior points directly.}

\item{enumeration_cap}{Largest bounding box enumerated.}
}
\value{
A list with \code{area}, \code{twice_area}, \code{boundary}, \code{interior},
\code{interior_enumerated}, \code{verified}, \code{pick_holds}, \code{warnings}.
}
\description{
\eqn{A = I + B/2 - 1} for a simple lattice polygon. The three
quantities are computed independently -- area by integer shoelace,
boundary by \eqn{\sum \gcd(|\Delta x|, |\Delta y|)}, interior by
testing every lattice point of the bounding box with exact integer
ray casting -- and the theorem is then CHECKED. A check that derives
one side from the other two would be a tautology.
}
\details{
Enumeration over a huge bounding box is refused rather than silently
skipped: \code{verified} comes back \code{NA} with a warning, so a cap never
masquerades as a pass. A self-intersecting polygon shows up as a
disagreement between the formula and the count, which is the
detection, not a defect.
}
\references{
Pick G (1899) \emph{Lotos} 19:311-319.
}
\examples{
D <- data.frame(x = c(1, 2, 3, 4), y = c(2, 4, 5, 9))
morie_pick_theorem(D)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aitlns.R
\name{Lgtnsim}
\alias{Lgtnsim}
\title{Draw compositions from a logistic-normal via the alr inverse}
\usage{
Lgtnsim(mu, Sigma, n, seed = 1, total = 1)
}
\arguments{
\item{mu}{Mean of the alr coordinates, length D-1.}

\item{Sigma}{Positive-definite covariance of the alr coordinates.}

\item{n}{Number of compositions drawn.}

\item{seed}{Seed for the pinned generator.}

\item{total}{Constant each composition sums to.}
}
\value{
List with \code{sample}, \code{alr}, \code{center},
\code{mean_alr}, \code{n}, \code{D}.
}
\description{
Sampling is done in the unconstrained coordinates and mapped back;
sampling the parts directly and renormalising gives a different
distribution. The Cholesky factor is used because it is unique for a
positive-definite Sigma, so both arms map the same normal draws to the
same compositions.
}
\details{
Formula: Z ~ N(0, I_\{D-1\}); Y = mu + L Z with L L' = Sigma;
X = alr^-1(Y) = C(exp(Y_1), ..., exp(Y_\{D-1\}), 1)
}
\examples{
Lgtnsim(mu = 5L, Sigma = 0.5, n = 5L)
}
\references{
Aitchison (1986), The Statistical Analysis of
Compositional Data, Chapter 6. The reference part is the LAST,
matching aitalr and aitalri.
}

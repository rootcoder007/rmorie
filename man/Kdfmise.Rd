% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fzkmis.R
\name{Kdfmise}
\alias{Kdfmise}
\alias{morie_fauzi_kdfe_mise}
\title{MISE of the standard kernel distribution function estimator}
\usage{
Kdfmise(n, h, rfp, varint, mu2 = 1, r1 = NULL)

morie_fauzi_kdfe_mise(n, h, rfp, varint, mu2 = 1, r1 = NULL)
}
\arguments{
\item{n}{Sample size.}

\item{h}{Bandwidth.}

\item{rfp}{\verb{int \[f'(x)\]^2 dx}, the roughness of the density.}

\item{varint}{\verb{int F(x)(1 - F(x)) dx}.}

\item{mu2}{\verb{int z^2 K(z) dz}.}

\item{r1}{Kernel constant (2.9); defaults to the Gaussian value.}
}
\value{
Named list with \code{mise}, \code{biasterm}, \code{varterm}, \code{smoothgain}, \code{hopt}, \code{r1}, \code{method}.
}
\description{
The MISE display of Sec. 2.1, assembled from (2.3) and (2.4):
\deqn{\mathrm{MISE}(\hat F_h) = \frac{h^4}{4}\Big\[\int z^2K(z)dz\Big\]^2\int \[f'(x)\]^2dx + \frac1n\int F(1-F)dx - \frac{2h}{n}r_1 + o(h^4 + h/n).}{MISE(Fhat_h) = (h^4/4)\[int z^2 K dz\]^2 int \[f'(x)\]^2 dx + (1/n) int F(1-F) dx - (2h/n) r1 + o(h^4 + h/n).}
}
\details{
Differentiating in \code{h} and setting to zero gives
\verb{hopt = (2 r1 / (n mu2^2 R(f')))^(1/3)} -- a CUBE root, the reason this
suite's bandwidth rule is \code{n^(-1/3)} and not the density estimator's
\code{n^(-1/5)}. For a Gaussian kernel against a normal reference it collapses
to \verb{4^(1/3) sigma n^(-1/3)}, which is what \code{.morie_kdfe_h} returns and what
Sec. 5.3.2 attributes to Azzalini.

\code{hopt} comes back alongside the MISE so the two cannot drift apart. When
\code{rfp} is zero the bias term vanishes and no optimum exists; \code{hopt} is then
\code{NA}, not an infinity dressed up as a number.
}
\examples{
Kdfmise(n = 100, h = 0.3, rfp = 0.2, varint = 0.5)
}
\references{
Fauzi and Maesono (2023), Sec. 2.1, Eqs. (2.3)-(2.4) and the MISE display.
}
\keyword{internal}

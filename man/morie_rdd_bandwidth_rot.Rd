% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rdd.R
\name{morie_rdd_bandwidth_rot}
\alias{morie_rdd_bandwidth_rot}
\title{Rule-of-thumb (ROT) bandwidth -- Silverman-style on running variable}
\usage{
morie_rdd_bandwidth_rot(x, y, cutoff = 0)
}
\arguments{
\item{x}{Numeric vector of running-variable values (used by
bandwidth selectors + density tests that don't take a
\code{data.frame}).}

\item{y}{Numeric vector of outcome values aligned with \code{x}.}

\item{cutoff}{Numeric scalar; the threshold on \code{running}. Default
\code{0} (the canonical normalisation).}
}
\value{
A named list with elements \code{bandwidth}, \code{method}, \code{details}.
}
\description{
Rule-of-thumb (ROT) bandwidth -- Silverman-style on running variable
}
\examples{
set.seed(1)
x <- runif(400, -1, 1)
y <- 0.3 * x + 1.0 * (x >= 0) + rnorm(400, sd = 0.3)
bw <- morie_rdd_bandwidth_rot(x, y)
bw$bandwidth
}

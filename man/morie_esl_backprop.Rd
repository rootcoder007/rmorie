% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/esl_native2.R
\name{morie_esl_backprop}
\alias{morie_esl_backprop}
\title{Backpropagation sweep}
\usage{
morie_esl_backprop(X, y, weights, task = "regression")
}
\arguments{
\item{X}{Inputs, n by p, already scaled as the network expects.}

\item{y}{Targets; numeric for regression, 1-based class indices otherwise.}

\item{weights}{List with \code{alpha} (p by M), \code{alpha0}, \code{beta} (M by K), \code{beta0}.}

\item{task}{"regression" or "classification".}
}
\value{
List with \code{grad_alpha}, \code{grad_alpha0}, \code{grad_beta}, \code{grad_beta0},
\code{delta}, \code{hidden}, \code{output}, \code{loss}.
}
\description{
Forward pass, then the gradients by the ESL eq. (11.5) backward recurrence.
The content of backprop is the chain rule REUSING the output error: it is
computed once and propagated, rather than recomputed per weight, which turns
an O(number of weights) finite-difference cost into a single extra sweep.
}
\details{
Returns the gradients rather than applying them, so the choice of optimiser
stays separate from the derivative.
}
\examples{
set.seed(1)
X <- matrix(rnorm(120), ncol = 3)
y <- rnorm(40)
W <- list(alpha = matrix(rnorm(12) * 0.5, 3, 4), alpha0 = numeric(4),
          beta = matrix(rnorm(4) * 0.5, 4, 1), beta0 = 0)
dim(morie_esl_backprop(X, y, W)$grad_alpha)
}
\references{
Hastie, T., et al. (2009). ESL (2nd ed.), Sec 11.4. Springer.
}

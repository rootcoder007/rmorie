% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geron_train_native.R
\name{morie_geron_encoder_decoder_seq2seq}
\alias{morie_geron_encoder_decoder_seq2seq}
\title{Encoder-decoder seq2seq decoding loop (Geron Ch 16, morie.fn gredsq)}
\usage{
morie_geron_encoder_decoder_seq2seq(
  encoder,
  decoder,
  x,
  max_out_len,
  start_token = NULL,
  eos_token = NULL
)
}
\arguments{
\item{encoder, decoder}{Functions; \code{decoder(y_prev, c, t)} with \code{t}
1-based as in the Python loop.}

\item{x}{Encoder input.}

\item{max_out_len}{Maximum number of decoding steps.}

\item{start_token}{Optional initial \code{y_prev}; default zeros like \code{c}.}

\item{eos_token}{Optional scalar that stops decoding.}

}
\value{
List with \code{outputs}, \code{context}, \code{context_dim}, \code{n_steps},
\code{stopped_early}.
}
\description{
c = encoder(x) once, then greedy y_t = decoder(y_prev, c, t) with the
shape and finiteness contracts enforced at every step.
}
\examples{
x <- matrix(c(1, 0, 0.5, 1), 2, byrow = TRUE)
morie_geron_encoder_decoder_seq2seq(function(x) c(sum(x), 1), 
    function(yp, cc, t) as.numeric(yp) * 0.5 + cc[1]/t, c(1, 
        2, 3), 4)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geron_w4c_native.R
\name{morie_geron_randomized_search}
\alias{morie_geron_randomized_search}
\title{Randomized hyperparameter search, scored by K-fold CV (Geron Ch 2, hmrsc)}
\usage{
morie_geron_randomized_search(
  param_dist,
  n_iter,
  X,
  y,
  estimator = NULL,
  K = 3,
  seed = 0,
  score = NULL
)
}
\arguments{
\item{param_dist}{Named list of distributions: an R \code{list(lo, hi)} of length 2 is a uniform
INTERVAL (Python's \verb{(lo, hi)} tuple); a plain vector is a discrete CHOICE list (Python's
\verb{[...]}), matching Python's own tuple-vs-list distinction. A function is \code{f(u)}.}

\item{n_iter, X, y, estimator, K, seed, score}{Controls; \code{estimator(params) -> list(fit=, predict=)}.}
}
\value{
A list with \code{best_params}, \code{best_score}, \code{best_index}, \code{candidates}, \code{scores}, \code{estimate}, \code{n}, \code{method}.
}
\description{
Randomized hyperparameter search, scored by K-fold CV (Geron Ch 2, hmrsc)
}
\examples{
morie_geron_randomized_search(param_dist = c(1, 2, 3, 4, 5, 6, 7, 8), n_iter = 5L,
  X = c(1, 2, 3, 4, 5, 6, 7, 8), y = c(1, 2, 3, 4, 5, 6, 7, 8))
}
\keyword{internal}

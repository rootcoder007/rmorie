% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wsm_native.R
\name{morie_wsm_kde}
\alias{morie_wsm_kde}
\title{Kernel density estimate with Silverman's window-width rules}
\usage{
morie_wsm_kde(x, data, h = NULL, rule = "3.31")
}
\arguments{
\item{x}{evaluation points.}

\item{data}{numeric sample.}

\item{h}{window width; \code{rule} decides it when \code{NULL}.}

\item{rule}{one of \code{"3.31"} (default), \code{"3.28"}, \code{"3.29"}.}
}
\value{
list: x, density, h, rule, adaptive_spread,
h_normal_reference, h_iqr, mass, is_density, n, method.
}
\description{
Silverman Eq. (2.2a), \verb{fhat(x) = (1/nh) sum K((x - X_i)/h)}. Since
\code{K} is itself a probability density, the estimate is one too, and
inherits every continuity property of \code{K}.
}
\details{
The default window width is the book's own recommendation,
Eq. (3.31) \verb{h = 0.9 A n^(-1/5)} with \code{A = min(sd, IQR/1.34)} from
(3.30). It is NOT \verb{1.06 sigma n^(-1/5)}: that is Eq. (3.28), the
pure normal reference, which the book presents as a starting point
and then improves on twice because it oversmooths skewed,
long-tailed and bimodal data.
}
\examples{
morie_wsm_kde(c(-1, 0, 1), stats::rnorm(200))$h
}
\references{
Silverman, B. W. (1986), \emph{Density Estimation for
Statistics and Data Analysis}, Chapman and Hall, Eq. (2.2a) and
Sec. 3.4.2 Eqs. (3.28)-(3.31); Rosenblatt (1956); Parzen (1962).
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dp_native.R
\name{morie_dp_laplace_mechanism}
\alias{morie_dp_laplace_mechanism}
\title{Laplace mechanism for pure epsilon-differential privacy}
\usage{
morie_dp_laplace_mechanism(y, sensitivity = 1, epsilon = 1, seed = NULL)
}
\arguments{
\item{y}{the true query answer; scalar or vector.}

\item{sensitivity}{L1 sensitivity of the query, positive.}

\item{epsilon}{privacy budget, positive.}

\item{seed}{optional integer seed.}
}
\value{
list with \code{release}, \code{noise_scale}, \code{noise_sd}
(\eqn{\sqrt{2}b}), \code{epsilon}, \code{sensitivity}.
}
\description{
Adds Laplace(0, \eqn{\Delta_1/\epsilon}) noise, where \eqn{\Delta_1} is
the L1 sensitivity of the query.
}
\details{
Sensitivity is a property of the QUERY, never of the data in hand.
Estimating it from the dataset is itself a non-private query, and a
sensitivity that is too small does not degrade the guarantee -- it
voids it.
}
\examples{
morie_dp_laplace_mechanism(42, sensitivity = 1, epsilon = 0.5,
                           seed = 1)$noise_scale
}
\references{
Dwork, C., McSherry, F., Nissim, K. and Smith, A. (2006).
Calibrating noise to sensitivity in private data analysis.
\emph{TCC}, 265-284.
}

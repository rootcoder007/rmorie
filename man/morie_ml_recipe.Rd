% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ml_pipeline.R
\name{morie_ml_recipe}
\alias{morie_ml_recipe}
\title{Specify a preprocessing recipe}
\usage{
morie_ml_recipe(
  outcome,
  predictors = NULL,
  impute = c("mean", "median", "none"),
  center = TRUE,
  scale = TRUE,
  center_target = NULL
)
}
\arguments{
\item{outcome}{Name of the response column.}

\item{predictors}{Character vector of predictor columns (default: all
other numeric columns).}

\item{impute}{One of "none", "mean", "median" (ML1.7a; default "mean").}

\item{center}{Logical; center predictors (ML2.2). Default TRUE.}

\item{scale}{Logical; scale predictors to unit SD. Default TRUE.}

\item{center_target}{Optional named numeric of explicit target means
(ML2.2); overrides the data mean where supplied.}
}
\value{
A \code{morie_ml_recipe} specification object.
}
\description{
A recipe records which transformations to apply to the predictors;
\code{morie_ml_prep()} estimates their parameters from training data and
\code{morie_ml_bake()} applies (or reverses) them. This is the dedicated
pre-processing-specification stage: it is defined and parametrised
separately from model fitting, records the values of all
transformations, and can be switched off.
}
\examples{
rec <- morie_ml_recipe("mpg", c("hp", "wt"))
rec <- morie_ml_prep(rec, mtcars)
head(morie_ml_bake(rec, mtcars))
}

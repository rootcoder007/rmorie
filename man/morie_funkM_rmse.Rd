% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/funkM_native.R
\name{morie_funkM_rmse}
\alias{morie_funkM_rmse}
\title{Root mean squared error on a held-out set}
\usage{
morie_funkM_rmse(ratings, mu, bu, bi, P, Q)
}
\arguments{
\item{ratings}{Passed to \code{.funkM_as_ratings}.}

\item{mu}{Passed to \code{.funkM_predict}.}

\item{bu}{A vector; indexed elementwise.}

\item{bi}{A vector; indexed elementwise.}

\item{P}{A matrix; indexed by row and column.}

\item{Q}{A matrix; indexed by row and column.}
}
\value{
A numeric value.
}
\description{
A step of the funkM_native implementation. No other function in the package calls it.
See the file header for the source the module follows.
source it follows.
}
\examples{
set.seed(1)
n_users <- 20L; n_items <- 15L
u <- sample.int(n_users, 300, TRUE) - 1L
i <- sample.int(n_items, 300, TRUE) - 1L
r <- 3 + 0.4 * (u \%\% 3) - 0.3 * (i \%\% 4) + rnorm(300, 0, 0.1)
ratings <- data.frame(u = u, i = i, r = r)
fit <- morie_funkM(ratings, n_users, n_items, factors = 3,
                   epochs = 20, lr = 0.02, seed = 1)
morie_funkM_rmse(ratings, fit$mu, fit$b_user, fit$b_item, fit$P, fit$Q)
}
\keyword{internal}

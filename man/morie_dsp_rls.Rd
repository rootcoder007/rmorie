% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dsp_filters.R
\name{morie_dsp_rls}
\alias{morie_dsp_rls}
\title{RLS adaptive filter}
\usage{
morie_dsp_rls(x, d, order = 16L, lam = 0.99, delta = 100)
}
\arguments{
\item{x}{Input (reference) vector.}

\item{d}{Desired vector, same length as \code{x}.}

\item{order}{Filter order (taps). Default 16.}

\item{lam}{Forgetting factor in (0, 1]. Default 0.99.}

\item{delta}{Initial P diagonal. Default 100.}
}
\value{
List with \code{y}, \code{e}.
}
\description{
Recursive least-squares with forgetting factor \code{lam} and initial
inverse-correlation \code{P0 = delta * I}. Faster convergence than LMS
at the cost of O(order^2) per sample.
}
\examples{
if (requireNamespace("ranger", quietly = TRUE)) {
  set.seed(1L)
  x <- rnorm(1000)
  d <- x + 0.01 * rnorm(1000)
  out <- morie_dsp_rls(x, d, order = 8L, lam = 0.99)
  mean(out$e[200:400]^2)
}
}
\references{
Rangayyan & Krishnan (2015), Ch. 3, sec. 3.6;
Haykin (2002).
}

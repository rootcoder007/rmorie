% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mambss_native.R
\name{selective_ssm_step}
\alias{selective_ssm_step}
\title{selective_ssm_step}
\usage{
selective_ssm_step(x, h, A, B, C, delta, rule = "zoh")
}
\arguments{
\item{x}{Coerced to numeric by the body, with \code{as.numeric}.}

\item{h}{A vector; its length is taken and its elements indexed.}

\item{A}{A vector; its length is taken.}

\item{B}{Passed to \code{discretize_zoh}.}

\item{C}{A vector; its length is taken.}

\item{delta}{Passed to \code{discretize_zoh}.}

\item{rule}{Passed to \code{discretize_zoh}. Defaults to \code{"zoh"}.}
}
\value{
A list with \code{h}, \code{y}.
}
\description{
A step of the mambss_native implementation. Called by \code{selective_scan}.
See the file header for the source the module follows.
source it follows.
}
\examples{
set.seed(1)
N <- 3
r <- selective_ssm_step(x = 0.5, h = rep(0, N),
                        A = -abs(rnorm(N)), B = rnorm(N), C = rnorm(N),
                        delta = 0.1)
str(r, max.level = 1)
}
\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spsemv.R
\name{spsemv}
\alias{spsemv}
\title{Empirical semivariogram: half the mean squared difference.}
\usage{
spsemv(coords, z, n_bins = 15, max_dist = NULL)
}
\arguments{
\item{coords}{Coordinate matrix (n by d).}

\item{z}{Numeric vector of length n.}

\item{n_bins}{Number of lag bins.}

\item{max_dist}{Largest lag retained; defaults to half the maximum
pair distance.}
}
\value{
Named list: lag, gamma, n_pairs.
}
\description{
gamma(h) = 0.5 E((Z(s) - Z(s+h))^2), estimated by Matheron's method of
moments over lag bins. The factor of one half is what makes gamma
comparable to a variance rather than to a squared difference.
}
\examples{
spsemv(coords = matrix(runif(200), 100, 2), z = rnorm(100), n_bins = 5)
}
\references{
Schabenberger & Gotway (2005), Sec 1.4.3 / Ch 4.
}

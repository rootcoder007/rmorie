% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ts_joseph.R
\name{morie_tide}
\alias{morie_tide}
\title{TiDE dense encoder-decoder}
\usage{
morie_tide(y, feats, fproj, enc, dec, tdec, wglobal, horizon)
}
\arguments{
\item{y}{the lookback window}

\item{feats}{list of covariate vectors, or NULL}

\item{fproj, enc, dec, tdec}{residual blocks, each
list(w1, b1, w2, b2, wskip)}

\item{wglobal}{global linear map from lookback to horizon}

\item{horizon}{forecast length}



}
\value{
list(forecast, temporal, global, horizon, p, encdim, nfeat,
first, last, mean)
}
\description{
Quoted from the paper: (3) "xtilde_\{i,t\} = ResidualBlock(x_\{i,t\})";
(4) "e^(i) = Encoder(y^i; xtilde^i; a^(i))"; (5) "g^(i) =
Decoder(e^(i))"; (6) "D^(i) = Reshape(g^(i))"; "yhat^i_\{L+t\} =
TemporalDecoder(d_\{i,t\}; xtilde^i_\{L+t\})" -- Das, A., Kong, W.,
Leach, A., Mathur, S., Sen, R. and Yu, R., Long-term Forecasting
with TiDE, TMLR 2023 (arXiv:2304.08424). The paper states the
residual block and the global linear residual in prose rather than
as numbered equations; both are implemented from that prose.
}
\examples{
set.seed(4)
y <- rnorm(6)
enc <- list(w1 = matrix(rnorm(36, 0, 0.3), 6, 6), b1 = rep(0, 6),
            w2 = matrix(rnorm(36, 0, 0.3), 6, 6), b2 = rep(0, 6),
            wskip = diag(6))
dec <- list(w1 = matrix(rnorm(36, 0, 0.3), 6, 6), b1 = rep(0, 6),
            w2 = matrix(rnorm(24, 0, 0.3), 4, 6), b2 = rep(0, 4),
            wskip = matrix(rnorm(24, 0, 0.3), 4, 6))
tdec <- list(w1 = matrix(rnorm(4, 0, 0.3), 2, 2), b1 = rep(0, 2),
             w2 = matrix(rnorm(2, 0, 0.3), 1, 2), b2 = 0,
             wskip = matrix(rnorm(2, 0, 0.3), 1, 2))
r <- morie_tide(y, feats = NULL, fproj = NULL, enc = enc, dec = dec,
                tdec = tdec,
                wglobal = matrix(rnorm(12, 0, 0.3), 2, 6), horizon = 2)
str(r, max.level = 1)
}

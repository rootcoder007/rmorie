% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clbuvc.R
\name{Clbuvc}
\alias{Clbuvc}
\title{CLUB upper bound on mutual information}
\usage{
Clbuvc(x, y, q = NULL)
}
\arguments{
\item{x}{Observations of X.}

\item{y}{Observations of Y.}

\item{q}{Optional (a, b, sigma2) of y | x ~ N(a + b x, sigma2).}
}
\value{
List with \code{estimate}, \code{club}, \code{positive},
\code{negative}, \code{a}, \code{b}, \code{sigma2}, \code{rho},
\code{mi_gauss}, \code{n}, \code{method}.
}
\description{
Formula: I_CLUB = E\link{log q(y|x)} - E_marg\link{log q(y|x)}
}
\details{
The second term averages the SAME conditional density over
mismatched pairs, so the bound is the average log-ratio between
matched and mismatched likelihoods.  It is an upper bound on I(X;Y)
whenever q equals the true conditional.  With a Gaussian q fitted by
least squares the value has the closed form b^2 var(x) / sigma2.
}
\examples{
V <- c(1, 2, 3, 4, 5, 6, 7, 8)
D <- data.frame(x = c(1, 2, 3, 4), y = c(2, 4, 5, 9))
Clbuvc(V, D)
}
\references{
Cheng, Hao, Dai, Liu, Gan & Carin (2020), CLUB, ICML
119:1779-1788.
}

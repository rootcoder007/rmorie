% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aaa_rangayyan_class2.R
\name{Infomax}
\alias{Infomax}
\title{Infomax}
\usage{
Infomax(X, ncomp = NULL, eta = 0.05, maxiter = 300, tol = 1e-08, seed = 1)
}
\arguments{
\item{X}{Passed to \code{.morie_bx_mat}.}

\item{ncomp}{Optional; may be \code{NULL}. Coerced to integer by the body, with
\code{as.integer}.}

\item{eta}{Numeric; combined arithmetically in the body. Defaults to \code{0.05}.}

\item{maxiter}{A count; the body uses it as \code{seq_len(...)}. Defaults to \code{300}.}

\item{tol}{Passed to \code{<=}. Defaults to \code{1e-08}.}

\item{seed}{Passed to \code{.morie_bx_rng}. Defaults to \code{1}.}
}
\value{
A list with \code{sources}, \code{unmixing}, \code{whitening}, \code{mean},
\code{iterations}, \code{change}, \code{method}.
}
\description{
A step of the rangayyan_class2 implementation. No other function in the package calls it.
See the file header for the source the module follows.
the source it follows.
}
\examples{
set.seed(5)
S <- rbind(sin(2 * pi * (1:300) / 25), sign(sin(2 * pi * (1:300) / 60)))
A <- matrix(c(1, 0.5, 0.4, 1), 2, 2)
X <- A \%*\% S
r <- Infomax(X, maxiter = 100)
str(r, max.level = 1)
}
\keyword{internal}

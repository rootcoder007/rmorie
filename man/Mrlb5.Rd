% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fzb5t.R
\name{Mrlb5}
\alias{Mrlb5}
\alias{morie_fauzi_b5_coefficient_mrl}
\title{The b_5 coefficient of the mean-residual-life variance (Eq. 4.28)}
\usage{
Mrlb5(dg, density, mrl, surv = NULL)

morie_fauzi_b5_coefficient_mrl(dg, density, mrl, surv = NULL)
}
\arguments{
\item{dg}{\verb{g'(g^-1(t))}.}

\item{density}{\code{f_X(t)}.}

\item{mrl}{\code{m_X(t)}.}

\item{surv}{\code{S_X(t)}; needed only for \code{varterm}.}
}
\value{
Named list with \code{estimate}, \code{varterm}, \code{method}.
}
\description{
Eq. (4.28), second half:
\deqn{b_5(t) = g'(g^{-1}(t))f_X(t)m_X^2(t).}{b5(t) = g'(g^-1(t)) f(t) m(t)^2.}
}
\details{
It multiplies the \code{-h/n} term of (4.27), so it is the SIZE OF THE GAIN from
smoothing:
\deqn{\mathrm{Var}\[m_{X,i}(t)\] = \frac{1}{n}\frac{b_4(t)}{S_X^2(t)} - \frac{h}{n}\frac{b_5(t)}{S_X^2(t)}\int V(y)W(y)dy + o(h/n).}{Var\[m(t)\] = (1/n) b4/S^2 - (h/n) b5/S^2 int V W dy + o(h/n).}

The sign is the same lesson as \code{r1} in Chapter 2 -- smoothing a
distribution-type functional REDUCES variance -- and the same reason the
bandwidth rate here is \code{n^(-1/3)}, not \code{n^(-1/5)}.

Note where \verb{g'} enters: the gain is proportional to the derivative of the
transformation at the point. A transformation that stretches near \code{t} buys
more variance reduction there, the mechanism behind Remark 5.1's observation
that \verb{g'(g^-1(x)) >= 1} makes the boundary-free estimator strictly better
than the naive one.
}
\examples{
Mrlb5(dg = 1, density = 0.4, mrl = 2)
}
\references{
Fauzi and Maesono (2023), Eqs. (4.27)-(4.28).
}
\keyword{internal}

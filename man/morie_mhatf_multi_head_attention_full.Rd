% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mhatf.R
\name{morie_mhatf_multi_head_attention_full}
\alias{morie_mhatf_multi_head_attention_full}
\alias{morie_multi_head_attention_full}
\title{Multi-head attention with output projection}
\usage{
morie_mhatf_multi_head_attention_full(
  x,
  num_heads = 2L,
  W_q = NULL,
  W_k = NULL,
  W_v = NULL,
  W_o = NULL,
  seed = 0L,
  deterministic_seed = NULL
)

morie_multi_head_attention_full(
  x,
  num_heads = 2L,
  W_q = NULL,
  W_k = NULL,
  W_v = NULL,
  W_o = NULL,
  seed = 0L,
  deterministic_seed = NULL
)
}
\arguments{
\item{x}{Numeric matrix \code{(seq_len, d_model)}.}

\item{num_heads}{Number of heads (must divide \code{d_model}).}

\item{W_q, W_k, W_v}{Q/K/V projections (default small random normal).}

\item{W_o}{Output projection (default identity).}

\item{seed}{RNG seed for default weights.}

\item{deterministic_seed}{Optional integer; if non-NULL, a SHA-keyed
seed from \code{\link{morie_det_rng}("mhatf", deterministic_seed)} is
installed before sampling so Py<->R streams agree.  Overrides
\code{seed} when set.}
}
\value{
Named list \code{(output, estimate, heads, num_heads, d_k,
  d_model, method)}.
}
\description{
R parity for \code{morie.fn.mhatf.multi_head_attention_full}.
}
\details{
\deqn{\mathrm{MultiHead}(x) =
      \mathrm{Concat}(\mathrm{head}_1, \ldots, \mathrm{head}_h) W^O}{MultiHead(x) = Concat(head_1, ..., head_h) W^O}
}
\examples{
# See the package vignettes for usage examples:
#   vignette(package = "morie")
}
\references{
Vaswani et al. (2017), NeurIPS.
}
\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coin_native.R
\name{morie_indep_test}
\alias{morie_indep_test}
\title{Native general independence permutation test}
\usage{
morie_indep_test(
  formula,
  data,
  alternative = "two.sided",
  distribution = "asymptotic"
)
}
\arguments{
\item{formula}{\code{y ~ x}.}

\item{data}{A data frame.}

\item{alternative}{\code{"two.sided"} (default), \code{"greater"},
\code{"less"}.}

\item{distribution}{\code{"asymptotic"} (only; kept for parity).}
}
\value{
A list with \code{statistic} (standardized) and \code{p.value}.
}
\description{
Reproduces \code{coin::independence_test} for a numeric response and
covariate (scalar linear statistic, asymptotic normal reference).
}
\examples{
if (requireNamespace("coin", quietly = TRUE)) {
  set.seed(1)
  df <- data.frame(y = rnorm(40), g = factor(rep(c("a", "b"), 20)))
  str(morie_indep_test(y ~ g, df), max.level = 1)
}
}

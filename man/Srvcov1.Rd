% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fzcov1.R
\name{Srvcov1}
\alias{Srvcov1}
\alias{morie_fauzi_cov_surv_est1}
\title{Covariance of the first cumulative-survival estimator with the survival estimator (Eq. 4.16)}
\usage{
Srvcov1(n, surv, cdf = NULL)

morie_fauzi_cov_surv_est1(n, surv, cdf = NULL)
}
\arguments{
\item{n}{Sample size.}

\item{surv}{\code{S_X(t)}.}

\item{cdf}{\code{F_X(t)}; defaults to \code{1 - surv}.}
}
\value{
Named list with \code{covariance}, \code{surv}, \code{cdf}, \code{n}, \code{method}.
}
\description{
Eq. (4.16):
\deqn{\mathrm{Cov}\[S_{X,1}(t), \tilde S_X(t)\] = \tfrac{1}{n}S_X(t)F_X(t) + o(h/n).}{Cov\[S_X1(t), Stilde(t)\] = (1/n) S(t) F(t) + o(h/n).}
}
\details{
Small, but not negligible: it is exactly the term that survives when
Theorem 4.3 forms the ratio \verb{m_X,1 = S_X,1 / Stilde} and linearises it.
Dropping it leaves the mean-residual-life variance wrong at order \code{1/n} --
its leading order.

The leading term is the empirical-df variance \code{F(1-F)/n} written the other
way round, since \code{S = 1 - F}. Not a coincidence: at leading order both
estimators are the empirical df, with the kernel smoothing entering only at
\code{O(h/n)}.
}
\examples{
Srvcov1(n = 100, surv = 0.4)
}
\references{
Fauzi and Maesono (2023), Eq. (4.16).
}
\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Gpdkl.R
\name{Gpdkl}
\alias{Gpdkl}
\title{Deep kernel learning}
\usage{
Gpdkl(
  X,
  y,
  X_test = NULL,
  nn = NULL,
  lengthscale = 1,
  variance = 1,
  noise = 0.01
)
}
\arguments{
\item{X}{Training inputs.}

\item{y}{Training responses.}

\item{X_test}{Test inputs; the training inputs by default.}

\item{nn}{List with W1, b1, W2, b2 and optionally tanh; NULL means the
identity map.}

\item{lengthscale, variance, noise}{GP hyperparameters.}
}
\value{
List with \code{estimate}, \code{mean}, \code{variance},
\code{loglik}, \code{features}, \code{n}, \code{method}.
}
\description{
A base kernel applied to the output of a neural network rather than
to the raw inputs.  The network is a fixed one-hidden-layer tanh map
whose weights are supplied by the caller, so there is no training
loop and both language arms follow the same deterministic map; an
identity map recovers the plain RBF GP exactly, which is the identity
the tests check.
}
\details{
Formula: k(x, x') -> k(g(x, w), g(x', w)).
}
\references{
Wilson, Hu, Salakhutdinov and Xing (2016), Deep kernel
learning, AISTATS 2016, PMLR 51:370-378, eq. (1). arXiv:1511.02222
}
\examples{
V <- c(1, 2, 3, 4, 5, 6, 7, 8)
Gpdkl(V, V)
}

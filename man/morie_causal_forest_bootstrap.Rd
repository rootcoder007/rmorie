% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causal_forest_honest.R
\name{morie_causal_forest_bootstrap}
\alias{morie_causal_forest_bootstrap}
\title{Bootstrap confidence intervals for the causal-forest CATE}
\usage{
morie_causal_forest_bootstrap(
  y,
  d,
  x,
  B = 40L,
  n_trees = 60L,
  min_leaf = 10L,
  seed = 0L,
  alpha = 0.05
)
}
\arguments{
\item{y, d, x}{As in \code{\link[=morie_causal_forest]{morie_causal_forest()}}.}

\item{B}{Bootstrap replicates.}

\item{n_trees, min_leaf, seed}{Per-forest hyperparameters.}

\item{alpha}{Two-sided interval level.}




}
\value{
List with \code{cate}, \code{ci_low}, \code{ci_high}, \code{se}, \code{ate},
\code{ate_ci}, \code{B}, \code{n}.
}
\description{
Refits an honest forest on each of \code{B} bootstrap resamples and takes
percentile intervals across replicates. Intervals from a single
forest's tree spread understate the uncertainty because the trees
share the training data. Mirrors \code{morie.fn.crfboot}.
}
\examples{
set.seed(2)
n <- 100
X <- matrix(rnorm(n * 2), n, 2)
d <- rbinom(n, 1, 0.5)
y <- 1 + X[, 1] + d * 0.8 + rnorm(n, 0, 0.3)
r <- morie_causal_forest_bootstrap(y, d, X, B = 8L, n_trees = 15L)
str(r, max.level = 1)
}
\references{
Wager S, Athey S (2018). Estimation and inference of
heterogeneous treatment effects using random forests.
\emph{JASA} 113(523), 1228-1242.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bounds_native.R
\name{morie_permutation_lm_loss}
\alias{morie_permutation_lm_loss}
\title{Permutation language-model loss under one factorization order}
\usage{
morie_permutation_lm_loss(
  logits,
  targets,
  permutation,
  num_predict = NULL,
  reduction = c("mean", "sum", "none")
)
}
\arguments{
\item{logits}{Numeric matrix, \code{T} rows by \code{V} vocabulary columns.}

\item{targets}{Integer vector of true token ids in \code{0:(V-1)}.}

\item{permutation}{Factorization order, a permutation of \code{0:(T-1)}.}

\item{num_predict}{Optional count of trailing positions to score.}

\item{reduction}{One of "mean", "sum" or "none".}
}
\value{
A list with \code{loss}, \code{token_nll}, \code{scored_positions},
\code{perplexity}, \code{order_invariant}, \code{mean_context_length}.
}
\description{
Equation (3) of the XLNet paper, negated:
\deqn{-\sum_{t} \log p_\theta(x_{z_t} \mid x_{z_{<t}}).}
}
\details{
With the logits held fixed the full-sequence loss does NOT depend on
the permutation, because reordering the terms of a sum leaves the sum
alone. The permutation acts only on what the network was allowed to
condition on when producing those logits -- that is, on the masks --
never on this arithmetic. \code{order_invariant} records that, and is TRUE
by construction whenever the whole sequence is scored.

Equation (5) is partial prediction: score only the trailing
\code{num_predict} positions of the factorization order, whose contexts
are the longest available. The scored subset then depends on the
permutation, so the loss does too, and \code{order_invariant} goes FALSE.
}
\references{
Yang et al (2019) arXiv:1906.08237, eq (3) and (5).
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wsm_native.R
\name{morie_wsm_bootstrap}
\alias{morie_wsm_bootstrap}
\title{Bootstrap variance estimator}
\usage{
morie_wsm_bootstrap(data, T, B = 1000, seed = 0, ddof = 1L)
}
\arguments{
\item{data}{numeric sample.}

\item{T}{the statistic, applied to a sample.}

\item{B}{replicates.}

\item{seed}{resampling seed.}

\item{ddof}{divisor is \code{B - ddof}; the book's (7.53) is 1.}
}
\value{
list: value, se, variance_ddof1, variance_ddof0,
mean_replicate, bias, replicates, B, n, ddof, method.
}
\description{
ESL Eq. (7.53), \verb{Var\[S\] = (1/(B-1)) sum_b (S(Z*b) - Sbar*)^2}.
}
\details{
The denominator is \code{B - 1}, not \code{B}, for the same reason a sample
variance carries \code{n - 1}: the replicates are centred at their own
mean. At the \code{B = 100} the book suggests the two differ by 1\%; at
the \code{B = 20} someone in a hurry will use, by 5\%. Both are
available and both are always reported.

What this estimates is the variance of \code{S} under sampling from the
EMPIRICAL distribution. Its bearing on the real one rests on the
bootstrap being consistent for the statistic at hand.
}
\examples{
morie_wsm_bootstrap(stats::rnorm(100), mean, B = 100)$se
}
\references{
Hastie, Tibshirani and Friedman (2009), Eq. (7.53) and
Fig. 7.12; Efron, B. (1979), \emph{Annals of Statistics} 7:1-26;
Kosorok (2008), Ch. 10.
}

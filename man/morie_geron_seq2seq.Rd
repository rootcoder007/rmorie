% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geron_w4d_native.R
\name{morie_geron_seq2seq}
\alias{morie_geron_seq2seq}
\title{Sequence-to-sequence encoder-decoder architecture}
\usage{
morie_geron_seq2seq(src, tgt, encoder, decoder, max_len = NULL, eos = NULL)
}
\arguments{
\item{src}{Source sequence, passed to encoder unchanged.}

\item{tgt}{Integer target token ids (0-based), used for teacher forcing.}

\item{encoder}{Function(src) -> numeric context vector z.}

\item{decoder}{Function(z, prefix) -> numeric scores, length V >= 2 (prefix is 0-based ids).}

\item{max_len}{Greedy decode length; default length(tgt).}

\item{eos}{Optional token id (0-based) that stops greedy decoding.}
}
\value{
list with z, loss, perplexity, token_logprobs, greedy, greedy_logprob,
greedy_matches, exposure_bias, vocab_size, estimate, n, method.
}
\description{
enc(src) -> z (context vector); dec(z, prefix) -> next-token scores.
Runs teacher-forced cross-entropy against tgt and a free-running
greedy decode.
}

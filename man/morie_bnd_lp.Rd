% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bnd_native.R
\name{morie_bnd_lp}
\alias{morie_bnd_lp}
\title{Sharp linear-programming bounds on a linear target}
\usage{
morie_bnd_lp(
  c,
  A_ub = NULL,
  b_ub = NULL,
  A_eq = NULL,
  b_eq = NULL,
  bounds = NULL
)
}
\arguments{
\item{c}{target functional's coefficients.}

\item{A_ub, b_ub}{inequality restrictions \verb{A_ub x <= b_ub}.}

\item{A_eq, b_eq}{equality restrictions (typically data moments).}

\item{bounds}{list of \code{c(lo, hi)} per coordinate; \verb{\[0, 1\]} each
when \code{NULL} -- the natural range for response probabilities.}


}
\value{
list: lower, upper, width, argmin, argmax, feasible,
bounded, sharp, k, n_inequalities, n_equalities, method.
}
\description{
The computational engine of Mogstad, Santos and Torgovitsky
(2018): when the object of interest is a linear functional \verb{c'x}
and everything the data and assumptions say about \code{x} is linear,
the identified set for the target is
\verb{\[min c'x, max c'x\]} over the feasible polytope -- and both ends
are linear programmes. SHARPNESS is the point: the feasible set is
convex, so every value between the optima is attained, and the
interval IS the identified set for the target.
}
\details{
Infeasibility is informative rather than an error: an empty
feasible set means the maintained assumptions contradict the data
moments -- a specification REJECTION.

Solved by a self-contained two-phase simplex with Bland's rule
(base R has no linear programming); at identification-bound
problem sizes the anti-cycling guarantee is worth more than speed.
}
\examples{
morie_bnd_lp(c(1, 1), A_eq = rbind(c(1, 2)), b_eq = 1)$lower
}
\references{
Mogstad, Santos and Torgovitsky (2018), \emph{Econometrica}
86:1589-1619, Prop. 2 and Sec. 4.
}

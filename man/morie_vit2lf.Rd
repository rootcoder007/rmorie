% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vit2lf_native.R
\name{morie_vit2lf}
\alias{morie_vit2lf}
\title{Attention with the logits scaled by log n, by a cosine, or by both}
\usage{
morie_vit2lf(
  q,
  k,
  v,
  mode = "logn",
  tau = 1,
  bias = NULL,
  mask = NULL,
  n = NULL,
  tau_floor = .VIT2LF_TAU_FLOOR
)
}
\arguments{
\item{q}{Queries, one row per query position.}

\item{k}{Keys, one row per key position, sharing the head dimension.}

\item{v}{Values, one row per key position.}

\item{mode}{A member of the mode list.}

\item{tau}{The cosine temperature, held at or above the floor.}

\item{bias}{An additive position bias, applied after the scaling, or
NULL.}

\item{mask}{A logical matrix, TRUE where a key is visible to a query,
or NULL.}

\item{n}{The length used for the log scaling, or NULL for the number
of keys.}

\item{tau_floor}{The lower bound on tau.}
}
\value{
A list with the attention weights, the context vectors, the
logits, the scale actually applied, the per-row entropy and the
largest weight -- the last two being how you see the scaling
working.
}
\description{
Attention with the logits scaled by log n, by a cosine, or by both
}
\examples{
morie_vit2lf(q = 0.5, k = c(1, 2, 3, 4, 5, 6, 7, 8), v = c(1, 2, 3, 4, 5, 6, 7, 8))
}
\keyword{internal}

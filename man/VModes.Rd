% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aaa_rangayyan_tf.R
\name{VModes}
\alias{VModes}
\title{Variational mode decomposition, Dragomiretskiy & Zosso (2014)}
\usage{
VModes(
  x,
  K = 3,
  alpha = 2000,
  tau = 0,
  init = "uniform",
  tol = 1e-07,
  max_iter = 300
)
}
\arguments{
\item{x}{Passed to \code{.tf_need}.}

\item{K}{Coerced to integer by the body, with \code{as.integer}. Defaults to \code{3}.}

\item{alpha}{Coerced to numeric by the body, with \code{as.numeric}. Defaults to \code{2000}.}

\item{tau}{Coerced to numeric by the body, with \code{as.numeric}. Defaults to \code{0}.}

\item{init}{Coerced to character by the body, with \code{as.character}. Defaults to
\code{"uniform"}.}

\item{tol}{Coerced to numeric by the body, with \code{as.numeric}. Defaults to \code{1e-07}.}

\item{max_iter}{Coerced to integer by the body, with \code{as.integer}. Defaults to \code{300}.}
}
\value{
A list with \code{modes}, \code{center_freqs}, \code{K}, \code{alpha},
\code{tau}, \code{iterations}, \code{converged}, \code{reconstruction_error},
\code{residual_energy}, \code{method}.
}
\description{
A step of the rangayyan_tf implementation. No other function in the package calls it.
See the file header for the source the module follows.
source it follows.
}
\examples{
V <- c(1, 2, 3, 4, 5, 6, 7, 8)
VModes(V)
}
\keyword{internal}

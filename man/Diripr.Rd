% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diripr.R
\name{Diripr}
\alias{Diripr}
\title{Dirichlet-multinomial conjugate model}
\usage{
Diripr(counts, alpha = 1)
}
\arguments{
\item{counts}{Observed category counts, non-negative.}

\item{alpha}{Dirichlet prior; a scalar is recycled.}
}
\value{
List with \code{estimate}, \code{post_mean}, \code{post_var},
\code{post_alpha}, \code{log_marginal}, \code{n}, \code{K},
\code{method}.
}
\description{
Formula: p ~ Dir(alpha); y ~ Multinomial(n, p)
}
\details{
The posterior is Dir(alpha + y), so the posterior mean of p_j is
(y_j + alpha_j) / (n + sum alpha) and the marginal likelihood is the
Polya distribution
n! Gamma(A)/Gamma(n+A) prod Gamma(y_j+a_j)/(y_j! Gamma(a_j)).  With
two categories this is exactly the beta-binomial.
}
\references{
Gelman et al. (2013), Bayesian Data Analysis, 3rd ed.,
CRC, ch. 3.
}
\examples{
V <- c(1, 2, 3, 4, 5, 6, 7, 8)
Diripr(V)
}

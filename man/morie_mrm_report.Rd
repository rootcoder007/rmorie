% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mrm_flagship.R
\name{morie_mrm_report}
\alias{morie_mrm_report}
\title{Render a publication-ready table from an MRM effect object}
\usage{
morie_mrm_report(
  effect,
  format = c("text", "markdown", "latex", "html"),
  digits = 3,
  caption = "MRM causal-effect estimates"
)
}
\arguments{
\item{effect}{A \code{morie_mrm_effect} object (or any list with a
compatible \code{results} data frame).}

\item{format}{One of \code{"text"}, \code{"markdown"},
\code{"latex"}, \code{"html"}.}

\item{digits}{Significant digits (default 3).}

\item{caption}{Table caption.}
}
\value{
Character vector of rendered lines (invisibly printed for
\code{"text"}).
}
\description{
rmorie's own renderer (no stargazer / gt / gtable): the same table
in plain text, Markdown, LaTeX (booktabs-style rules without
requiring the package), or HTML.
}
\examples{
set.seed(1)
df <- data.frame(y = rnorm(300), t = rbinom(300, 1, 0.5),
                 x = rnorm(300))
eff <- morie_mrm_estimate_causal_effect(df, "t", "y", "x",
                                        methods = "ate")
cat(morie_mrm_report(eff, format = "markdown"), sep = "\n")
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/toronto_neighbourhoods.R
\name{morie_tps_disaggregate_140_to_158}
\alias{morie_tps_disaggregate_140_to_158}
\title{Disaggregate per-140 counts to per-158 counts (uniform-density)}
\usage{
morie_tps_disaggregate_140_to_158(
  df,
  hood_140_col = "HOOD_140",
  count_cols = NULL,
  crosswalk = NULL
)
}
\arguments{
\item{df}{A \code{data.frame} keyed on a 140-hood column.}

\item{hood_140_col}{Name of the 140-hood column. Default
\code{"HOOD_140"}.}

\item{count_cols}{Character vector of numeric count columns to
disaggregate. Default: every numeric column in \code{df} except
\code{hood_140_col}.}

\item{crosswalk}{Optional pre-loaded crosswalk; defaults to
\code{morie_to_hood_crosswalk()}.}
}
\value{
A \code{data.frame} with columns \code{hood_158}, \code{name_158},
\code{hood_140}, all chosen \code{count_cols} (now per-158 fractional
counts), and \code{pct_140_in_158} (the cake-cut weight applied).
}
\description{
Cake-cutting in the FORWARD direction. Given a \code{data.frame} of per-
historical-hood counts (one row per \code{hood_140}, one or more numeric
count columns), splits each 140's count across its 158-children in
proportion to \code{pct_140_in_158 / 100}. For 1:1 hoods the count is
passed through unchanged. For splits the count is partitioned
(e.g. 140-75 Church-Yonge Corridor's 100 incidents become 59.24 in
158-168 Downtown Yonge East and 40.76 in 158-167 Church-Wellesley).
}
\details{
This assumes UNIFORM SPATIAL DENSITY of the underlying events
inside each 140-hood -- which is the best you can do without per-
incident lat/lon. If you have lat/lon, prefer re-binning from
points (via \code{sf::st_join} against
\code{morie_to_neighbourhoods("158", offline = FALSE)}) over this
uniform-density approximation.
}
\examples{
df <- data.frame(HOOD_140 = c("075", "001"),
                 incidents = c(100, 42))
morie_tps_disaggregate_140_to_158(df)
}

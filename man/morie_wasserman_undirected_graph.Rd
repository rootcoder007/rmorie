% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wasserman_native.R
\name{morie_wasserman_undirected_graph}
\alias{morie_wasserman_undirected_graph}
\title{Exact undirected clique model (Ch 17, wsmund)}
\usage{
morie_wasserman_undirected_graph(graph, psi)
}
\arguments{
\item{graph}{List(n_nodes, cliques) with cliques as 0-based index vectors.}

\item{psi}{List of clique potentials, each returning a positive value.}
}
\value{
List with \code{estimate} (Z), \code{probabilities}, \code{n_nodes},
\code{n_cliques}, \code{method}.
}
\description{
p(x) = prod_C psi_C(x_C) / Z with Z summed over all 2^n binary
configurations -- the didactic exact version, capped at 20 nodes.
Configuration index is the binary number with x_0 as the most
significant bit.
}
\examples{
agree <- function(t) if (t[1] == t[2]) 2 else 1
morie_wasserman_undirected_graph(list(2, list(c(0, 1))), list(agree))$estimate
}

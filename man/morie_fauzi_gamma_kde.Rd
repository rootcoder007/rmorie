% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fauzi_native.R
\name{morie_fauzi_gamma_kde}
\alias{morie_fauzi_gamma_kde}
\title{Chen gamma-kernel density estimate}
\usage{
morie_fauzi_gamma_kde(x, grid = NULL, h = NULL, modified = FALSE, a = 2)
}
\arguments{
\item{x}{non-negative numeric sample.}

\item{grid}{evaluation points in \verb{[0, Inf)}; data-driven if \code{NULL}.}

\item{h}{bandwidth; \code{sd(x) * n^{-2/5}} if \code{NULL}.}

\item{modified}{apply the self-elimination correction.}

\item{a}{the correction's second-bandwidth multiplier, not 1.}
}
\value{
list: grid, density, bandwidth, modified, a,
boundary_consistent (\code{TRUE}), bias_order, mass, why_it_works, n,
method.
}
\description{
The gamma kernel's support IS \verb{[0, Inf)}, so no mass ever lands on
the negative half-line and the boundary bias simply does not
arise: the estimator is consistent at zero, where a
Gaussian-kernel estimate is not. \code{modified = TRUE} applies Fauzi's
self-elimination correction, which cancels the leading bias term.
}
\examples{
morie_fauzi_gamma_kde(stats::rexp(200), grid = c(0, 1), h = 0.1)$density
}
\references{
Fauzi and Maesono (2023), Ch. 1; Chen (1999).
}

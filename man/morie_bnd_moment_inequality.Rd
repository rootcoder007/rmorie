% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bnd_native.R
\name{morie_bnd_moment_inequality}
\alias{morie_bnd_moment_inequality}
\title{Moment-inequality criterion and confidence set}
\usage{
morie_bnd_moment_inequality(
  data,
  g,
  theta_grid,
  alpha = 0.05,
  B = 500,
  seed = 0
)
}
\arguments{
\item{data}{observations, rows passed to \code{g}.}

\item{g}{\verb{function(data, theta)} returning an (n, J) matrix
oriented so the model says \code{E[g_j] <= 0}.}

\item{theta_grid}{candidate parameter values.}

\item{alpha}{miss probability.}

\item{B}{multiplier-bootstrap draws.}

\item{seed}{bootstrap seed.}
}
\value{
list: theta_grid, criterion, critical_value,
in_confidence_set, set_estimate, confidence_set_bounds, n, J,
method.
}
\description{
Chernozhukov, Hong and Tamer (2007). The sample criterion
\verb{Q_n(theta) = sum_j [sqrt(n) gbar_j / sd_j]_+^2} counts only
VIOLATED inequalities, so deep inside the identified set -- where
every sample moment is negative -- it is EXACTLY zero. The
confidence region is the level set of \code{Q_n} under a multiplier
bootstrap of the RECENTRED moments: the null sits at the boundary
of the binding inequalities, not at the sample slack, and skipping
the recentring makes the cutoff grow with slack.
}
\examples{
d <- cbind(stats::rnorm(50, 1), stats::rnorm(50, 3))
g <- function(d, th) cbind(d[, 1] - th, th - d[, 2])
morie_bnd_moment_inequality(d, g, c(2))$criterion
}
\references{
Chernozhukov, Hong and Tamer (2007), \emph{Econometrica}
75:1243-1284; Andrews and Soares (2010), \emph{Econometrica}
78:119-157.
}

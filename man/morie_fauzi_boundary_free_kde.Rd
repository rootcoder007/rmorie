% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fauzi_native.R
\name{morie_fauzi_boundary_free_kde}
\alias{morie_fauzi_boundary_free_kde}
\title{Boundary-free kernel density estimate by bijection}
\usage{
morie_fauzi_boundary_free_kde(x, grid = NULL, h = NULL, transform = "log")
}
\arguments{
\item{x}{numeric sample strictly inside the support.}

\item{grid}{evaluation points strictly inside the support.}

\item{h}{bandwidth on the transformed scale.}

\item{transform}{\code{"log"} or \code{"identity"}.}
}
\value{
list: grid, density, bandwidth, transform, jacobian
(\verb{= 1/g'(g^\{-1\}(t))}), g_prime, mass, boundary_bias_order,
jacobian_note, n, method.
}
\description{
Estimates on the transformed scale, where a symmetric kernel is
legitimate because the support is unbounded, then maps back:
\verb{f~_X(t) = 1/(n h g'(g^\{-1\}(t))) sum K((g^\{-1\}(t) - g^\{-1\}(X_i))/h)}.
}
\details{
The factor \verb{1/g'(g^\{-1\}(t))} is mandatory. Without it the result
is a density on the transformed scale, not the original one. (The
distilled text file in the reference library omits it; the PDF
does not.)
}
\examples{
morie_fauzi_boundary_free_kde(stats::rexp(300), grid = c(0.1, 1))$density
}
\references{
Fauzi and Maesono (2023), Ch. 4. Transcribed from the PDF.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/effects.R
\name{estimate_pliv}
\alias{estimate_pliv}
\title{Partially Linear IV (PLIV) / Local Average Treatment Effect}
\usage{
estimate_pliv(
  data,
  treatment,
  outcome,
  instrument,
  covariates,
  n_folds = 5L,
  random_state = 42L
)
}
\arguments{
\item{data}{Data frame with all required columns.}

\item{treatment}{Endogenous treatment column name.}

\item{outcome}{Outcome column name.}

\item{instrument}{Instrument column name.}

\item{covariates}{Exogenous covariate column names.}

\item{n_folds}{Cross-fitting folds (DoubleML path). Default 5.}

\item{random_state}{RNG seed. Default 42.}
}
\value{
Named list with \code{late}, \code{se}, \code{ci_lower}, \code{ci_upper},
\code{pval}, \code{n_obs}, \code{method}.
}
\description{
Native 2SLS in base R: first stage \code{D ~ Z + X}, second stage
\code{Y ~ D_hat + X}. Cross-validated against \pkg{DoubleML} in the
package's cross tests but not calling it at runtime.
}
\examples{
set.seed(1)
n <- 80
x1 <- rnorm(n)
x2 <- rnorm(n)
z <- rbinom(n, 1, 0.5)
d <- as.integer(plogis(0.3 + 0.8 * z + 0.4 * x1) > runif(n))
y <- 1 + 0.5 * d + 0.3 * x1 + rnorm(n)
df <- data.frame(y, d, z, x1, x2)
res <- suppressWarnings(estimate_pliv(df, "d", "y", "z", c("x1", "x2")))
res$late
}

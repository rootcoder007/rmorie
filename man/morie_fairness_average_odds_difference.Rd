% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fairness_metrics.R, R/frns_metrics.R
\name{morie_fairness_average_odds_difference}
\alias{morie_fairness_average_odds_difference}
\title{Average Odds Difference (mean of TPR and FPR gaps)}
\usage{
morie_fairness_average_odds_difference(
  y_true,
  y_pred,
  group,
  privileged = NULL,
  favorable = 1
)

morie_fairness_average_odds_difference(
  y_true,
  y_pred,
  group,
  privileged = NULL,
  favorable = 1
)
}
\arguments{
\item{y_true}{Vector of realised ground-truth outcomes.}

\item{y_pred}{Vector of system decisions.}

\item{group}{Vector of protected-attribute values.}

\item{privileged}{The reference group (inferred if \code{NULL}).}

\item{favorable}{The value treated as the positive class (default \code{1}).}
}
\value{
A \code{morie_fairness_result}; headline value is the
largest absolute AOD across groups.

A named list: \code{value} (largest absolute AOD),
\code{average_odds_difference}, \code{rates}, \code{privileged}, \code{warnings},
\code{interpretation}.
}
\description{
For each non-reference group, the average odds difference is
\code{0.5 * ((FPR_group - FPR_ref) + (TPR_group - TPR_ref))}.
Zero means parity of errors; values away from zero mean the
combined error profile favours one group over another. Used in
IBM AIF360 and in the COMPAS \emph{XAI Stories} audit.

For each non-reference group,
\code{0.5 * ((FPR_group - FPR_ref) + (TPR_group - TPR_ref))}. Zero means
parity of errors. This is the single-number summary used in IBM
AIF360 and the COMPAS \emph{XAI Stories} audit.
}
\examples{
truth <- c(1, 0, 1, 0, 1, 0, 1, 0)
pred <- c(1, 0, 1, 0, 1, 1, 0, 1)
race <- c(rep("A", 4), rep("B", 4))
res <- morie_fairness_average_odds_difference(truth, pred, race,
  privileged = "A"
)
res$value # 0.25
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aaa_sp_fill.R
\name{ShrinkPred}
\alias{ShrinkPred}
\title{Thetahat_j = muhat + (1 - lambda_j)(ybar_j - muhat),}
\usage{
ShrinkPred(y, cluster, sigma2_u, sigma2_e)
}
\arguments{
\item{y}{Passed to \code{.morie_spx_chkv}.}

\item{cluster}{Passed to \code{.morie_spx_chkv}.}

\item{sigma2_u}{Coerced to numeric by the body, with \code{as.numeric}.}

\item{sigma2_e}{Coerced to numeric by the body, with \code{as.numeric}.}
}
\value{
A list with \code{clusters}, \code{shrunk}, \code{raw}, \code{lambda},
\code{sizes}, \code{grand_mean}, \code{sigma2_u}, \code{sigma2_e},
\code{shrinkage_depends_on_cluster_size}, \code{n}, \code{method}.
}
\description{
lambda_j = sigma2_e / (sigma2_e + n_j sigma2_u), and muhat the GLS mean
sum_j w_j ybar_j / sum_j w_j with w_j = 1/(sigma2_u + sigma2_e/n_j)
(reported as \code{grand_mean}).  lambda depends on
the CLUSTER'S OWN SIZE; a common lambda over-shrinks the large
clusters. Stein (1956); Morris (1983) JASA 78:47-55.  NOT in
Schabenberger & Gotway -- a fixed-string search for "shrinkage"
returns nothing.
}
\examples{
set.seed(4)
y <- c(rnorm(5, 1), rnorm(8, 2), rnorm(3, 0))
cl <- c(rep(0, 5), rep(1, 8), rep(2, 3))
ShrinkPred(y, cl, sigma2_u = 0.5, sigma2_e = 1)
}
\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geron_train_native.R
\name{morie_geron_transformer_encoder_block}
\alias{morie_geron_transformer_encoder_block}
\title{Transformer encoder block, post-norm (Geron Ch 16, morie.fn grteb)}
\usage{
morie_geron_transformer_encoder_block(
  x,
  mha_weights,
  ffn_weights,
  mask = NULL,
  eps = 1e-05
)
}
\arguments{
\item{x}{(T, d_model) tokens.}

\item{mha_weights}{Attention weights, optionally with \code{gamma}/\code{beta}.}

\item{ffn_weights}{Feed-forward weights, optionally with
\code{gamma}/\code{beta}.}

\item{mask}{Optional attention mask.}

\item{eps}{Layer-norm floor.}
}
\value{
List with \code{output}, \code{attention_output}, \code{attention_weights},
\code{hidden}, \code{ffn_output}.
}
\description{
h = LN(x + MHA(x)); y = LN(h + FFN(h)).
}
\examples{
set.seed(2)
X <- matrix(rnorm(12), 3, 4)
h1 <- matrix(rnorm(8, 0, 0.4), 4, 2)
mha <- list(WQ = list(h1, h1), WK = list(h1, h1), WV = list(h1, h1),
            WO = matrix(rnorm(16, 0, 0.4), 4, 4),
            gamma = rep(1, 4), beta = rep(0, 4))
ffn <- list(W1 = matrix(rnorm(32, 0, 0.4), 4, 8),
            W2 = matrix(rnorm(32, 0, 0.4), 8, 4),
            gamma = rep(1, 4), beta = rep(0, 4))
r <- morie_geron_transformer_encoder_block(X, mha, ffn)
str(r, max.level = 1)
}

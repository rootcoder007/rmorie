% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geostat_native.R
\name{morie_spatial_variogram}
\alias{morie_spatial_variogram}
\title{Empirical semivariogram}
\usage{
morie_spatial_variogram(coords, values, n_bins = 15L, cutoff = NULL)
}
\arguments{
\item{coords}{Two-column matrix (or data frame) of coordinates.}

\item{values}{Numeric vector of observations.}

\item{n_bins}{Number of distance bins (default 15).}

\item{cutoff}{Maximum distance considered (default one third of the
maximum pairwise distance, the gstat convention).}
}
\value{
A data frame with \code{dist} (bin centre), \code{gamma}
(semivariance), and \code{np} (pair count per bin).
}
\description{
Classical Matheron estimator: half the average squared difference
of values within each distance bin.
}
\examples{
set.seed(1)
coords <- cbind(runif(60, 0, 10), runif(60, 0, 10))
values <- coords[, 1] * 0.5 + rnorm(60, 0, 0.3)
str(morie_spatial_variogram(coords, values), max.level = 1)
}
\references{
Matheron, G. (1963). Principles of geostatistics.
\emph{Economic Geology}, 58(8), 1246--1266.
}

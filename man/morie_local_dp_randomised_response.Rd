% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial_models.R
\name{morie_local_dp_randomised_response}
\alias{morie_local_dp_randomised_response}
\title{k-ary randomised response (local differential privacy)}
\usage{
morie_local_dp_randomised_response(truth, k, epsilon, seed = 2)
}
\arguments{
\item{truth}{integer vector of true values in \verb{0..k-1}}

\item{k}{number of categories}

\item{epsilon}{privacy parameter; smaller is more private}

\item{seed}{RNG seed}
}
\value{
list with \code{reports}, \code{observed}, \code{estimate}, \code{p_keep},
\code{p_flip}
}
\description{
\verb{P(report = v | true = u)} is \code{e^eps / (k - 1 + e^eps)} when
\code{v == u} and \code{1 / (k - 1 + e^eps)} otherwise, so the ratio of any
two conditional probabilities is at most \code{e^eps} -- the
eps-local-differential-privacy guarantee.  Each respondent perturbs
their own value, so the collector never sees the truth.  Raw counts
are biased towards uniform, hence the debiased estimate that
inverts the transition matrix.
}
\examples{
morie_local_dp_randomised_response(truth = list(a = 1, b = 2), k = 5L, epsilon = 5L)
}

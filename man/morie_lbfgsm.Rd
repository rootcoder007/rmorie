% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lbfgsm_native.R
\name{morie_lbfgsm}
\alias{morie_lbfgsm}
\title{morie_lbfgsm}
\usage{
morie_lbfgsm(
  fun,
  x0,
  grad,
  m = 10,
  max_iter = 200,
  tol = 1e-08,
  tol_type = c("absolute", "relative", "initial"),
  precond = NULL,
  c1 = 1e-04,
  c2 = 0.9,
  max_ls = 60
)
}
\arguments{
\item{fun}{Accepted by the signature and not used anywhere in the body.}

\item{x0}{Coerced to numeric by the body, with \code{as.numeric}.}

\item{grad}{Accepted by the signature and not used anywhere in the body.}

\item{m}{Coerced to integer by the body, with \code{as.integer}. Defaults to \code{10}.}

\item{max_iter}{Coerced to integer by the body, with \code{as.integer}. Defaults to \code{200}.}

\item{tol}{Passed to \code{<=}. Defaults to \code{1e-08}.}

\item{tol_type}{Which stopping criterion \code{tol} applies to.
\code{"absolute"} (the default) tests the gradient norm directly.
\code{"initial"} tests it against its value at the starting point, and
\code{"relative"} against \verb{1 + |f|} -- the form SciPy and NLopt default
to. See the note below before choosing \code{"relative"}.}

\item{precond}{Diagonal preconditioner for the initial inverse
Hessian: \code{NULL} for none, a positive numeric vector giving the
Hessian's diagonal, or \code{"auto"} to estimate it from the most recent
curvature pair. Measured on the quadratics below, \code{"auto"} did not
help; a supplied diagonal is the useful form.}

\item{c1}{Numeric; combined arithmetically in the body. Defaults to \code{1e-04}.}

\item{c2}{Numeric; combined arithmetically in the body. Defaults to \code{0.9}.}

\item{max_ls}{Coerced to integer by the body, with \code{as.integer}. Defaults to \code{60}.}
}
\value{
A list with \code{estimate}, \code{x}, \code{fun}, \code{grad},
\code{grad_norm}, \code{iterations}, \code{n_fun}, \code{memory}, \code{converged},
\code{status} (why the loop stopped: the gradient test, a stall at
the optimum, a line search that found no Wolfe step, or the
iteration limit),
\code{history}, \code{method}.
}
\description{
A step of the lbfgsm_native implementation. No other function in the package calls it.
See the file header for the source the module follows.
source it follows.
}
\section{Conditioning, and what the tolerance means}{

An \strong{absolute} gradient test depends on how the objective is scaled:
multiply \code{f} by a thousand and the same point stops passing. It is
also a strict thing to ask of L-BFGS on an ill-conditioned problem.
Measured over 120 random twenty-dimensional quadratics with condition
numbers from about 10 to 1.4e5, at \code{tol = 1e-7}:\tabular{llll}{
   setting \tab not converged \tab convergence claimed away from the optimum \tab median error in \code{x} \cr
   \code{"absolute"}, \code{m = 10} \tab 72 / 120 \tab 0 \tab 2.6e-07 \cr
   \code{"absolute"}, \code{m = 30} \tab 63 / 120 \tab 0 \tab 2.5e-08 \cr
   \code{"initial"}, \code{m = 10} \tab 58 / 120 \tab 0 \tab 3.9e-07 \cr
   \code{"relative"}, \code{m = 10} \tab 21 / 120 \tab \strong{11} \tab 4.7e-07 \cr
}


Read the third column before the second. \code{"relative"} converges far
more often and reaches that number partly by declaring convergence at
points that are not stationary -- eleven of 120, at errors above
1e-5. Failing to reach an unnaturally tight tolerance is better than
reporting success at a non-stationary point, so \code{"absolute"} remains
the default and \code{"relative"} is offered for callers who want the
SciPy convention and know what it admits. \code{"initial"} is the
scale-free criterion that does not have this failure mode.

A problem with a condition number at or above 1e4 naturally stalls
around a relative error of 1e-5, and no choice of tolerance changes
that -- the iteration stops at the \emph{first} iterate under the
threshold, so the margin is about 1.5 times at every tolerance.
Raising \code{m} is the adjustment that helps: from 10 to 30 cut the
failures and improved the median solution by an order of magnitude,
at the cost of holding three times the history. \code{grad_norm},
\code{grad_norm_relative} and \code{grad_norm_ratio} are all reported so the
behaviour can be judged rather than inferred.
}

\keyword{internal}

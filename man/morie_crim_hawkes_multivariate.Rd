% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crim_native.R
\name{morie_crim_hawkes_multivariate}
\alias{morie_crim_hawkes_multivariate}
\title{Multivariate Hawkes process (exponential kernels)}
\usage{
morie_crim_hawkes_multivariate(times, marks, t_max = NULL, beta = NULL)
}
\arguments{
\item{times}{Numeric event times.}

\item{marks}{Integer/factor component labels (length of times).}

\item{t_max}{Observation horizon. Default \code{max(times)}.}

\item{beta}{Fixed decay rate (shared); estimated when NULL via an
outer profile grid.}
}
\value{
List of class \code{"morie_mv_hawkes"}: mu (K-vector),
A (KxK excitation matrix), beta, loglik, spectral_radius, n,
converged, call.
}
\description{
K-dimensional mutually exciting process with kernel matrix
\eqn{\phi_{jk}(u) = a_{jk} \beta e^{-\beta u}}: MLE via the exact
log-likelihood with the O(n) exponential recursion per (j,k) pair.
}
\examples{
set.seed(2)
tt <- sort(runif(150, 0, 100))
mk <- sample(1:2, 150, TRUE)
morie_crim_hawkes_multivariate(tt, mk, beta = 1)
}
\references{
Hawkes (1971) Biometrika 58(1).
}

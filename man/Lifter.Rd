% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aaa_rangayyan_cep.R
\name{Lifter}
\alias{Lifter}
\title{Section 4.7.3: the vocal tract lives at LOW quefrency, the glottal}
\usage{
Lifter(cepstrum_values, low = NULL, high = NULL, keep = "low")
}
\arguments{
\item{cepstrum_values}{Coerced to numeric by the body, with \code{as.numeric}.}

\item{low}{Optional; may be \code{NULL}. Coerced to integer by the body, with \code{as.integer}.}

\item{high}{Optional; may be \code{NULL}. Coerced to integer by the body, with \code{as.integer}.}

\item{keep}{One of \code{"band"}, \code{"high"}, \code{"low"}. Defaults to \code{"low"}.}
}
\value{
A list with \code{liftered}, \code{n}, \code{low}, \code{high}, \code{keep},
\code{symmetric}, \code{n_kept}, \code{energy_kept}, \code{method}.
}
\description{
excitation at the pitch period and its multiples.  The window is
applied SYMMETRICALLY about zero quefrency, because the cepstrum of a
mixed-phase signal is two-sided (eq 4.72) and keeping only the causal
half would discard the maximum-phase component.
}
\examples{
V <- c(1, 2, 3, 4, 5, 6, 7, 8)
Lifter(V)
}
\keyword{internal}

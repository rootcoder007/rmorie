% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convgs.R
\name{Convgs}
\alias{Convgs}
\title{Convergent validity of a reflective construct}
\usage{
Convgs(loadings, residuals = NULL)
}
\arguments{
\item{loadings}{Factor loadings of the indicators on their construct.}

\item{residuals}{Indicator residual variances, or NULL for
1 - lambda^2.}
}
\value{
List with \code{estimate} (AVE), \code{ave}, \code{cr},
\code{adequate}, \code{n_items}, \code{method}.
}
\description{
Formula: AVE = sum lambda_i^2 / (sum lambda_i^2 + sum theta_i)
}
\details{
With standardised loadings the residual variance of an indicator is
theta_i = 1 - lambda_i^2, which is what is used when \code{residuals}
is not supplied.  Composite reliability follows the same algebra,
CR = (sum lambda_i)^2 / ((sum lambda_i)^2 + sum theta_i).  Fornell
and Larcker's rule is AVE >= 0.5 with CR >= 0.7.
}
\examples{
Convgs(loadings = c(0.7, 0.8, 0.75, 0.6))
}
\references{
Fornell & Larcker (1981), J. Marketing Research
18(1):39-50.
}

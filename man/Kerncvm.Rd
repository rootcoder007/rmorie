% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fznkc.R
\name{Kerncvm}
\alias{Kerncvm}
\alias{morie_fauzi_naive_kernel_cvm}
\title{Naive kernel-smoothed Cramer-von Mises statistic (Eq. 5.4)}
\usage{
Kerncvm(x, quantile, h = NULL, ngrid = 2001L)

morie_fauzi_naive_kernel_cvm(x, quantile, h = NULL, ngrid = 2001L)
}
\arguments{
\item{x}{Sample.}

\item{quantile}{The null quantile function \code{F^-1(u)} for \code{u} in \verb{(0,1)}.}

\item{h}{Bandwidth; defaults to the distribution-function rule.}

\item{ngrid}{Number of \code{u}-nodes; fixed, never adapted.}
}
\value{
Named list with \code{statistic}, \code{p_value}, \code{h}, \code{n}, \code{method}.
}
\description{
Eq. (5.4): \deqn{\widehat{CvM} = n\int [\hat F_X(x)-F(x)]^2 dF(x),}{CvMhat = n int [Fhat_X(x) - F(x)]^2 dF(x),}
with \code{Fhat_X} the naive kernel distribution function estimator.
}
\details{
Unlike the empirical \code{CvM_n} this has no finite-sum closed form: \code{Fhat_X} is
smooth, so the integral does not collapse onto the order statistics. It is
evaluated by substituting \code{u = F(x)} and integrating over \code{u} in \verb{(0,1)} on
a fixed equally spaced grid -- which turns \code{dF(x)} into \code{du} and needs no
density, only the quantile function.

This module previously carried a copy of the empirical KS body. It now
computes a Cramer-von Mises statistic, and a smoothed one.

Theorem 5.1 gives \verb{|CvM_n - CvMhat| ->_p 0}, so the Cramer-von Mises
critical values still apply.
}
\examples{
Kerncvm(c(0.1, 0.3, 0.5, 0.7, 0.9), quantile = function(u) u)
}
\references{
Fauzi and Maesono (2023), Eq. (5.4), Theorem 5.1.
}
\keyword{internal}

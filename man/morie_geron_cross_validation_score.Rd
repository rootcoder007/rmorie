% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geron_ml_native.R
\name{morie_geron_cross_validation_score}
\alias{morie_geron_cross_validation_score}
\title{K-fold cross-validation score (Geron Ch 1/2, morie.fn grcvs)}
\usage{
morie_geron_cross_validation_score(
  X,
  y,
  K,
  fit = NULL,
  predict = NULL,
  score = NULL,
  shuffle = FALSE,
  random_state = NULL
)
}
\arguments{
\item{X, y}{Data. @param K Folds, 2 <= K <= m.}

\item{fit, predict, score}{Optional callables.}

\item{shuffle}{Must be FALSE. @param random_state Ignored.}
}
\value{
List with \code{cv_score}, \code{fold_scores}, \code{fold_sizes}, \code{se},
\code{worst_fold}, \code{spread}.
}
\description{
Folds follow numpy's array_split: the first m \%\% K folds get one extra
row. Default model is OLS via minimum-norm least squares, scored by
R^2. \code{shuffle} is not supported, because numpy's default_rng stream
cannot be reproduced in R -- passing TRUE is an error rather than a
silently different split.
}

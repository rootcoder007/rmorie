% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rgpsd.R
\name{rgpsd}
\alias{rgpsd}
\alias{morie_rangayyan_psd}
\title{Welch power spectral density -- Rangayyan Ch 4}
\usage{
rgpsd(x, fs = 1, nperseg = NULL, window = "hann")

morie_rangayyan_psd(x, fs = 1, nperseg = NULL, window = "hann")
}
\arguments{
\item{x}{Numeric vector.}

\item{fs}{Sampling frequency (Hz).}

\item{nperseg}{Segment length (default \code{min(N, 256)}).}

\item{window}{Window name (\code{"hann"} default).}
}
\value{
Named list \code{freqs}, \code{psd}, \code{fs}, \code{nperseg}, \code{peak_freq},
\code{total_power}.
}
\description{
Welch-averaged one-sided PSD via \code{\link[stats:spec.pgram]{stats::spec.pgram()}} on overlapping
detrended segments. Same algorithm as SciPy's \code{welch} so band powers
agree to ~1e-3 for typical EEG/ECG inputs.
}
\examples{
\donttest{
set.seed(0)
fs <- 100
t <- seq(0, 10, length.out = 1000)
x <- sin(2 * pi * 10 * t)
r <- rgpsd(x, fs = fs, nperseg = 256)
abs(r$peak_freq - 10) < 1
}
}
\references{
Welch (1967); Rangayyan Ch 4.
}
\keyword{internal}

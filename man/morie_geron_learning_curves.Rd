% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geron_train_native.R, R/geron_w4b_native.R
\name{morie_geron_learning_curves}
\alias{morie_geron_learning_curves}
\title{Learning curves (Geron Ch 4, morie.fn grlrnc)}
\usage{
morie_geron_learning_curves(
  X,
  y,
  n_splits = 10,
  val_fraction = 0.2,
  fit = NULL,
  predict = NULL,
  seed = 0
)

morie_geron_learning_curves(
  X,
  y,
  n_splits = 10,
  val_fraction = 0.2,
  fit = NULL,
  predict = NULL,
  seed = 0
)
}
\arguments{
\item{X, y}{Data. @param n_splits Sizes to evaluate. @param val_fraction Held-out fraction.}

\item{n_splits}{Number of training sizes (unique after rounding).}

\item{val_fraction}{Validation share in (0, 1).}

\item{fit, predict}{Optional callables (default OLS via lstsq). @param seed Split seed.}
}
\value{
List with \code{train_sizes}, \code{train_rmse}, \code{val_rmse},
\code{final_gap}, \code{val_size}.

List with train_sizes, rmse_train, rmse_val, final_gap, verdict, estimate, n, method.
}
\description{
OLS refit (normal equation) on growing prefixes of the training half;
the split is the FIRST m - n_val rows, no shuffling.

Split order via the shared LCG stream, not numpy PCG64 permutation.
}

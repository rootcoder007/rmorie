% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ksr08.R
\name{Multboot}
\alias{Multboot}
\alias{morie_ksr08_kosorok_multiplier_bootstrap}
\alias{morie_kosorok_multiplier_bootstrap}
\title{Multiplier bootstrap with exponential weights (Dirichlet weights)}
\usage{
Multboot(x, B = 200, seed = 1)

morie_ksr08_kosorok_multiplier_bootstrap(x, B = 200, seed = 1)

morie_kosorok_multiplier_bootstrap(x, B = 200, seed = 1)
}
\arguments{
\item{x}{The sample.}

\item{B}{Number of replicates (fixed budget).}

\item{seed}{Seed for the pinned generator.}
}
\value{
List with \code{estimate}, \code{boot_mean}, \code{boot_sd},
\code{process_sd}, \code{ci_lower}, \code{ci_upper}, \code{mu},
\code{tau}, \code{B}, \code{n}.
}
\description{
The weights are divided by their own mean so the total weight stays n.
With standard exponential multipliers mu = tau = 1, so the scaling
factor is 1 -- written out rather than dropped, because it is not 1 for
any other weight distribution.
}
\details{
Formula: Ptilde_n f = n^-1 sum_i (xi_i / xibar_n) f(X_i);
Gtilde_n = sqrt(n) (mu/tau) (Ptilde_n - P_n), xi ~ Exp(1)
}
\examples{
V <- c(1, 2, 3, 4, 5, 6, 7, 8)
Multboot(V)
}
\references{
Kosorok (2008), Introduction to Empirical Processes and
Semiparametric Inference, Section 2.2.3. Fetched as the full text of
the book.
}
\keyword{internal}

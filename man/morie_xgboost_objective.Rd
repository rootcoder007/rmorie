% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xgbst.R
\name{morie_xgboost_objective}
\alias{morie_xgboost_objective}
\title{XGBoost regularized objective (R parity)}
\usage{
morie_xgboost_objective(
  x,
  y,
  n_estimators = 100L,
  learning_rate = 0.1,
  max_depth = 3L,
  reg_lambda = 1,
  reg_alpha = 0,
  task = "auto",
  seed = 0L,
  deterministic_seed = NULL
)
}
\arguments{
\item{x}{Numeric predictor matrix.}

\item{y}{Response.}

\item{n_estimators}{Number of boosting rounds.}

\item{learning_rate}{eta / shrinkage.}

\item{max_depth}{Tree depth.}

\item{reg_lambda}{L2 leaf penalty.}

\item{reg_alpha}{L1 leaf penalty.}

\item{task}{"auto", "classification", or "regression".}

\item{seed}{RNG seed.}

\item{deterministic_seed}{Integer or NULL.  If supplied, the RNG state
is derived from the SHA-keyed \code{\link[=morie_det_rng]{morie_det_rng()}} so Py<->R streams
agree on the canonical fixture.  When \code{NULL} (default), behaviour
is unchanged: \code{seed} drives \code{set.seed()} directly.}
}
\value{
Named list: estimate, train_score, feature_importances, backend,
n_estimators, learning_rate, max_depth, reg_lambda, reg_alpha, task,
n, method.
}
\description{
Wraps the \code{xgboost} package.  If xgboost isn't installed, falls
back to \code{gbm} (gradient boosting) so users still get a usable
boosted-trees result; the backend is flagged in the output.
}
\examples{
morie_xgboost_objective(x = rnorm(50), y = rnorm(50))
}

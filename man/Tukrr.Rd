% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tukrr.R
\name{Tukrr}
\alias{Tukrr}
\title{Robust regression by iteratively reweighted least squares}
\usage{
Tukrr(X, y, c = 4.685, n_iter = 25)
}
\arguments{
\item{X}{Design matrix; supply your own intercept column.}

\item{y}{Response.}

\item{c}{Tuning constant.}

\item{n_iter}{Reweighting sweeps.}
}
\value{
List with \code{estimate}, \code{scale}, \code{weights},
\code{fitted}, \code{resid}, \code{n}.
}
\description{
Least squares gives a single bad row unbounded influence. The
biweight bounds it and, past the tuning constant, removes it. The
scale is re-estimated by MAD on every sweep, so the weights are not
decided by a scale the outliers themselves inflated.
}
\details{
Determinism: fixed sweeps, ordinary least squares start, no tolerance.

Formula: iterate \eqn{beta = argmin sum w_i (y_i - x_i primebeta)^2} with
\eqn{w_i = \[1 - (r_i/(c s))^2\]^2} inside and 0 outside.
}
\examples{
V <- c(1, 2, 3, 4, 5, 6, 7, 8)
Tukrr(V, V)
}
\references{
Beaton, A. E. & Tukey, J. W. (1974). Technometrics
16:147-185; Holland, P. W. & Welsch, R. E. (1977). Commun Statist
6:813-827.
}

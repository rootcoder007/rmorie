% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qbcfgs_native.R
\name{morie_qbcfgs}
\alias{morie_qbcfgs}
\title{A stratum-balanced treatment effect and its balance table}
\usage{
morie_qbcfgs(
  y,
  D,
  X,
  quantile = 0.5,
  n_strata = 4L,
  weight = "ate",
  n_trees = 8L,
  min_leaf = 3L,
  max_depth = 3L,
  seed = 0,
  clip = 0.01
)
}
\arguments{
\item{y}{Outcome.}

\item{D}{Binary treatment.}

\item{X}{Covariates.}

\item{quantile}{Which propensity quantile the focal stratum is read
at.}

\item{n_strata}{Number of equal-size strata.}

\item{weight}{"ate" weights each unit towards the whole population,
"att" towards the treated.}

\item{n_trees}{Trees in the propensity forest.}

\item{min_leaf}{Minimum leaf size.}

\item{max_depth}{Maximum depth.}

\item{seed}{The random stream.}

\item{clip}{The propensity is held inside the interval before it
enters a denominator. Reported, not silent.}
}
\value{
A list with the per-stratum effects and sizes, the overall
effect, the balance before and after weighting, and the focal
stratum.
}
\description{
A stratum-balanced treatment effect and its balance table
}
\examples{
N <- 48L
X <- matrix(0, N, 2)
D <- vapply(0:(N - 1L), function(i) if ((((i * 7)\%\%11)/10) +
    (((i * 5)\%\%13)/12) + ((i * 3)\%\%5) * 0.1 > 1.1) 1L else 0L,
    integer(1))
Y <- vapply(0:(N - 1L), function(i) round(2 + 1.5 * D[i + 1L] +
    2 * (((i * 7)\%\%11)/10) - 1 * (((i * 5)\%\%13)/12) + ((i * 11)\%\%7) *
    0.1, 4), numeric(1))
morie_qbcfgs(Y, D, X, 0.5, 4L, "ate", 6L, 3L, 3L, 6)
}
\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mrm_diagnostics.R
\name{mrm_standardised_difference}
\alias{mrm_standardised_difference}
\title{Imbens-Rubin standardised \%SMD per covariate}
\usage{
mrm_standardised_difference(data, treatment_col, covariates)
}
\arguments{
\item{data}{data.frame.}

\item{treatment_col}{Binary 0/1 treatment column name.}

\item{covariates}{Character vector of covariate columns.}
}
\value{
data.frame with covariate, mean_treated, mean_control,
pooled_sd, smd_pct, imbalanced columns.
}
\description{
For continuous X:  SMD = (mean_t - mean_c) / sqrt((s2_t + s2_c)/2)
For binary X:      SMD = (p_t - p_c) / sqrt((p_t(1-p_t) + p_c(1-p_c))/2)
Returned as percent.  |SMD| > 10 is the Austin (2009) imbalance threshold.
}
\examples{
set.seed(2026)
n <- 200L
df <- data.frame(
  D   = rbinom(n, 1, 0.4),
  age = rnorm(n, 50, 10),
  bmi = rnorm(n, 27, 4)
)
df$age[df$D == 1] <- df$age[df$D == 1] + 3 # deliberate imbalance
mrm_standardised_difference(df,
  treatment_col = "D",
  covariates = c("age", "bmi")
)
}

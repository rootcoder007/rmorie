% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fzsmpq.R
\name{Smpqnt}
\alias{Smpqnt}
\alias{morie_fauzi_sample_quantile}
\title{Sample quantile from the empirical distribution function}
\usage{
Smpqnt(x, p = 0.5)

morie_fauzi_sample_quantile(x, p = 0.5)
}
\arguments{
\item{x}{Sample.}

\item{p}{Probabilities in \verb{(0, 1]}.}
}
\value{
Named list with \code{estimate}, \code{index}, \code{p}, \code{n}, \code{method}.
}
\description{
\deqn{\hat Q(p) = \inf\{t : F_n(t) \ge p\},}{Qhat(p) = inf{t : F_n(t) >= p},}
the generalised inverse of the empirical df -- concretely the order
statistic \verb{X_(ceiling(n p))}, and the estimator Chapter 3 measures the
kernel quantile estimator against.
}
\details{
Its asymptotic variance is \verb{p(1-p) / (n f^2(Q(p)))} (Eq. 3.2), the same
first-order quantity the kernel estimator attains -- Remark 3.3 is explicit
that a kernel can only MATCH the sample quantile to first order, so any
gain must show up in the Edgeworth term. That is why Chapter 3 exists.

No interpolation and no plotting-position convention: the definition is a
strict infimum, so this is \code{stats::quantile(type = 1)} and NOT the default
\code{type = 7}. An interpolating quantile would silently change the estimand.
}
\examples{
Smpqnt(c(1, 2, 3, 4), p = 0.5)
}
\references{
Fauzi and Maesono (2023), Eq. (3.1) and the display defining the sample quantile in Sec. 3.2.
}
\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ddqn_native.R
\name{morie_ddqn}
\alias{morie_ddqn}
\title{Tabular Double Q-learning}
\usage{
morie_ddqn(
  P,
  R,
  gamma,
  alpha = 0.1,
  epsilon = 0.1,
  n_episodes = 100L,
  start = 0L,
  terminal = c(),
  max_steps = 1000L,
  seed = 0
)
}
\arguments{
\item{P}{List of A transition matrices, each (S, S) with rows
summing to 1.}

\item{R}{Reward, (S, A) matrix or a length-A list of (S, S)
matrices; see \code{\link{morie_mdpval}}.}

\item{gamma}{Discount factor.}

\item{alpha}{Step size.}

\item{epsilon}{Exploration probability.}

\item{n_episodes}{Number of episodes.}

\item{start}{Start state, 0-based.}

\item{terminal}{Terminal states, 0-based.}

\item{max_steps}{Step cap per episode.}

\item{seed}{Seed for the reproducible generator shared with the
Python arm.}
}
\value{
A list with \code{estimate} (the averaged (S, A) table),
\code{q1}, \code{q2}, \code{policy}, \code{v}, \code{n_steps},
\code{n_episodes} and \code{method}.
}
\description{
Maintains two independent action-value tables and, on each step,
updates one of them using the other for evaluation:
\eqn{Q_1(s,a) \leftarrow Q_1(s,a) + \alpha[r + \gamma
Q_2(s', \arg\max_{a'} Q_1(s',a')) - Q_1(s,a)]} (and symmetrically).
Because the table that selects the action is not the table that
evaluates it, the positive bias of the single \eqn{\max} in
\code{\link{morie_qlearn}} is removed (van Hasselt 2010).
}
\references{
van Hasselt, H. (2010). Double Q-learning. Advances in
Neural Information Processing Systems, 23, 2613-2621.
}

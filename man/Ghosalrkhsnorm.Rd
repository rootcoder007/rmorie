% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gh_c11_2.R
\name{Ghosalrkhsnorm}
\alias{Ghosalrkhsnorm}
\title{Concentration-function terms}
\usage{
Ghosalrkhsnorm(f0_coefs, lambdas, eps, n_sim = 3000, seed = 42)
}
\arguments{
\item{f0_coefs}{Coefficients of the target w0.}

\item{lambdas}{Coordinate variances of the process, all positive.}

\item{eps}{Radius.}

\item{n_sim}{Monte Carlo draws for the small-ball term.}

\item{seed}{Seed for the deterministic draws.}
}
\value{
List with \code{estimate} (phi), \code{decentering_norm2},
\code{small_ball_exponent}, \code{method}.
}
\description{
phi_{w0}(eps) = inf{ ||h||_H^2 / 2 : ||h - w0|| <= eps }
\itemize{
\item log P(||W|| < eps).
The two terms are the whole of Gaussian contraction theory: a
decentering (RKHS approximation) term and a small-ball term, and the
rate is where they balance.  For a diagonal series process the
infimum is attained by keeping the largest coordinates of w0 and
zeroing the rest, so the first term is a finite sum; the small-ball
probability is estimated on the l2 norm by Monte Carlo.
}
}
\details{
Formula: ||h||\emph{H^2 = sum}{i kept} f0_i^2 / lambda_i;
small-ball exponent = -log P(sqrt(sum_i lambda_i Z_i^2) < eps).
}
\references{
Ghosal & van der Vaart (2017), Fundamentals of
Nonparametric Bayesian Inference, CUP, eq. (11.11).
}
\examples{
Ghosalrkhsnorm(f0_coefs = c(1, 2, 3, 4, 5, 6, 7, 8), lambdas = c(1, 2, 3, 4, 5, 6, 7, 8), eps = 0.5)
}

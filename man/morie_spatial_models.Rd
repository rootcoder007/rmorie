% Generated by hand; roxygen2 is not run on this package.
\name{morie_spatial_models}
\alias{morie_spatial_models}
\alias{morie_spatial_lag_model}
\alias{morie_spatial_error_model}
\alias{morie_ripley_k}
\alias{morie_cokriging}
\alias{morie_local_dp_randomised_response}
\title{Maximum-likelihood spatial models, point patterns and local privacy}
\usage{
morie_spatial_lag_model(y, X, W, add_intercept = TRUE)
morie_spatial_error_model(y, X, W, add_intercept = TRUE)
morie_ripley_k(coords, r_grid, area = NULL, edge_correction = TRUE)
morie_cokriging(coords, z1, z2, s0, cross_vario = NULL, model = NULL)
morie_local_dp_randomised_response(truth, k, epsilon, seed = 2)
}
\arguments{
\item{y}{numeric response}
\item{X}{predictor matrix}
\item{W}{spatial weights matrix}
\item{add_intercept}{prepend an intercept column to X}
\item{coords}{matrix of point coordinates}
\item{r_grid}{radii at which to evaluate K}
\item{area}{window area; the bounding box by default}
\item{edge_correction}{apply Ripley's isotropic correction}
\item{z1}{primary variable}
\item{z2}{secondary variable}
\item{s0}{prediction location}
\item{cross_vario}{cross-variogram function of distance}
\item{model}{direct variogram function of distance}
\item{truth}{integer vector of true values in 0..k-1}
\item{k}{number of categories}
\item{epsilon}{privacy parameter; smaller is more private}
\item{seed}{RNG seed}
}
\value{
A list: the spatial parameter with \code{beta}, \code{sigma2} and
\code{loglik} for the regressions; \code{r}, \code{K} and \code{L}
for the point pattern; \code{prediction} with its kriging
\code{variance} and weights; and the perturbed \code{reports} with a
debiased \code{estimate} for the privacy mechanism.
}
\description{
The spatial lag and spatial error models fitted by maximum likelihood,
Ripley's K with edge correction, ordinary co-kriging, and k-ary
randomised response under local differential privacy.
}
\details{
The two regressions are checked against \code{spatialreg::lagsarlm}
and \code{spatialreg::errorsarlm} -- those functions live in
\pkg{spatialreg}, not \pkg{spdep}, since spdep was split.  The same
methods exist in the Python package as \code{morie.fn._robust_core}.
}
\references{
Ord, J. K. (1975) Estimation methods for models of spatial interaction.
\emph{JASA} 70, 120-126.

Ripley, B. D. (1976) The second-order analysis of stationary point
processes. \emph{Journal of Applied Probability} 13, 255-266.

Warner, S. L. (1965) Randomized response. \emph{JASA} 60, 63-69.
}
\keyword{spatial}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tsalls.R
\name{Tsalls}
\alias{Tsalls}
\title{Tsallis entropy of a probability vector that is already known}
\usage{
Tsalls(p, q)
}
\arguments{
\item{p}{Non-negative weights, renormalised to sum to one.}

\item{q}{Entropic index; \code{q = 1} gives Shannon entropy in nats.}
}
\value{
List with \code{estimate}, \code{k}, \code{q}.
}
\description{
The vector is renormalised on entry, so a pmf that sums to 0.999
through rounding does not silently shift the entropy and a caller who
passes counts gets the answer they meant.
}
\details{
Formula: \code{S_q = (1 - sum_i p_i^q) / (q - 1)}.
}
\examples{
Tsalls(p = 0.5, q = 0.5)
}
\references{
Tsallis, C. (1988). J Stat Phys 52:479-487, equation (1).
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ts_forecast.R
\name{morie_ts_forecast}
\alias{morie_ts_forecast}
\title{Forecast from a fitted time-series model}
\usage{
morie_ts_forecast(model, h = 10L, level = 0.95, xreg = NULL)
}
\arguments{
\item{model}{A \code{morie_ts_model}.}

\item{h}{Forecast horizon (number of steps ahead).}

\item{level}{Prediction-interval coverage (default 0.95).}

\item{xreg}{Future exogenous regressors (required if the model used
\code{xreg}), with \code{h} rows.}
}
\value{
A \code{morie_ts_forecast}: \code{mean}, \code{lower}, \code{upper}, \code{se}
(each length \code{h}), plus the \code{observed} series.
}
\description{
Produces multi-step-ahead forecasts with prediction intervals. The
returned object keeps forecast values clearly separate from the
observed (model) values, and the interval widths grow with the
forecast horizon.
}
\examples{
m <- morie_ts_arima(cumsum(rnorm(80)), order = c(1, 1, 0))
fc <- morie_ts_forecast(m, h = 12)
}

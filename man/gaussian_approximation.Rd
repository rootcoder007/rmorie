% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inlasm_native.R
\name{gaussian_approximation}
\alias{gaussian_approximation}
\title{gaussian_approximation}
\usage{
gaussian_approximation(
  log_lik,
  log_lik_d1,
  log_lik_d2,
  prior_mean,
  prior_precision,
  x0 = 0,
  iters = 60,
  tol = 1e-12
)
}
\arguments{
\item{log_lik}{Accepted by the signature and not used anywhere in the body.}

\item{log_lik_d1}{Accepted by the signature and not used anywhere in the body.}

\item{log_lik_d2}{Accepted by the signature and not used anywhere in the body.}

\item{prior_mean}{Coerced to numeric by the body, with \code{as.numeric}.}

\item{prior_precision}{Coerced to numeric by the body, with \code{as.numeric}.}

\item{x0}{Coerced to numeric by the body, with \code{as.numeric}. Defaults to \code{0}.}

\item{iters}{Coerced to integer by the body, with \code{as.integer}. Defaults to \code{60}.}

\item{tol}{Coerced to numeric by the body, with \code{as.numeric}. Defaults to \code{1e-12}.}
}
\value{
A list with \code{mode}, \code{precision}, \code{sd}, \code{iterations},
\code{log_norm}, \code{note}.
}
\description{
A step of the inlasm_native implementation. No other function in the package calls it.
See the file header for the source the module follows.
source it follows.
}
\examples{
ga <- gaussian_approximation(
  log_lik = function(x) -0.5 * (x - 2)^2,
  log_lik_d1 = function(x) -(x - 2),
  log_lik_d2 = function(x) -1,
  prior_mean = 0, prior_precision = 1)
abs(ga$mode - 1) < 1e-8
}
\keyword{internal}

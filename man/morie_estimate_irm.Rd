% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/irm.R
\name{morie_estimate_irm}
\alias{morie_estimate_irm}
\title{Estimate the ATE via the Interactive Regression Model (IRM)}
\usage{
morie_estimate_irm(
  data,
  treatment,
  outcome,
  covariates,
  n_folds = 5L,
  random_state = 42L
)
}
\arguments{
\item{data}{A \code{data.frame} containing outcome, treatment, and covariates.}

\item{treatment}{Column name of the binary treatment.}

\item{outcome}{Column name of the outcome.}

\item{covariates}{Character vector of covariate column names.}

\item{n_folds}{Number of cross-fitting folds (default 5).}

\item{random_state}{Random seed (default 42).}
}
\value{
A list with components: \code{ate}, \code{se}, \code{ci_lower}, \code{ci_upper},
\code{n}, \code{method} (\code{"IRM (rmorie native)"}).
}
\description{
Native rmorie implementation of the interactive regression model
(cross-fit logistic propensity + GCV-ridge outcome regressions,
AIPW orthogonal score), mirroring the Python sibling
\code{morie.estimate_irm()}. Cross-validated against \code{DoubleML} in the
package's cross tests; no DoubleML at runtime.
}
\details{
Following the DoubleML R package's own conventions, this uses
the \code{mlr3} ecosystem for the nuisance learners (\code{ml_g} for
\eqn{E[Y|T,X]} and \code{ml_m} for \eqn{P(T=1|X)}). Defaults are
\code{lrn("regr.lm")} and \code{lrn("classif.log_reg")}, which require nothing
beyond \code{stats}. For higher-capacity defaults, install \code{ranger} and pass
\code{lrn("regr.ranger")} / \code{lrn("classif.ranger")} via the underlying
\code{DoubleML::DoubleMLIRM$new()} directly.

Following Chernozhukov et al. (2018), the IRM extends the partially linear
model by allowing fully heterogeneous treatment effects:
\deqn{Y = g_0(T, X) + U,\quad E[U|T,X] = 0}{Y = g_0(T, X) + U, E[U|T,X] = 0}
\deqn{T = m_0(X) + V,\quad E[V|X] = 0}{T = m_0(X) + V, E[V|X] = 0}
}
\section{CRAN \code{Suggests}}{

Runs on base R alone — no suggested packages required.
If any are unavailable, the function raises an informative error.
}

\examples{
set.seed(1)
n <- 200
X <- matrix(rnorm(n * 5), n, 5)
ps <- plogis(X[, 1] - X[, 2])
T <- rbinom(n, 1, ps)
Y <- 0.5 * T + X[, 1] + rnorm(n)
df <- data.frame(Y = Y, T = T, X)
morie_estimate_irm(df,
  treatment = "T", outcome = "Y",
  covariates = paste0("X", 1:5)
)
}
\references{
Chernozhukov, V., Chetverikov, D., Demirer, M., Duflo, E., Hansen, C.,
Newey, W., & Robins, J. (2018). Double/debiased machine learning for
treatment and structural parameters. \emph{The Econometrics Journal}, 21(1),
C1--C68. \doi{10.1111/ectj.12097}

Bach, P., Chernozhukov, V., Kurz, M. S., & Spindler, M. (2024). DoubleML --
An object-oriented implementation of double machine learning in R.
\emph{Journal of Statistical Software}, 108(3). \doi{10.18637/jss.v108.i03}
}

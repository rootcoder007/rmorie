% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geron_ml_native.R
\name{morie_geron_a2c}
\alias{morie_geron_a2c}
\title{Advantage actor-critic, A2C (Geron Ch 19, morie.fn hma2c)}
\usage{
morie_geron_a2c(
  env,
  actor,
  critic,
  epochs = 100,
  lr = 0.1,
  gamma = 0.99,
  critic_lr = NULL,
  max_steps = 200,
  seed = 0
)
}
\arguments{
\item{env}{List with reset/step.}

\item{actor}{Matrix (n_actions, n_features).}

\item{critic}{Value weights.}

\item{epochs}{Episodes.}

\item{lr}{Actor step.}

\item{gamma}{Discount.}

\item{critic_lr}{Critic step.}

\item{max_steps}{Episode cap.}

\item{seed}{LCG seed.}
}
\value{
List with \code{actor}, \code{critic}, \code{returns}, \code{policy}, \code{value},
\code{advantages}.
}
\description{
Linear softmax policy plus linear value baseline, trained by
Monte-Carlo advantage. \code{env} is a list with \code{reset()} and
\code{step(action)} (action arrives 0-based, as in Python) returning
list(state, reward, done). Actions are sampled from the same integer
LCG stream as Python, so runs match token for token.
}
\examples{
a <- morie_geron_adagrad(2, s = 4, eta = 0.1, eps = 0)
env <- list(reset = function() 1, step = function(a) list(1, 
    if (a == 0) 1 else 0, TRUE))
morie_geron_a2c(env, matrix(c(0, 0), 2, 1), 0, epochs = 25, lr = 0.5, 
    seed = 1)
}

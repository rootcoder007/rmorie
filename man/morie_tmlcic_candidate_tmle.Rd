% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tmlcic_native.R
\name{morie_tmlcic_candidate_tmle}
\alias{morie_tmlcic_candidate_tmle}
\title{morie_tmlcic_candidate_tmle}
\usage{
morie_tmlcic_candidate_tmle(
  y,
  A,
  W,
  cand,
  g1,
  rows = NULL,
  eval_rows = NULL,
  ridge = 1e-08,
  target_step = TRUE
)
}
\arguments{
\item{y}{A vector; its length is taken.}

\item{A}{A vector; indexed elementwise.}

\item{W}{Passed to \code{.tmlcic_fit_working_model}.}

\item{cand}{Passed to \code{.tmlcic_fit_working_model}.}

\item{g1}{Accepted by the signature and not used anywhere in the body.}

\item{rows}{Optional; may be \code{NULL}. Passed to \code{.tmlcic_fit_working_model}.}

\item{eval_rows}{Optional; may be \code{NULL}. Passed to \code{is.null}.}

\item{ridge}{Passed to \code{.tmlcic_fit_working_model}. Defaults to \code{1e-08}.}

\item{target_step}{A flag; the body branches on it. Defaults to \code{TRUE}.}
}
\value{
A list with \code{q1}, \code{q0}, \code{qa}, \code{info}.
}
\description{
A step of the tmlcic_native implementation. Called by
\code{morie_tmlcic_adaptive_prespecification}, \code{morie_tmlcic_tmle_cluster_ic}.
See the file header for the source the module follows.
source it follows.
}
\examples{
set.seed(1)
n <- 40
W <- matrix(rnorm(n * 2), n, 2)
A <- rbinom(n, 1, 0.5)
y <- plogis(-0.3 + 0.8 * A + 0.5 * W[, 1] + rnorm(n, 0, 0.5))
cand <- list(name = "W1", cols = 0L, interact = FALSE)
r <- morie_tmlcic_candidate_tmle(y, A, W, cand, g1 = function(i) 0.5)
str(r, max.level = 1)
}
\keyword{internal}

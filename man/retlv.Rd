% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/retlv.R
\name{retlv}
\alias{retlv}
\alias{morie_return_level}
\title{GEV return-level estimation (Coles 2001, Ch. 3)}
\usage{
retlv(x, return_period = 100)

morie_return_level(x, return_period = 100)
}
\arguments{
\item{x}{numeric vector of block maxima.}

\item{return_period}{numeric T (default 100).}
}
\value{
list: z, se, return_period, mu, sigma, xi, n, method.
}
\description{
For block maxima with GEV(mu, sigma, xi), the T-block return level
is z_T = mu - (sigma/xi) (1 - (-log(1 - 1/T))^(-xi))  if xi != 0,
else z_T = mu - sigma log(-log(1 - 1/T)).  Delta-method SE uses the
diagonal of the observed information returned by \code{extvm}.
}
\keyword{internal}

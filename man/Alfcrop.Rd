% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Alfcrop.R
\name{Alfcrop}
\alias{Alfcrop}
\title{Contiguous residue cropping of AlphaFold training features}
\usage{
Alfcrop(
  seqlen,
  cropsize,
  start = 1,
  target = NULL,
  pair = NULL,
  msa = NULL,
  mode = "clamped"
)
}
\arguments{
\item{seqlen}{Number of residues before cropping.}

\item{cropsize}{Number of residues to keep.}

\item{start}{One-based index of the first residue kept.}

\item{target}{Optional per-residue feature matrix to crop by row.}

\item{pair}{Optional pair feature matrix to crop on both axes.}

\item{msa}{Optional MSA feature matrix, cropped along its residue axis.}

\item{mode}{Either "clamped" or "unclamped".}
}
\value{
A list with the kept indices \code{idx}, the cropped
\code{target}, \code{pair} and \code{msa}, \code{startmax},
\code{estimate} and \code{method}.
}
\description{
Supplement section 1.2.8 of Jumper et al. (2021), pp. 7-8.  Training
crops the residue dimension of every feature to one contiguous window.
Because the window is contiguous and shared by all features, pair
features must be cropped on both axes with the same index set, which is
what keeps residue \code{i} of the cropped target aligned with row
\code{i} of the cropped pair tensor.
}
\details{
The crop start is an argument, not sampled here, so the function is
deterministic.  The valid range the spec would sample from is returned
alongside the crop.
}
\references{
Jumper et al (2021) Nature 596:583-589, Suppl. section 1.2.8
}

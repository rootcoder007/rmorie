% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bicarp.R
\name{Bicarp}
\alias{Bicarp}
\title{BIC order selection for an autoregression, on the Schwarz penalty}
\usage{
Bicarp(x, max_p)
}
\arguments{
\item{x}{The series, in time order.}

\item{max_p}{Largest order considered; orders 0, 1, ..., max_p are compared.}
}
\value{
List with \code{estimate} (the selected order), \code{order}, \code{bic}, \code{bic_raw}, \code{sigma2}, \code{coefficients}, \code{n}, \code{T}, \code{max_p}, \code{method}.
}
\description{
The penalty per parameter is \code{log n}, not 2, so BIC is consistent for the true order where AIC is not. Every candidate order is fitted on the same \code{T = n - max_p} observations, so the likelihoods being compared are likelihoods of the same data.
}
\examples{
set.seed(1)
Bicarp(as.numeric(filter(rnorm(200), 0.7, method = "recursive")), 4)$order
}
\references{
Schwarz, G. (1978), Annals of Statistics 6(2):461-464, doi:10.1214/aos/1176344136, Proposition, p. 462.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aaa_rangayyan_phys.R
\name{PsgStage}
\alias{PsgStage}
\title{PsgStage: polysomnography signal fusion for sleep staging}
\usage{
PsgStage(eeg, eog, emg, fs, epoch_len = 30)
}
\arguments{
\item{eeg}{Coerced to numeric by the body, with \code{as.numeric}.}

\item{eog}{Coerced to numeric by the body, with \code{as.numeric}.}

\item{emg}{Coerced to numeric by the body, with \code{as.numeric}.}

\item{fs}{Numeric; combined arithmetically in the body.}

\item{epoch_len}{Numeric; combined arithmetically in the body. Defaults to \code{30}.}
}
\value{
A list with \code{epochs}, \code{stage_sequence}, \code{stage_minutes},
\code{total_sleep_time_min}, \code{recording_time_min}, \code{sleep_efficiency},
\code{n_epochs}, \code{epoch_len_s}, \code{fs_hz}, \code{heuristic}, \code{units},
\code{method}.
}
\description{
A step of the rangayyan_phys implementation. No other function in the package calls it.
See the file header for the source the module follows.
the source it follows.
}
\examples{
set.seed(3)
fs <- 100
n <- fs * 90
eeg <- rnorm(n)
eog <- rnorm(n, 0, 0.5)
emg <- rnorm(n, 0, 0.8)
r <- PsgStage(eeg, eog, emg, fs = fs, epoch_len = 30)
str(r, max.level = 1)
}
\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/esl_native.R
\name{morie_esl_kernel_density}
\alias{morie_esl_kernel_density}
\title{Parzen kernel density estimate}
\usage{
morie_esl_kernel_density(x, data, lambda_ = NULL)
}
\arguments{
\item{x}{evaluation points: a vector, or a matrix with \code{p} columns.}

\item{data}{sample: a vector, or a matrix with \code{p} columns.}

\item{lambda_}{kernel standard deviation; Silverman's normal
reference \verb{1.06 sigma N^(-1/5)} when \code{NULL}, which is a DENSITY
rule and correct here.}
}
\value{
list: x, density, lambda, n, p, mass (1-D only),
normaliser, is_convolution, method.
}
\description{
ESL Eqs. (6.23)-(6.24). In one dimension the estimate IS the
empirical distribution convolved with a Gaussian of standard
deviation \code{lambda}: \code{F_hat} puts mass \code{1/N} at each observation
and is jumpy, and the estimate smooths it by adding independent
Gaussian noise to each point. That is why \code{lambda} is a standard
deviation here rather than a generic bandwidth.
}
\details{
In \code{p} dimensions the Gaussian product kernel (6.24) normalises by
\verb{N (2 lambda^2 pi)^(p/2)} -- the exponent is \code{p/2}, not \code{p}.
}
\examples{
morie_esl_kernel_density(c(-1, 0, 1), stats::rnorm(200))$density
}
\references{
Hastie, Tibshirani and Friedman (2009), Sec. 6.6,
Eqs. (6.22)-(6.24); Parzen (1962).
}

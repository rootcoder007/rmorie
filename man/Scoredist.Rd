% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tqscr.R
\name{Scoredist}
\alias{Scoredist}
\title{Distortion of an inner-product score under TurboQuant_prod.}
\usage{
Scoredist(b, d, query_norm = 1, n_keys = 1)
}
\arguments{
\item{b}{Bits per coordinate.}

\item{d}{Key dimension.}

\item{query_norm}{||q||_2 of the query.}

\item{n_keys}{Number of keys scored against.}
}
\value{
List with \code{variance}, \code{rms}, \code{lower_bound},
\code{ratio}, \code{expected_max}, \code{b}, \code{d},
\code{n_keys}.
}
\description{
The 1/d in the bound means the inner-product distortion falls with
DIMENSION as well as with bits. \code{expected_max} is a Gaussian-tail
ESTIMATE of the largest of n_keys errors, not a bound from the paper.
}
\details{
Formula: D_prod <= sqrt(3) pi^2 ||q||^2 / d . 4^-b (Theorem 2);
rms = sqrt(D_prod); expected max ~= rms sqrt(2 log n)
}
\examples{
Scoredist(b = 5L, d = 5L)
}
\references{
Zandieh et al., arXiv:2504.19874, Theorem 2 and Theorem 3.
Fetched from arXiv. The expected_max figure is the standard Gaussian
maximum approximation and is NOT from the paper.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metsem_native.R
\name{morie_metsem}
\alias{morie_metsem}
\title{Assemble a community from its reads}
\usage{
morie_metsem(
  reads,
  k,
  tip_length = NULL,
  tip_ratio = 0.2,
  bubble_ratio = 0.5,
  rounds = 2L,
  min_length = NULL
)
}
\arguments{
\item{reads}{The reads.}

\item{k}{The k-mer length.}

\item{tip_length}{A dead-end shorter than this is a tip candidate.
NULL is twice k, the usual choice, stated rather than hidden.}

\item{tip_ratio}{A tip goes only if its coverage is below this
fraction of the best branch it leaves -- RELATIVE, which is the
whole point: an absolute cut-off deletes the rare organisms.}

\item{bubble_ratio}{The thinner side of a bubble goes if it is below
this fraction of the thicker.}

\item{rounds}{Cleaning passes.}

\item{min_length}{Contigs shorter than this are reported separately
rather than thrown away.}
}
\value{
A list with the contigs longest first, their coverage, the
N50, and what the cleaning removed.
}
\description{
Assemble a community from its reads
}
\examples{
morie_metsem(reads = data.frame(x = c(1, 2, 3, 4), y = c(2, 4, 5, 9)), k = 5L)
}
\keyword{internal}

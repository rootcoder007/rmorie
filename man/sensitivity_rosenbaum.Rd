% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/effects.R
\name{sensitivity_rosenbaum}
\alias{sensitivity_rosenbaum}
\title{Rosenbaum bounds sensitivity analysis (data-frame interface)}
\usage{
sensitivity_rosenbaum(
  data,
  treatment,
  outcome,
  covariates,
  gamma_range = c(1, 3),
  n_gamma = 20L
)
}
\arguments{
\item{data}{Data frame with treatment + outcome columns.}

\item{treatment}{Binary treatment column (0/1).}

\item{outcome}{Outcome column.}

\item{covariates}{Covariates (used only for matching approximation,
here a simple rank-match).}

\item{gamma_range}{c(min, max) of Gamma. Default c(1, 3).}

\item{n_gamma}{Number of Gamma values. Default 20.}
}
\value{
Data frame with \code{Gamma}, \code{p_lower}, \code{p_upper}.
}
\description{
Native base R rank-matched-pair signed-rank bounds across a Gamma
grid, via the normal approximation to the Wilcoxon signed-rank
statistic.
}
\examples{
if (requireNamespace("EValue", quietly = TRUE) && requireNamespace("rbounds", quietly = TRUE)) {
  set.seed(1)
  df <- data.frame(d = rbinom(60, 1, 0.5), x1 = rnorm(60))
  df$y <- df$d * 0.5 + df$x1 + rnorm(60)
  res <- try(sensitivity_rosenbaum(df, "d", "y", "x1"))
  if (!inherits(res, "try-error")) str(res, max.level = 1)
}
}

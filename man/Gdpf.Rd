% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Gdpf.R
\name{Gdpf}
\alias{Gdpf}
\title{Gaussian differential privacy: the trade-off function}
\usage{
Gdpf(mech, mu, alpha = NULL, epsilon = 1)
}
\arguments{
\item{mech}{Placeholder for the mechanism description; unused.}

\item{mu}{Non-negative GDP parameter.}

\item{alpha}{Type I error rates at which to evaluate the trade-off.}

\item{epsilon}{Epsilon at which the (eps, delta) conversion is given.}
}
\value{
List with \code{estimate} (delta), \code{trade_off},
\code{alpha}, \code{delta}, \code{mu}, \code{epsilon}, \code{n},
\code{method}.
}
\description{
Privacy is expressed as the trade-off between the two error rates of
the hypothesis test that distinguishes neighbouring databases; a
mechanism is mu-GDP when that trade-off is at least that of testing
N(0, 1) against N(mu, 1).  mu = 0 gives the diagonal 1 - alpha,
perfect privacy, and delta = 0 for every epsilon; both limits are
exact and are what the tests check.
}
\details{
Formula: G_mu(alpha) = Phi(Phi^{-1}(1 - alpha) - mu);
delta(eps) = Phi(-eps/mu + mu/2) - exp(eps) Phi(-eps/mu - mu/2).
}
\examples{
Gdpf(mech = c(1, 2, 3, 4, 5, 6, 7, 8), mu = 5L)
}
\references{
Dong, Roth and Su (2022), Gaussian differential privacy,
JRSS B 84(1):3-37, Corollary 2.13. \doi{10.1111/rssb.12454}
}

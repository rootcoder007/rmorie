% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distributions.R
\name{morie_dist_integrate}
\alias{morie_dist_integrate}
\title{Integrate a function against a distribution's support, with checks}
\usage{
morie_dist_integrate(dist, integrand = NULL, lower = NULL, upper = NULL)
}
\arguments{
\item{dist}{A \code{morie_distribution} (continuous).}

\item{integrand}{Function of \code{x} to integrate (default the density).}

\item{lower, upper}{Integration limits (default the distribution's
effective support via extreme quantiles).}
}
\value{
A list with \code{value} and the numeric \code{abs.error}.
}
\description{
Integrates \code{integrand} (default: the density, giving total mass 1)
over the distribution's support. Stability conditions -- the integrand
must be finite and the density non-negative and integrable -- are
documented and pre-checked before \code{\link[stats:integrate]{stats::integrate()}} is called.
}
\examples{
morie_dist_integrate(morie_distribution("normal", mean = 0, sd = 1))$value
}

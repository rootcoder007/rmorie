% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dp_native.R
\name{morie_dp_histogram}
\alias{morie_dp_histogram}
\title{Differentially private histogram}
\usage{
morie_dp_histogram(
  x,
  bins = 10,
  epsilon = 1,
  range_ = NULL,
  seed = NULL,
  nonneg = TRUE
)
}
\arguments{
\item{x}{values to bin.}

\item{bins}{bin count or explicit edges.}

\item{epsilon}{privacy budget.}

\item{range_}{optional \code{c(min, max)} for the binning range.}

\item{seed}{optional integer seed.}

\item{nonneg}{clamp negative counts to zero (post-processing, free).}
}
\value{
list with \code{release}, \code{raw}, \code{true_counts},
\code{edges}, \code{noise_scale}.
}
\description{
Laplace noise on each bin count. Because the bins are disjoint, one
record touches exactly one of them: this is PARALLEL composition, so
the same epsilon covers every bin at once. Dividing the budget across
bins is the common error and wastes it entirely.
}
\details{
The sensitivity used is 2, which covers the add/remove-one neighbouring
relation where a record can move between bins.
}
\examples{
set.seed(1)
morie_dp_histogram(rnorm(500), bins = 5, epsilon = 1)$noise_scale
}
\references{
McSherry, F. (2009). Privacy integrated queries.
\emph{SIGMOD}, 19-30. (parallel composition)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ppoc.R
\name{Ppoclip}
\alias{Ppoclip}
\title{PPO's clipped surrogate objective}
\usage{
Ppoclip(
  env,
  policy = NULL,
  clip_eps = 0.2,
  ratio = NULL,
  adv = NULL,
  logp_new = NULL,
  logp_old = NULL,
  v_pred = NULL,
  v_targ = NULL,
  entropy = NULL,
  c1 = 0.5,
  c2 = 0.01
)
}
\arguments{
\item{env}{the advantages A_t.}

\item{policy}{the probability ratios r_t.}

\item{clip_eps}{the clipping range.}

\item{ratio, adv}{explicit ratios and advantages.}

\item{logp_new, logp_old}{log probabilities; ratio = exp(new - old).}

\item{v_pred, v_targ}{value predictions and targets.}

\item{entropy}{per-step policy entropy.}

\item{c1, c2}{value and entropy coefficients.}
}
\value{
list: estimate, l_clip, l_vf, l_entropy, total, frac_clipped,
n, method.
}
\description{
Schulman, Wolski, Dhariwal, Radford and Klimov (2017), Proximal policy
optimization algorithms, arXiv:1707.06347 (FETCHED as PDF), equation
(7): L^CLIP = E_t\link{min(r_t A_t, clip(r_t, 1 - eps, 1 + eps) A_t)} with
r_t = pi_theta(a_t|s_t) / pi_old(a_t|s_t), the paper taking the minimum
so the objective is a lower bound on the unclipped one.  Equation (9)
adds the value and entropy terms, L^(CLIP+VF+S) = E_t[L^CLIP - c1 L^VF
\itemize{
\item c2 S], included when the value targets and entropy are supplied.  The
objective is maximised.
}

[L^CLIP - c1 L^VF
\itemize{
\item c2 S]: R:L\%5ECLIP\%20-\%20c1\%20L\%5EVF\%0A+\%20c2\%20S
}
}
\examples{
Ppoclip(c(1, -1), c(1.3, 0.8))$l_clip
}
\keyword{internal}

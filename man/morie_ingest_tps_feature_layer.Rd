% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ingest_tps.R
\name{morie_ingest_tps_feature_layer}
\alias{morie_ingest_tps_feature_layer}
\title{Fetch every feature from a TPS ArcGIS FeatureServer layer}
\usage{
morie_ingest_tps_feature_layer(
  layer_url,
  where = "1=1",
  out_fields = "*",
  return_geometry = FALSE,
  page_size = 2000L,
  max_features = NULL,
  user_agent = .MORIE_TPS_DEFAULT_UA,
  timeout = .MORIE_TPS_DEFAULT_TIMEOUT
)
}
\arguments{
\item{layer_url}{Full URL to a FeatureServer layer, e.g. one of
the entries in \code{\link{morie_ingest_tps_layers}}.}

\item{where}{ArcGIS WHERE clause.  Default \code{"1=1"} fetches
everything.  Examples: \code{"OCC_YEAR = 2024"},
\code{"OCC_YEAR BETWEEN 2020 AND 2025"}.}

\item{out_fields}{Comma-separated attribute list, or \code{"*"}.}

\item{return_geometry}{If \code{TRUE}, includes \code{geom_x} /
\code{geom_y} columns (longitude / latitude in EPSG:4326).}

\item{page_size}{Records per request; clamped server-side to 2,000
for TPS layers.}

\item{max_features}{Optional hard cap on total returned rows.}

\item{user_agent, timeout}{Standard request knobs.}
}
\value{
A base R \code{data.frame}.
}
\description{
ArcGIS FeatureServer queries cap at the layer's server-side
\code{maxRecordCount} (2,000 for the TPS layers).  This function
pages through transparently using \code{resultOffset} and the
\code{exceededTransferLimit} flag, emitting one data.frame.
}
\examples{
\dontrun{
df <- morie_ingest_tps_feature_layer(
  morie_ingest_tps_layers()$url[
    morie_ingest_tps_layers()$name == "major-crime"
  ],
  where = "OCC_YEAR >= 2023",
  max_features = 5000L
)
nrow(df)
}
}

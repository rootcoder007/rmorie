% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rfens.R
\name{morie_random_forest_ensemble}
\alias{morie_random_forest_ensemble}
\title{Random Forest ensemble (R parity)}
\usage{
morie_random_forest_ensemble(
  x,
  y,
  n_estimators = 100L,
  max_depth = NULL,
  task = "auto",
  seed = 0L,
  deterministic_seed = NULL
)
}
\arguments{
\item{x}{Numeric predictor matrix.}

\item{y}{Response.}

\item{n_estimators}{Number of trees.}

\item{max_depth}{Max tree depth (NULL -> unrestricted).}

\item{task}{"auto", "classification", or "regression".}

\item{seed}{RNG seed.}

\item{deterministic_seed}{Integer or NULL.  If supplied, the RNG state
is derived from the SHA-keyed \code{\link[=morie_det_rng]{morie_det_rng()}} so Py<->R streams
agree on the canonical fixture.  When \code{NULL} (default), behaviour
is unchanged: \code{seed} drives \code{set.seed()} directly.}
}
\value{
Named list: estimate, train_score, oob_score, feature_importances,
n_estimators, task, n, method.
}
\description{
Wraps \code{randomForest::randomForest}.  Auto-detects task from y
(factor / integer-like -> classification, otherwise regression).
}
\examples{
morie_random_forest_ensemble(x = rnorm(50), y = rnorm(50))
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/perk_native.R
\name{morie_perk}
\alias{morie_perk}
\title{Periodic kernel matrix}
\usage{
morie_perk(x1, x2 = NULL, period = 1, lengthscale = 1, variance = 1)
}
\arguments{
\item{x1}{Numeric vector of inputs.}

\item{x2}{Optional second input vector (defaults to \code{x1}).}

\item{period}{Period p > 0.}

\item{lengthscale}{Lengthscale l > 0.}

\item{variance}{Signal variance sigma^2 > 0.}
}
\value{
A list with elements \code{K} (matrix), \code{shape},
\code{period}, \code{lengthscale}, \code{variance},
\code{diag_is_variance}, \code{method}.
}
\description{
MacKay's warping construction u(x) = (cos, sin)(2 pi x / p) with a
squared-exponential kernel in u-space, giving
k(x, x') = sigma^2 exp(-2 sin^2(pi (x - x')/p)/l^2): positive
definite, period p in x - x', maximum sigma^2 on the diagonal.
}
\references{
MacKay, D. J. C. (1998). Introduction to Gaussian
processes. In Bishop (ed.), Neural Networks and Machine
Learning, Springer.  Rasmussen, C. E. and Williams, C. K. I.
(2006). Gaussian Processes for Machine Learning, MIT Press,
Sec. 4.2.3.
}
\examples{
set.seed(1)
r <- morie_perk(x1 = rnorm(10)); TRUE
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geron_ml_native.R
\name{morie_geron_dcgan_generator}
\alias{morie_geron_dcgan_generator}
\title{DCGAN generator forward pass (Geron Ch 18, morie.fn grdcgan)}
\usage{
morie_geron_dcgan_generator(
  z,
  weights,
  seed_shape = NULL,
  stride = 2,
  batch_norm = TRUE
)
}
\arguments{
\item{z}{Latent vector. @param weights List: projection matrix then kernels.}

\item{seed_shape}{Optional c(h0, w0). @param stride Upsampling stride.}

\item{batch_norm}{Apply batch norm on hidden layers.}
}
\value{
List with \code{image}, \code{image_shape}, \code{seed}, \code{layer_shapes},
\code{upsample_factor}, \code{latent_dim}.
}
\description{
Latent -> projection -> spatial seed -> transposed-conv stack, batch
norm + ReLU on every layer but the last, tanh on the output (which is
why DCGAN data is scaled to \link{-1, 1}). The batch norm here is the
whole-tensor population form, as in Python.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aaa_rangayyan_stat.R
\name{KatzFd}
\alias{KatzFd}
\title{Katz (1988): FD = log10(n) / (log10(n) + log10(d/L)), with L the path}
\usage{
KatzFd(x, dt = 1)
}
\arguments{
\item{x}{Coerced to numeric by the body, with \code{as.numeric}.}

\item{dt}{Coerced to numeric by the body, with \code{as.numeric}. Defaults to \code{1}.}
}
\value{
A list with \code{fd}, \code{total_length}, \code{max_distance},
\code{mean_step}, \code{n_steps}, \code{n}, \code{scale_sensitive}, \code{method}.
}
\description{
length, d the greatest distance from the first point, n = L/a.
Rangayyan (2024) Section 5.13.2 covers the ruler, box-counting and
Higuchi methods instead, so this is cited to Katz.  It mixes the
amplitude and time axes, so rescaling the signal changes the answer.
}
\examples{
V <- c(1, 2, 3, 4, 5, 6, 7, 8)
KatzFd(V)
}
\keyword{internal}

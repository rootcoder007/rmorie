% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gh_c1_3.R
\name{Ghosalpriorposteriorupdate}
\alias{Ghosalpriorposteriorupdate}
\title{Sequential prior-to-posterior updating}
\usage{
Ghosalpriorposteriorupdate(
  x,
  data = NULL,
  log_lik_one = NULL,
  log_prior = NULL
)
}
\arguments{
\item{x}{Grid of parameter values.}

\item{data}{Observations; c(0.8, 1.2, 1) when NULL.}

\item{log_lik_one}{Log-likelihood of one observation, f(theta, d).}

\item{log_prior}{Log-prior as a function of theta.}
}
\value{
List with \code{estimate}, \code{posterior},
\code{sequential_batch_gap}, \code{method}.
}
\description{
dPi_n/dPi(theta) = p_theta(X^n) / int p_eta(X^n) dPi(eta).  Batch
updating and one-observation-at-a-time updating must give the same
posterior -- coherence is what makes the Bayesian recipe well defined
at all -- and the two are computed separately here so the agreement is
measured rather than assumed.
}
\details{
Formula: batch log weight = sum_j log p(d_j | theta) + log pi(theta);
sequential accumulates the same terms one at a time.
}
\references{
Ghosal & van der Vaart (2017), Fundamentals of
Nonparametric Bayesian Inference, CUP, section 1.3.
}
\examples{
V <- c(1, 2, 3, 4, 5, 6, 7, 8)
Ghosalpriorposteriorupdate(V)
}

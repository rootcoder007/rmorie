% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grucl.R
\name{morie_grucl_gru_cell}
\alias{morie_grucl_gru_cell}
\alias{morie_gru_cell}
\title{GRU cell forward pass}
\usage{
morie_grucl_gru_cell(
  x,
  h_prev = NULL,
  W = NULL,
  U = NULL,
  b = NULL,
  hidden_size = NULL,
  seed = 0L,
  deterministic_seed = NULL
)

morie_gru_cell(
  x,
  h_prev = NULL,
  W = NULL,
  U = NULL,
  b = NULL,
  hidden_size = NULL,
  seed = 0L,
  deterministic_seed = NULL
)
}
\arguments{
\item{x}{Numeric vector.}

\item{h_prev}{Previous hidden state.}

\item{W, U, b}{Weight matrices / bias.}

\item{hidden_size}{Hidden size.}

\item{seed}{RNG seed.}

\item{deterministic_seed}{Optional integer; if non-NULL, a SHA-keyed
seed from \code{\link{morie_det_rng}("grucl", deterministic_seed)} is
installed before sampling so Py<->R streams agree.  Overrides
\code{seed} when set.}
}
\value{
Named list \code{(h, estimate, z, r, n, method)}.
}
\description{
R parity for \code{morie.fn.grucl.gru_cell}.  Gates stacked as
\code{[W_z; W_r; W_n]}.
}
\details{
\deqn{z, r = \sigma(\ldots); \quad n = \tanh(W_n x + r \odot U_n h); \quad
      h = (1 - z) \odot n + z \odot h_\text{prev}}{z, r = sigma(...); n = tanh(W_n x + r odot U_n h); h = (1 - z) odot n + z odot h_prev}
}
\examples{
morie_grucl_gru_cell(x = rnorm(50))
}
\references{
Cho et al. (2014), EMNLP.
}
\keyword{internal}

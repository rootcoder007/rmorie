% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/esl_native.R
\name{morie_esl_residual_variance}
\alias{morie_esl_residual_variance}
\title{Unbiased residual variance for a linear model}
\usage{
morie_esl_residual_variance(X, y, beta = NULL)
}
\arguments{
\item{X}{design matrix, with or without a leading column of ones.}

\item{y}{response.}

\item{beta}{optional coefficients; least squares when \code{NULL}.}
}
\value{
list: value, sigma, rss, df, n, p, intercept_in_X,
mle_variance, bias_factor, fitted, residuals, method.
}
\description{
ESL Eq. (3.8), \code{sigma^2 = RSS / (N - p - 1)}. The book is explicit
about the denominator: \code{N - p - 1} rather than \code{N} is what makes
\verb{E(sigma^2_hat) = sigma^2}. The \code{p + 1} counts the slopes plus the
intercept, so a design that already carries a column of ones must
not be charged for it twice.
}
\details{
Dividing by \code{N} gives the maximum-likelihood estimator, biased
downward by exactly \code{(N-p-1)/N}; both are returned. Eq. (3.11)
adds \verb{(N-p-1) sigma^2_hat ~ sigma^2 chi^2_\{N-p-1\}}.
}
\examples{
X <- matrix(stats::rnorm(120), 40)
morie_esl_residual_variance(X, stats::rnorm(40))$df
}
\references{
Hastie, Tibshirani and Friedman (2009), Sec. 3.2,
Eqs. (3.8) and (3.11).
}

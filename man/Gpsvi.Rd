% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Gpsvi.R
\name{Gpsvi}
\alias{Gpsvi}
\title{Sparse variational GP regression}
\usage{
Gpsvi(
  X,
  y,
  X_test = NULL,
  inducing = NULL,
  batch_size = NULL,
  lengthscale = 1,
  variance = 1,
  noise = 0.1,
  jitter = 1e-09
)
}
\arguments{
\item{X}{Training inputs.}

\item{y}{Training responses.}

\item{X_test}{Test inputs; the training inputs by default.}

\item{inducing}{Inducing inputs; the training inputs by default.}

\item{batch_size}{Unused placeholder kept for the interface.}

\item{lengthscale, variance, noise}{GP hyperparameters.}

\item{jitter}{Ridge added to K_mm.}
}
\value{
List with \code{estimate}, \code{mean}, \code{variance},
\code{elbo}, \code{gaussian_term}, \code{trace_term}, \code{n},
\code{method}.
}
\description{
For a Gaussian likelihood the optimal variational distribution is
available in closed form, so the stochastic gradient loop of the
paper is unnecessary and the collapsed bound can be evaluated
exactly.  When the inducing inputs ARE the training inputs the trace
term is zero and both the bound and the predictive mean coincide with
full GP regression -- that identity is the anchor, and it is the one
thing a sparse approximation must satisfy.
}
\details{
Formula: Q = K_nm K_mm^{-1} K_mn;
L = log N(y; 0, Q + s2 I) - tr(K_nn - Q) / (2 s2).
}
\examples{
V <- c(1, 2, 3, 4, 5, 6, 7, 8)
Gpsvi(V, V)
}
\references{
Hensman, Fusi and Lawrence (2013), Gaussian processes for
big data, UAI 2013, arXiv:1309.6835; Titsias (2009), AISTATS 2009,
PMLR 5:567-574.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dssm_native.R
\name{morie_dssm_letter_ngrams}
\alias{morie_dssm_letter_ngrams}
\alias{morie_dssm}
\title{Letter n-grams of a bracketed word}
\usage{
morie_dssm_letter_ngrams(word, n = 3L, boundary = "#")

morie_dssm(word, n = 3L, boundary = "#")
}
\arguments{
\item{word}{character(1). The input word; coerced via \code{as.character}
and lower-cased.}

\item{n}{integer(1). Length of the n-grams. Default \code{3}.}

\item{boundary}{character(1). Bracket character pasted on both sides
before sliding the window. Default \code{"#"}.}
}
\value{
A character vector of length \code{max(1, nchar(w) - n + 1)}.
When the bracketed word is shorter than \code{n}, the entire
bracketed string is returned as a single "n-gram".
}
\description{
Bracketing matters: it distinguishes a prefix from the same letters
inside a word. With \code{boundary = "#"} and \code{n = 3} the word
"good" becomes the trigrams of \code{"#good#"}.
}
\examples{
morie_dssm_letter_ngrams(word = 5L)
}
\keyword{internal}

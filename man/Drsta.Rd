% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Drsta.R
\name{Drsta}
\alias{Drsta}
\title{Doubly robust DiD over a staggered adoption design: the ATT(g,t) table}
\usage{
Drsta(y, D, unit, time, cohort, X = NULL)
}
\arguments{
\item{y}{Outcome, long format, one entry per unit-period.}

\item{D}{Treatment indicator, carried for the interface.}

\item{unit, time}{Unit and period identifiers.}

\item{cohort}{First treated period of the row's unit; 0 or Inf marks a
never-treated unit.}

\item{X}{Optional baseline covariates, one row per unit-period.}
}
\value{
List with \code{estimate}, \code{cohorts}, \code{event_time},
\code{att} (row-major cohort x event-time), \code{n_cells},
\code{n_post}, \code{n}.
}
\description{
One doubly robust estimate per (cohort, event-time) cell the panel can
support, each between the base period \code{g - 1} and period
\code{g + e}, against the never-treated units.  Pre-treatment cells
are computed on identical footing and returned rather than suppressed.
}
\examples{
set.seed(1)
r <- Drsta(y = rnorm(10), D = rbinom(10, 1, 0.5), unit = rnorm(10), time = sort(runif(10)), cohort = rnorm(10)); TRUE
}
\references{
Callaway, B. and Sant'Anna, P. H. C. (2021). Journal of
Econometrics 225(2), 200-230.  Sant'Anna, P. H. C. and Zhao, J.
(2020). Journal of Econometrics 219(1), 101-122, equation (2.6).
Roth, J. and Sant'Anna, P. H. C. (2023). Journal of Political
Economy Microeconomics 1(4), 669-709.
}

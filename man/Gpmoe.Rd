% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gpmoe.R
\name{Gpmoe}
\alias{Gpmoe}
\title{Predict from K Gaussian-process experts under a softmax gate}
\usage{
Gpmoe(X, y, X_test, K, ell = 1, noise = 1e-06)
}
\arguments{
\item{X}{Training inputs.}

\item{y}{Training targets.}

\item{X_test}{Test inputs.}

\item{K}{Number of experts.}

\item{ell}{Squared-exponential length scale.}

\item{noise}{Observation noise on each expert diagonal.}
}
\value{
List with \code{estimate}, \code{mean}, \code{var}, \code{gate}, \code{n}, \code{K}.
}
\description{
One GP over the whole input is both slow and wrong when different
regions want different smoothness. Tresp fits several GPs and lets a
gate decide per test point. The predictive variance picks up both the
experts own uncertainty and their disagreement.
}
\details{
Determinism: experts partition the training set into K contiguous
blocks along the sorted first coordinate and the gate is a softmax of
negative squared distance to each block centroid. No EM, no random
initialisation.

Formula: \code{mu(x) = sum_k pi_k(x) mu_k(x)},
\code{var(x) = sum_k pi_k(x) [s_k^2(x) + mu_k(x)^2] - mu(x)^2}.
}
\references{
Tresp, V. (2001). Mixtures of Gaussian processes. NIPS
13, 654-660.
}

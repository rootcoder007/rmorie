% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Alftmpl.R
\name{Alftmpl}
\alias{Alftmpl}
\title{Template pointwise attention of AlphaFold}
\usage{
Alftmpl(t, z, wq, wk, wv, wo)
}
\arguments{
\item{t}{Template pair features, an \code{ntempl x n x n x ct} array.}

\item{z}{Pair representation, an \code{n x n x cz} array.}

\item{wq}{List of per-head query projections from \code{z}, \code{c x cz}.}

\item{wk, wv}{Lists of per-head key and value projections from \code{t},
each \code{c x ct}.}

\item{wo}{Output projection, \code{cz x (nhead * c)}.}

}
\value{
A list with \code{z}, \code{attn}, \code{estimate}, \code{n},
\code{ntempl} and \code{method}.
}
\description{
Algorithm 17 of the Supplementary Information to Jumper et al. (2021),
p. 21.  For each residue pair the pair representation queries the stack
of template features at that same pair, and the templates are pooled by
attention.  The attention runs over templates only, never across pairs,
which is why it is pointwise: pair \code{ij} never sees pair \code{kl}.
}
\references{
Jumper et al (2021) Nature 596:583-589, Suppl. Algorithm 17
}

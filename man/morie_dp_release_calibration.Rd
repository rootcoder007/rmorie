% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dp_native2.R
\name{morie_dp_release_calibration}
\alias{morie_dp_release_calibration}
\title{Calibrate a privacy budget to a tolerable error, or the reverse}
\usage{
morie_dp_release_calibration(
  sensitivity = 1,
  target_error = NULL,
  epsilon = NULL,
  confidence = 0.95,
  n = 1
)
}
\arguments{
\item{sensitivity}{query sensitivity.}

\item{target_error}{desired half-width. Supply this OR \code{epsilon}.}

\item{epsilon}{budget. Supply this OR \code{target_error}.}

\item{confidence}{coverage for the half-width, in (0, 1).}

\item{n}{number of records the query averages over.}
}
\value{
list with \code{epsilon}, \code{half_width},
\code{noise_scale}, \code{direction}.
}
\description{
For Laplace noise of scale \eqn{b = \Delta/(n\epsilon)}, the
confidence half-width is \eqn{b\log(1/(1-c))}. Supply a target error
and get the epsilon it implies, or supply epsilon and get the error
it buys.
}
\details{
The point is to set the budget FROM a tolerable error rather than by
convention. An epsilon above 10 means a likelihood ratio above 22000
between neighbouring datasets, which is close to no guarantee at all;
the function warns rather than returning it silently.
}
\examples{
round(morie_dp_release_calibration(1,
  target_error = 0.01,
  n = 1000
)$epsilon, 4)
}
\references{
Dwork, C. and Roth, A. (2014). \emph{FnTTCS}, 9(3-4).
}

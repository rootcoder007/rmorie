% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aaa_rangayyan_class.R
\name{BayesNorm}
\alias{BayesNorm}
\title{Eq (10.72).  The book takes logarithms at eq (10.71) because the}
\usage{
BayesNorm(x, means, covs, priors = NULL, full = FALSE)
}
\arguments{
\item{x}{Coerced to numeric by the body, with \code{as.numeric}.}

\item{means}{A vector; its length is taken and its elements indexed.}

\item{covs}{A vector; its length is taken and its elements indexed.}

\item{priors}{Optional; may be \code{NULL}. Coerced to numeric by the body, with
\code{as.numeric}.}

\item{full}{A flag; the body branches on it. Defaults to \code{FALSE}.}
}
\value{
A list with \code{d}, \code{d_full}, \code{d_dropped_constant},
\code{assigned}, \code{priors}, \code{constant_term},
\code{surfaces_are_hyperquadrics}, \code{linear_when_covariances_are_equal},
\code{log_form_avoids_underflow}, \code{method}.
}
\description{
normal PDF is an exponential and ln is monotonic: the ranking is
unchanged while the arithmetic stops underflowing.  It then drops the
(n/2) ln(2 pi) term, which "does not depend upon i", giving eq
(10.73) -- safe for CLASSIFYING, wrong for reading the value as a log
density, so both forms are returned.  The surfaces are hyperquadrics
and reduce to hyperplanes exactly when the covariances are equal.
}
\examples{
BayesNorm(x = 5L, means = c(1, 2, 3, 4, 5, 6, 7, 8), covs = c(1, 2, 3, 4, 5, 6, 7, 8))
}
\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rgenv.R
\name{rgenv}
\alias{rgenv}
\alias{morie_rangayyan_envelope}
\title{Hilbert envelope -- Rangayyan Ch 5}
\usage{
rgenv(x)

morie_rangayyan_envelope(x)
}
\arguments{
\item{x}{Numeric vector.}
}
\value{
Named list \code{envelope}, \code{analytic}, \code{instantaneous_phase},
\code{instantaneous_freq}.
}
\description{
Analytic-signal envelope via the discrete Hilbert transform.
}
\details{
\deqn{\mathrm{env}(t) = |x(t) + j\,H\{x(t)\}|}{env(t) = |x(t) + j H\{x(t)\}|}

Uses \code{\link[seewave:hilbert]{seewave::hilbert()}} if available, otherwise computes the analytic
signal directly from the FFT.
}
\examples{
t <- seq(0, 1, length.out = 200)
x <- cos(2 * pi * 5 * t) * (1 + 0.3 * cos(2 * pi * 0.5 * t))
r <- rgenv(x)
length(r$envelope)
}
\references{
Rangayyan Ch 5.
}
\keyword{internal}

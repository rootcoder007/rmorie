% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geron_w4b_native.R
\name{morie_geron_hidden_layers_heuristic}
\alias{morie_geron_hidden_layers_heuristic}
\title{Depth selection by validation-error plateau (Geron Ch 9, morie.fn hmhplm)}
\usage{
morie_geron_hidden_layers_heuristic(
  model,
  X,
  y,
  max_layers = 10,
  min_layers = 1,
  patience = 2,
  tol = 1e-04,
  val_fraction = 0.2,
  seed = 0
)
}
\arguments{
\item{model}{function(n_layers, X_train, y_train, X_val, y_val) -> val_error.}

\item{X, y}{Data.}

\item{max_layers, min_layers}{Depth range.}

\item{patience}{Non-improving depths tolerated.}

\item{tol}{Minimum improvement.}

\item{val_fraction}{Held-out fraction.}

\item{seed}{Split seed.}
}
\value{
List with best_n_layers, best_error, errors, depths_tried, stopped_early, estimate, n, method.
}
\description{
Validation split order comes from the shared LCG stream, not numpy's
PCG64 permutation.
}
\examples{
X <- matrix(c(1, 2, 3, 4), ncol = 1)
y <- c(3, 6, 9, 12)
vshape <- function(L, Xt, yt, Xv, yv) abs(L - 3) + 0.5
morie_geron_hidden_layers_heuristic(vshape, X, y, max_layers = 10, 
    patience = 2)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcmc_w509_native.R
\name{Bayisr}
\alias{Bayisr}
\title{Sampling-importance-resampling (SIR)}
\usage{
Bayisr(samples, log_target, log_proposal, m, seed = 0L)
}
\arguments{
\item{samples}{Draws from the proposal (vector or list of vectors).}

\item{log_target, log_proposal}{Log densities up to constants.}

\item{m}{Resample size.}

\item{seed}{SplitMix64 seed.}
}
\value{
List with \code{resample}, \code{indices} (0-based, matching
the Python arm), \code{weights}, \code{ess}, \code{n}, \code{m},
\code{seed}, \code{method}.
}
\description{
Importance weights w_i = exp(log p - log g) (max-shifted), then m
resampled draws with replacement, P(i) proportional to w_i, via
one uniform per draw and inverse-CDF on the unnormalized cumulative
weights (bit-identical to the Python arm).
}
\examples{
set.seed(1)
xs <- rnorm(500, 0, 2)
r <- Bayisr(xs, log_target = function(x) -0.5 * x^2,
            log_proposal = function(x) -0.5 * (x / 2)^2 - log(2),
            m = 100)
str(r, max.level = 1)
}
\references{
Rubin, D. B. (1988), in Bayesian Statistics 3, Oxford
UP, 395-402; Smith, A. F. M. and Gelfand, A. E. (1992), The
American Statistician 46(2), 84-88.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Gpcgs.R
\name{Gpcgs}
\alias{Gpcgs}
\title{Sparse variational GP classification}
\usage{
Gpcgs(
  X,
  y,
  X_test = NULL,
  M = 3,
  lengthscale = 1,
  variance = 1,
  iters = 40,
  step = 0.05,
  nodes = 11,
  jitter = 1e-08
)
}
\arguments{
\item{X}{Training inputs, one row per point.}

\item{y}{Binary labels, 0 or 1.}

\item{X_test}{Test inputs; the training inputs by default.}

\item{M}{Number of inducing points, between 1 and n.}

\item{lengthscale, variance}{RBF hyperparameters.}

\item{iters, step}{Gradient ascent budget and step size.}

\item{nodes}{Gauss-Hermite nodes.}

\item{jitter}{Ridge added to K_mm.}
}
\value{
List with \code{estimate}, \code{p}, \code{predicted},
\code{latent_mean}, \code{elbo}, \code{kl}, \code{elbo_path},
\code{n}, \code{method}.
}
\description{
The variational bound over M inducing values, with the expectation
taken by Gauss-Hermite quadrature whose nodes come from the
Golub-Welsch tridiagonal (so both language arms generate the same
nodes rather than carrying a hard-coded table).  The variational
parameters move by fixed-step gradient ascent with central-difference
gradients -- deterministic, unlike the stochastic natural gradients
of the paper, so the two arms follow the same path.  At the starting
point q(u) = p(u) the KL term is exactly zero.
}
\details{
Formula: L = sum_i E_q[log p(y_i | f_i)] - KL(q(u) || p(u)),
mu_i = a_i' m, var_i = k_ii - a_i'(K_mm - S) a_i.
}
\examples{
set.seed(1)
X <- matrix(rnorm(20), 10, 2)
y <- rbinom(10, 1, 0.5)
Gpcgs(X, y)
}
\references{
Hensman, Matthews and Ghahramani (2015), Scalable
variational Gaussian process classification, AISTATS 2015, PMLR
38:351-360. arXiv:1411.2005
}

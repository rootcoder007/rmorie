% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fairness_metrics.R, R/frns_metrics.R
\name{morie_fairness_bias_amplification}
\alias{morie_fairness_bias_amplification}
\title{Bias Amplification Score (parity gap x Gini of group rates)}
\usage{
morie_fairness_bias_amplification(
  y_pred,
  group,
  privileged = NULL,
  favorable = 1
)

morie_fairness_bias_amplification(
  y_pred,
  group,
  privileged = NULL,
  favorable = 1
)
}
\arguments{
\item{y_pred}{Vector of decisions/assignments, one per individual.}

\item{group}{Vector of protected-attribute values (e.g. race).}

\item{privileged}{The reference group. If \code{NULL} (default) the
highest-rate group is used and a warning is attached.}

\item{favorable}{The value of \code{y_pred} counted as the favourable
outcome (default \code{1}).}
}
\value{
A \code{morie_fairness_result}; headline value is BAS.

A named list: \code{value} (BAS), \code{bias_amplification_score},
\code{demographic_parity_gap}, \code{gini}, \code{rates}, \code{privileged},
\code{warnings}, \code{interpretation}.
}
\description{
\code{BAS = delta_parity * G}, where \code{delta_parity} is the
demographic-parity gap of the worst-affected group and \code{G}
is the Gini coefficient of the per-group favourable-outcome rates.
Large only when a directional disparity coincides with high
cross-group inequality.

\code{BAS = Delta_parity * G}, where \code{Delta_parity} is the demographic
parity gap of the worst-affected group and \code{G} is the Gini
coefficient of the per-group favourable-outcome rates. Large only
when a directional disparity coincides with high overall inequality.
}
\details{
Reimplemented from Barman & Barman, arXiv:2603.18987.

Reimplemented from Barman & Barman, "Unmasking Algorithmic Bias in
Predictive Policing" (arXiv:2603.18987).
}
\examples{
pred <- c(1, 1, 1, 1, 0, 0, 0, 0)
race <- c(rep("A", 4), rep("B", 4))
res <- morie_fairness_bias_amplification(pred, race, privileged = "A")
res$value # -0.5  (parity gap -1.0 times Gini 0.5)
}

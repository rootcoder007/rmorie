% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causal_forest_honest.R
\name{morie_causal_survival_forest}
\alias{morie_causal_survival_forest}
\title{Causal survival forest on IPCW restricted-mean-survival pseudo outcomes}
\usage{
morie_causal_survival_forest(
  time,
  event,
  d,
  x,
  horizon = NULL,
  n_trees = 200L,
  min_leaf = 15L,
  seed = 0L
)
}
\arguments{
\item{time}{Follow-up times (positive).}

\item{event}{1 = event observed, 0 = right-censored.}

\item{d}{Binary 0/1 treatment.}

\item{x}{Covariates.}

\item{horizon}{Restriction time; default the 90th percentile of \code{time}.}

\item{n_trees, min_leaf, seed}{Forest hyperparameters.}
}
\value{
List with \code{cate}, \code{cate_oob}, \code{ate}, \code{horizon},
\code{pseudo_outcome}, \code{n}, \code{forest}.
}
\description{
Censoring makes the raw event time unusable as a forest outcome, so
the honest forest runs on an inverse-probability-of-censoring-weighted
pseudo outcome whose expectation is the RMST at \code{horizon}. The
resulting CATE is a difference in restricted mean survival time, not
a hazard ratio. Mirrors \code{morie.fn.csfgrf} (and \code{morie.fn.survcfg}).
}
\references{
Cui Y, Kosorok MR, Sverdrup E, Wager S, Zhu R (2023).
Estimating heterogeneous treatment effects with right-censored data
via causal survival forests. \emph{JRSS-B} 85(2), 179-211.
}
\examples{
set.seed(3)
n <- 150
X <- matrix(rnorm(n * 2), n, 2)
d <- rbinom(n, 1, 0.5)
tt <- rexp(n, rate = exp(-0.5 * d - 0.3 * X[, 1]))
ev <- as.numeric(tt < quantile(tt, 0.8))
tt <- pmin(tt, quantile(tt, 0.8))
r <- morie_causal_survival_forest(tt, ev, d, X, n_trees = 20L)
str(r, max.level = 1)
}

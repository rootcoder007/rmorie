% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fzgth.R
\name{Hsumcdf}
\alias{Hsumcdf}
\alias{morie_fauzi_g_theta_distribution}
\title{The function G(theta), distribution function of the half-sum (X_1+X_2)/2}
\usage{
Hsumcdf(
  theta = 0,
  cdf = NULL,
  density = NULL,
  lo = -10,
  hi = 10,
  ngrid = 4001L
)

morie_fauzi_g_theta_distribution(
  theta = 0,
  cdf = NULL,
  density = NULL,
  lo = -10,
  hi = 10,
  ngrid = 4001L
)
}
\arguments{
\item{theta}{Location parameter.}

\item{cdf}{\code{F}; defaults to the standard normal.}

\item{density}{\code{f}; defaults to the standard normal.}

\item{lo, hi}{Quadrature limits.}

\item{ngrid}{Number of nodes; fixed, never adapted.}
}
\value{
Named list with \code{estimate}, \code{theta}, \code{method}.
}
\description{
Sec. 5.3: \deqn{G(\theta) = \int F(2\theta+u)f(u)\,du,}{G(theta) = int F(2 theta + u) f(u) du,}
the distribution function of \code{(X_1 + X_2)/2}.
}
\details{
It is the population quantity the Mann-Whitney form of Wilcoxon's signed
rank statistic estimates: \verb{W = sum_\{i<=j\} I(X_i + X_j >= 0)} has
\verb{E(Wtilde) = n(n+1)/2 \{G(theta) + O(h^2)\}}. Under \code{H0} with \code{F} symmetric,
\code{G(0) = 1/2}.

Two equivalent forms appear, \verb{int F(2 theta + u) f(u) du} and
\verb{int F(2 theta - u) f(u) du}; they agree because the integrand is
symmetrised by \code{f}. The book prints the first, and the primary source --
Maesono, Y., Moriyama, T. and Lu, M. (2018), "Smoothed nonparametric tests
and their properties", Annals of the Institute of Statistical Mathematics
70(5):969-982 (arXiv:1610.02145) -- prints both and states they are equal.

Integrated on a fixed trapezoid grid; with \code{cdf} and \code{density} both standard
normal, \code{G(0)} returns 1/2 to quadrature accuracy.
}
\examples{
Hsumcdf(theta = 0)
}
\references{
Fauzi and Maesono (2023), Sec. 5.3; Maesono, Moriyama and Lu (2018), AISM 70:969-982.
}
\keyword{internal}

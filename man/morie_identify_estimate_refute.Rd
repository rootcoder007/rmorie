% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/abundance_text_voting_native.R
\name{morie_identify_estimate_refute}
\alias{morie_identify_estimate_refute}
\title{Model, identify, estimate, refute}
\usage{
morie_identify_estimate_refute(
  dag,
  data,
  treatment,
  outcome,
  adjustment = NULL,
  n_refute = 100L,
  seed = 0L,
  alpha = 0.05
)
}
\arguments{
\item{dag}{Logical adjacency matrix; \verb{dag\[i, j\]} means i -> j.}

\item{data}{Observations, one column per node.}

\item{treatment, outcome}{Column indices (1-based).}

\item{adjustment}{Force a particular set instead of identifying one.}

\item{n_refute}{Replications per refutation.}

\item{seed}{Random seed.}

\item{alpha}{Two-sided level.}

}
\value{
A list with \code{estimate}, \code{se}, \code{identified}, \code{adjustment_set},
\code{placebo_effect}, \code{random_cause_effect}, \code{subset_sd},
\code{refutations_passed}, \code{warnings}.
}
\description{
The value is not the number at step three. It is that identification
says WHICH quantity the data can speak to, estimation produces it,
and refutation asks whether it survives perturbations that ought not
to change it: a placebo treatment (should collapse to zero), a random
common cause (should not move it) and random subsets (spread should
match the standard error).
}
\details{
Adjusting for everything measured is not conservative. Conditioning
on a mediator removes part of the effect being measured, and
conditioning on a collider CREATES an association where none existed.
Both are flagged and the estimate still returned, so the damage is
visible rather than merely warned about.
}
\references{
Pearl J (2009) \emph{Causality}, 2nd ed., Sec 3.3.
Sharma A, Kiciman E (2020) arXiv:2011.04216.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fzse.R
\name{Ssgnmom}
\alias{Ssgnmom}
\alias{morie_fauzi_sign_moments}
\title{Mean and variance of the smoothed sign test statistic}
\usage{
Ssgnmom(
  n,
  ftheta = 0.5,
  h = NULL,
  f0 = NULL,
  fpp0 = NULL,
  a11 = NULL,
  a13 = NULL
)

morie_fauzi_sign_moments(
  n,
  ftheta = 0.5,
  h = NULL,
  f0 = NULL,
  fpp0 = NULL,
  a11 = NULL,
  a13 = NULL
)
}
\arguments{
\item{n}{Sample size.}

\item{ftheta}{\code{F(theta)}; 1/2 under the null.}

\item{h}{Bandwidth, needed for the Theorem 5.10 refinement.}

\item{f0, fpp0}{\code{f(0)} and \verb{f''(0)}.}

\item{a11, a13}{\verb{A_\{1,1\}} and \verb{A_\{1,3\}}.}
}
\value{
Named list with \code{mean}, \code{variance}, \code{se}, \code{refined}, \code{n}, \code{method}.
}
\description{
Sec. 5.3.1. With \verb{Stilde = n - sum_i K(-X_i/h)},
\deqn{E_\theta(\tilde S) = n\{F(\theta) + O(h^2)\},\quad V_\theta(\tilde S) = n\[\{1-F(\theta)\}F(\theta) + O(h)\].}{E(Stilde) = n{F(theta) + O(h^2)}, V(Stilde) = n\[{1 - F(theta)} F(theta) + O(h)\].}
}
\details{
Under \code{H0} these reduce to \code{n/2} and \code{n/4}, and Theorem 5.10 refines the
variance to \verb{n/4 - 2 n h f(0) A11 - (n h^3 / 3) f''(0) A13 + o(1)} with
\verb{A_\{i,j\} = int K^i(u) k(u) u^j du}. Pass \code{a11} and \code{a13} with \code{f0}/\code{fpp0}
for that refinement; otherwise the leading forms are returned and \code{refined}
is \code{FALSE}.

The point is in the error terms. The ordinary sign test \code{S} is discrete, so
its standardised version jumps by \code{O(n^-1/2)} and no Edgeworth expansion can
be valid for it. \code{Stilde} is continuous, and under \code{H0} its leading moments
do not depend on \code{F} at all -- asymptotically distribution-free, which makes
Theorem 5.9 possible.

This module previously carried a copy of a Kolmogorov-Smirnov
implementation, returning a KS statistic under the name of the sign test's
moments. It now computes the moments.

Verified against Maesono, Moriyama and Lu (2018), AISM 70(5):969-982
(arXiv:1610.02145), Sec. 3.
}
\examples{
Ssgnmom(n = 100)
}
\references{
Fauzi and Maesono (2023), Sec. 5.3.1 and Theorem 5.10; Maesono, Moriyama and Lu (2018), AISM 70:969-982.
}
\keyword{internal}

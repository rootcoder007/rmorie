% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trpoc.R
\name{Trpostep}
\alias{Trpostep}
\title{TRPO's constrained surrogate and its step size}
\usage{
Trpostep(
  env,
  policy = NULL,
  kl_max = 0.01,
  ratio = NULL,
  adv = NULL,
  kl = NULL,
  g = NULL,
  F = NULL
)
}
\arguments{
\item{env}{the advantages A_t.}

\item{policy}{the probability ratios r_t.}

\item{kl_max}{the trust-region radius delta.}

\item{ratio, adv, kl}{explicit ratios, advantages, per-state KLs.}

\item{g}{gradient of the surrogate.}

\item{F}{Fisher information matrix.}
}
\value{
list: estimate, surrogate, kl_mean, feasible, step, step_size,
delta, n, method.
}
\description{
Schulman, Levine, Moritz, Jordan and Abbeel (2015), Trust region policy
optimization, ICML 37, 1889-1897 (arXiv:1502.05477 -- FETCHED),
equation (14): maximise E\link{pi_theta(a|s)/pi_old(a|s) A} subject to
Dbar_KL^(rho_old)(theta_old, theta) <= delta -- the AVERAGE KL over the
state distribution, substituted for the maximum KL of equation (12)
because the max "is impractical to solve".  Section 6 solves the
quadratic approximation: with g the surrogate gradient and F the Fisher
information, the step is s = sqrt(2 delta / (x' F x)) x with x = F^-1 g,
the largest step in that direction inside the trust region.
}
\examples{
Trpostep(c(1, -1), c(1.1, 0.9), 0.01)$surrogate
}
\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causdiddc_native.R
\name{morie_causdiddc_weights}
\alias{morie_causdiddc_weights}
\title{The w_\{g,t\} of Theorem 1}
\usage{
morie_causdiddc_weights(D, group, period, weights = NULL)
}
\arguments{
\item{D}{Binary 0/1 treatment, long format.  Treated status must
be constant within every (group, period) cell.}

\item{group, period}{Panel identifiers.}

\item{weights}{Optional observation weights.}
}
\value{
A list with \code{weights} (named numeric keyed by
"(group,period)" strings) and \code{residual} (the within-
transformed treatment).
}
\description{
The weights of de Chaisemartin & D'Haultfoeuille (2020) Theorem 1,
from the residual of the treatment indicator on the two sets of
fixed effects, scaled to sum to one over the treated cells.  They
can be NEGATIVE -- the regression everyone runs is not an average
treatment effect.
}
\examples{
set.seed(3)
n_g <- 6; n_t <- 4
grid <- expand.grid(g = seq_len(n_g), t = seq_len(n_t))
D <- as.numeric(grid$g <= 2 & grid$t >= 3)
r <- morie_causdiddc_weights(D, grid$g, grid$t)
str(r, max.level = 1)
}
\references{
de Chaisemartin, C. & D'Haultfoeuille (2020), Theorem 1.
}
\keyword{internal}

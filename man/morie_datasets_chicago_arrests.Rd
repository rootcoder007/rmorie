% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datasets.R
\name{morie_datasets_chicago_arrests}
\alias{morie_datasets_chicago_arrests}
\title{City of Chicago Open Data -- Arrests feed (\code{dpt3-jri9})}
\usage{
morie_datasets_chicago_arrests(
  year = NULL,
  max_features = NULL,
  offline = TRUE,
  resource_id = NULL,
  mode = c("soda2", "soda3"),
  paginate = FALSE,
  page_size = 1000L,
  max_pages = 200L,
  app_token = NULL
)
}
\arguments{
\item{year}{Integer or \code{NULL}; server-side year filter (uses
\verb{date_extract_y(arrest_date) = <year>} SoQL).}

\item{max_features}{Integer or \code{NULL}; cap on returned rows. When
\code{paginate = TRUE} this is the total cap across walked pages.}

\item{offline}{Logical; if \code{TRUE} (default, safer post-3EE), read
the bundled synthetic frame.}

\item{resource_id}{Optional Socrata resource id override. Accepts
the UUID (\code{dpt3-jri9}, default) or the publisher's alias
(\code{arrests}).}

\item{mode}{One of \code{"soda2"} (default JSON resource endpoint) or
\code{"soda3"} (SoQL \code{query} endpoint). 3VV+ dual-mode dispatch.}

\item{paginate}{Logical; if \code{TRUE} and \code{offline = FALSE}, walk
SODA2 \verb{$offset} in \code{page_size} chunks. Default \code{FALSE}.}

\item{page_size}{Per-page row count when paginating (default 1,000,
the unauthenticated SODA2 ceiling).}

\item{max_pages}{Safety net on paginated walks (default 200).}

\item{app_token}{Optional Socrata API app token for higher rate
limits; passed as the \code{X-App-Token} header.}
}
\value{
A \code{data.frame} with the documented 24-col Socrata schema.
}
\description{
Wraps the City of Chicago "Arrests" open dataset (Socrata resource
id \code{dpt3-jri9}; portal landing
\url{https://data.cityofchicago.org/Public-Safety/Arrests/dpt3-jri9/about_data}).
24 columns covering up to four charges per arrest plus the
pipe-concatenated rollup quartet (\code{charges_statute} /
\code{charges_description} / \code{charges_type} / \code{charges_class}).
}
\details{
Socrata accepts two resource specifiers interchangeably -- the
numeric/UUID id (\verb{/resource/dpt3-jri9.json}) and the human-readable
alias the publisher assigned (\verb{/resource/arrests.json}). morie
defaults to the UUID for stability; pass \code{resource_id = "arrests"}
if you want to exercise the alias path.

Offline mode reads a bundled 5-row synthetic fixture
(\code{inst/extdata/chicago_arrests_dpt3_jri9_sample.csv}) carrying the
real upstream snake_case schema. Live mode hits the SODA2 endpoint
via \code{.morie_dataset_socrata_fetch()} and honours the 3OO opt-in
pagination (\code{paginate = TRUE}).
}
\examples{
df <- morie_datasets_chicago_arrests(offline = TRUE)
df$arrest_date
}
\references{
City of Chicago Data Portal, "Arrests" (\code{dpt3-jri9}).
}

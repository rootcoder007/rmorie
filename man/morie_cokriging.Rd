% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial_models.R
\name{morie_cokriging}
\alias{morie_cokriging}
\title{Ordinary co-kriging}
\usage{
morie_cokriging(coords, z1, z2, s0, cross_vario = NULL, model = NULL)
}
\arguments{
\item{coords}{n x d matrix of coordinates}

\item{z1}{primary variable}

\item{z2}{secondary variable}

\item{s0}{prediction location}

\item{cross_vario}{cross-variogram function of distance}

\item{model}{direct variogram function of distance}
}
\value{
list with \code{prediction}, \code{variance}, \code{lambda} and \code{mu}
}
\description{
\verb{Z1*(s0) = sum lambda_i Z1(s_i) + sum mu_j Z2(s_j)}, solved under
the constraints \verb{sum lambda = 1} and \verb{sum mu = 0}.  The covariate
contributes only through the cross-variogram: set it to zero and the
mu weights vanish, leaving ordinary kriging on Z1.
}
\examples{
cokrg(x = rnorm(50), y = rnorm(50), coords = matrix(runif(100), 50, 2), target = rnorm(50))
}

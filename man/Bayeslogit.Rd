% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/baylog.R
\name{Bayeslogit}
\alias{Bayeslogit}
\title{Posterior mode and normal approximation for logistic regression}
\usage{
Bayeslogit(X, y, prior_sd = 10, iters = 50, tol = 1e-12)
}
\arguments{
\item{X}{Predictors, WITHOUT an intercept column.}

\item{y}{Binary responses in \{0, 1\}.}

\item{prior_sd}{Standard deviation of the coefficient prior.}

\item{iters}{Maximum Newton steps.}

\item{tol}{Convergence tolerance on the maximum coefficient change.}
}
\value{
List with \code{estimate}, \code{se}, \code{log_posterior},
\code{iterations}, \code{converged}, \code{n}, \code{p}.
}
\description{
The N(0, prior_sd^2) prior keeps the Hessian invertible under
separation. The approximation is centred at the MODE and scaled by the
inverse Hessian. An intercept column is prepended; do not include one.
}
\details{
Formula: Newton step beta <- beta + (X'WX + I/s^2)^-1
(X'(y - p) - beta/s^2), W = diag(p_i(1 - p_i));
Var(beta) ~= (X'WX + I/s^2)^-1
}
\examples{
set.seed(1)
X <- cbind(1, matrix(rnorm(40), 20, 2))
y <- rbinom(20, 1, 0.5)
Bayeslogit(X, y)
}
\references{
Gelman, Carlin, Stern, Dunson, Vehtari & Rubin (2013),
Bayesian Data Analysis, 3rd edition, Section 4.1 and Chapter 16.
Fetched as the full text of the book from the author's own copy.
}

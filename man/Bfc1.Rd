% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fzc1x.R
\name{Bfc1}
\alias{Bfc1}
\alias{morie_fauzi_c1_coefficient}
\title{The c_1 bias coefficient of the boundary-free KDFE (Eq. 5.8)}
\usage{
Bfc1(dg, d2g, density, fp)

morie_fauzi_c1_coefficient(dg, d2g, density, fp)
}
\arguments{
\item{dg, d2g}{\verb{g'(g^-1(x))} and \verb{g''(g^-1(x))}.}

\item{density}{\code{f_X(x)}.}

\item{fp}{\verb{f_X'(x)}.}

}
\value{
Named list with \code{estimate}, \code{method}.
}
\description{
Eq. (5.8):
\deqn{c_1(x) = g''(g^{-1}(x))f_X(x) + \[g'(g^{-1}(x))\]^2 f_X'(x),}{c1(x) = g''(g^-1(x)) f(x) + \[g'(g^-1(x))\]^2 f'(x),}
the coefficient in \verb{Bias\[Ftilde(x)\] = (h^2/2) c1(x) mu2(K) + o(h^2)} of
Theorem 5.2.
}
\details{
Compare the naive KDFE, whose bias coefficient is just \verb{f'(x) mu2 / 2} --
i.e. (5.8) with \code{g} the identity, where \verb{g'' = 0} and \verb{g' = 1}. The
transformation adds one term and rescales the other; that is the entire cost
of removing the boundary bias.

This is the same expression as \code{b1(t)} in (4.14) of Chapter 4: the survival
and distribution estimators are the same construction applied to \code{1-F} and
\code{F}, so they share a bias coefficient -- and Theorem 4.1 duly carries a
minus sign in front of it where Theorem 5.2 does not.
}
\examples{
Bfc1(dg = 1, d2g = 0, density = 0.3, fp = 0.2)
}
\references{
Fauzi and Maesono (2023), Eq. (5.8); the same expression as (4.14).
}
\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evt_native.R
\name{morie_evt_extremal_sliding}
\alias{morie_evt_extremal_sliding}
\title{Sliding-blocks estimator of the extremal index}
\usage{
morie_evt_extremal_sliding(x, threshold = NULL, block_length = NULL)
}
\arguments{
\item{x}{stationary series.}

\item{threshold}{ignored; present for signature parity.}

\item{block_length}{block size b; \code{floor(sqrt(n))} when \code{NULL}.}
}
\value{
list: theta, theta_disjoint, block_length,
n_sliding_blocks, n_disjoint_blocks, n, method.
}
\description{
Northrop (2015): for a series with extremal index theta,
\verb{-b log F_hat(block max)} is approximately Exp(theta), so
\code{theta = 1/mean}. SLIDING blocks use each observation in b windows
instead of one, and Northrop shows the sliding version has
strictly smaller asymptotic variance than the disjoint one -- the
entire point of preferring it, and both are returned so the
comparison is visible. \code{threshold} is accepted only for signature
parity with the other extremal-index estimators and is ignored.
}
\examples{
morie_evt_extremal_sliding(stats::rnorm(400), block_length = 20)$theta
}
\references{
Northrop (2015), \emph{Extremes} 18:585-603.
}

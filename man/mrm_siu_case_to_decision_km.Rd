% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mrm_siu.R
\name{mrm_siu_case_to_decision_km}
\alias{mrm_siu_case_to_decision_km}
\title{KM survival of SIU case-to-decision gap per police service}
\usage{
mrm_siu_case_to_decision_km(
  data,
  incident_col = "date_of_incident_iso",
  decision_col = "date_of_director_decision_iso",
  service_col = "police_service",
  censor_open_cases = TRUE,
  min_n = 5L
)
}
\arguments{
\item{data}{A data.frame with the SIU case schema.}

\item{incident_col}{Column with ISO incident date
(default \code{"date_of_incident_iso"}).}

\item{decision_col}{Column with ISO Director's decision date
(default \code{"date_of_director_decision_iso"}).}

\item{service_col}{Stratifying jurisdiction column
(default \code{"police_service"}).}

\item{censor_open_cases}{Logical (default \code{TRUE}). If \code{TRUE},
rows with missing \code{decision_col} contribute right-censored
observations from incident to the most recent decision date
in the data set. If \code{FALSE}, they are dropped.}

\item{min_n}{Minimum cases per service to retain in the per-service
summary (default \code{5L}).}
}
\value{
A list with elements:
\itemize{
\item \code{pooled}: a single-row data.frame with the pooled median,
mean, IQR, n, n_censored.
\item \code{by_service}: per-service data.frame with the same columns.
}
}
\description{
Computes the gap (in days) between the incident date and the
Director's decision date for every SIU case, dropping rows where
either date is missing. Reports per-stratum median + IQR + n.
Cases without a decision date as of the snapshot are right-censored
if \code{censor_open_cases = TRUE} (default).
}
\details{
This is the substantive "time-to-outcome" analysis the MA-thesis
"210-day TTR" claim should have been; it operates on real per-case
dates with a stable jurisdiction identifier.
}
\examples{
if (FALSE) {
  siu <- read.csv("SIU.csv")
  res <- mrm_siu_case_to_decision_km(siu)
  head(res$by_service)
}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chzlt.R
\name{Chzlt}
\alias{Chzlt}
\title{Cinelli-Hazlett sensitivity to an unobserved confounder}
\usage{
Chzlt(model, treat = NULL, cov = NULL, R2_yu = 0, R2_du = 0, q = 1)
}
\arguments{
\item{model}{Outcome vector y.}

\item{treat}{Treatment vector D.}

\item{cov}{An n x k matrix of observed covariates, or NULL.}

\item{R2_yu}{Hypothesised partial R2 with the outcome.}

\item{R2_du}{Hypothesised partial R2 with the treatment.}

\item{q}{Fraction of the estimate the confounder would explain.}
}
\value{
List with \code{estimate}, \code{tau}, \code{se}, \code{t},
\code{df}, \code{bias}, \code{adjusted_se}, \code{rv_q},
\code{r2_yd_x}, \code{robust}, \code{n}, \code{method}.
}
\description{
Formula: adjusted estimate vs (R2_y~u.x, R2_d~u.x)
}
\details{
The bias an omitted confounder U can produce is bounded by
|bias| = se sqrt(df) sqrt(R2_yu R2_du / (1 - R2_du)), so a claim
survives U exactly when the adjusted estimate keeps its sign.  The
robustness value RV_q is the common partial R2 at which the adjusted
estimate is driven to zero: feeding RV back in as both R2 values
returns an adjusted estimate of exactly zero, the identity used to
check this implementation.
}
\references{
Cinelli & Hazlett (2020), Making Sense of Sensitivity,
JRSS B 82(1):39-67.
}
\examples{
set.seed(1)
Chzlt(model = rnorm(50), treat = rbinom(50, 1, 0.5),
      cov = matrix(rnorm(100), 50, 2), R2_yu = 0.1, R2_du = 0.1)
}

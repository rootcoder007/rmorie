% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mrm_lisa.R
\name{mrm_tps_polygon_moran_per_year}
\alias{mrm_tps_polygon_moran_per_year}
\title{Per-year global Moran's I time series across a polygon surface}
\usage{
mrm_tps_polygon_moran_per_year(
  data,
  year_cols,
  lat_col = "lat",
  lon_col = "lon",
  k = 6L,
  n_permutations = 999L,
  seed = 42L
)
}
\arguments{
\item{data}{Polygon-level data.frame.}

\item{year_cols}{Character vector of per-year count column names
(e.g. \code{c("ASSAULT_2014", ..., "ASSAULT_2024")}).}

\item{lat_col, lon_col, k, n_permutations, seed}{as in
\code{mrm_tps_lisa}.}
}
\value{
data.frame with columns \code{year}, \code{n_events},
\code{moran_I}, \code{global_p_value}.
}
\description{
Convenience wrapper that loops \code{mrm_tps_lisa} over a vector
of per-year count columns.
}
\examples{
# 4 x 4 polygon grid with two yearly count columns.
set.seed(2026)
grid <- expand.grid(
  lat = 43.6 + (0:3) * 0.02,
  lon = -79.4 + (0:3) * 0.02
)
grid$ASSAULT_2023 <- rpois(nrow(grid), lambda = grid$lat * 10)
grid$ASSAULT_2024 <- rpois(nrow(grid), lambda = grid$lat * 12)
res <- mrm_tps_polygon_moran_per_year(
  grid,
  year_cols = c("ASSAULT_2023", "ASSAULT_2024"),
  lat_col = "lat", lon_col = "lon",
  k = 4L, n_permutations = 99L, seed = 42L
)
res
}

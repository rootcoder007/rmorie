% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Gpcla.R
\name{Gpcla}
\alias{Gpcla}
\title{Gaussian process classification by the Laplace approximation}
\usage{
Gpcla(
  X,
  y,
  X_test = NULL,
  kernel = NULL,
  lengthscale = 1,
  variance = 1,
  iters = 40
)
}
\arguments{
\item{X}{Training inputs, one row per point.}

\item{y}{Binary labels, 0 or 1.}

\item{X_test}{Test inputs; the training inputs by default.}

\item{kernel}{Unused placeholder kept for the interface.}

\item{lengthscale, variance}{RBF kernel hyperparameters.}

\item{iters}{Newton iterations.}
}
\value{
List with \code{estimate}, \code{p}, \code{predicted},
\code{f_mode}, \code{latent_mean}, \code{latent_var},
\code{objective}, \code{n}, \code{method}.
}
\description{
With the probit likelihood, Newton's method on
Psi(f) = log p(y|f) - 0.5 f' K^{-1} f gives the posterior mode; the
averaged predictive probability is Phi(mu*/sqrt(1 + var*)).  The mode
is where the gradient vanishes, and the tests check that directly by
differentiating Psi numerically rather than trusting the iteration.
}
\details{
Formula: f <- (I + K W)^{-1} K (W f + grad log p(y|f));
p* = Phi(mu* / sqrt(1 + var*)).
}
\references{
Williams and Barber (1998), Bayesian classification with
Gaussian processes, IEEE TPAMI 20(12):1342-1351,
\doi{10.1109/34.735807}; Rasmussen and Williams (2006), ch. 3,
Algorithm 3.1 and eq. (3.82).
}
\examples{
set.seed(1)
X <- matrix(rnorm(20), 10, 2)
y <- rbinom(10, 1, 0.5)
Gpcla(X, y)
}

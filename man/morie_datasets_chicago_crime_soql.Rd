% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datasets.R
\name{morie_datasets_chicago_crime_soql}
\alias{morie_datasets_chicago_crime_soql}
\title{City of Chicago Crimes feed -- arbitrary-SoQL escape hatch}
\usage{
morie_datasets_chicago_crime_soql(
  where = NULL,
  select = "*",
  order = NULL,
  max_features = NULL,
  offline = TRUE,
  resource_id = NULL,
  paginate = FALSE,
  page_size = 1000L,
  max_pages = 200L,
  app_token = NULL
)
}
\arguments{
\item{where}{Optional SoQL \code{WHERE} fragment (without leading
\code{WHERE}). e.g. \code{"primary_type='HOMICIDE' AND year=2024"}.}

\item{select}{Projection list (default \code{"*"}).}

\item{order}{Optional SoQL \verb{ORDER BY} fragment.}

\item{max_features}{Optional total row cap.}

\item{offline}{Logical; if \code{TRUE} (default), read the 22-col
\code{chicago_crime_synthetic.csv} fixture (same one
\code{\link[=morie_datasets_chicago_crime]{morie_datasets_chicago_crime()}} uses).}

\item{resource_id}{Optional view id override (default
\code{"ijzp-q8t2"}; pass \code{"crimes"} for the publisher alias path).}

\item{paginate}{Logical; opt-in pagination.}

\item{page_size}{Per-page row count when paginating.}

\item{max_pages}{Safety net.}

\item{app_token}{Optional Socrata app token (sent as header).}
}
\value{
A \code{data.frame}.
}
\description{
Sibling to \code{\link[=morie_datasets_chicago_crime]{morie_datasets_chicago_crime()}} but hits the
\strong{SODA3} \verb{/api/v3/views/crimes/query.json} endpoint instead of
SODA2's \verb{/resource/ijzp-q8t2.json}. The 8.56M-row scale of the
base feed makes SODA2's URL-param \verb{$where} clumsy for non-trivial
filters; SODA3 lets you send the full SoQL \verb{SELECT ... WHERE ... ORDER BY ...} string in one go.
}
\examples{
df <- morie_datasets_chicago_crime_soql(offline = TRUE)
nrow(df)
}

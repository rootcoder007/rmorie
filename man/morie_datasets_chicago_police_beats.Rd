% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datasets.R
\name{morie_datasets_chicago_police_beats}
\alias{morie_datasets_chicago_police_beats}
\title{Chicago Police Beats (current) boundaries (\code{n9it-hstw})}
\usage{
morie_datasets_chicago_police_beats(
  offline = TRUE,
  geometry = FALSE,
  max_features = NULL,
  resource_id = NULL,
  mode = c("soda2", "soda3"),
  paginate = FALSE,
  page_size = 1000L,
  max_pages = 200L,
  app_token = NULL
)
}
\arguments{
\item{offline}{If \code{TRUE} (default), read the bundled fixture.}

\item{geometry}{If \code{TRUE} and \code{offline = FALSE}, include
\code{the_geom} (MultiPolygon).}

\item{max_features}{Optional row cap.}

\item{resource_id}{Optional Socrata resource id override (UUID
default; pass \code{"police-beats"} style alias if known).}

\item{paginate}{Logical; 3OO opt-in pagination.}

\item{page_size}{Per-page row count when paginating.}

\item{max_pages}{Safety net on paginated walks.}
}
\value{
A \code{data.frame} with 4 attribute cols (offline) or 5
including \code{the_geom} (live, \code{geometry = TRUE}).
}
\description{
Wraps the City of Chicago "Boundaries - Police Beats (current)"
open dataset (Socrata resource id \code{n9it-hstw}; portal landing
\url{https://data.cityofchicago.org/Public-Safety/Boundaries-Police-Beats-current-/aerh-rz74}).
Returns 277 Chicago Police beats with their parent sector + district
codes (verified live 2026-05). Attribute schema:
}
\details{
\describe{
\item{beat_num}{4-digit beat id (district + 2-digit beat).}
\item{beat}{Within-sector beat sequence number (string).}
\item{sector}{Within-district sector number (string).}
\item{district}{Parent district number (string).}
}

Offline mode reads a bundled attribute-only fixture
(\code{inst/extdata/chicago_police_beats.csv}) -- the \code{the_geom}
MultiPolygon column is stripped to keep bundle size sane.
Live mode hits the SODA2 JSON endpoint via
\code{.morie_dataset_socrata_fetch()} (mockable); pass \code{geometry = TRUE}
to include \code{the_geom}. Threads through the 3OO pagination args.
}
\examples{
df <- morie_datasets_chicago_police_beats(offline = TRUE)
head(df)
}
\references{
City of Chicago Data Portal, "Boundaries - Police
Beats (current)" (\code{n9it-hstw}).
}

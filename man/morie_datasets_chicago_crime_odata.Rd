% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datasets.R
\name{morie_datasets_chicago_crime_odata}
\alias{morie_datasets_chicago_crime_odata}
\title{City of Chicago Crimes feed via OData v4 (\code{ijzp-q8t2})}
\usage{
morie_datasets_chicago_crime_odata(
  filter = NULL,
  select = NULL,
  orderby = NULL,
  top = NULL,
  skip = NULL,
  max_features = NULL,
  offline = TRUE,
  resource_id = NULL,
  paginate = FALSE,
  max_pages = 200L,
  app_token = NULL
)
}
\arguments{
\item{filter}{Optional OData \verb{$filter} string (caller-supplied
verbatim; see limitation above).}

\item{select}{Optional comma-separated column list.}

\item{orderby}{Optional OData \verb{$orderby}.}

\item{top}{Optional per-request row count (= \verb{$top}).}

\item{skip}{Optional start offset (= \verb{$skip}).}

\item{max_features}{Optional total row cap across pages.}

\item{offline}{Logical; default \code{TRUE} reads the bundled 22-col
\code{chicago_crime_synthetic.csv} fixture.}

\item{resource_id}{Optional view id override (default
\code{"ijzp-q8t2"}; pass \code{"crimes"} for the publisher alias).}

\item{paginate}{Logical; if \code{TRUE}, follow \verb{@odata.nextLink}.}

\item{max_pages}{Safety net on paginated walks.}

\item{app_token}{Optional Socrata app token (sent as \code{X-App-Token}).}
}
\value{
A \code{data.frame}.
}
\description{
Third Socrata API mode: OData v4 at
\verb{/api/odata/v4/<view_id>}, the same protocol Tableau / Power BI /
Excel speak natively. Use this when you want morie to consume the
Crimes feed the same way those tools do, or when you want
server-driven \verb{@odata.nextLink} pagination instead of the
client-driven \verb{$offset} walk that SODA2/SODA3 use.
}
\details{
When to reach for which API mode:

\tabular{lll}{
\strong{Mode}      \tab \strong{morie wrapper}                       \tab \strong{best for} \cr
SODA2              \tab \code{\link[=morie_datasets_chicago_crime]{morie_datasets_chicago_crime()}}            \tab base-feed pulls + \verb{$where} filtering \cr
SODA3 (SoQL)       \tab \code{\link[=morie_datasets_chicago_crime_soql]{morie_datasets_chicago_crime_soql()}}       \tab arbitrary \verb{SELECT ... WHERE} \cr
SODA3 (map view)   \tab \code{\link[=morie_datasets_chicago_crime_map]{morie_datasets_chicago_crime_map()}}        \tab derived/filtered views (ahwe-kpsy) \cr
OData v4           \tab \code{morie_datasets_chicago_crime_odata()}      \tab third-party tool ingestion \cr
}

\strong{Known Socrata limitation.} \verb{$filter} is unreliable on Socrata's
OData implementation -- the parser frequently rejects equality
filters with \code{"The types 'Edm.Boolean' and 'Edm.String' (or 'Edm.Decimal') are not compatible."}. \verb{$top} / \verb{$skip} /
\verb{$select} / \verb{$orderby} all work; for filtering, use SODA3.
}
\examples{
df <- morie_datasets_chicago_crime_odata(offline = TRUE)
nrow(df)
}
\references{
Socrata OData docs:
\url{https://support.socrata.com/hc/en-us/articles/115005364207-Access-Data-Insights-Data-using-OData}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geron_ml2_native.R
\name{morie_geron_logistic_cost}
\alias{morie_geron_logistic_cost}
\title{Binary log loss (Geron Ch 4, morie.fn hmlogcl)}
\usage{
morie_geron_logistic_cost(X, y, theta, add_bias = FALSE)
}
\arguments{
\item{X, y, theta}{Design, 0/1 labels, coefficients.}

\item{add_bias}{Prepend a ones column.}


}
\value{
List with \code{cost}, \code{per_instance}, \code{p_hat}, \code{logits},
\code{baseline_cost}, \code{estimate}, \code{n}.
}
\description{
Computed from the LOGITS as -y z + max(z, 0) + log1p(exp(-|z|)),
which is stable where -log(p) is not. Probabilities are delegated
to \code{morie_geron_logistic_probability}, as hmlogcl delegates to
hmlogp. The class-rate baseline is the bar to beat.
}
\examples{
set.seed(4)
X <- cbind(1, rnorm(30))
y <- rbinom(30, 1, plogis(X \%*\% c(-0.5, 1.5)))
morie_geron_logistic_cost(X, y, theta = c(-0.5, 1.5))
}

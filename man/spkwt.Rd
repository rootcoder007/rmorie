% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spkwt.R
\name{spkwt}
\alias{spkwt}
\title{Solve the kriging system for the weights}
\usage{
spkwt(cov_matrix, cov_target, coords = NULL, unbiased = FALSE)
}
\arguments{
\item{cov_matrix}{Sigma, the (n by n) covariance among observations.}

\item{cov_target}{sigma, covariance between observations and the
target, length n.}

\item{coords}{Unused; accepted for call-site compatibility.}

\item{unbiased}{Impose the sum-to-one constraint (ordinary rather than
simple kriging).}
}
\value{
Named list: weights, weight_sum, lagrange, unbiased.
}
\description{
Simple kriging solves Sigma lambda = sigma. Ordinary kriging adds the
unbiasedness constraint sum(lambda) = 1 through a Lagrange multiplier,
bordering the system with a row and column of ones.
}
\examples{
spkwt(diag(3) * 2, c(1, 0.5, 0.25), unbiased = TRUE)
}
\references{
Schabenberger & Gotway (2005), Sec 5.2.
}

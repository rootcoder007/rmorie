% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fzt11.R
\name{Gkrawbv}
\alias{Gkrawbv}
\alias{morie_fauzi_thm1_1_bias_mgkde}
\title{Bias and variance of the raw gamma-kernel function A_h (Theorem 1.1)}
\usage{
Gkrawbv(x, h, n, fp, fpp, f, boundary = FALSE, c = NULL)

morie_fauzi_thm1_1_bias_mgkde(x, h, n, fp, fpp, f, boundary = FALSE, c = NULL)
}
\arguments{
\item{x}{Evaluation point, \code{x >= 0}.}

\item{h}{Bandwidth, \code{h > 0}.}

\item{n}{Sample size.}

\item{fp, fpp, f}{The values \verb{f'(x)}, \verb{f''(x)} and \code{f(x)}.}

\item{boundary}{Logical; use the boundary branch of (1.11).}

\item{c}{The constant in \code{x/h -> c}; required when \code{boundary} is TRUE.}
}
\value{
Named list: bias, variance, mse, rnum, rden, region, h, n, method.
}
\description{
\code{A_h} is the raw gamma-kernel function of Eq. (1.9). Remark 1.1 is
explicit that it is NOT the proposed estimator, only the object the
proposed one is extrapolated from. Its moments are
\deqn{\mathrm{Bias}\[A_h(x)\] = (f'(x) + x^2 f''(x)/2)\sqrt{h} + o(\sqrt{h}),}{Bias\[A_h(x)\] = (f'(x) + x^2 f''(x)/2) sqrt(h) + o(sqrt(h)),}
\deqn{\mathrm{Var}\[A_h(x)\] = \frac{R^2(h^{-1/2}-1) f(x)}{2(x+\sqrt{h})\sqrt{\pi}(1-\sqrt{h})R(2h^{-1/2}-2) n h^{1/4}},}{Var\[A_h(x)\] = R^2(h^-1/2 - 1) f(x) / (2(x+sqrt(h)) sqrt(pi) (1-sqrt(h)) R(2h^-1/2 - 2) n h^(1/4)),}
in the interior, and the same with \code{(x+sqrt(h))} replaced by
\verb{(c sqrt(h) + 1)} and \code{h^(1/4)} by \code{h^(3/4)} in the boundary region.
\code{R} is Eq. (1.12).
}
\details{
The bias is O(sqrt(h)) -- WORSE than Chen's O(h). That is the tension of
Sec. 1.2: fixing the gamma shape at \code{h^(-1/2)} and moving the scale buys
a variance of order \verb{n^(-1) h^(-1/4)} instead of Chen's
\verb{n^(-1) h^(-1/2)}, and pays for it in bias. Theorem 1.2 buys the bias
back by geometric extrapolation.
}
\examples{
Gkrawbv(x = 1, h = 0.01, n = 100, fp = 0.1, fpp = -0.2, f = 0.3)
}
\references{
Fauzi and Maesono (2023), Theorem 1.1, Eqs. (1.9)-(1.12).
}
\keyword{internal}

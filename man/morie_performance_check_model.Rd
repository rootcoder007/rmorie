% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extenders_stats.R
\name{morie_performance_check_model}
\alias{morie_performance_check_model}
\title{Regression-diagnostic plots via \pkg{performance}}
\usage{
morie_performance_check_model(model, ...)
}
\arguments{
\item{model}{A fitted model object supported by \pkg{insight} /
\pkg{performance}.}

\item{...}{Further arguments forwarded to
\code{performance::check_model} (e.g. \code{check}, \code{panel},
\code{theme}).}
}
\value{
A list with \code{$method = "performance::check_model"}
and \code{$raw} (the upstream \code{check_model} object).
}
\description{
Thin extender over \code{performance::check_model} that returns
the diagnostic-plot grob list for a fitted model.
}
\examples{
if (requireNamespace("performance", quietly = TRUE) &&
  requireNamespace("see", quietly = TRUE)) {
  set.seed(1)
  df <- data.frame(y = rnorm(50), x = rnorm(50))
  res <- morie_performance_check_model(stats::lm(y ~ x, df))
  class(res)
}
}

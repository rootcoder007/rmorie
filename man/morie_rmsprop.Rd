% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optim_native.R
\name{morie_rmsprop}
\alias{morie_rmsprop}
\title{RMSProp optimiser step}
\usage{
morie_rmsprop(g, rho = 0.9, lr = 0.001, eps = 1e-08, state = NULL)
}
\arguments{
\item{g}{Gradient at the current parameters.}

\item{rho}{Decay rate in [0, 1).}

\item{lr}{Step size.}

\item{eps}{Denominator floor.}

\item{state}{List from the previous call, or \code{NULL}.}
}
\value{
List with \code{update}, \code{state}, \code{v}, \code{t}, \code{step_norm}.
}
\description{
Exponentially decayed average of squared gradients, so the effective step
size settles rather than decaying to zero as AdaGrad's does. No momentum
and no bias correction, so the first steps are larger than the asymptotic
ones.
}
\examples{
st <- NULL
for (i in 1:500) { r <- morie_rmsprop(1, lr = 0.1, state = st); st <- r$state }
abs(abs(r$update) - 0.1) < 1e-3
}
\references{
Tieleman, T., & Hinton, G. (2012). Lecture 6.5 -- RMSProp.
}

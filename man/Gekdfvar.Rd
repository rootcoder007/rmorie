% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fzt23.R
\name{Gekdfvar}
\alias{Gekdfvar}
\alias{morie_fauzi_thm2_3_var_brdkdfe}
\title{Variance of the bias-reduced KDFE (Theorem 2.3)}
\usage{
Gekdfvar(n, h, a, fx, density, r1 = NULL, r2 = NULL)

morie_fauzi_thm2_3_var_brdkdfe(n, h, a, fx, density, r1 = NULL, r2 = NULL)
}
\arguments{
\item{n}{Sample size.}

\item{h}{Bandwidth.}

\item{a}{Second smoothing parameter; \code{a > 0}, \code{a != 1}.}

\item{fx}{\code{F_X(x)}.}

\item{density}{\code{f_X(x)}.}

\item{r1, r2}{Kernel constants; default to the Gaussian \code{r1} and the \code{r2}
evaluated at this \code{a}.}
}
\value{
Named list with \code{variance}, \code{se}, \code{edfvar}, \code{gain}, \code{r1}, \code{r2}, \code{method}.
}
\description{
Theorem 2.3:
\deqn{\mathrm{Var}\[\tilde F_X(x)\] = \frac{F(1-F)}{n} - \frac{h}{n}\Big\[\frac{2(a^4+1)}{(a^2-1)^2}r_1 + r_2\Big\]f_X(x) + o(h/n),}{Var\[Ftilde(x)\] = F(1-F)/n - (h/n)\[2(a^4+1)/(a^2-1)^2 r1 + r2\] f(x) + o(h/n),}
with \code{r1} from (2.9) and \code{r2} from (2.10).
}
\details{
The ORDER does not change from the plain KDFE -- it cannot, since \code{Ftilde}
linearises to a fixed linear combination of two KDFEs -- but the CONSTANT
does, and the book's claim is that it is smaller. That is the content: bias
improved from \code{O(h^2)} to \code{O(h^4)} at no cost in variance order, and a gain
in its constant.

The bracket blows up as \code{a -> 1}: \code{(a^2-1)^2} is in the denominator and the
estimator is undefined there. Small \code{a} is fine; \code{a} near 1 is not, and the
function refuses it rather than returning a huge number that looks like a
result.
}
\examples{
Gekdfvar(n = 100, h = 0.2, a = 2, fx = 0.5, density = 0.4)
}
\references{
Fauzi and Maesono (2023), Theorem 2.3, Eqs. (2.9)-(2.10).
}
\keyword{internal}

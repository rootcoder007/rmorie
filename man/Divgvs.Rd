% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/divgvs.R
\name{Divgvs}
\alias{Divgvs}
\title{Fornell-Larcker discriminant validity}
\usage{
Divgvs(AVE, factor_correlations)
}
\arguments{
\item{AVE}{Average variance extracted, one entry per construct.}

\item{factor_correlations}{k x k matrix of inter-construct
correlations.}
}
\value{
List with \code{estimate} (minimum margin), \code{sqrt_ave},
\code{pass_factor}, \code{n_violations}, \code{discriminant},
\code{k}, \code{method}.
}
\description{
Formula: sqrt(AVE_i) > correlation(F_i, F_j)
}
\details{
A construct passes when the square root of its average variance
extracted exceeds its correlation with every other construct, i.e. it
shares more variance with its own indicators than with any other
factor.  The reported margin is the smallest sqrt(AVE_i) - |r_ij|
over all off-diagonal pairs: positive means the whole model passes.
}
\references{
Fornell & Larcker (1981), J. Marketing Research
18(1):39-50.
}
\examples{
AVE <- c(0.6, 0.7)
factor_correlations <- matrix(c(1, 0.4, 0.4, 1), 2, 2)
Divgvs(AVE, factor_correlations)
}

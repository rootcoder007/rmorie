% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geron_train_native.R
\name{morie_geron_reverse_mode_autodiff}
\alias{morie_geron_reverse_mode_autodiff}
\title{Reverse-mode automatic differentiation (Geron Ch 12, morie.fn grrad)}
\usage{
morie_geron_reverse_mode_autodiff(graph, loss_grad = 1, output = NULL)
}
\arguments{
\item{graph}{Named list of named partial-derivative lists.}

\item{loss_grad}{Seed gradient at the output node.}

\item{output}{Optional output node name; inferred when unique.}
}
\value{
List with \code{gradients}, \code{leaf_gradients}, \code{order}, \code{output}.
}
\description{
dL/dx = sum over children of (dL/dy)(dy/dx) in one reverse sweep;
fan-out ACCUMULATES. The graph is a named list node ->
named list parent -> local partial.
}
\examples{
g <- list(L = list(a = 2, b = -1), a = list(x = 3), b = list(x = 0.5, 
    y = 1))
morie_geron_reverse_mode_autodiff(g, 1)
}

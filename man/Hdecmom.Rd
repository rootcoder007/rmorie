% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fzmiq.R
\name{Hdecmom}
\alias{Hdecmom}
\alias{morie_fauzi_moment_ineq_ustat}
\title{Moment bound for the H-decomposition projections}
\usage{
Hdecmom(n, k, q, rhomom = 1, c = 1)

morie_fauzi_moment_ineq_ustat(n, k, q, rhomom = 1, c = 1)
}
\arguments{
\item{n}{Sample size.}

\item{k}{Projection order, \code{k >= 1}.}

\item{q}{Moment order, \code{q >= 2}.}

\item{rhomom}{\verb{E|rho_k|^q}.}

\item{c}{The unspecified constant of (3.12).}
}
\value{
Named list with \code{bound_over_c}, \code{bound}, \code{exponent}, \code{naive}, \code{method}.
}
\description{
Eq. (3.12): for \code{q >= 2}, if \verb{E|nu(X_1,...,X_r)|^q < Inf} there is a
constant \code{C} depending on \code{nu} and \code{F} but NOT on \code{n} with
\deqn{E|A_k|^q \le C n^{qk/2} E|\rho_k(X_{i_1},\dots,X_{i_k})|^q,}{E|A_k|^q <= C n^(qk/2) E|rho_k|^q,}
where \code{A_k} is the \code{k}-th projection of the H-decomposition (3.10)-(3.11).
}
\details{
The exponent is the useful part. A naive count would put \code{choose(n, k) ~ n^k}
terms in \code{A_k}, giving \code{n^(qk)}; the martingale property (3.9),
\verb{E[rho_k | X_1,...,X_\{k-1\}] = 0}, halves the exponent to \code{n^(qk/2)}. That
square root makes the higher projections negligible and lets Lemma 3.1 stop
at \code{k = 3} with an \code{o_L(n^-1/2)} remainder.

\code{C} is genuinely unspecified in the text -- a generic constant the book says
"may change its meaning at different places". It defaults to 1 and the
returned bound is labelled \code{bound_over_c} for that reason; treating it as an
absolute number would be a fabrication.
}
\examples{
Hdecmom(n = 100, k = 2, q = 2)
}
\references{
Fauzi and Maesono (2023), Eqs. (3.9)-(3.13).
}
\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bnd_native.R
\name{morie_bnd_manski}
\alias{morie_bnd_manski}
\title{Manski worst-case bounds on a partially observed mean}
\usage{
morie_bnd_manski(y, observed, support, treatment = NULL)
}
\arguments{
\item{y}{outcome; unobserved entries are ignored.}

\item{observed}{logical, whether the outcome was seen.}

\item{support}{the logical range \code{c(K0, K1)} -- the one assumption.}

\item{treatment}{optional binary treatment; switches to ATE bounds.}
}
\value{
list: lower, upper, width, p_observed, identified, or the
ate_* keys plus contains_zero, n, method.
}
\description{
With \code{P(obs)} the share of outcomes seen and \verb{[K0, K1]} the
outcome's logical support, \code{E[Y]} lies in
\verb{[E[Y|obs] P(obs) + K0 (1-P(obs)), E[Y|obs] P(obs) + K1 (1-P(obs))]}.
The width is EXACTLY \code{(K1 - K0)(1 - P(obs))}. Nothing about the
missing data is assumed -- that is the point, and the reason the
bounds are wide: they are what the data alone say before any
missing-at-random or selection assumption is spent.
}
\details{
With \code{treatment} supplied the same construction bounds each
potential-outcome mean and differences them. The no-assumption ATE
interval always has width exactly \code{K1 - K0} and therefore ALWAYS
contains zero: worst-case bounds never sign an effect on their
own, and bounds that do have smuggled in an assumption.
}
\examples{
y <- stats::runif(100)
morie_bnd_manski(y, y > 0.2, c(0, 1))$width
}
\references{
Manski (1989), \emph{J. Human Resources} 24:343-360;
Manski (1990), \emph{AER P&P} 80:319-323; Manski and Tamer (2002),
\emph{Econometrica} 70:519-546.
}

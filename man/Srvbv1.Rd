% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fzt41.R
\name{Srvbv1}
\alias{Srvbv1}
\alias{morie_fauzi_thm4_1_surv_bias_var}
\title{Bias and variance of the boundary-free survival and cumulative-survival estimators (Theorem 4.1)}
\usage{
Srvbv1(t, n, h, surv, cdf, cumsurv, b1, b2, dg, density, mu2 = 1, vw = NULL)

morie_fauzi_thm4_1_surv_bias_var(
  t,
  n,
  h,
  surv,
  cdf,
  cumsurv,
  b1,
  b2,
  dg,
  density,
  mu2 = 1,
  vw = NULL
)
}
\arguments{
\item{t}{Evaluation point.}

\item{n}{Sample size.}

\item{h}{Bandwidth.}

\item{surv}{\code{S_X(t)}.}

\item{cdf}{\code{F_X(t)}.}

\item{cumsurv}{\code{Sbar_X(t)}, the cumulative survival.}

\item{b1, b2}{The coefficients (4.14) and (4.15).}

\item{dg}{\verb{g'(g^-1(t))}.}

\item{density}{\code{f_X(t)}.}

\item{mu2}{\verb{int y^2 K(y) dy}.}

\item{vw}{\verb{int V(y)W(y)dy}; defaults to the Gaussian \code{1/sqrt(pi)}.}

}
\value{
Named list with \code{biassurv}, \code{varsurv}, \code{biascum}, \code{varcum}, \code{vw}, \code{h}, \code{n}, \code{method}.
}
\description{
Theorem 4.1, Eqs. (4.10)-(4.16):
\deqn{\mathrm{Bias}\[\tilde S_X(t)\] = -\tfrac{h^2}{2}b_1(t)\mu_2(K) + o(h^2),}{Bias\[Stilde(t)\] = -(h^2/2) b1(t) mu2(K) + o(h^2),}
\deqn{\mathrm{Var}\[\tilde S_X(t)\] = \tfrac{1}{n}\tilde S_X(t)F_X(t) - \tfrac{h}{n}g'(g^{-1}(t))f_X(t)\int V(y)W(y)dy + o(h/n),}{Var\[Stilde(t)\] = (1/n) Stilde(t) F(t) - (h/n) g'(g^-1(t)) f(t) int V(y)W(y)dy + o(h/n),}
\deqn{\mathrm{Bias}\[S_{X,1}(t)\] = \tfrac{h^2}{2}b_2(t)\mu_2(K) + o(h^2),\quad \mathrm{Var}\[S_{X,1}(t)\] = \tfrac{1}{n}\[2\bar S_X(t) - S_X^2(t)\] + o(h/n),}{Bias\[S_X1(t)\] = (h^2/2) b2(t) mu2(K), Var\[S_X1(t)\] = (1/n)\[2 Sbar(t) - S(t)^2\],}
with \verb{b1(t) = g''(g^-1(t)) f(t) + \[g'(g^-1(t))\]^2 f'(t)} from (4.14) and
\code{b2} from (4.15).
}
\details{
Note the SIGNS: the survival estimator's bias carries a minus and the
cumulative one a plus, because \code{S = 1 - F} flips the leading term while the
cumulative survival integrates it back. They are not interchangeable, so
both come back rather than one behind a flag.

For the Gaussian kernel \verb{int V(y)W(y)dy = int (1-W)W dy = 1/sqrt(pi)}, used
in closed form.
}
\examples{
Srvbv1(t = 1, n = 100, h = 0.1, surv = 0.4, cdf = 0.6, cumsurv = 0.5,
       b1 = 0.2, b2 = 0.3, dg = 1, density = 0.35)
}
\references{
Fauzi and Maesono (2023), Theorem 4.1, Eqs. (4.10)-(4.15).
}
\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/toronto_neighbourhoods.R
\name{morie_tps_aggregate_158_to_140}
\alias{morie_tps_aggregate_158_to_140}
\title{Aggregate per-158 counts to per-140 counts (EXACT for pure cake-cuts)}
\usage{
morie_tps_aggregate_158_to_140(
  df,
  hood_158_col = "HOOD_158",
  count_cols = NULL,
  crosswalk = NULL
)
}
\arguments{
\item{df}{A \code{data.frame} keyed on a 158-hood column.}

\item{hood_158_col}{Name of the 158-hood column. Default
\code{"HOOD_158"}.}

\item{count_cols}{Character vector of numeric count columns. By
default every numeric column except \code{hood_158_col}.}

\item{crosswalk}{Optional pre-loaded crosswalk.}
}
\value{
A \code{data.frame} with one row per 140-hood, columns
\code{hood_140}, \code{name_140}, and the summed \code{count_cols}.
}
\description{
Cake-cutting in the REVERSE direction. Given a \code{data.frame} of per-
current-hood counts (one row per \code{hood_158}, one or more numeric
count columns), sums across the 158-children of each 140 weighted
by \code{pct_158_in_140 / 100}. For 1:1 hoods the count passes through.
For splits the children's counts are summed exactly (each child has
\code{pct_158_in_140 == 100} for clean cake-cuts).
}
\details{
Unlike \code{\link[=morie_tps_disaggregate_140_to_158]{morie_tps_disaggregate_140_to_158()}}, this requires NO
uniform-density assumption when the source is a clean cake-cut --
the partition is exhaustive and disjoint by construction. The only
lossy case is the \code{split+merge} edge (one Willowdale East sliver
in the bundled OT data); the function handles it via the
\code{pct_158_in_140} weights regardless.
}
\examples{
df <- data.frame(HOOD_158 = c("167", "168", "001"),
                 incidents = c(40, 60, 42))
morie_tps_aggregate_158_to_140(df)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/esl_native2.R
\name{morie_esl_score_match}
\alias{morie_esl_score_match}
\title{Score-matching objective}
\usage{
morie_esl_score_match(score, X, grad_score = NULL, eps = 1e-05)
}
\arguments{
\item{score}{Function returning the model score, \verb{(n, d)} to \verb{(n, d)}.}

\item{X}{Samples, n by d.}

\item{grad_score}{Optional function returning the diagonal of the score
Jacobian; a central difference is used when absent.}

\item{eps}{Finite-difference step.}
}
\value{
List with \code{objective}, \code{trace_term}, \code{norm_term}, \code{per_point}.
}
\description{
Fitting an energy model needs \verb{log Z}, which is usually intractable. Score
matching sidesteps it by matching the gradient of the log density with
respect to x, in which \code{Z} -- constant in x -- has already cancelled.
Hyvarinen's result is that the objective then depends only on the model, so
the DATA's own score is never needed. That is the entire trick, and it is
what lets an unnormalised model be fitted at all.
}
\details{
The Jacobian trace costs \code{d} extra evaluations per point when \code{grad_score}
is not supplied, which is what makes plain score matching expensive in high
dimension and motivated the sliced and denoising variants.
}
\examples{
set.seed(1)
X <- matrix(rnorm(400, 2), ncol = 1)
J <- sapply(c(0, 1, 2, 3), function(m)
  morie_esl_score_match(function(Z) -(Z - m), X)$objective)
which.min(J)
}
\references{
Hyvarinen, A. (2005). Estimation of non-normalized statistical
models by score matching. JMLR 6, 695-709.
}

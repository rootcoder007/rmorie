% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extenders_rdd.R
\name{morie_rdd_local_randinf}
\alias{morie_rdd_local_randinf}
\title{Local-randomisation RDD inference via \pkg{rdlocrand}}
\usage{
morie_rdd_local_randinf(Y, R, wl, wr, ...)
}
\arguments{
\item{Y}{Numeric outcome vector.}

\item{R}{Numeric running / forcing variable.}

\item{wl}{Numeric scalar; left edge of the randomisation window.}

\item{wr}{Numeric scalar; right edge of the randomisation window.}

\item{...}{Further arguments forwarded to
\code{rdlocrand::rdrandinf} (e.g. \code{statistic}, \code{p},
\code{nulltau}, \code{reps}, \code{seed}).}
}
\value{
A list with \code{$method = "rdlocrand::rdrandinf"} and
\code{$raw} (an \code{rdrandinf} object with the
randomisation-inference p-values and the observed statistic).
}
\description{
Thin extender over \code{rdlocrand::rdrandinf} for finite-sample
randomisation-inference around the cutoff window
(Cattaneo, Frandsen & Titiunik, 2015; Cattaneo, Titiunik & Vazquez-Bare,
2016).
}
\examples{
\donttest{
  if (requireNamespace("rdlocrand", quietly = TRUE)) {
    set.seed(1)
    R <- runif(200, -1, 1)
    Y <- 0.5 * R + (R >= 0) * 0.3 + rnorm(200, sd = 0.5)
    morie_rdd_local_randinf(Y, R, wl = -0.1, wr = 0.1)
  }
}
}

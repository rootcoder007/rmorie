% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survvae_native.R
\name{morie_survvae_exact_loglik}
\alias{morie_survvae_exact_loglik}
\title{The true mixture log-likelihood the bound sits underneath}
\usage{
morie_survvae_exact_loglik(
  X,
  y_lower,
  events,
  W,
  bias,
  shapes,
  scales,
  primitive = "weibull",
  alpha = 1
)
}
\arguments{
\item{X}{List of covariate vectors, length \code{n}.}

\item{y_lower}{Numeric vector of observation times, length
\code{n}.}

\item{events}{Event indicator, length \code{n} (truthy =
uncensored).}

\item{W}{List of \code{K} weight vectors of length \code{d}.}

\item{bias}{Numeric vector of length \code{K}.}

\item{shapes}{Numeric vector of \code{K} positive shape
parameters.}

\item{scales}{Numeric vector of \code{K} positive scale
parameters.}

\item{primitive}{One of \code{"weibull"} or \code{"lognormal"}.}

\item{alpha}{Discount on the censored term, in \code{[0, 1]}.}
}
\value{
Named list with \code{loglik}, \code{uncensored},
\code{censored}.
}
\description{
Computes \eqn{\ln \sum_k g_k \exp(\cdot)} rather than the
Jensen-relaxed sum. The anchor verifies this is at least as large
as the ELBO from \code{morie_survvae_elbo} on every fit.
}
\examples{
set.seed(1)
X <- lapply(1:20, function(i) rnorm(3))
tt <- rexp(20, 0.1)
ev <- rbinom(20, 1, 0.7)
W <- matrix(rnorm(9, 0, 0.3), 3, 3)
bias <- rep(0, 3)
r <- morie_survvae_exact_loglik(X, tt, ev, W, bias, shapes = c(1, 1.5, 2),
                                scales = c(5, 10, 15))
str(r, max.level = 1)
}
\references{
Nagpal et al. (2021), Sec. III-C.
}
\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stlAn_native.R
\name{morie_stl_decompose}
\alias{morie_stl_decompose}
\title{STL seasonal-trend decomposition by loess}
\usage{
morie_stl_decompose(
  x,
  period,
  s_window = 7L,
  t_window = NULL,
  l_window = NULL,
  s_degree = 1L,
  t_degree = 1L,
  l_degree = 1L,
  inner = 2L,
  outer = 0L
)
}
\arguments{
\item{x}{Numeric series.}

\item{period}{Number of observations per cycle, at least 2; the
series needs at least two full cycles.}

\item{s_window}{Seasonal loess span \eqn{n_s}, forced odd.}

\item{t_window, l_window}{Trend and low-pass spans; \code{NULL} uses
the Sec. 3.4 defaults.}

\item{s_degree, t_degree, l_degree}{Local polynomial degrees.}

\item{inner}{Inner-loop iterations.}

\item{outer}{Robustness iterations; 0 gives the non-robust fit.
Both routes the paper defines are available.}
}
\value{
A list with \code{seasonal}, \code{trend},
\code{remainder}, \code{weights}, \code{s_window},
\code{t_window}, \code{l_window}.
}
\description{
The decomposition of Cleveland et al. (1990): an inner loop that
alternates cycle-subseries smoothing and trend smoothing, and an
optional outer loop of bisquare robustness weights that makes the
fit resistant to outliers.
}
\references{
Cleveland, R. B., Cleveland, W. S., McRae, J. E. and
Terpenning, I. (1990). STL: a seasonal-trend decomposition
procedure based on loess. Journal of Official Statistics, 6(1),
3-73.
}
\examples{
set.seed(1)
x <- sin(2 * pi * (1:60) / 12) + 0.02 * (1:60) + rnorm(60) * 0.1
morie_stl_decompose(x, period = 12)
}

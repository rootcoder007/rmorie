% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extenders_stats.R
\name{morie_performance_r2}
\alias{morie_performance_r2}
\title{R-squared family via \pkg{performance}}
\usage{
morie_performance_r2(model, ...)
}
\arguments{
\item{model}{A fitted model object supported by \pkg{performance}.}

\item{...}{Further arguments forwarded to \code{performance::r2}
(e.g. \code{tolerance}, \code{ci}, \code{verbose}).}
}
\value{
A list with \code{$method = "performance::r2"} and
\code{$raw} (the upstream R-squared object).
}
\description{
Thin extender over \code{performance::r2} returning the
appropriate R-squared (Nakagawa, McFadden, Tjur, ...) for a
supported fitted model.
}
\examples{
if (requireNamespace("performance", quietly = TRUE)) {
  set.seed(1)
  df <- data.frame(y = rnorm(50), x = rnorm(50))
  morie_performance_r2(stats::lm(y ~ x, df))
}
}

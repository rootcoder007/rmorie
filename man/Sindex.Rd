% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hrzsim.R
\name{Sindex}
\alias{Sindex}
\alias{morie_horowitz_single_index_model}
\title{Semiparametric WNLS estimator of a single-index model}
\usage{
Sindex(
  x,
  y,
  h = NULL,
  weights = NULL,
  trim = 0.01,
  niter = 12L,
  delta = 1,
  b0 = NULL
)

morie_horowitz_single_index_model(
  x,
  y,
  h = NULL,
  weights = NULL,
  trim = 0.01,
  niter = 12L,
  delta = 1,
  b0 = NULL
)
}
\arguments{
\item{x}{Numeric matrix of covariates, n by d, with no constant column.}

\item{y}{Numeric outcome vector.}

\item{h}{Numeric bandwidth for (2.23)-(2.24); default n^(-1/5).}

\item{weights}{Optional numeric W(X_i) in (2.22); default ones.}

\item{trim}{Numeric trimming constant eta defining A_x.}

\item{niter}{Integer sweeps of the coordinate search.}

\item{delta}{Numeric initial step of the coordinate search.}

\item{b0}{Optional numeric starting value for betatilde.}
}
\value{
Named list with estimate, se, objective, ghat, index,
bandwidth, ntrim, n, method.
}
\description{
Horowitz (2009), Semiparametric and Nonparametric Methods in
Econometrics, Section 2.5.1, equations (2.22) to (2.25) and Theorem
2.2, equation (2.26) (pages 20-21); the estimator is Ichimura's
(1993).  G is replaced by the leave-one-out, trimmed, weighted
kernel estimator (2.23)-(2.24) and betatilde solves (2.25), the
minimisation being over betatilde only so that beta_1 = 1.
}
\details{
Minimisation uses the shelf's fixed-schedule coordinate search: a
set number of sweeps, a fixed step ladder, no tolerance-based early
exit and no random restart.
}
\examples{
n <- 150
x <- cbind(seq(-2, 2, length.out = n), cos(seq_len(n) * 0.9))
z <- as.numeric(x \%*\% c(1, 0.8))
Sindex(x, z / (1 + abs(z)), h = 0.35)$estimate
}
\keyword{internal}

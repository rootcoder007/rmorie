% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ssintc.R
\name{Ssintc}
\alias{Ssintc}
\title{Nonparametric survival when each event time is only bracketed}
\usage{
Ssintc(L, R, event = NULL, n_iter = 200)
}
\arguments{
\item{L}{Left endpoints; 0 for left-censored.}

\item{R}{Right endpoints; Inf for right-censored.}

\item{event}{Kept for interface compatibility.}

\item{n_iter}{EM sweeps.}
}
\value{
List with \code{estimate}, \code{p}, \code{surv}, \code{q},
\code{r}, \code{n}, \code{m}.
}
\description{
Interval censoring only says the event happened somewhere in
\code{(L, R]}. The likelihood cannot distinguish points inside an
interval, so all mass sits on the innermost intervals and the NPMLE is
a discrete distribution on those. The self-consistency equation is an
EM run for a fixed number of sweeps, so the arms cannot stop apart.
}
\details{
Formula: with \code{alpha_ij = 1} when region j lies inside
observation i, iterate
\code{p_j = (1/n) sum_i alpha_ij p_j / sum_k alpha_ik p_k}.
}
\references{
Turnbull, B. W. (1976). JRSS B 38:290-295, equation (10).
}
\examples{
V <- c(1, 2, 3, 4, 5, 6, 7, 8)
Ssintc(V, V)
}

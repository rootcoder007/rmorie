% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spnest.R
\name{spnest}
\alias{spnest}
\title{Nested semivariogram: the linear model of regionalization.}
\usage{
spnest(h, components)
}
\arguments{
\item{h}{Numeric vector of non-negative lag distances.}

\item{components}{List of components, each a list with \code{model}, \code{sill}
and \code{range}. Use \code{model = "nugget"} for a pure white-noise component.}
}
\value{
Named list: gamma, components, total_sill.
}
\description{
With Z(s) = mu + sum_j a_j U_j(s) and the U_j orthogonal,
gamma_z(h) = sum_j a_j^2 gamma_j(h). Validity relies on the components
being orthogonal, which the book notes is hard to justify except when
a white-noise measurement-error term is nested with one other model to
create a nugget.
}
\examples{
spnest(h = c(0, 0.5, 2), components = list(
  list(model = "nugget", sill = 0.2),
  list(model = "spherical", sill = 1, range = 1.5)))
}
\references{
Schabenberger & Gotway (2005), Sec 4.3.6, eqs (4.22)-(4.23), p. 150.
}

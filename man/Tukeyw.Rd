% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tukeyw.R
\name{Tukeyw}
\alias{Tukeyw}
\title{Biweight M-estimate of location, with redescending weights}
\usage{
Tukeyw(y, c = 4.685, n_iter = 20)
}
\arguments{
\item{y}{Sample.}

\item{c}{Tuning constant; 4.685 gives 95 percent Gaussian efficiency.}

\item{n_iter}{Reweighting sweeps.}
}
\value{
List with \code{estimate}, \code{scale}, \code{weights}, \code{n}.
}
\description{
Huber weights taper but never vanish, so a point far enough out still
moves the estimate. The biweight redescends to exactly zero past c.
The price is a non-convex objective, so the start matters -- the
median and a MAD scale are used.
}
\details{
Determinism: fixed number of reweighting sweeps, no tolerance.

Formula: \code{w(r) = (1 - (r/c)^2)^2} for \code{|r| <= c}, else 0,
with \code{r = (y - mu)/s} and s the consistency-scaled MAD.
}
\references{
Beaton, A. E. & Tukey, J. W. (1974). Technometrics
16:147-185.
}

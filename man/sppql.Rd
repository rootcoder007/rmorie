% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sppql.R
\name{sppql}
\alias{sppql}
\title{Fit a spatial GLMM by pseudo-likelihood}
\usage{
sppql(
  z,
  X,
  Sigma_S,
  family = "poisson",
  link_kind = NULL,
  sigma2 = 1,
  R = NULL,
  max_iter = 100L,
  tol = 1e-08,
  check_score = TRUE
)
}
\arguments{
\item{z}{Observed responses.}

\item{X}{Design matrix.}

\item{Sigma_S}{Covariance of the latent spatial field.}

\item{family}{One of "poisson", "binomial", "gaussian".}

\item{link_kind}{Link; defaults to the canonical link.}

\item{sigma2}{Dispersion parameter.}

\item{R}{Working correlation of the conditional part; default identity.}

\item{max_iter, tol}{Outer relinearisation controls.}

\item{check_score}{Evaluate the PQL score equations at the solution.}
}
\value{
A list with \code{beta}, \code{se_beta}, \code{cov_beta}, \code{S}, \code{mu}, \code{sigma2},
\code{reml}, \code{converged}, and when checked \code{score_beta_max}, \code{score_S_max}
and \code{pql_pl_equivalent}.
}
\description{
The problem is linearised with a first-order Taylor expansion of the link,
giving the pseudo-data of eq (6.78), whose marginal moments make a linear
mixed model. Estimates then follow from (6.80)-(6.82) with the latent
field predicted by (6.81). Because the pseudo-data covariance depends on
the coefficients the whole thing is relinearised and repeated -- the
six-step algorithm the text sets out, which this follows exactly.
}
\details{
Wolfinger and O'Connell (1993) call this pseudo-likelihood; Breslow and
Clayton (1993) reach the same place by a Laplace approximation and call it
penalized quasi-likelihood. The text settles it: the two objectives
"differ ... only by a constant amount. The two approaches will thus yield
the same estimates." With \code{check_score = TRUE} the PQL score equations are
evaluated at the fitted values, so the equivalence is checked on every fit
rather than assumed.

There is a choice about where the spatial dependence lives. A marginal
model puts it in \code{R} and sets the latent field to zero; a conditional
model puts it in \code{Sigma_S} and takes \code{R} to be the identity.
}
\references{
Schabenberger Ch 6, Sec 6.3.5, eqs (6.78)-(6.85)
}

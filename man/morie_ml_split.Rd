% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ml_pipeline.R
\name{morie_ml_split}
\alias{morie_ml_split}
\title{Partition data into train / test / validation sets}
\usage{
morie_ml_split(
  data,
  prop = c(train = 0.6, test = 0.2, validation = 0.2),
  strata = NULL,
  seed = 42L
)
}
\arguments{
\item{data}{A data.frame.}

\item{prop}{Named numeric of partition proportions; names are matched
case-insensitively and by partial match to "train"/"test"/
"validation" (ML1.1b). Must sum to 1.}

\item{strata}{Optional column name to stratify the split on.}

\item{seed}{RNG seed for reproducibility.}
}
\value{
An object of class \code{morie_ml_split}: a list with \code{train},
\code{test}, and (if requested) \code{validation} data.frames plus a \code{roles}
vector giving each row's assignment.
}
\description{
Splits a single tabular data object into distinctly-labelled
training, test, and (optionally) validation partitions, with optional
stratification on a column.
}
\examples{
sp <- morie_ml_split(mtcars, c(train = 0.6, test = 0.4), seed = 1)
nrow(sp$train) + nrow(sp$test)
}

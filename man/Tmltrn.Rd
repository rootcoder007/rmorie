% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tmltrn.R
\name{Tmltrn}
\alias{Tmltrn}
\title{TMLE transporting a treatment effect to a target population}
\usage{
Tmltrn(y, D, X, S)
}
\arguments{
\item{y}{Outcome; entries with \code{S = 0} are ignored.}

\item{D}{Binary treatment; entries with \code{S = 0} are ignored.}

\item{X}{Covariates, in both populations.}

\item{S}{1 for a source (trial) row, 0 for a target-population row.}
}
\value{
List with \code{estimate}, \code{se}, \code{eps},
\code{n_source}, \code{n_target}, \code{n}.
}
\description{
Transport is an extrapolation in the covariate distribution, not in
the outcome model: the effect is identified in the target population
only under S-admissibility.  Relative to a subgroup analysis what
changes is the weight -- the source rows are reweighted by the
SAMPLING ODDS,
\code{H = I(S = 1)/P(S = 0) * (1 - p(X))/p(X) *
\[D/g(X) - (1 - D)/(1 - g(X))\]} with \code{p(X) = P(S = 1 | X)}.
Target-population outcomes are never used; only its covariates enter.
}
\examples{
set.seed(1)
r <- Tmltrn(y = rnorm(10), D = rbinom(10, 1, 0.5), X = rnorm(10), S = rnorm(10)); TRUE
}
\references{
Rudolph, K. E. & van der Laan, M. J. (2017). JRSS B
79(5):1509-1525.
}

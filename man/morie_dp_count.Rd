% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dp_native.R
\name{morie_dp_count}
\alias{morie_dp_count}
\title{Differentially private count}
\usage{
morie_dp_count(D, epsilon = 1, predicate = NULL, seed = NULL, nonneg = TRUE)
}
\arguments{
\item{D}{data: a logical vector, a 0/1 vector, or anything with a
length (in which case the count is the number of rows).}

\item{epsilon}{privacy budget.}

\item{predicate}{optional function applied to each element; the count
is the number of TRUE results.}

\item{seed}{optional integer seed.}

\item{nonneg}{clamp a negative release to zero. The clamp is a
post-processing step and costs no privacy, but it does bias the
release upward near zero, which is recorded in \code{clamped}.}
}
\value{
list with \code{release}, \code{raw} (pre-clamp),
\code{true_count}, \code{noise_scale}, \code{clamped}.
}
\description{
A count has sensitivity 1 no matter how large the dataset is -- one
person changes it by one -- so the noise is O(1) and the RELATIVE
error falls like 1/n. Counting is the query differential privacy is
kindest to.
}
\examples{
morie_dp_count(rep(TRUE, 1000), epsilon = 1, seed = 1)$noise_scale
}
\references{
Dwork, C. and Roth, A. (2014). \emph{FnTTCS}, 9(3-4),
Sec. 3.3.
}

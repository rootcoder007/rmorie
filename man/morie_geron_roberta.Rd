% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geron_ml_native.R
\name{morie_geron_roberta}
\alias{morie_geron_roberta}
\title{RoBERTa pretraining with dynamic masking (Geron Ch 15, morie.fn hmbrob)}
\usage{
morie_geron_roberta(
  X,
  n_layers = 2,
  n_heads = 2,
  d_model = 8,
  vocab_size = NULL,
  d_ff = NULL,
  mask_prob = 0.15,
  epochs = 4,
  seed = 0
)
}
\arguments{
\item{X}{0-based token ids.}

\item{n_layers, n_heads, d_model, d_ff}{Geometry.}

\item{vocab_size}{Optional.}

\item{mask_prob}{Fraction in (0, 1).}

\item{epochs}{Dynamic-masking passes.}

\item{seed}{LCG seed.}
}
\value{
List with \code{hidden}, \code{mlm_loss}, \code{epoch_losses}, \code{masks},
\code{dynamic_masking}, \code{has_nsp_head}, \code{n_params}, \code{n_params_with_nsp},
\code{n_masked_per_sequence}.
}
\description{
Same encoder stack as BERT with two deliberate differences: no NSP head
(so \code{n_params} is d_model*2 smaller than \code{n_params_with_nsp}), and a
fresh mask drawn every epoch instead of frozen in preprocessing.
\code{masks} records sequence 0's positions per epoch, 0-based.
}
\examples{
V <- c(1, 2, 3, 4, 5, 6, 7, 8)
morie_geron_roberta(V)
}

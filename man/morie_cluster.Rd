% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster_model.R
\name{morie_cluster}
\alias{morie_cluster}
\title{k-means clustering with a full model-object contract}
\usage{
morie_cluster(
  x,
  k = 2L,
  scale = FALSE,
  na_action = c("omit", "fail"),
  case_labels = NULL,
  nofit = FALSE,
  seed = 42L
)
}
\arguments{
\item{x}{A numeric matrix or data.frame (numeric columns used).}

\item{k}{Number of clusters.}

\item{scale}{Standardise columns before clustering. Applying \code{scale}
removes the dominance of large-variance columns; not applying it lets
them dominate the distance metric -- the examples contrast both.}

\item{na_action}{"omit" (drop incomplete rows) or "fail" (error).}

\item{case_labels}{Optional labels for cases when \code{x} has no row names.}

\item{nofit}{If TRUE, return an unfitted specification.}

\item{seed}{RNG seed.}
}
\value{
A \code{morie_cluster} object (or \code{morie_cluster_spec} if
\code{nofit = TRUE}) whose cluster labels are ordered by decreasing size.
}
\description{
k-means clustering with a full model-object contract
}
\examples{
# with vs without scaling changes which columns drive the clusters
morie_cluster(iris[1:4], k = 3)
morie_cluster(iris[1:4], k = 3, scale = TRUE)
}

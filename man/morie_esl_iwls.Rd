% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/esl_native.R
\name{morie_esl_iwls}
\alias{morie_esl_iwls}
\title{Iteratively reweighted least squares for a GLM}
\usage{
morie_esl_iwls(
  X,
  y,
  beta0 = NULL,
  family = "binomial",
  max_iter = 50L,
  tol = 1e-08,
  add_intercept = TRUE
)
}
\arguments{
\item{X}{Design matrix, n by p, without an intercept column.}

\item{y}{Response: 0/1 for binomial, non-negative counts for Poisson.}

\item{beta0}{Starting coefficients; defaults to zeros.}

\item{family}{"binomial" or "poisson".}

\item{max_iter}{Maximum IRLS iterations.}

\item{tol}{Convergence tolerance on the coefficient change.}

\item{add_intercept}{Prepend a column of ones.}
}
\value{
List with \code{beta}, \code{se}, \code{z}, \code{p_value}, \code{fitted}, \code{loglik},
\code{deviance}, \code{n_iter}, \code{converged}, \code{separated}.
}
\description{
Fisher scoring for the binomial and Poisson families with their canonical
links. Separation is detected from the fitted probabilities rather than
from a coefficient threshold: under separation the probabilities are pinned
at 0/1 long before any coefficient grows large enough to trip a cut-off.
}
\examples{
set.seed(1)
X <- matrix(rnorm(2000), ncol = 2)
y <- rbinom(1000, 1, plogis(-0.5 + 1.5 * X[, 1] - X[, 2]))
round(morie_esl_iwls(X, y)$beta, 1)
}
\references{
Hastie, T., et al. (2009). ESL (2nd ed.), Sec 4.4.1. Springer.
}

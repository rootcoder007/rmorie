% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aaa_sp_fill.R
\name{SpecDec}
\alias{SpecDec}
\title{Alpha = sum_x min(p,q) = 1 - TV(p,q); E[tokens] =}
\usage{
SpecDec(draft, target, gamma = 4L)
}
\arguments{
\item{draft}{Passed to \code{.morie_spx_chkv}.}

\item{target}{Passed to \code{.morie_spx_chkv}.}

\item{gamma}{Coerced to integer by the body, with \code{as.integer}. Defaults to \code{4L}.}
}
\value{
A list with \code{alpha}, \code{tv_distance}, \code{expected_tokens},
\code{gamma}, \code{max_tokens}, \code{deterministic_expectation_not_a_sampled_run},
\code{n}, \code{method}.
}
\description{
(1 - alpha^(gamma+1))/(1 - alpha), capped at gamma+1 (a rejected
token is resampled from the residual and still counts).  Leviathan,
Kalman & Matias (2023).  NOT in Schabenberger & Gotway.
Deterministic: the expectation, not a sampled run.
}
\examples{
SpecDec(draft = c(0.5, 0.3, 0.2), target = c(0.4, 0.4, 0.2), gamma = 4L)
}
\keyword{internal}

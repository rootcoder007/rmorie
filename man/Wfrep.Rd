% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Wfrep.R
\name{Wfrep}
\alias{Wfrep}
\title{Weighted frequency distribution}
\usage{
Wfrep(y, weights = NULL, cells = NULL)
}
\arguments{
\item{y}{Category labels, compared as character.}

\item{weights}{Optional design weights; equal weights if NULL.}

\item{cells}{Optional vector of levels in reporting order; defaults to
the sorted distinct labels of \code{y}.}
}
\value{
List with \code{estimate} (largest cell total), \code{levels},
\code{freq}, \code{prop}, \code{sumw}, \code{n}, \code{k},
\code{method}.
}
\description{
Each sampled unit contributes its design weight to the cell it falls
in, so a cell total estimates the population count.  With unit weights
the totals are exactly the ordinary contingency counts.
}
\details{
Formula: f_k = sum over i in cell k of w_i; p_k = f_k / sum(f).
}
\examples{
Wfrep(c(1, 1, 2, 3, 3, 3))
}
\references{
Lohr, S. L. (2010). Sampling: Design and Analysis, 2nd ed.
Brooks/Cole, section 7.2.
}

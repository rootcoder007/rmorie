% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spstvg.R
\name{spstvg}
\alias{spstvg}
\title{Empirical spatio-temporal semivariogram}
\usage{
spstvg(
  coords,
  times,
  z,
  n_space_bins = 10L,
  n_time_bins = 5L,
  max_dist = NULL,
  max_time = NULL,
  at_time = NULL,
  model_fn = NULL
)
}
\arguments{
\item{coords}{Matrix of coordinates, one row per observation.}

\item{times}{Numeric vector of observation times.}

\item{z}{Numeric vector of observed values.}

\item{n_space_bins, n_time_bins}{Numbers of spatial and temporal lag
classes -- the two tolerances.}

\item{max_dist, max_time}{Largest lags retained; default to half the
observed maxima.}

\item{at_time}{Optional time at which to also compute eq (9.19).}

\item{model_fn}{Optional \eqn{\gamma(h, k; \theta)}. When supplied the
weighted least squares criterion of Sec. 9.4 is evaluated against the
empirical surface.}


}
\value{
A list with \code{st_variogram}, \code{counts}, \code{space_lags}, \code{time_lags},
\code{n_pairs}, \code{n_cells_estimated}, and optionally \code{conditional},
\code{wls_objective} and \code{fitted}.
}
\description{
For a stationary spatio-temporal process the semivariogram relates to the
covariance function exactly as in the purely spatial case:
\eqn{\gamma(h,k) = C(0,0) - C(h,k)}. The estimator is the spatio-temporal
Matheron, eq (9.18),
\eqn{\hat\gamma(h,k) = \frac{1}{2|N(h,k)|} \sum_{N(h,k)}
\{Z(s_i,t_i) - Z(s_j,t_j)\}^2}, where \eqn{N(h,k)} is the set of pairs
within spatial distance \eqn{h} AND time lag \eqn{k} of each other.
}
\details{
Two things the text insists on. The lag tolerances in space and time must
be chosen separately, because data are generally irregular in both and a
single tolerance cannot give enough pairs at each spatio-temporal lag;
\code{n_space_bins} and \code{n_time_bins} are independent for that reason, and the
pair counts are returned so thin cells are visible rather than inferred.

And (9.18) is a joint estimator, not a conditional one. Supplying \code{at_time}
additionally returns the conditional spatial semivariogram of eq (9.19),
which is what a two-stage analysis uses. They are different quantities;
Sec. 9.1 sets out why the two-stage route is weaker.

Cells containing no pairs are returned as \code{NA} with a count of zero, never
filled. An unestimated semivariogram and a zero one are different claims.
}
\examples{
spstvg(coords = c(1, 2, 3, 4, 5, 6, 7, 8), times = c(1, 2, 3, 4, 5, 6, 7, 8),
  z = c(1, 2, 3, 4, 5, 6, 7, 8))
}
\references{
Schabenberger Ch 9, Sec 9.4, eqs (9.18)-(9.19)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geron_train_native.R
\name{morie_geron_glorot_xavier_init}
\alias{morie_geron_glorot_xavier_init}
\title{Glorot (Xavier) initialization (Geron Ch 11, morie.fn grxvi)}
\usage{
morie_geron_glorot_xavier_init(
  fan_in,
  fan_out,
  distribution = "normal",
  seed = 42
)
}
\arguments{
\item{fan_in, fan_out}{Positive widths.}

\item{distribution}{"normal" or "uniform".}

\item{seed}{LCG seed.}
}
\value{
List with \code{weights}, \code{target_variance}, \code{achieved_variance},
\code{scale}, \code{distribution}.
}
\description{
Var(W) = 2/(fan_in + fan_out); the uniform limit is
sqrt(6/(in+out)). The normal branch consumes an even LCG budget,
builds the cos half and the sin half SEPARATELY, and concatenates
cos-block then sin-block before truncating -- NOT the interleaved
pair order used by grhei / grgrp. \code{achieved_variance} is the
ddof = 0 variance. The weight matrix is reshaped ROW-MAJOR into
(fan_in, fan_out).
}
\examples{
morie_geron_glorot_xavier_init(fan_in = 5L, fan_out = 5L)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causrddf_native.R
\name{morie_causrddf}
\alias{morie_causrddf}
\title{Fuzzy regression discontinuity (Wald ratio)}
\usage{
morie_causrddf(
  x,
  y,
  treat,
  cutoff = 0,
  h = NULL,
  h_treat = NULL,
  kernel = "triangular"
)
}
\arguments{
\item{x}{Running variable.}

\item{y}{Outcome.}

\item{treat}{Treatment received (0/1 or a probability).}

\item{cutoff}{Threshold, default 0.}

\item{h}{Bandwidth for the outcome fit; \code{NULL} uses
\code{\link{morie_causrddh}} on the outcome.}

\item{h_treat}{Bandwidth for the first stage; \code{NULL} uses
\code{\link{morie_causrddh}} on the treatment.}

\item{kernel}{\code{"triangular"} (default) or \code{"uniform"}.}
}
\value{
A list with \code{estimate}, \code{se}, \code{ci},
\code{jump_outcome}, \code{jump_treatment}, \code{se_outcome},
\code{se_treatment}, \code{h_outcome}, \code{h_treatment},
\code{kernel}, \code{sharp_outcome}, \code{sharp_treatment},
\code{se_note}, \code{method}.
}
\description{
Estimates \eqn{\tau = \tau_y / \tau_w}, the ratio of the outcome
discontinuity to the treatment discontinuity at the cutoff, each
from a sharp local linear fit (Hahn, Todd and van der Klaauw 2001,
Theorem 3).  When treatment switches deterministically at the
cutoff the denominator is 1 and the estimate reduces to the sharp
one of \code{\link{morie_causrdd}}.
}
\examples{
set.seed(1)
x <- runif(200, -1, 1)
y <- 0.5 * x + (x >= 0) * 0.3 + rnorm(200) * 0.1
treat <- as.integer((x >= 0) & (runif(200) < 0.85))
morie_causrddf(x, y, treat)
}
\references{
Imbens, G. and Lemieux, T. (2008). Regression
discontinuity designs: a guide to practice. Journal of
Econometrics, 142(2), 615-635.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ingest_a2aj.R
\name{morie_ingest_a2aj_search}
\alias{morie_ingest_a2aj_search}
\title{Search A2AJ case law or legislation}
\usage{
morie_ingest_a2aj_search(
  query,
  search_type = c("full_text", "name"),
  doc_type = c("cases", "laws"),
  size = 10L,
  search_language = c("en", "fr"),
  sort_results = c("default", "newest_first", "oldest_first"),
  dataset = NULL,
  start_date = NULL,
  end_date = NULL,
  timeout = .MORIE_A2AJ_DEFAULT_TIMEOUT,
  user_agent = .MORIE_A2AJ_DEFAULT_UA
)
}
\arguments{
\item{query}{Search string. The API accepts its own advanced syntax
(quoted phrases, boolean operators); see
\url{https://api.a2aj.ca/docs}.}

\item{search_type}{\code{"full_text"} (default) or \code{"name"}
(titles only).}

\item{doc_type}{\code{"cases"} (default) or \code{"laws"}.}

\item{size}{Number of hits, 1 to 50.}

\item{search_language}{\code{"en"} (default) or \code{"fr"}.}

\item{sort_results}{\code{"default"} (relevance with the corpus's
court weighting), \code{"newest_first"} or \code{"oldest_first"}.}

\item{dataset}{Optional character vector of dataset codes to
restrict to (for example \code{c("SCC", "ONCA")}); see
\code{\link{morie_ingest_a2aj_coverage}}.}

\item{start_date, end_date}{Optional \code{"YYYY-MM-DD"} bounds on
the document date.}

\item{timeout}{HTTP timeout in seconds.}

\item{user_agent}{User-Agent header sent with the request.}
}
\value{
A data.frame, one row per hit, with the record's metadata
fields (\code{dataset}, \code{citation_en}, \code{name_en},
\code{document_date_en}, \code{url_en}, \code{upstream_license},
...) and the search \code{score}. Zero rows when nothing matched.
}
\description{
Full-text or title search over the A2AJ corpus. The API returns at
most 50 hits per call and does not page, so narrow with
\code{dataset} and the date window rather than asking for more.
}
\examples{
\donttest{
hits <- try(morie_ingest_a2aj_search("roncarelli",
  search_type = "name", dataset = "SCC"
))
if (!inherits(hits, "try-error")) hits[, c("citation_en", "name_en")]
}
}

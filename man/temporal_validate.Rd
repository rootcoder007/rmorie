% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation.R
\name{temporal_validate}
\alias{temporal_validate}
\title{Train on earlier data, test on later data}
\usage{
temporal_validate(
  fit_fn,
  predict_fn,
  X,
  y,
  date_col,
  split_date = NULL,
  split_quantile = 0.7,
  scoring = "roc_auc"
)
}
\arguments{
\item{fit_fn, predict_fn}{As in \code{cross_validate}.}

\item{X}{Data frame including \code{date_col}.}

\item{y}{Target vector.}

\item{date_col}{Name of date column in \code{X}.}

\item{split_date}{Date to split on, or NULL.}

\item{split_quantile}{Quantile of dates (if \code{split_date} is NULL).}

\item{scoring}{Scoring metric.}
}
\value{
An object of class \code{"class_name"}.
}
\description{
Train on earlier data, test on later data
}
\examples{
set.seed(1)
X <- data.frame(x = rnorm(60), date = seq.Date(as.Date("2020-01-01"),
                                              by = "day", length.out = 60))
y <- X$x + rnorm(60)
res <- try(temporal_validate(
  fit_fn = function(Xt, yt) stats::lm(yt ~ x, data = cbind(Xt, yt = yt)),
  predict_fn = function(m, Xt) stats::predict(m, Xt),
  X = X, y = y, date_col = "date"))
if (!inherits(res, "try-error")) str(res, max.level = 1)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mfomf.R
\name{Modelrl}
\alias{Modelrl}
\title{Model-based RL: fit a tabular model, then plan in it}
\usage{
Modelrl(
  env,
  model = NULL,
  planner = "vi",
  n_states = NULL,
  n_actions = NULL,
  gamma = 0.95,
  tol = 1e-12,
  max_iter = 1000
)
}
\arguments{
\item{env}{transitions, one row per experience: (s, a, r, s').}

\item{model}{ignored; present for signature stability.}

\item{planner}{only "vi" is implemented.}

\item{n_states, n_actions}{tabular sizes; inferred when absent.}

\item{gamma}{discount.}

\item{tol}{sweep tolerance.}

\item{max_iter}{sweep cap.}
}
\value{
list: estimate, v, policy, sweeps, n_states, n_actions, method.
}
\description{
Sutton (1991), Dyna, an integrated architecture for learning, planning,
and reacting, SIGART Bulletin 2(4), 160-163: the same experience is
used twice, once to learn a model and once to plan with it.  The model
is the maximum-likelihood tabular one, phat(s'|s,a) = N(s,a,s')/N(s,a)
and rhat(s,a) = sum of rewards / N(s,a); the planner is value iteration
(Bellman 1957, Dynamic Programming), V(s) <- max_a \link[=rhat(s,a) + gamma
sum_s' phat(s'|s,a) V(s')]{rhat(s,a) + gamma sum_s' phat(s'|s,a) V(s')}.  Neither source was available here as a
full text; both equations are quoted in their standard published form
and reproduced identically in Sutton and Barto (2018) sections 8.1-8.2
and 4.4 (FETCHED).  Unvisited (s, a) pairs are excluded from the
maximisation rather than assigned an invented value.
}
\examples{
Modelrl(matrix(c(0, 0, 1, 1, 1, 0, 0, 0), 2, 4, byrow = TRUE))$v
}
\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Alfembed.R
\name{Alfembed}
\alias{Alfembed}
\title{Initial representation embeddings of AlphaFold}
\usage{
Alfembed(
  target_feat,
  residue_index,
  msa_feat,
  wa,
  wb,
  wrel,
  wmsa,
  wtgt,
  bins = NULL
)
}
\arguments{
\item{target_feat}{Per-residue target features, an \code{n x ctf} matrix.}

\item{residue_index}{Residue index per position; only differences matter.}

\item{msa_feat}{MSA features, an \code{s x n x cmf} array.}

\item{wa, wb}{The two target projections of line 1, each \code{cz x ctf}.}

\item{wrel}{Relative position projection, \code{cz x length(bins)}.}

\item{wmsa}{MSA feature projection, \code{cm x cmf}.}

\item{wtgt}{Target projection added to every MSA row, \code{cm x ctf}.}

\item{bins}{Relative position bins; defaults to -32, ..., 32.}
}
\value{
A list with the initial \code{z} and \code{m}, the relative
position contribution \code{pos}, \code{estimate}, \code{n} and
\code{method}.
}
\description{
Algorithms 3-5 of the Supplementary Information to Jumper et al. (2021),
p. 13.  The pair representation starts as an outer sum of two
projections of the target features plus a relative position encoding;
the MSA representation starts as a projection of the MSA features with
the target projection broadcast across sequences.
}
\details{
The relative position encoding clips the separation at 32 residues, so
nothing beyond that is distinguished.  That is deliberate: it
de-emphasises primary sequence distance and lets the network run on
chains longer than those it was trained on.
}
\references{
Jumper et al (2021) Nature 596:583-589, Suppl. Algorithms 3-5
}

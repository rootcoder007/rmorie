% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tmlcde.R
\name{Tmlecde}
\alias{Tmlecde}
\title{Controlled direct effect: the treatment effect with the mediator fixed.}
\usage{
Tmlecde(Y, A, M, QAM, Q1m, Q0m, g1W, hmW, m = 1, gbound = 0.025, level = 0.95)
}
\arguments{
\item{Y}{Outcome in [0, 1].}

\item{A}{Binary treatment.}

\item{M}{Mediator level of each observation.}

\item{QAM}{Initial E[Y | A, M, W] at the observed (A, M).}

\item{Q1m, Q0m}{Initial E[Y | A = 1/0, M = m, W].}

\item{g1W}{Initial P(A = 1 | W).}

\item{hmW}{Initial P(M = m | A, W).}

\item{m}{The level the mediator is set to.}

\item{gbound}{Truncation on both nuisance probabilities.}

\item{level}{Confidence level.}
}
\value{
List with \code{estimate}, \code{se}, \code{ci_lower},
\code{ci_upper}, \code{mu1}, \code{mu0}, \code{epsilon},
\code{min_denominator}, \code{max_weight}, \code{n}.
}
\description{
The clever covariate carries BOTH nuisance densities in its
denominator, so positivity is far more demanding than for a total
effect; \code{min_denominator} exists for that reason.
}
\details{
Formula: H_a = 1\{A = a, M = m\} / (g_a(W) h_m(A, W));
psi = E[Q*(1, m, W)] - E[Q*(0, m, W)]
}
\references{
van der Laan & Petersen (2008), International Journal of
Biostatistics 4(1), Article 23 -- the row's own citation. That paper
was NOT obtainable, so the estimator is assembled from the components
verified in the CRAN package tmle 2.1.1 (Gruber & van der Laan), with
the clever covariate's denominator extended from g_a(W) to
g_a(W) h_m(A, W) as the CDE identification requires.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geron_ml2_native.R
\name{morie_geron_value_function}
\alias{morie_geron_value_function}
\title{State value function by exact policy evaluation (Geron Ch 18, morie.fn hmvf)}
\usage{
morie_geron_value_function(s, pi, gamma, P = NULL, R = NULL)
}
\arguments{
\item{s}{0-based query state. @param pi Policy. @param gamma In [0, 1).}

\item{P}{(n_states, n_actions, n_states) array.}

\item{R}{(n_states, n_actions) or (n_states, n_actions, n_states).}
}
\value{
List with \code{V}, \code{value}, \code{r_pi}, \code{P_pi}, \code{residual},
\code{gamma}, \code{state}, \code{estimate}, \code{n}.
}
\description{
V = (I - gamma P_pi)^-1 r_pi, solved as a linear system. This is the
INDEPENDENT route the parity tests check \code{morie_geron_value_iteration}
against: the two must agree to solver tolerance on the same MDP.
}
\details{
\code{pi} is either a length-n_states vector of 0-based action indices or
an (n_states, n_actions) stochastic matrix; \code{s} is 0-based.
}

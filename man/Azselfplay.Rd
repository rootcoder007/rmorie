% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alphas.R
\name{Azselfplay}
\alias{Azselfplay}
\title{One AlphaZero self-play game and its training targets}
\usage{
Azselfplay(
  state,
  policy,
  value = NULL,
  mcts_iter = 16,
  step = NULL,
  terminal = NULL,
  outcome = NULL,
  max_moves = 32,
  temp_threshold = 30,
  c_puct = 1.25
)
}
\arguments{
\item{state}{starting state id.}

\item{policy}{function s -> list(p, v), or s -> p with \code{value} given.}

\item{value}{optional function s -> v.}

\item{mcts_iter}{simulations per move.}

\item{step, terminal, outcome}{environment callbacks.}

\item{max_moves}{hard cap on game length.}

\item{temp_threshold}{moves played at tau = 1.}

\item{c_puct}{exploration constant.}
}
\value{
list: estimate, states, pis, actions, zs, moves, final_state,
method.
}
\description{
Silver et al. (2018), arXiv:1712.01815 (FETCHED): a game is played by
running MCTS at every move and playing a_t ~ pi_t, the normalised root
visit count; the record is (s_t, pi_t, z) with z the outcome from the
point of view of the player to move at t, and those triples are the
targets for l = (z - v)^2 - pi' log p + c ||theta||^2.
}
\details{
Determinism: selection is greedy once the temperature has decayed
(AlphaGo Zero: after 30 moves) and, while tau = 1, uses the inverse CDF
of pi at a van der Corput point rather than a draw.  The rollout budget
is a fixed simulation count.
}
\examples{
Azselfplay(0, function(s) list(c(0.5, 0.5), 0), mcts_iter = 4,
           max_moves = 1)$moves
}
\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/esl_native.R
\name{morie_esl_gaussian_mixture}
\alias{morie_esl_gaussian_mixture}
\title{Gaussian mixture density}
\usage{
morie_esl_gaussian_mixture(X, k = 2, newdata = NULL, ...)
}
\arguments{
\item{X}{Training data, n by p.}

\item{k}{Number of components.}

\item{newdata}{Points at which to evaluate; defaults to \code{X}.}

\item{...}{Passed to \code{morie_esl_em_gmm()}.}
}
\value{
List with \code{density}, \code{log_density}, \code{pi}, \code{mu}, \code{sigma}, \code{loglik},
\code{aic}, \code{bic}.
}
\description{
Fits a mixture with \code{morie_esl_em_gmm()} and evaluates the resulting
density. ESL Sec 6.8 presents the mixture as a density estimate -- a
smoother with a data-driven bandwidth -- rather than as a clustering
device, and the density is the object that view needs.
}
\examples{
set.seed(1)
x <- c(rnorm(300, -3), rnorm(300, 3))
d <- morie_esl_gaussian_mixture(x, k = 2, newdata = c(-3, 0))
d$density[1] > d$density[2]
}
\references{
Hastie, T., et al. (2009). ESL (2nd ed.), Sec 6.8. Springer.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wasserman_native.R
\name{morie_wasserman_directed_graph}
\alias{morie_wasserman_directed_graph}
\title{Directed graphical model factorisation (Ch 17, wsmdir)}
\usage{
morie_wasserman_directed_graph(dag, x)
}
\arguments{
\item{dag}{List of nodes, each \verb{list(parents = <indices>, cpt = <list>)}
where cpt maps a parent bit-string key to P(X_i = 1).}

\item{x}{Binary configuration of the same length.}
}
\value{
List with \code{estimate} (joint probability), \code{log_joint},
\code{factors}, \code{n_nodes}, \code{method}.
}
\description{
p(x) = prod_i p(x_i | pa(x_i)) for a binary DAG given in topological
order; a parent index at or after its child is refused, which is
also what rules out cycles.
}
\examples{
dag <- list(list(parents = integer(0), cpt = list(0.3)),
            list(parents = 0L, cpt = list(`0` = 0.2, `1` = 0.9)))
round(morie_wasserman_directed_graph(dag, c(1, 1))$estimate, 12)
}

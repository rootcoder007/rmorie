% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fznks.R
\name{Kernks}
\alias{Kernks}
\alias{morie_fauzi_naive_kernel_ks}
\title{Naive kernel-smoothed Kolmogorov-Smirnov statistic (Eq. 5.3)}
\usage{
Kernks(x, cdf, h = NULL, ngrid = 2001L, pad = 4)

morie_fauzi_naive_kernel_ks(x, cdf, h = NULL, ngrid = 2001L, pad = 4)
}
\arguments{
\item{x}{Sample.}

\item{cdf}{The null distribution \code{F(t)}.}

\item{h}{Bandwidth; defaults to the distribution-function rule.}

\item{ngrid}{Grid size for the supremum; fixed, never adapted.}

\item{pad}{How many bandwidths to extend the grid beyond the sample range.}
}
\value{
Named list with \code{statistic}, \code{p_value}, \code{argmax}, \code{h}, \code{n}, \code{method}.
}
\description{
Eq. (5.3): \deqn{\widehat{KS} = \sup_x |\hat F_X(x) - F(x)|,}{KShat = sup_x |Fhat_X(x) - F(x)|,}
with \code{Fhat_X} the NAIVE kernel distribution function estimator,
\verb{n^-1 sum_i W((x - X_i)/h)}.
}
\details{
Because \code{Fhat_X} is continuous the supremum is no longer attained at a jump,
so unlike the empirical version it genuinely needs a grid. The grid is fixed
and deterministic: \code{ngrid} equally spaced points spanning the sample
extended by \code{pad} bandwidths on each side, since the smoothed estimator has
not reached 0 and 1 at the extreme order statistics.

This module previously carried a copy of the empirical KS body, which
ignored the bandwidth entirely. It now smooths.

Theorem 5.1 says \verb{|KS_n - KShat| ->_p 0}, so the Kolmogorov critical values
still apply and the p-value uses them. Smoothing buys not a different limit
but better small-sample calibration, per Sec. 5.1.
}
\examples{
Kernks(c(0.1, 0.3, 0.5, 0.7, 0.9), cdf = function(t) pmin(pmax(t, 0), 1))
}
\references{
Fauzi and Maesono (2023), Eq. (5.3), Theorem 5.1.
}
\keyword{internal}

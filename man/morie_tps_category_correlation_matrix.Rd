% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tps_crime.R
\name{morie_tps_category_correlation_matrix}
\alias{morie_tps_category_correlation_matrix}
\title{Pearson correlation across TPS categories' per-hood counts}
\usage{
morie_tps_category_correlation_matrix(dfs)
}
\arguments{
\item{dfs}{Named list of TPS data.frames keyed by category.}
}
\value{
A \code{morie_tps_result} named list with a single
correlation table.
}
\description{
For every category in \code{dfs}, computes per-HOOD_158 counts,
aligns onto a common (union) hood index, and reports the Pearson
correlation matrix.
}
\examples{
set.seed(1)
df <- data.frame(OCC_YEAR = rep(2014:2023, each = 30),
                 OCC_MONTH = sample(month.name, 300, TRUE),
                 HOOD_158 = sample(sprintf("\%03d", 1:20), 300, TRUE),
                 LAT_WGS84 = runif(300, 43.6, 43.8),
                 LONG_WGS84 = runif(300, -79.5, -79.2))
dfs <- list(a = df, b = df)
res <- try(morie_tps_category_correlation_matrix(dfs))
if (!inherits(res, "try-error")) str(res, max.level = 1)
}

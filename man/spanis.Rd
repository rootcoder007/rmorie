% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spanis.R
\name{spanis}
\alias{spanis}
\title{Geometric anisotropy: correcting direction dependence by a linear map.}
\usage{
spanis(coords, z, A_matrix = NULL, n_bins = 15, max_dist = NULL)
}
\arguments{
\item{coords}{Coordinate matrix (n by d).}

\item{z}{Numeric vector of length n.}

\item{A_matrix}{The (d by d) correction A = B^-1. Defaults to identity.}

\item{n_bins}{Number of lag bins.}

\item{max_dist}{Largest lag retained.}
}
\value{
Named list: lag, gamma, n_pairs, gamma_raw, coords_corrected,
A_matrix.
}
\description{
If Z1(s) is stationary with isotropic covariance C1, then
Z(s) = Z1(Bs) has C(h) = C1(||Bh||), which is geometrically
anisotropic -- its iso-correlation contours are ellipses rather than
circles. The transformation is reversed by s* = As with A = B^-1.
}
\details{
Returns the empirical semivariogram in the corrected space alongside
the uncorrected one, so the improvement is visible rather than
asserted.
}
\examples{
spanis(coords = matrix(runif(200), 100, 2), z = rnorm(100),
       A_matrix = diag(2), n_bins = 5)
}
\references{
Schabenberger & Gotway (2005), Sec 4.3.7, p. 151.
}

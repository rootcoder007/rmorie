% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fzt14.R
\name{Gkcov}
\alias{Gkcov}
\alias{morie_fauzi_thm1_4_asympnorm_mgkde}
\title{Covariance of the gamma-kernel functions at bandwidths h and 4h (Theorem 1.4)}
\usage{
Gkcov(
  x,
  h,
  n,
  f = NULL,
  boundary = FALSE,
  c = NULL,
  density = NULL,
  sample = NULL,
  upper = 20,
  ngrid = 2001L
)

morie_fauzi_thm1_4_asympnorm_mgkde(
  x,
  h,
  n,
  f = NULL,
  boundary = FALSE,
  c = NULL,
  density = NULL,
  sample = NULL,
  upper = 20,
  ngrid = 2001L
)
}
\arguments{
\item{x}{Evaluation point, \code{x >= 0}.}

\item{h}{Bandwidth of the first estimator; the second uses \verb{4h}.}

\item{n}{Sample size.}

\item{f}{\code{f_X(x)}; selects the Theorem 1.4 closed form.}

\item{boundary}{Logical; use the boundary branch. Requires \code{c}.}

\item{c}{The constant in \code{x/h -> c}.}

\item{density}{\code{f_X} on \verb{\[0, upper\]}; selects the exact plug-in form.}

\item{sample}{Observed data; selects the exact plug-in form.}

\item{upper}{Upper limit of the quadrature grid.}

\item{ngrid}{Number of grid points; fixed, never adapted.}
}
\value{
Named list with \code{covariance}, \code{form}, \code{cross}, \code{jh}, \code{j4h}, \code{h}, \code{n}, \code{method}.
}
\description{
Theorem 1.4. With \code{R} the Stirling ratio (1.12) and \code{s = sqrt(h)}, the
interior form (\code{x/h -> Inf}) is
\deqn{\mathrm{Cov}\[A_h, A_{4h}\] = \frac{R(s^{-1}-1)R((2s)^{-1}-1)(\frac32-2s)^{\frac{3}{2s}-\frac32}}{2\sqrt{\pi}R(\frac{3}{2s}-2)(3x+5s)(2-2s)^{\frac1s-\frac12}(1-2s)^{\frac{1}{2s}-\frac12}}\Big(\frac{x+s}{3x+5s}\Big)^{\frac{1}{2s}-1}\Big(\frac{2x+4s}{3x+5s}\Big)^{\frac1s-1}\frac{f_X(x)}{nh^{1/4}},}{Cov\[A_h, A_4h\] = \[R(1/s - 1) R(1/(2s) - 1) (3/2 - 2s)^(3/(2s) - 3/2)\] / \[2 sqrt(pi) R(3/(2s) - 2) (3x + 5s) (2-2s)^(1/s - 1/2) (1-2s)^(1/(2s) - 1/2)\] * ((x+s)/(3x+5s))^(1/(2s) - 1) * ((2x+4s)/(3x+5s))^(1/s - 1) * f(x)/(n h^(1/4)),}
and the boundary form (\code{x/h -> c}) replaces \verb{3x+5s} by \verb{3cs+5}, \code{x+s} by
\code{cs+1}, \verb{2x+4s} by \verb{2cs+4}, and \code{h^(1/4)} by \code{h^(3/4)}.
}
\details{
The exponents are unreadable in the book's PDF text layer, which drops
stacked fractions. They are taken instead from the primary source, where
they are legible: Fauzi, R. R. (2020), "Bias Reduction of Kernel-Type
Estimators without Boundary Problems", doctoral thesis, Kyushu University
(Kyushu University Institutional Repository, \code{math0257}), Theorem 2.1.7 --
the same result the book reproduces as Theorem 1.4.

Everything is evaluated through logarithms: \code{1/s} is of order 10 for
\code{h = 0.01} and the individual factors overflow long before their product
does. The bases \verb{2-2s} and \verb{1-2s} must be positive, so the formula needs
\code{h < 1/4} -- the regime it is an asymptotic statement about anyway.

Passing \code{sample} or \code{density} instead of \code{f} returns the EXACT finite-sample
covariance \verb{(1/n)(E\[K(X;x,h)K(X;x,4h)\] - J_h J_4h)} rather than its
asymptotic evaluation, which is useful for checking how far into the
asymptotics a given \code{h} actually is.
}
\examples{
Gkcov(x = 1, h = 0.01, n = 100, f = 0.3)
}
\references{
Fauzi and Maesono (2023), Theorem 1.4; Fauzi (2020), Kyushu University doctoral thesis, Theorem 2.1.7.
}
\keyword{internal}

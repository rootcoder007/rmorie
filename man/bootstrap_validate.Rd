% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation.R
\name{bootstrap_validate}
\alias{bootstrap_validate}
\title{Bootstrap .632 / .632+ validation}
\usage{
bootstrap_validate(
  fit_fn,
  predict_fn,
  X,
  y,
  n_bootstraps = 200L,
  scoring = "roc_auc",
  method = "632plus",
  random_state = 42L
)
}
\arguments{
\item{fit_fn}{A function \code{(X, y) -> model}.}

\item{predict_fn}{A function \code{(model, X) -> probability vector}.}

\item{X}{Matrix or data frame of features.}

\item{y}{Vector of targets.}

\item{n_bootstraps}{Number of bootstrap replicates.}

\item{scoring}{"roc_auc", "accuracy", "brier".}

\item{method}{"632" or "632plus".}

\item{random_state}{Seed.}
}
\value{
An object of class \code{"class_name"}.
}
\description{
Bootstrap .632 / .632+ validation
}
\examples{
set.seed(1)
X <- matrix(rnorm(120 * 3), 120, 3)
y <- as.integer(plogis(X[, 1] - 0.5 * X[, 2]) > runif(120))
fit <- function(X, y) suppressWarnings(
  glm(y ~ ., data = data.frame(y = y, X), family = binomial()))
pred <- function(m, X) predict(m, newdata = data.frame(X), type = "response")
res <- bootstrap_validate(fit, pred, X, y, n_bootstraps = 20L,
                          method = "632", scoring = "accuracy")
res$mean
}

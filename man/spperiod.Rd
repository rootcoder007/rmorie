% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spperiod.R
\name{spperiod}
\alias{spperiod}
\title{Periodogram of a process observed on a rectangular lattice}
\usage{
spperiod(
  z_lattice,
  coords = NULL,
  omit_zero_frequency = TRUE,
  check_identity = TRUE
)
}
\arguments{
\item{z_lattice}{The r by c lattice of observations.}

\item{coords}{Accepted and ignored; the estimator is defined on the
row-column lattice, not on arbitrary coordinates.}

\item{omit_zero_frequency}{Remove the mean first (default \code{TRUE}). At a
non-zero Fourier frequency this changes nothing (p. 191); at the origin
it removes the squared mean, where eq (4.59) does not apply.}

\item{check_identity}{Verify eq (4.59) numerically (default \code{TRUE}).}
}
\value{
A list with \code{periodogram}, \code{omega1}, \code{omega2}, \code{j}, \code{k},
\code{covariance}, \code{lags_j}, \code{lags_k}, \code{mean_invariant}, \code{nonzero_mask},
\code{r}, \code{c}, and when checked \code{identity_max_abs_diff}, \code{identity_holds}
and \code{periodogram_from_covariance}.
}
\description{
eq (4.57): \eqn{I(\omega_1,\omega_2) = \{(2\pi)^2 rc\}^{-1} |\sum_u
\sum_v Z(u,v) \exp\{-i(\omega_1 u + \omega_2 v)\}|^2} at the Fourier
frequencies, the multiples of \eqn{2\pi/r} and \eqn{2\pi/c}.
}
\details{
The section's central claim is eq (4.59): away from the origin the
periodogram \emph{is} the Fourier transform of the sample covariance function.
With \code{check_identity} the right-hand side is computed independently and
the largest discrepancy returned; it should sit at machine precision.
That check pins the \eqn{(2\pi)^2} normalisation, which the placeholder
this replaces had dropped in favour of \eqn{1/n}.
}
\references{
Schabenberger Ch 4, Sec 4.7.1, eqs (4.56)-(4.59), pp. 190-192
}
\examples{
M <- matrix(c(1, 2, 3, 4, 5, 6), nrow = 2)
spperiod(M)
}

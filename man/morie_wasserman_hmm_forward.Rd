% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wasserman_native.R
\name{morie_wasserman_hmm_forward}
\alias{morie_wasserman_hmm_forward}
\title{HMM forward algorithm with per-step scaling (Rabiner 1989; Wasserman Ch 23 covers the underlying Markov chains) (wsmhmm)}
\usage{
morie_wasserman_hmm_forward(obs, A, B, pi)
}
\arguments{
\item{obs}{0-based observation indices, at least one.}

\item{A}{Transition matrix (S x S), rows summing to 1.}

\item{B}{Emission matrix (S x M), rows summing to 1.}

\item{pi}{Initial distribution of length S.}
}
\value{
List with \code{estimate} (log-likelihood), \code{filtered}, \code{T}, \code{S}, \code{method}.
}
\description{
Each step is normalised and the log of the scale factors accumulates
the exact log-likelihood, so long sequences cannot underflow.
}
\examples{
A <- rbind(c(0.7, 0.3), c(0.4, 0.6)); B <- rbind(c(0.9, 0.1), c(0.2, 0.8))
round(morie_wasserman_hmm_forward(c(0, 1, 0), A, B, c(0.6, 0.4))$estimate, 6)
}

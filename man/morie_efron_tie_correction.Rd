% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cox_native.R
\name{morie_efron_tie_correction}
\alias{morie_efron_tie_correction}
\title{Cox model with the Efron tie correction}
\usage{
morie_efron_tie_correction(time, event, X, ...)
}
\arguments{
\item{time}{follow-up times, non-negative.}

\item{event}{1 for an event, 0 for right-censored.}

\item{X}{covariate matrix, one row per subject.}

\item{...}{passed to the fitter (\code{max_iter}, \code{tol},
\code{offset}).}
}
\value{
as \code{\link{morie_breslow_tie_correction}}.
}
\description{
Efron's l/d weighting shrinks the risk set progressively across the
d tied events, which approximates the exact discrete likelihood at
essentially Breslow's cost. With no ties the two coincide exactly --
a useful test, and one the parity suite asserts.
}
\examples{
set.seed(1)
X <- matrix(rnorm(120), ncol = 2)
tt <- round(rexp(60, exp(X \%*\% c(0.7, -0.4))), 1)   # ties on purpose
morie_efron_tie_correction(tt, rep(1, 60), X)$beta
}
\references{
Efron, B. (1977). The efficiency of Cox's likelihood
function for censored data. \emph{JASA}, 72(359), 557-565.
}

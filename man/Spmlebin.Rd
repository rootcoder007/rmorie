% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hrzsmle.R
\name{Spmlebin}
\alias{Spmlebin}
\alias{morie_horowitz_semipar_mle_binary}
\title{Klein-Spady semiparametric MLE for a binary-response index model}
\usage{
Spmlebin(
  x,
  y,
  h = NULL,
  trim = 0.01,
  floor = 1e-04,
  niter = 12L,
  delta = 1,
  b0 = NULL
)

morie_horowitz_semipar_mle_binary(
  x,
  y,
  h = NULL,
  trim = 0.01,
  floor = 1e-04,
  niter = 12L,
  delta = 1,
  b0 = NULL
)
}
\arguments{
\item{x}{Numeric matrix of covariates, n by d.}

\item{y}{Numeric binary 0/1 outcome vector.}

\item{h}{Numeric bandwidth; default n^(-1/5).}

\item{trim}{Numeric density trimming constant defining A_x.}

\item{floor}{Numeric; Ghat is clipped into \link{floor, 1-floor}.}

\item{niter}{Integer sweeps of the coordinate search.}

\item{delta}{Numeric initial step of the coordinate search.}

\item{b0}{Optional numeric starting value for betatilde.}
}
\value{
Named list with estimate, se, loglik, ghat, index,
bandwidth, ntrim, n, method.
}
\description{
Horowitz (2009), Semiparametric and Nonparametric Methods in
Econometrics, Section 2.5.3, equation (2.33) and the semiparametric
analog on page 28; the estimator is Klein and Spady's (1993).  With
Y in {0,1}, G(x'beta) = P(Y = 1 | X = x); the known-G MLE maximises
(2.33), and in the semiparametric case G is replaced by a
leave-one-out kernel estimator.  Because Var(Y|X=x) depends on x
only through the index, the weight cancels and the UNWEIGHTED
estimator of G already attains the efficiency bound (page 28).
}
\details{
Maximisation uses the shelf's fixed-schedule coordinate search: no
tolerance-based early exit, no random restarts.
}
\examples{
n <- 200
x <- cbind(seq(-2, 2, length.out = n), cos(seq_len(n) * 0.8))
z <- as.numeric(x \%*\% c(1, 0.6))
Spmlebin(x, as.numeric(z >= 0), h = 0.4)$estimate
}
\keyword{internal}

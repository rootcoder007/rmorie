% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datasets_tps_psdp.R
\name{morie_datasets_tps_psdp_resolved}
\alias{morie_datasets_tps_psdp_resolved}
\title{One-call TPS PSDP data + boundary metadata join}
\usage{
morie_datasets_tps_psdp_resolved(
  layer_key,
  year = NULL,
  max_features = NULL,
  offline = TRUE,
  layer_url = NULL,
  resolvers = c("division", "hood158", "hood140", "nia", "psdp_class")
)
}
\arguments{
\item{layer_key}{One of the PSDP \code{layer_key}s from
\code{\link[=morie_tps_psdp_layers]{morie_tps_psdp_layers()}} (e.g., \code{"assault"}, \code{"autotheft"},
\code{"homicides"}).}

\item{year}{Optional year filter passed through to the underlying
loader.}

\item{max_features}{Optional row cap.}

\item{offline}{If \code{TRUE} (default), all data come from bundled
fixtures.}

\item{layer_url}{Backward-compat override for non-canonical
FeatureServer URL.}

\item{resolvers}{Character subset of
\code{c("division", "hood158", "hood140", "nia", "psdp_class")}.
Default joins all five.}
}
\value{
A wide \code{data.frame}: PSDP columns first, then prefixed
resolver columns.
}
\description{
Phase 3CCC3. Pulls a TPS PSDP crime dataset
(\code{morie_datasets_tps_assault()} etc.) and left-joins its native
DIVISION + HOOD_158 + HOOD_140 columns against the bundled
boundary metadata loaders (\code{\link[=morie_datasets_tps_police_divisions]{morie_datasets_tps_police_divisions()}},
\code{\link[=morie_to_neighbourhoods]{morie_to_neighbourhoods()}} 158 + 140 + NIA flags) and the PSDP
layer registry (\code{\link[=morie_tps_psdp_layers]{morie_tps_psdp_layers()}}).
}
\details{
Per-row PSDP datasets carry these ID columns natively:
\itemize{
\item \code{DIVISION} (e.g., "D11") -- joins to police_divisions.DIV
\item \code{HOOD_158} (integer 1-158) -- joins to 158-neighbourhoods
\item \code{NEIGHBOURHOOD_158} (denormalised name)
\item \code{HOOD_140} (integer 1-140) -- joins to 140-neighbourhoods
\item \code{NEIGHBOURHOOD_140} (denormalised name)
}

Resolver columns are prefixed \verb{division_*}, \verb{hood158_*},
\verb{hood140_*}, \verb{nia_*}, \verb{psdp_*} to avoid collisions. Left-join
semantics (row count preserved).

Mirrors the Chicago \code{morie_datasets_chicago_crime_resolved()}
(3VV+) and NYPD \code{morie_datasets_nyc_nypd_resolved()} (3AAA-3CCC1)
patterns.
}
\examples{
df <- morie_datasets_tps_psdp_resolved("assault", offline = TRUE)
names(df)
}

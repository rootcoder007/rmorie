% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tqval.R
\name{Vcquant}
\alias{Vcquant}
\title{Quantize a value cache with the inner-product variant of TurboQuant.}
\usage{
Vcquant(V, b = 3, seed = 1)
}
\arguments{
\item{V}{Value cache, one value vector per row.}

\item{b}{Total bits per coordinate, b >= 2.}

\item{seed}{Seed for the pinned rotation and projection.}
}
\value{
List with \code{reconstruction}, \code{mse},
\code{residual_norm}, \code{mean_mse}, \code{n}, \code{d}, \code{b}.
}
\description{
One bit per coordinate is spent on the sign sketch of the RESIDUAL, so
the scalar stage gets only b - 1 and b must be at least 2.
}
\details{
Formula: idx <- Quant_mse(x) at b - 1 bits; r <- x - DeQuant_mse(idx);
qjl <- sign(S . r);
xtilde <- DeQuant_mse(idx) + (sqrt(pi/2)/d) ||r|| S' qjl
}
\references{
Zandieh et al., arXiv:2504.19874, Algorithm 2 lines 2-12.
Fetched from arXiv.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bnd_native.R
\name{morie_bnd_polya_tree}
\alias{morie_bnd_polya_tree}
\title{Polya-tree posterior mean density}
\usage{
morie_bnd_polya_tree(
  y,
  grid = NULL,
  tree_depth = 6L,
  alpha = 1,
  lo = NULL,
  hi = NULL
)
}
\arguments{
\item{y}{sample.}

\item{grid}{evaluation points; data-driven when \code{NULL}.}

\item{tree_depth}{partition levels; resolution \code{2^tree_depth}.}

\item{alpha}{the scale in \verb{alpha_m = alpha m^2}.}

\item{lo, hi}{support of the base measure; padded data range
when \code{NULL}.}

}
\value{
list: grid, density, mass, tree_depth, alpha, alpha_rule,
n, method.
}
\description{
Lavine (1992). On the level-\code{m} dyadic partition the prior puts
independent \code{Beta(alpha_m, alpha_m)} splits at every node;
conjugacy updates each to \code{Beta(alpha_m + n_L, alpha_m + n_R)},
and the posterior mean density multiplies the expected splits
along the path to each cell.
}
\details{
The canonical \verb{alpha_m = alpha m^2} is what makes the prior put
probability one on ABSOLUTELY CONTINUOUS distributions (Kraft
1964); a CONSTANT \code{alpha_m} behaves like a Dirichlet process,
whose realisations are discrete. Larger \code{alpha} smooths toward the
uniform base measure, smaller \code{alpha} follows the empirical
histogram, and both limits are tested.
}
\examples{
morie_bnd_polya_tree(stats::rnorm(100), grid = c(-1, 0, 1))$density
}
\references{
Lavine (1992), \emph{Annals of Statistics} 20:1222-1235;
Ferguson (1974), \emph{Annals of Statistics} 2:615-629; Kraft (1964),
\emph{J. Applied Probability} 1:385-388; Hanson (2006), \emph{JASA}
101:1548-1565.
}

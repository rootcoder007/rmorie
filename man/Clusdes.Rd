% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clstrs.R
\name{Clusdes}
\alias{Clusdes}
\title{Choose the cluster size that buys the most precision per unit cost.}
\usage{
Clusdes(rho, S2, c1, c2, budget)
}
\arguments{
\item{rho}{Intraclass correlation, 0 < rho <= 1.}

\item{S2}{Element variance in the population.}

\item{c1}{Cost of adding one cluster.}

\item{c2}{Cost of adding one element within a cluster.}

\item{budget}{Total budget.}
}
\value{
List with \code{k_opt}, \code{k}, \code{m}, \code{variance},
\code{deff}, \code{cost}, \code{elements}.
}
\description{
The optimum is reported both exactly and rounded to the better of its
two neighbouring integers, compared on the achieved variance rather
than on the rounding.
}
\details{
Formula: k_opt = sqrt( c1 (1 - rho) / (c2 rho) );
m = budget / (c1 + c2 k); V(ybar) = (S^2 / (m k)) [1 + (k - 1) rho]
}
\references{
Cochran (1977), Sampling Techniques, 3rd edition, Chapter
9, which develops the design effect 1 + (k - 1) rho and optimises k
against the linear cost function c1 m + c2 m k. Chapter 9 was NOT in
the scanned excerpt available to this batch, so the standard
published form is used.
}
\examples{
Clusdes(rho = 0.05, S2 = 1, c1 = 10, c2 = 1, budget = 1000)
}

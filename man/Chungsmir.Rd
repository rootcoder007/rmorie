% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fzchsp.R
\name{Chungsmir}
\alias{Chungsmir}
\alias{morie_fauzi_chung_smirnov}
\title{Chung-Smirnov statistic for the kernel distribution function estimator}
\usage{
Chungsmir(x, cdf, h = NULL, grid = NULL)

morie_fauzi_chung_smirnov(x, cdf, h = NULL, grid = NULL)
}
\arguments{
\item{x}{Sample.}

\item{cdf}{The hypothesised \code{F(t)}.}

\item{h}{Bandwidth; defaults to \verb{4^(1/3) sigma n^(-1/3)}.}

\item{grid}{Points at which the supremum is taken; defaults to the sample.}
}
\value{
Named list with \code{statistic}, \code{supdiff}, \code{scale}, \code{h}, \code{n}, \code{method}.
}
\description{
Sec. 2.1 records the Chung-Smirnov property of the KDFE:
\deqn{\limsup_n \sqrt{\frac{2n}{\log\log n}}\sup_x|\hat F_h(x) - F_X(x)| = 1\ \mathrm{a.s.}}{limsup_n sqrt(2n/log log n) sup_x |Fhat_h(x) - F(x)| = 1 a.s.}
}
\details{
A law of the iterated logarithm, not a distributional limit: it pins the
ALMOST-SURE fluctuation of the uniform error at exactly
\verb{sqrt(log log n / (2n))}, with constant 1.

This returns the normalised statistic
\verb{sqrt(2n / log log n) sup_x |Fhat_h(x) - F(x)|}, whose limsup the theorem
sets to 1. Values persistently above 1 are evidence against \code{F}; a single
value above 1 is not, since a limsup is exceeded infinitely often on the way.

Undefined for \code{n <= 15}, where \verb{log log n <= 1} makes the normaliser
meaningless; the function says so rather than returning a number.
}
\examples{
Chungsmir(1:50, cdf = function(t) pmin(pmax((t - 1) / 49, 0), 1))
}
\references{
Fauzi and Maesono (2023), Sec. 2.1, the Chung-Smirnov display.
}
\keyword{internal}

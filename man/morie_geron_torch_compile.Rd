% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geron_w4d_native.R
\name{morie_geron_torch_compile}
\alias{morie_geron_torch_compile}
\title{torch.compile: graph-capturing JIT for forward and backward passes}
\usage{
morie_geron_torch_compile(model, mode = "default", example_inputs)
}
\arguments{
\item{model}{List of ops (see \code{morie_geron_trace}).}

\item{mode}{"default", "reduce-overhead", or "max-autotune".}

\item{example_inputs}{Required input used for graph capture.}
}
\value{
list with compiled, graph, output, eager_output, max_diff, n_ops,
n_compiled, fused_runs, flops_eager, flops_compiled, speedup, mode, estimate, n, method.
}
\description{
Traces the model (\code{morie_geron_trace}), fuses consecutive
"linear" ops into one matmul (exact, verified by replay), and under
mode = "max-autotune" costs the fused product via the matrix-chain DP.
}
\examples{
A2 <- diag(2) * 2
B2 <- diag(2) * 3
C2 <- diag(2) * 5
morie_geron_torch_compile(list(list(kind = "linear", param = A2), 
    list(kind = "linear", param = B2), list(kind = "linear", 
        param = C2)), example_inputs = matrix(c(1, 1), 1, 2))
}

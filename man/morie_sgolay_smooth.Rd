% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/signal.R
\name{morie_sgolay_smooth}
\alias{morie_sgolay_smooth}
\title{Savitzky-Golay smoothing filter}
\usage{
morie_sgolay_smooth(x, window_length = 11L, polyorder = 3L)
}
\arguments{
\item{x}{Numeric vector.}

\item{window_length}{Window length (odd integer, default 11).}

\item{polyorder}{Polynomial order (default 3).}
}
\value{
List with \code{filtered} (numeric vector) and \code{name}.
}
\description{
Polynomial-fit smoothing filter. Preserves higher moments (peak heights,
shape) better than a moving average and is the standard tool for
chromatography, spectroscopy, and biosensor smoothing.
}
\examples{
\donttest{
if (requireNamespace("signal", quietly = TRUE)) {
  set.seed(1)
  t <- seq(0, 1, length.out = 200)
  x <- sin(2 * pi * 3 * t) + rnorm(200, sd = 0.2)
  y <- morie_sgolay_smooth(x, window_length = 11, polyorder = 3)
  length(y$filtered)
}
}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distributions.R
\name{morie_dist_sum}
\alias{morie_dist_sum}
\title{Sum a discrete distribution's mass (or a moment) with convergence check}
\usage{
morie_dist_sum(dist, moment_order = 0L, center = 0, tol = 1e-10)
}
\arguments{
\item{dist}{A discrete \code{morie_distribution}.}

\item{moment_order}{If > 0, sum \code{(k - center)^order * P(k)} instead of
the mass.}

\item{center}{Centering value for central moments.}

\item{tol}{Tail-mass tolerance for truncation.}
}
\value{
The summed value (total mass, or the requested moment).
}
\description{
Uses discrete summation, justified because the probability-mass series
of a proper discrete distribution converges to 1. Before truncating an
unbounded support (e.g. Poisson), the tail mass is confirmed to fall
below \code{tol}, demonstrating the series has a finite limit.
}
\examples{
morie_dist_sum(morie_distribution("poisson", lambda = 3))
}

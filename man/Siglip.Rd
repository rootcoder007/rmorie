% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/siglip.R
\name{Siglip}
\alias{Siglip}
\title{Contrastive loss that scores each pair on its own}
\usage{
Siglip(image_emb, text_emb, t_prime = 1, bias = 0)
}
\arguments{
\item{image_emb}{Image embeddings; L2-normalised here.}

\item{text_emb}{Text embeddings; L2-normalised here.}

\item{t_prime}{Logit scale.}

\item{bias}{Logit bias.}
}
\value{
List with \code{estimate}, \code{logits}, \code{acc}, \code{n}.
}
\description{
CLIP softmax normalises over the whole batch, so every pair loss
depends on every other pair and the batch must be huge and globally
synchronised. A sigmoid per pair removes that coupling: the loss
decomposes and needs no all-gather. The learnable bias exists because
a batch is overwhelmingly negatives.
}
\details{
Formula:
\code{L = -(1/|B|) sum_ij log sigmoid(z_ij (t x_i . y_j + b))},
\code{z_ij = 1} on the diagonal, \code{-1} elsewhere.
}
\references{
Zhai, Mustafa, Kolesnikov & Beyer (2023). ICCV 2023,
equation (3).
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gat.R
\name{Gat}
\alias{Gat}
\title{Let each node decide how much to weight each neighbour}
\usage{
Gat(A, X, W, a, alpha_leaky = 0.2)
}
\arguments{
\item{A}{Adjacency; self-loops are added.}

\item{X}{Node features.}

\item{W}{Shared linear map.}

\item{a}{Attention vector of length 2 f_out.}

\item{alpha_leaky}{LeakyReLU negative slope.}
}
\value{
List with \code{H}, \code{alpha}, \code{estimate}, \code{n}, \code{f_out}.
}
\description{
Fixed aggregation treats every neighbour alike, which is wrong when
some edges matter more. Attention makes the weight a learned function
of the two endpoints, computed per edge from node features alone, so
the layer never needs the whole graph and transfers to unseen graphs.
}
\details{
Formula: \code{e_ij = LeakyReLU(a'\[W h_i || W h_j\])},
\code{alpha_ij = softmax_j(e_ij)},
\code{h_i' = sum_j alpha_ij W h_j}.
}
\examples{
Gat(A = c(1, 2, 3, 4, 5, 6, 7, 8), X = c(1, 2, 3, 4, 5, 6, 7, 8), W = 5L, a = c(1, 2, 3, 4, 5, 6, 7, 8))
}
\references{
Velickovic et al. (2018). Graph attention networks. ICLR
2018, equations (1)-(4).
}

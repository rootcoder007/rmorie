% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geron_train_native.R
\name{morie_geron_pyramid_vit_stage}
\alias{morie_geron_pyramid_vit_stage}
\title{Pyramid ViT spatial-reduction attention (morie.fn grpvt)}
\usage{
morie_geron_pyramid_vit_stage(X, WQ, WK, WV, reduction_ratio = 2)
}
\arguments{
\item{X}{(H, W, d_model) array.}

\item{WQ, WK, WV}{Projections with d_model rows.}

\item{reduction_ratio}{R dividing both H and W.}


}
\value{
List with \code{output}, \code{weights}, \code{reduced_tokens},
\code{compression}, \code{reduced_map}.
}
\description{
Queries at full resolution, keys and values from an R x R
average-pooled map. The (H, W, d) array is flattened ROW-MAJOR into
H*W tokens, which in R means indexing [i, j, ] in that order.
}
\examples{
set.seed(4)
X <- array(rnorm(4 * 4 * 3), dim = c(4, 4, 3))
W <- diag(3)
morie_geron_pyramid_vit_stage(X, WQ = W, WK = W, WV = W,
                              reduction_ratio = 2)
}

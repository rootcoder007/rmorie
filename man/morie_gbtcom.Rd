% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gbtcom_native.R
\name{morie_gbtcom}
\alias{morie_gbtcom}
\title{Goodman-Bacon decomposition, vector interface}
\usage{
morie_gbtcom(y, D, unit, time)
}
\arguments{
\item{y}{Outcome, one entry per unit-period.}

\item{D}{Treatment indicator, same length.}

\item{unit}{Unit identifier, same length.}

\item{time}{Period identifier, same length.}
}
\value{
As \code{Gbtcom}.
}
\description{
The signature-compatible mirror of Python \code{morie.fn.gbtcom},
which takes the four columns as vectors. \code{Gbtcom} keeps the
data-frame form for R callers who already hold a panel.
}
\examples{
set.seed(1)
nu <- 6; T <- 5
unit <- rep(1:nu, each = T)
time <- rep(1:T, nu)
ft <- rep(c(NA, NA, NA, 4, 4, 3), each = T)
D <- as.integer(!is.na(ft) & time >= ft)
y <- rnorm(nu * T) + D * 1.5
morie_gbtcom(y, D, unit, time)
}
\keyword{internal}

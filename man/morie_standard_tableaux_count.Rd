% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/algebraic_combinatorics_native.R
\name{morie_standard_tableaux_count}
\alias{morie_standard_tableaux_count}
\title{The hook length formula}
\usage{
morie_standard_tableaux_count(shape)
}
\arguments{
\item{shape}{Integer vector, a partition.}
}
\value{
A list with \code{count}, \code{exact}, \code{factorial}, \code{hook_product},
\code{remainder}, \code{divides_exactly}, \code{hooks}, \code{shape}, \code{n}.
}
\description{
\eqn{f^\lambda = n! / \prod_{(i,j)} h(i,j)} counts standard Young
tableaux of the given shape -- fillings by \eqn{1, \ldots, n}
increasing along every row and down every column.
}
\details{
That a product of hook lengths divides \eqn{n!} exactly is not
obvious, so it is verified rather than assumed. The count is built
from prime exponents, which never overflows, and then multiplied
back against the hook product and compared with \eqn{n!} in
arbitrary precision. \code{remainder} reports the shortfall, which must
be zero for a genuine partition.

Doubles stop being exact integers past \eqn{2^{53}}, so \code{factorial},
\code{hook_product} and \code{count} are accompanied by exact decimal strings.
}
\examples{
morie_standard_tableaux_count(shape = 5L)
}

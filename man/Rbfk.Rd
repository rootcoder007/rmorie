% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classictk.R
\name{Rbfk}
\alias{Rbfk}
\title{Radial basis (Gaussian) kernel}
\usage{
Rbfk(x, y, sigma)
}
\arguments{
\item{x, y}{Vectors of the same length.}

\item{sigma}{Bandwidth, positive.}

}
\value{
Named list with \code{estimate}, \code{value}, \code{sq_distance},
\code{distance}, \code{sigma}, \code{gamma}, \code{method}.
}
\description{
\deqn{k(x,y) = \exp(-\|x-y\|^2/(2\sigma^2))}{k = exp(-||x-y||^2/(2 sigma^2))}
This takes the bandwidth, not the rate; the equivalent
\eqn{\gamma = 1/(2\sigma^2)} is returned so the two conventions
cannot be confused.
}
\examples{
Rbfk(c(0, 0), c(3, 4), 2)
}
\references{
Standard kernel method; no single owning source.
}

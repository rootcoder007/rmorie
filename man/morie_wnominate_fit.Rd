% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/abundance_text_voting_native.R
\name{morie_wnominate_fit}
\alias{morie_wnominate_fit}
\title{W-NOMINATE ideal points by alternating optimisation}
\usage{
morie_wnominate_fit(
  votes,
  n_dims = 1L,
  polarity = NULL,
  max_iter = 250L,
  tol = 1e-07,
  ridge = 0.001
)
}
\arguments{
\item{votes}{Matrix, 1 yea / 0 nay / NA absent.}

\item{n_dims}{Dimensions of the policy space.}

\item{polarity}{Index of a legislator fixed to positive on dim 1.}

\item{max_iter, tol}{Alternating-optimisation controls.}

\item{ridge}{Small penalty for near-separating roll calls.}
}
\value{
A list with \code{ideal_points}, \code{rollcall_normals},
\code{correct_classification}, \code{modal_baseline}, \code{aggregate_pre},
\code{log_likelihood}, \code{converged}, \code{warnings}.
}
\description{
ESTIMATES legislator ideal points and roll-call parameters. This is
not \code{morie_wnominate} in R/wnom.R, which EVALUATES the NOMINATE
likelihood at ideal points already in hand.
}
\details{
Reparameterised so each half-step is an ordinary probit: with
\eqn{a_j = \beta(|z^N_j|^2 - |z^Y_j|^2)} and
\eqn{w_j = 2\beta(z^Y_j - z^N_j)} the linear predictor is exactly
\eqn{\eta_{ij} = a_j + x_i \cdot w_j}.

Three things are structural rather than numerical. The configuration
is identified only up to rotation, reflection and scale, so raw
coordinates are not comparable across fits and \code{polarity} fixes the
sign. Unanimous roll calls separate nobody and are dropped.
Correct classification must be read against the modal baseline, so
\code{aggregate_pre} is the number worth quoting.

The objective is NOT jointly concave, so the start matters: from a
random configuration 60 sweeps reached a log-likelihood of -19801
with an ideal-point correlation of 0.47 against the truth, while
seeding from the leading singular vectors reached -6913 and 0.990 in
26 sweeps.
}
\references{
Poole KT, Rosenthal H (1985) \emph{AJPS} 29(2):357-384,
\doi{10.2307/2111172}.
}
\examples{
set.seed(3)
n_leg <- 20; n_votes <- 15
ideal <- rnorm(n_leg)
votes <- sapply(1:n_votes, function(j) {
  cut <- rnorm(1)
  as.integer(ideal > cut + rnorm(n_leg, 0, 0.3))
})
r <- morie_wnominate_fit(votes, n_dims = 1L)
str(r, max.level = 1)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/awltrn_native.R
\name{fit_aol}
\alias{fit_aol}
\title{Single-stage AOL (or plain OWL, for comparison)}
\usage{
fit_aol(
  R,
  A,
  H,
  propensity = NULL,
  method = "aol",
  prognostic = NULL,
  shift = NULL,
  ridge = 1e-06
)
}
\arguments{
\item{R}{Numeric vector of outcomes.}

\item{A}{Integer vector of treatments.}

\item{H}{Matrix of covariates.}

\item{propensity}{Randomisation probabilities.}

\item{method}{Either \code{"aol"} or \code{"owl"}.}

\item{prognostic}{Optional prognostic values for AOL.}

\item{shift}{Optional additive constant for OWL.}

\item{ridge}{Ridge penalty for the weighted classifier.}
}
\value{
A list with \code{estimate}, \code{value}, \code{rule},
\code{coef}, \code{weights}, \code{labels}, \code{weight_cv},
\code{method}, \code{n}, \code{n_flipped} (AOL only, else
\code{NULL}), \code{shift} (OWL only, else \code{NULL}) and
\code{reference}.
}
\description{
Computes the weights, fits a weighted linear classifier, and
reports its inverse-probability value on the observed data. The
return is a named list whose names match the Python RichResult
payload keys.
}
\examples{
set.seed(2)
n <- 40
H <- matrix(rnorm(n * 2), n, 2)
A <- sample(c(-1, 1), n, replace = TRUE)
R <- 1 + A * H[, 1] + rnorm(n, 0, 0.3)
r <- fit_aol(R, A, H)
str(r, max.level = 1)
}
\references{
Liu, Y., Wang, Y., Kosorok, M. R., Zhao, Y. & Zeng,
D. (2018), Statistics in Medicine, doi:10.1002/sim.7844.
}
\keyword{internal}

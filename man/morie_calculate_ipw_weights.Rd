% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ipw_weights.R
\name{morie_calculate_ipw_weights}
\alias{morie_calculate_ipw_weights}
\title{Calculate inverse probability of treatment weights (IPTW)}
\usage{
morie_calculate_ipw_weights(
  data,
  treatment,
  ps_col,
  stabilized = FALSE,
  trim_quantiles = NULL
)
}
\arguments{
\item{data}{A \code{data.frame} containing treatment assignment and propensity
scores.}

\item{treatment}{Column name (string) of the binary treatment.}

\item{ps_col}{Column name (string) of the propensity scores.}

\item{stabilized}{If \code{TRUE}, return stabilised IPW weights. Default \code{FALSE}.}

\item{trim_quantiles}{Optional length-2 numeric vector \eqn{(q_l, q_u)}
in \eqn{`[0, 1]`}; if supplied, weights are clipped to the
\eqn{q_l}-th and \eqn{q_u}-th quantiles of the unclipped weight
distribution (Crump et al. 2009 trimming). Default \code{NULL}.}
}
\value{
Numeric vector of IPTW weights, length \code{nrow(data)}.
}
\description{
Mirrors the Python \code{morie.calculate_ipw_weights()}. Pure-R, no extra
dependencies.
}
\details{
Standard IPW: \eqn{w_i = T_i / e_i + (1 - T_i)/(1 - e_i)}, with the
propensity score \eqn{e_i} clipped at [0.01, 0.99] for stability.
Stabilised IPW replaces \eqn{T} and \eqn{1 - T} with the marginal
treatment probability \eqn{P(T = 1)} and \eqn{P(T = 0)} respectively.
}
\examples{
set.seed(1)
df <- data.frame(
  t = rbinom(100, 1, 0.4),
  ps = pmin(pmax(runif(100, 0.05, 0.95), 0.05), 0.95)
)
w <- morie_calculate_ipw_weights(df, treatment = "t", ps_col = "ps")
summary(w)
}

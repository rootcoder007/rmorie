% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/weights.R
\name{morie_weights_rake}
\alias{morie_weights_rake}
\title{Raking calibration (iterative proportional fitting).}
\usage{
morie_weights_rake(
  weights,
  df,
  margins,
  max_iter = 100,
  tol = 1e-06,
  bounds = NULL
)
}
\arguments{
\item{weights}{Initial numeric weights (length n).}

\item{df}{data.frame containing the calibration variables.}

\item{margins}{Named list of named numeric vectors.}

\item{max_iter}{Maximum IPF iterations (default 100).}

\item{tol}{Convergence tolerance on max relative adjustment (default 1e-6).}

\item{bounds}{Optional \code{c(lo, hi)} to clip the per-iteration multiplier.}
}
\value{
list with \code{weights}, \code{converged}, \code{iterations}, \code{max_adjustment},
\code{diagnostics} (from \code{morie_weights_diagnostics}).
}
\description{
Adjusts weights so that within each calibration variable the weighted sums
match the supplied marginal targets. \code{margins} is a named list keyed by
variable name; each entry is a named numeric vector mapping category values
(as strings) to target totals.
}

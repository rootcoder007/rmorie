% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mctsr_native.R
\name{morie_mctsr}
\alias{morie_mctsr}
\title{Monte Carlo tree search with UCT and random rollouts}
\usage{
morie_mctsr(
  root_state,
  actions,
  step,
  reward,
  is_terminal,
  n_iter = 200,
  c = 1/sqrt(2),
  seed = 0,
  backup = "sum",
  final = "robust"
)
}
\arguments{
\item{root_state}{Starting state.}

\item{actions}{Function(state) returning legal actions.}

\item{step}{Function(state, action) returning the next state.}

\item{reward}{Function(terminal_state) returning a numeric reward.}

\item{is_terminal}{Function(state) returning TRUE/FALSE.}

\item{n_iter}{Iterations (computational budget).}

\item{c}{Exploration constant Cp.}

\item{seed}{SplitMix64 seed (mirrors the Python arm).}

\item{backup}{"sum" (Algorithm 2) or "negamax" (Algorithm 3).}

\item{final}{"robust" (most visited) or "max" (highest mean).}
}
\value{
A list with elements \code{action}, \code{root_visits},
\code{child_visits}, \code{child_values}, \code{n_iter},
\code{c}, \code{backup}, \code{final}, \code{seed},
\code{method}.
}
\description{
Each iteration runs Browne et al. Algorithm 2: TREEPOLICY descends
by BESTCHILD while fully expanded, EXPAND adds one untried action,
DEFAULTPOLICY plays uniformly at random to a terminal state, and
BACKUP propagates the reward. Selection uses
UCT_j = Xbar_j + 2\emph{Cp}sqrt(2*log(n)/n_j), unvisited children
valued Inf. Cp = 1/sqrt(2) is the Kocsis-Szepesvari value for
rewards in [0, 1].  Both survey backups are provided: "sum"
(Algorithm 2) and "negamax" (Algorithm 3, two-player zero-sum).
Both final-move criteria: "robust" (most-visited root child,
default) and "max" (highest mean reward).
}
\references{
Browne, C. B. et al. (2012). A survey of Monte Carlo
tree search methods. IEEE Transactions on Computational
Intelligence and AI in Games, 4(1), 1-43.
}

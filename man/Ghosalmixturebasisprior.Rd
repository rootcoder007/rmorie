% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gh_c2_6.R
\name{Ghosalmixturebasisprior}
\alias{Ghosalmixturebasisprior}
\title{Mixture-of-kernels density prior}
\usage{
Ghosalmixturebasisprior(x, K = 5, seed = 42, bandwidth = 0.15)
}
\arguments{
\item{x}{Evaluation points.}

\item{K}{Number of mixture components, at least 1.}

\item{seed}{Seed for the deterministic draws.}

\item{bandwidth}{Kernel bandwidth, positive.}
}
\value{
List with \code{estimate} (mean density), \code{density},
\code{weights}, \code{locations}, \code{method}.
}
\description{
f = sum_k w_k K(x; theta_k) with Dirichlet weights.  Unlike the
histogram the mixture is smooth, and unlike a fixed basis its
components move: the locations are themselves random, which is what
lets a small number of components track structure anywhere on the
domain.
}
\details{
Formula: w ~ Dirichlet(1, ..., 1) by gamma normalisation,
theta_k ~ U(0, 1), f(x) = sum_k w_k phi((x - theta_k)/h) / h.
}
\references{
Ghosal & van der Vaart (2017), Fundamentals of
Nonparametric Bayesian Inference, CUP, section 2.3.3.
}
\examples{
V <- c(1, 2, 3, 4, 5, 6, 7, 8)
Ghosalmixturebasisprior(V)
}

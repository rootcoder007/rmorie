% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fzt12.R
\name{Gkgeoext}
\alias{Gkgeoext}
\alias{morie_fauzi_thm1_2_var_mgkde}
\title{Geometric extrapolation of the raw gamma-kernel mean (Theorem 1.2)}
\usage{
Gkgeoext(jh, j4h, h = NULL, a = NULL, b = NULL, f = NULL)

morie_fauzi_thm1_2_var_mgkde(jh, j4h, h = NULL, a = NULL, b = NULL, f = NULL)
}
\arguments{
\item{jh}{\verb{J_h(x) = E\[A_h(x)\]}.}

\item{j4h}{\verb{J_4h(x) = E\[A_4h(x)\]}, the same object at bandwidth \verb{4h}.}

\item{h}{Bandwidth; needed only for the explicit remainder.}

\item{a, b, f}{The coefficients (1.16), (1.17) and \code{f(x)}.}
}
\value{
Named list with \code{estimate}, \code{remainder}, \code{t1}, \code{t2}, \code{method}.
}
\description{
Theorem 1.2, Eq. (1.13): \eqn{J_h^2(x)\[J_{4h}(x)\]^{-1} = f_X(x) + O(h)}{J_h^2(x)\[J_4h(x)\]^-1 = f(x) + O(h)},
with \eqn{J_h = E[A_h]}{J_h = E[A_h]}.
}
\details{
The exponents are not a guess. Writing
\verb{log J_h = log f + a sqrt(h)/f + (b - a^2/(2f)) h/f}, the pair
\verb{(t1, t2)} must satisfy \code{t1 + t2 = 1} (keep \verb{log f}) and
\verb{t1 + 2 t2 = 0} (kill \code{sqrt(h)}); the unique solution is \verb{(2, -1)}.
The \code{O(sqrt(h))} bias of Theorem 1.1 is cancelled by a weighted
GEOMETRIC mean of two smoothings, not an arithmetic one, because the
expansion is multiplicative in \code{f}.

Supply \code{a}, \code{b} and \code{f} to also get the explicit \code{O(h)} remainder
\verb{-2(b - a^2/(2f)) h}; without them \code{remainder} is \code{NA}.
}
\examples{
Gkgeoext(jh = 0.4, j4h = 0.5)
}
\references{
Fauzi and Maesono (2023), Theorem 1.2, Eq. (1.13).
}
\keyword{internal}

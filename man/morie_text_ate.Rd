% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/abundance_text_voting_native.R
\name{morie_text_ate}
\alias{morie_text_ate}
\title{Treatment effect adjusting for what the text reveals}
\usage{
morie_text_ate(
  texts,
  T,
  Y,
  X = NULL,
  n_components = 10L,
  trim = 0.02,
  alpha = 0.05,
  embedding = NULL
)
}
\arguments{
\item{texts}{Character vector, one document per unit; may be NULL
when \code{embedding} is supplied.}

\item{T}{Binary treatment.}

\item{Y}{Outcome.}

\item{X}{Optional extra numeric covariates.}

\item{n_components}{Dimension of the text representation.}

\item{trim}{Propensity clipping bound.}

\item{alpha}{Two-sided level.}

\item{embedding}{Optional precomputed representation.}
}
\value{
A list with \code{estimate}, \code{se}, \code{naive_difference},
\code{adjustment_movement}, \code{propensity}, \code{max_weight_share}, \code{warnings}.
}
\description{
The confounder is written down but not coded -- clinical notes, case
files, judicial reasons. The text becomes a low-dimensional
representation, that joins the adjustment set, and the estimator is
ordinary augmented inverse-probability weighting.
}
\details{
This identifies the effect only if the representation captures ALL
the confounding the text carries, which is not testable from the
data. A bag of words discards order and negation: "history of
psychosis" and "no history of psychosis" have cosine 0.78 under this
encoding and exactly 1.0 once min_df drops "no". Passing \code{embedding}
is the supported route to a stronger representation and deliberately
the only one -- the function consumes a numeric matrix and takes no
model-runtime dependency, so it stays native and offline.

\code{naive_difference} is returned so the movement from the unadjusted
contrast is visible: an adjustment that moves nothing has either
found no confounding or failed to represent it, and those two look
identical from outside.
}
\examples{
set.seed(2)
n <- 60
emb <- matrix(rnorm(n * 5), n, 5)
T <- rbinom(n, 1, plogis(emb[, 1]))
Y <- 1 + 0.8 * T + emb[, 1] + rnorm(n, 0, 0.5)
texts <- replicate(n, paste(sample(letters, 5), collapse = " "))
r <- morie_text_ate(texts, T, Y, n_components = 3L, embedding = emb)
str(r, max.level = 1)
}
\references{
Veitch V, Sridhar D, Blei DM (2020) arXiv:1905.12741.
}

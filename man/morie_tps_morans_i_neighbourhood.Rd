% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tps_spatial.R
\name{morie_tps_morans_i_neighbourhood}
\alias{morie_tps_morans_i_neighbourhood}
\title{Global Moran's I on neighbourhood-level incident counts}
\usage{
morie_tps_morans_i_neighbourhood(
  df,
  hood_col = "HOOD_158",
  ds_name = "?",
  k_neighbours = 5L,
  lat_col = "LAT_WGS84",
  lon_col = "LONG_WGS84",
  use_spdep = FALSE
)
}
\arguments{
\item{df}{Incident-level data.frame.}

\item{hood_col}{Character. Neighbourhood id column (default
\code{"HOOD_158"}).}

\item{ds_name}{Character. Tag for the result title.}

\item{k_neighbours}{k for the k-NN spatial weights graph
(default 5).}

\item{lat_col, lon_col}{WGS84 column names (default
\code{"LAT_WGS84"} / \code{"LONG_WGS84"}).}

\item{use_spdep}{If \code{TRUE} and \pkg{spdep} is installed,
delegate the test to \code{spdep::moran.test} (with a row-
standardised listw). Default \code{FALSE}.}
}
\value{
A named list with classes \code{morie_tps_spatial_result},
\code{morie_rich_result}, \code{list}. Numeric outputs include
\code{moran_I}, \code{expected_I}, \code{var_I}, \code{z_score},
\code{p_value}, \code{n}.
}
\description{
Builds a k-NN spatial weights matrix from neighbourhood centroids
(mean LAT/LONG of incidents in each hood) and computes the global
Moran's I on the count vector. The Cliff-Ord normal-assumption
variance is used for the z-score and two-sided p-value.
}
\examples{
set.seed(2026)
n_inc <- 400
df <- data.frame(
  HOOD_158 = sample(letters[1:20], n_inc, replace = TRUE),
  LAT_WGS84 = 43.6 + runif(n_inc, 0, 0.2),
  LONG_WGS84 = -79.4 + runif(n_inc, 0, 0.2)
)
morie_tps_morans_i_neighbourhood(df)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/digest_native.R
\name{morie_digest}
\alias{morie_digest}
\title{Hash an R object, string, raw vector or file (digest's digest, natively)}
\usage{
morie_digest(
  object,
  algo = c("md5", "sha1", "crc32", "sha256", "sha512", "xxhash32", "xxhash64",
    "murmur32", "crc32c", "sha224", "sha384"),
  serialize = TRUE,
  file = FALSE,
  length = Inf,
  skip = "auto",
  ascii = FALSE,
  raw = FALSE,
  seed = 0,
  errormode = c("stop", "warn", "silent"),
  serializeVersion = getOption("serializeVersion", 2L)
)
}
\arguments{
\item{object}{the object, string, raw vector, or (with \code{file}) path.}

\item{algo}{one of md5, sha1, crc32, sha256, sha512, xxhash32, xxhash64,
murmur32, crc32c, sha224, sha384.}

\item{serialize, file, length, skip, ascii, raw, seed, errormode, serializeVersion}{as in digest.}
}
\value{
the hex digest (character), or a raw vector when \code{raw = TRUE}.
}
\description{
Same contract as \code{digest::digest()}: by default the object is
\code{serialize()}d (version 2, the 14-byte header skipped so the hash does
not depend on the R version), \code{serialize = FALSE} hashes a character
string or raw vector as-is, \code{file = TRUE} hashes a file's bytes, \code{length}
and \code{skip} window the input, \code{raw = TRUE} returns the digest bytes, and
\code{seed} feeds xxhash32 / xxhash64 / murmur32.
}
\examples{
morie_digest("abc", algo = "sha256", serialize = FALSE)
morie_digest(1:10)
morie_digest(mtcars, algo = "xxhash64")
}

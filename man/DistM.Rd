% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distM.R
\name{DistM}
\alias{DistM}
\title{Bilinear triple score with a diagonal relation matrix}
\usage{
DistM(triples, dim, E = NULL, R = NULL, seed = 1)
}
\arguments{
\item{triples}{Rows \code{[head, relation, tail]}, zero-based.}

\item{dim}{Embedding dimension.}

\item{E}{Entity embeddings, optional.}

\item{R}{Relation embeddings, optional.}

\item{seed}{Seed for the shared generator.}
}
\value{
List with \code{estimate}, \code{scores},
\code{symmetric_gap}, \code{m}, \code{dim}.
}
\description{
Restricting the relation matrix to its diagonal drops the parameter
count from d^2 to d and makes the score a three-way inner product. The
cost is the symmetry it cannot escape:
\code{score(h, r, t) = score(t, r, h)} always, so an antisymmetric
relation is unrepresentable. That is what ComplEx was written to fix.
}
\details{
Formula: \code{score = <h, r, t> = sum_k h_k r_k t_k}.
}
\references{
Yang, Yih, He, Gao & Deng (2015). ICLR 2015, equation (3).
}
\examples{
V <- c(1, 2, 3, 4, 5, 6, 7, 8)
DistM(V, V)
}

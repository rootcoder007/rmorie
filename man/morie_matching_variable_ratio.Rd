% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/matching.R
\name{morie_matching_variable_ratio}
\alias{morie_matching_variable_ratio}
\title{Variable-ratio matching on propensity score}
\usage{
morie_matching_variable_ratio(
  data,
  treatment,
  covariates,
  min_ratio = 1L,
  max_ratio = 5L,
  caliper = 0.2,
  ps = NULL
)
}
\arguments{
\item{data}{Data frame.}

\item{treatment}{Binary treatment column name.}

\item{covariates}{Character vector of covariates.}

\item{min_ratio, max_ratio}{Match-count bounds per treated unit.}

\item{caliper}{Caliper on the propensity score (in SD units).}

\item{ps}{Optional pre-computed propensity scores (ignored; retained
for back-compat).}
}
\value{
A list of class \code{morie_match_result}.
}
\description{
Thin wrapper around \code{MatchIt::matchit(method = "nearest",
ratio = max_ratio, min.controls = min_ratio)} which supports
variable-ratio nearest-neighbour matching natively.
}
\examples{
\dontshow{if (requireNamespace("MatchIt", quietly = TRUE)) withAutoprint(\{ # examplesIf}
\donttest{
set.seed(1)
df <- data.frame(y = rnorm(200), d = rbinom(200, 1, 0.4),
                 x1 = rnorm(200), x2 = rnorm(200))
morie_matching_variable_ratio(df, "d", c("x1", "x2"),
                              min_ratio = 1, max_ratio = 3)
}
\dontshow{\}) # examplesIf}
}

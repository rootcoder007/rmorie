% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wasserman_native.R
\name{morie_wasserman_posterior}
\alias{morie_wasserman_posterior}
\title{Grid posterior p(theta | x) (Ch 11, wsmbay)}
\usage{
morie_wasserman_posterior(data, f, prior)
}
\arguments{
\item{data}{Numeric sample, non-empty.}

\item{f}{Density f(x, theta) vectorised in x; NULL = N(theta, 1).}

\item{prior}{List/pair (theta_grid, prior_density); grid strictly
increasing with at least 2 points, density non-negative.}
}
\value{
List with \code{estimate} (posterior mean), \code{posterior},
\code{theta_grid}, \code{evidence}, \code{map_theta}, \code{n}, \code{method}.
}
\description{
The likelihood is accumulated in the log domain at each grid point
and the posterior normalised by trapezoid quadrature, so the
evidence p(x) is reported rather than divided away invisibly.
}
\examples{
g <- seq(-5, 5, length.out = 2001)
round(morie_wasserman_posterior(c(1, 0.5, 1.5), NULL, list(g, rep(1, 2001)))$estimate, 6)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/baysmplr_native.R
\name{morie_baysmplr_mh}
\alias{morie_baysmplr_mh}
\title{Random-walk Metropolis with an isotropic normal proposal}
\usage{
morie_baysmplr_mh(
  log_p,
  x0,
  n_iter,
  e,
  scale = NULL,
  adapt = FALSE,
  target_accept = 0.234
)
}
\arguments{
\item{log_p}{The log density.}

\item{x0}{Starting point.}

\item{n_iter}{Iterations.}

\item{e}{A generator environment from .ghc_rng.}

\item{scale}{Proposal scale, or NULL for the default.}

\item{adapt}{Tune the scale during the first half.}

\item{target_accept}{Target acceptance rate.}
}
\value{
A list with the draws, the acceptance rate and the settings.
}
\description{
The default scale is 2.38 / sqrt(d), the optimal-scaling value for a
product target; adapt then tunes it towards target_accept by
Robbins-Monro on the log scale during the first half of the run,
after which it is frozen so the chain that is kept is homogeneous.
}
\examples{
MU3 <- c(1, -0.5, 2)
Q3 <- matrix(c(2, 0.6, 0.1, 0.6, 1.5, -0.3, 0.1, -0.3, 1.2),
    3, 3, byrow = TRUE)
lp3 <- function(x) {
    dv <- x - MU3
    terms <- numeric(9)
    k <- 1L
    for (i in 1:3) for (j in 1:3) {
        terms[k] <- dv[i] * Q3[i, j] * dv[j]
        k <- k + 1L
    }
    -0.5 * .w3_csum(terms)
}
e <- rmorie:::.ghc_rng(13)
e <- rmorie:::.ghc_rng(13)
e <- rmorie:::.ghc_rng(13)
e <- rmorie:::.ghc_rng(13)
e <- rmorie:::.ghc_rng(13)
e <- rmorie:::.ghc_rng(13)
morie_baysmplr_mh(lp3, c(0, 0, 0), 60L, e)
}
\keyword{internal}

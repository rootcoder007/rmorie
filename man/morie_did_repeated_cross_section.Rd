% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/did.R
\name{morie_did_repeated_cross_section}
\alias{morie_did_repeated_cross_section}
\title{Repeated cross-section DiD (optionally weighted)}
\usage{
morie_did_repeated_cross_section(
  data,
  outcome,
  treatment,
  post,
  covariates = NULL,
  weights = NULL,
  cluster = NULL,
  alpha = 0.05
)
}
\arguments{
\item{data}{A data frame containing the outcome, treatment, post and
any covariate columns.}

\item{outcome}{Name of the outcome column.}

\item{treatment}{Name of the binary (0/1) treatment-group column.}

\item{post}{Name of the binary (0/1) post-period column.}

\item{covariates}{Optional character vector of covariate column names.}

\item{weights}{Optional column of (sampling / survey) weights.}

\item{cluster}{Optional cluster ID column for CR1 standard errors.}

\item{alpha}{Significance level for confidence intervals (default 0.05).}
}
\value{
A list of class results; see \code{\link{morie_did_2x2}}.
}
\description{
Same specification as \code{\link{morie_did_2x2}} but accepts a survey
weight column.  When \code{weights} is supplied, weighted least
squares is used.
}
\examples{
set.seed(1)
n <- 400
d <- rbinom(n, 1, 0.5)
p <- rbinom(n, 1, 0.5)
y <- 1 + 0.3 * d + 0.4 * p + 0.5 * d * p + rnorm(n, sd = 0.5)
df <- data.frame(
  y = y, d = d, post = p,
  w = runif(n, 0.5, 2)
)
res <- morie_did_repeated_cross_section(df, "y", "d", "post",
  weights = "w"
)
res$estimate
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alf3df_native.R
\name{morie_alf3df_augment}
\alias{morie_alf3df_augment}
\title{Recentre on the centroid, then rotate at random}
\usage{
morie_alf3df_augment(x, e)
}
\arguments{
\item{x}{A coordinate matrix, one row per atom.}

\item{e}{A random stream.}
}
\value{
A list with the augmented coordinates and the old centroid.
}
\description{
The centring is exact: the returned centroid is zero to rounding, not
approximately zero. The rotation preserves every interatomic
distance, which is the property that makes the augmentation harmless
to the structure and useful to the model.
}
\examples{
NA_ <- 6L
TRUEX <- matrix(0, NA_, 3L)
e <- rmorie:::.ghc_rng(5)
e <- rmorie:::.ghc_rng(5)
morie_alf3df_augment(TRUEX, e)
}
\keyword{internal}

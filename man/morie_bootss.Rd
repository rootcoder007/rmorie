% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootss_native.R
\name{morie_bootss}
\alias{morie_bootss}
\title{Rao-Wu-Yue rescaled bootstrap for stratified cluster samples}
\usage{
morie_bootss(
  y,
  weights,
  strata,
  clusters,
  statistic = NULL,
  B = 200,
  m = NULL,
  seed = 0
)
}
\arguments{
\item{y}{Numeric observations (ultimate units).}

\item{weights}{Positive survey weights.}

\item{strata}{Stratum label per observation.}

\item{clusters}{Cluster (PSU) label per observation.}

\item{statistic}{Function(y, w) -> scalar; default the weighted
total sum(w * y).}

\item{B}{Number of bootstrap replicates.}

\item{m}{Clusters drawn per stratum (scalar or named list);
default n_h - 1.}

\item{seed}{SplitMix64 seed (mirrors the Python arm).}
}
\value{
A list with elements \code{estimate}, \code{variance},
\code{se}, \code{replicates}, \code{B}, \code{n_strata},
\code{seed}, \code{method}.
}
\description{
Within each stratum draw m_h of the n_h clusters with replacement
and rescale the survey weights by Eq. 3.4:
w* = \link{(1 - r) + r (n_h/m_h) m*} w with r = sqrt(m_h/(n_h - 1));
the bootstrap variance is (1/B) sum_b (theta*_b - theta_hat)^2
(Eq. 3.5).  m_h = n_h - 1 (default) keeps all weights
non-negative; for n_h = 2 a selected cluster doubles and the other
zeroes, as printed in the paper.
}
\references{
Rao, J. N. K., Wu, C. F. J. and Yue, K. (1992). Some
recent work on resampling methods for complex surveys. Survey
Methodology, 18(2), 209-217.  Rao, J. N. K. and Wu, C. F. J.
(1988). Resampling inference with complex survey data. JASA,
83, 231-241.
}

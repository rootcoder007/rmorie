% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/median_frames_dose_native.R
\name{morie_effective_dose}
\alias{morie_effective_dose}
\title{Effective dose with Fieller's interval}
\usage{
morie_effective_dose(
  intercept,
  slope,
  cov,
  level = 0.5,
  alpha = 0.05,
  link = c("probit", "logit"),
  log_scale = TRUE
)
}
\arguments{
\item{intercept, slope}{Fitted coefficients.}

\item{cov}{2x2 covariance matrix.}

\item{level}{Response level to invert at.}

\item{alpha}{Two-sided level.}

\item{link}{"probit" or "logit".}

\item{log_scale}{Were the doses logged?}
}
\value{
A list with \code{ed}, \code{lower}, \code{upper}, \code{fieller_g}, \code{bounded},
\code{se_delta}, and dose-scale versions when \code{log_scale}.
}
\description{
The dose solving \eqn{\mu(\alpha + \beta x) = p} is
\eqn{(g_p - \alpha)/\beta}, a RATIO of estimated quantities, whose
sampling distribution is not normal. Fieller inverts the test
directly. The quantity that decides everything is
\eqn{g = t^2 V_{\beta\beta}/\beta^2}: when \eqn{g \ge 1} the leading
coefficient changes sign and the interval is UNBOUNDED. That is not a
failure to patch over -- if the slope is not distinguishable from
zero the data do not bound the median dose, and a delta-method
interval returning two tidy finite numbers there is lying.
}
\references{
Fieller EC (1954) \emph{JRSS B} 16(2):175-185,
\doi{10.1111/j.2517-6161.1954.tb00159.x}.
}

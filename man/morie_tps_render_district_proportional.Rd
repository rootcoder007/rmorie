% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tps_render.R
\name{morie_tps_render_district_proportional}
\alias{morie_tps_render_district_proportional}
\title{District-level proportional-symbol map}
\usage{
morie_tps_render_district_proportional(
  polys,
  count_col,
  max_radius_km = 3,
  outfile = NULL
)
}
\arguments{
\item{polys}{data.frame with one row per polygon, including a
\code{centroid_lat} / \code{centroid_lon} (or \code{LAT_WGS84} / \code{LONG_WGS84})
and the count column.}

\item{count_col}{Name of the numeric column.}

\item{max_radius_km}{Largest symbol radius in km.}

\item{outfile}{Optional output path.}
}
\value{
ggplot object or invisible NULL.
}
\description{
Renders one centroid-anchored circle per polygon row, sized
proportionally to \code{count_col}.  Useful for showing per-district
incident counts without colour-coding the polygons themselves.
}
\examples{
set.seed(1)
polys <- data.frame(centroid_lat = runif(6, 43.62, 43.82),
                    centroid_lon = runif(6, -79.55, -79.20),
                    n = c(5, 12, 3, 8, 20, 9))
out <- morie_tps_render_district_proportional(polys, "n",
                                              outfile = tempfile(fileext = ".png"))
}

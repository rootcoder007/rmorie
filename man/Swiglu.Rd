% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/swiglu.R
\name{Swiglu}
\alias{Swiglu}
\title{SwiGLU gated activation}
\usage{
Swiglu(
  y,
  x = NULL,
  W = NULL,
  V = NULL,
  b = NULL,
  c = NULL,
  beta = 1,
  W2 = NULL
)
}
\arguments{
\item{y}{the input x (first slot, for signature stability).}

\item{x}{the input; wins over y.}

\item{W, V}{gate and value projections, input units in rows.}

\item{b, c}{biases.}

\item{beta}{the Swish parameter.}

\item{W2}{optional output projection.}
}
\value{
list: estimate, out, gate, ffn, beta, method.
}
\description{
Shazeer (2020), GLU variants improve transformer, arXiv:2002.05202
(FETCHED), prints the family verbatim: ReGLU = max(0, xW + b) * (xV +
c), GEGLU = GELU(xW + b) * (xV + c), SwiGLU = Swish_beta(xW + b) * (xV
\itemize{
\item c), with Swish_beta(z) = z sigma(beta z) (Ramachandran et al. 2017)
and GELU(z) = z Phi(z) (Hendrycks and Gimpel 2016).  The paper's
FFN_SwiGLU reduces the hidden width by 2/3 to match the parameter count
of an ordinary FFN; that adjustment is NOT applied here, because the
caller supplies W and V and has already fixed the width.
}
}
\examples{
Swiglu(c(1, -1), W = diag(2), V = diag(2))$out
}
\keyword{internal}

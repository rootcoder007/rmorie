% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/agtmpd.R
\name{Tempdecay}
\alias{Tempdecay}
\title{AlphaZero temperature decay for move selection}
\usage{
Tempdecay(move_count, threshold = 30L, N = NULL)
}
\arguments{
\item{move_count}{zero-based index of the move about to be played.}

\item{threshold}{number of opening moves played at tau = 1.}

\item{N}{optional root visit counts; the policy pi is then returned.}
}
\value{
list: estimate (tau), tau, greedy, pi, threshold, method.
}
\description{
Silver et al. (2018), arXiv:1712.01815 (FETCHED), and Silver et al.
(2017), Nature 550, 354-359: during self-play a move is sampled in
proportion to N(s,a)^(1/tau), with tau = 1 for the first \code{threshold}
moves (AlphaGo Zero uses 30) and tau -> 0 thereafter, which makes the
selection greedy.  Passing tau to zero would divide by zero, so the
greedy branch is taken directly; ties break to the lowest index.
}
\examples{
Tempdecay(5, 30, c(10, 20, 5))$pi
}
\keyword{internal}

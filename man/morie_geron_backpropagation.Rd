% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geron_ml_native.R
\name{morie_geron_backpropagation}
\alias{morie_geron_backpropagation}
\title{Backpropagation through a feed-forward net (Geron Ch 9, morie.fn hmbp)}
\usage{
morie_geron_backpropagation(X, y, weights, activations, loss = "mse")
}
\arguments{
\item{X}{Inputs (n, d0).}

\item{y}{Targets or class labels.}

\item{weights}{List of matrices or list(W, b) pairs.}

\item{activations}{Character vector, one per layer.}

\item{loss}{"mse" or "ce".}
}
\value{
List with \code{grads_W}, \code{grads_b}, \code{deltas}, \code{loss}, \code{output}.
}
\description{
delta_L = grad_a L * phi'(z_L); delta_l = \verb{(W_\{l+1\} delta_\{l+1\})} phi'(z_l).
\code{weights} entries are either a matrix W or a list(W, b). Class labels
for loss = "ce" are 0-based, as in Python.
}
\examples{
W1 <- matrix(c(1, -1), 1, 2)
W2 <- matrix(c(2, 3), 2, 1)
morie_geron_backpropagation(matrix(c(1, 2), ncol = 1), matrix(c(0, 
    1), ncol = 1), list(W1, W2), c("relu", "identity"))
}

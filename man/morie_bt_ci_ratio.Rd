% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/btvol_native.R
\name{morie_bt_ci_ratio}
\alias{morie_bt_ci_ratio}
\title{Percentile bootstrap CI for a ratio}
\usage{
morie_bt_ci_ratio(
  x,
  y,
  stat_x = NULL,
  stat_y = NULL,
  B = 2000L,
  alpha = 0.05,
  seed = 0,
  paired = FALSE
)
}
\arguments{
\item{x, y}{the two samples.}

\item{stat_x, stat_y}{numerator and denominator statistics; the
mean when \code{NULL}.}

\item{B}{replicates, at least 100.}

\item{alpha}{miss probability.}

\item{seed}{resampling seed.}

\item{paired}{resample pairs rather than independently.}
}
\value{
list: ratio, ci, replicates, se,
small_denominator_fraction, paired, B, alpha, n_x, n_y, method.
}
\description{
Davison-Hinkley (1997): read the interval off the resampled
ratios. Ratios are where the delta method quietly fails (skewness,
small denominators, Fieller half-lines); the percentile interval
needs none of that but inherits the caveat that a denominator
resampling near zero produces wild replicates -- the
small-denominator fraction is reported. \code{paired} is a MODELLING
statement: paired data resample as pairs to keep the dependence,
independent samples separately, and getting it wrong biases the
width in whichever direction the dependence points.
}
\examples{
morie_bt_ci_ratio(stats::rnorm(50, 2), stats::rnorm(50, 1),
  B = 200
)$ci
}
\references{
Davison and Hinkley (1997), \emph{Bootstrap Methods and
their Application}, Chs. 2-3, 5; Fieller (1954).
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prsLL_native.R
\name{.prsLL_parse_rd}
\alias{.prsLL_parse_rd}
\title{.prsLL_parse_rd}
\usage{
.prsLL_parse_rd(g, table, toks, A, pos)
}
\arguments{
\item{g}{A list; the body reads \code{$rules} from it.}

\item{table}{Passed to \code{.prsLL_pick}.}

\item{toks}{A vector; indexed elementwise.}

\item{A}{Passed to \code{.prsLL_pick}.}

\item{pos}{Numeric; combined arithmetically in the body.}
}
\value{
The value of \code{list}.
}
\description{
A step of the prsLL_native implementation. Called by \code{.prsLL_parse}.
See the file header for the source the module follows.
source it follows.
}
\examples{
# The expression grammar of Aho, Lam, Sethi & Ullman (2nd ed., 4.4.2):
#   E -> T E';  E' -> + T E' | eps;  T -> F T';  T' -> * F T' | eps;
#   F -> ( E ) | id
rules <- list(
  list("E",  c("T", "Ep")),
  list("Ep", c("+", "T", "Ep")),
  list("Ep", character(0)),
  list("T",  c("F", "Tp")),
  list("Tp", c("*", "F", "Tp")),
  list("Tp", character(0)),
  list("F",  c("(", "E", ")")),
  list("F",  "id")
)
g <- .prsLL_grammar(rules, start = "E")
t <- .prsLL_ll1_table(g)
res <- .prsLL_parse_rd(g, t$table, c("id", "*", "id", "$"), "E", 0L)
.prsLL_linearise(res[[1]])
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/splfun.R
\name{splfun}
\alias{splfun}
\title{L-function, the variance-stabilised K-function}
\usage{
splfun(
  points,
  lambda_est = NULL,
  r = NULL,
  region = NULL,
  correction = "border"
)
}
\arguments{
\item{points}{Event coordinates (n by 2).}

\item{lambda_est}{Intensity; estimated when NULL.}

\item{r}{Distances at which to evaluate.}

\item{region}{c(xmin, ymin, xmax, ymax) or vertices.}

\item{correction}{"border" or "none".}
}
\value{
Named list: r, l, l_minus_r, k, lambda_est.
}
\description{
L(h) = sqrt(K(h) / pi). Under CSR K(h) = pi h^2, so L(h) = h exactly
and L(h) - h is a horizontal reference line at zero. The book
recommends plotting L(h) - h against h: clustering appears as positive
values at short distances, regularity as negative ones.
}
\examples{
pts <- matrix(runif(400), 200, 2) * 10
splfun(pts, r = seq(0.1, 1.5, length.out = 8), region = c(0, 0, 10, 10))
}
\references{
Schabenberger & Gotway (2005), Sec 3.4.2, p. 103.
}

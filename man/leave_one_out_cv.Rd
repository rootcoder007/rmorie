% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap_methods.R
\name{leave_one_out_cv}
\alias{leave_one_out_cv}
\title{Leave-one-out cross-validation}
\usage{
leave_one_out_cv(X, y, model_fn, score_fn)
}
\arguments{
\item{X}{Numeric matrix or data.frame of predictors.}

\item{y}{Numeric or factor outcome vector aligned with rows of \code{X}.}

\item{model_fn}{Function \verb{(X, y) -> fitted-model} used on each
training fold.}

\item{score_fn}{Function \verb{(y_true, y_pred) -> numeric} returning a
single performance metric.}
}
\value{
A \code{morie_cv_result}.
}
\description{
Convenience wrapper that calls \code{.boot_cross_validate()}
with \code{n_folds = length(y)}. \code{rsample::loo_cv} is the
tidymodels equivalent (cross-referenced).
}
\examples{
if (requireNamespace("boot", quietly = TRUE) && requireNamespace("ranger", quietly = TRUE)) {
  set.seed(1)
  X <- matrix(rnorm(40), ncol = 2)
  y <- rnorm(20)
  model_fn <- function(Xt, yt) stats::lm.fit(cbind(1, Xt), yt)
  score_fn <- function(yt, yp) mean((yt - yp)^2)
  predict.lm_lite2 <- function(object, newdata, ...) {
    drop(cbind(1, newdata) \%*\% object$coef)
  }
  registerS3method("predict", "lm_lite2", predict.lm_lite2)
  res <- leave_one_out_cv(X, y,
    model_fn = function(Xt, yt) {
      fit <- stats::lm.fit(cbind(1, Xt), yt)
      structure(list(coef = fit$coefficients), class = "lm_lite2")
    },
    score_fn = score_fn
  )
  str(res, max.level = 1)
}
}
\seealso{
\code{rsample::loo_cv}, \code{caret::trainControl}.
}

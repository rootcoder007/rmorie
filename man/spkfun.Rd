% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spkfun.R
\name{spkfun}
\alias{spkfun}
\title{Ripley's K-function}
\usage{
spkfun(
  points,
  lambda_est = NULL,
  r = NULL,
  region = NULL,
  correction = "border"
)
}
\arguments{
\item{points}{Event coordinates (n by 2).}

\item{lambda_est}{Intensity; estimated by eq (3.8) when NULL.}

\item{r}{Distances at which to evaluate; a default grid when NULL.}

\item{region}{c(xmin, ymin, xmax, ymax) or vertices; bounding box of
\code{points} when NULL.}

\item{correction}{"border" (default) or "none" (naive, biased).}
}
\value{
Named list: r, k, k_csr, lambda_est, correction.
}
\description{
K(h) = (2 pi / lambda^2) integral_0^h x lambda_2(x) dx. For a simple
process lambda K(h) is the expected number of EXTRA events within
distance h of an arbitrary event. Under the homogeneous Poisson
process that is lambda pi h^2, so K(h) = pi h^2 -- the reference
curve every CSR comparison is made against. Clustering shows as
K(h) > pi h^2 at short lags, regularity as K(h) < pi h^2.
}
\examples{
pts <- matrix(runif(400), 200, 2) * 10
spkfun(pts, r = seq(0.1, 1.5, length.out = 8), region = c(0, 0, 10, 10))
}
\references{
Schabenberger & Gotway (2005), Secs 3.4.1-3.4.2,
eqs (3.7)-(3.8), pp. 101-102.
}

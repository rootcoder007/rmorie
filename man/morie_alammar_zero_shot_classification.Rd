% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alammar_llm_native.R
\name{morie_alammar_zero_shot_classification}
\alias{morie_alammar_zero_shot_classification}
\alias{morie_alammar_t5_classify}
\alias{morie_alammar_llm_as_judge}
\alias{morie_alammar_output_verification}
\title{Zero-shot, T5 and judge orchestration around caller functions}
\usage{
morie_alammar_zero_shot_classification(
  text,
  candidate_labels,
  nli_model,
  hypothesis_template = "This example is about \%s."
)

morie_alammar_t5_classify(input_text, label_tokens, model, prefix = "")

morie_alammar_llm_as_judge(responses, rubric, judge_model, n_samples = 1)

morie_alammar_output_verification(response, criteria, verifier_model)
}
\arguments{
\item{text}{Input.}

\item{candidate_labels}{Labels.}

\item{nli_model}{function(premise, hypothesis) -> score.}

\item{hypothesis_template}{Format string with one \%s.}

\item{input_text}{Input.}

\item{label_tokens}{Closed label set.}

\item{model}{function(input, label) -> log-probability.}

\item{prefix}{Optional task prefix.}

\item{responses}{Character vector.}

\item{rubric}{Judge rubric.}

\item{judge_model}{function(rubric, response, sample_index) -> score.}

\item{n_samples}{Judge samples per response.}

\item{response}{Text under test.}

\item{criteria}{Character vector.}

\item{verifier_model}{function(response, criterion) -> "PASS"|"FAIL".}
}
\value{
A list with \code{probabilities}, \code{predicted_label}, \code{entailment_scores}, \code{estimate}, \code{n}, \code{method}.
}
\description{
Zero-shot, T5 and judge orchestration around caller functions
}
\examples{
h <- morie_alammar_classification_head(c(1, 2), rbind(c(1, 0),
    c(0, 1)), c(0, 0))
nli <- function(p, h) if (grepl("sport", h) && grepl("goal",
    p)) 5 else 0
morie_alammar_zero_shot_classification("a late goal won it",
    c("sport", "finance"), nli)
i <- morie_alammar_infonce_loss(c(1, 0), c(1, 0), rbind(c(0,
    1), c(-1, 0)), 0.1)
morie_alammar_t5_classify("good film", c("positive", "negative"),
    function(i, l) {
        if (l == "positive" && grepl("good", i))
            0
        else -3
    })
s <- morie_alammar_sampling_decoding(matrix(rep(c(1, 0), 10),
    10, 2, byrow = TRUE), seed = 7)
judge <- function(r, resp, s) {
    if (resp == "a")
        c(1, 3)[s + 1]
    else c(2, 2)[s + 1]
}
morie_alammar_llm_as_judge(c("a", "b"), "rubric", judge, n_samples = 2)
verifier <- function(response, criterion)
  if (grepl(criterion, response, fixed = TRUE)) "PASS" else "FAIL"
r <- morie_alammar_output_verification("The answer is 42.",
                                       c("42", "answer"), verifier)
str(r, max.level = 1)
}
\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dag_native.R
\name{morie_dag_refute}
\alias{morie_dag_refute}
\title{Refute an estimated causal effect}
\usage{
morie_dag_refute(
  dag,
  data,
  method = c("placebo_treatment", "random_common_cause", "data_subset"),
  estimator = "backdoor.linear",
  n_reps = 20L,
  seed = 42L
)
}
\arguments{
\item{dag}{A \code{morie_dag}.}

\item{data}{The data frame used for estimation.}

\item{method}{One of \code{"placebo_treatment"},
\code{"random_common_cause"}, \code{"data_subset"}.}

\item{estimator}{Passed through to \code{\link[=morie_dag_estimate]{morie_dag_estimate()}}.}

\item{n_reps}{Number of refutation replications (default 20).}

\item{seed}{Random seed.}
}
\value{
List with \code{original}, \code{refuted} (mean over reps), \code{reps},
\code{passed} (logical heuristic), \code{method}.
}
\description{
DoWhy-style robustness checks, all native: a placebo (permuted)
treatment should give an effect near zero; adding a random common
cause or re-estimating on subsets should leave the estimate stable.
}
\examples{
set.seed(1)
z <- rnorm(400)
x <- rbinom(400, 1, plogis(z))
y <- 0.8 * x + z + rnorm(400)
df <- data.frame(z = z, x = x, y = y)
g <- morie_dag(c("z -> x", "z -> y", "x -> y"), "x", "y")
morie_dag_refute(g, df,
  method = "placebo_treatment",
  estimator = "backdoor.linear", n_reps = 5
)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lottosi.R
\name{Ppssamp}
\alias{Ppssamp}
\title{Draw n units with probability proportional to size, and estimate.}
\usage{
Ppssamp(z, y, n, seed = 1)
}
\arguments{
\item{z}{Size measure of every unit, strictly positive.}

\item{y}{Value of the variable of interest for every unit.}

\item{n}{Number of draws (with replacement), n >= 2.}

\item{seed}{Seed for the pinned generator.}
}
\value{
List with \code{index}, \code{p}, \code{estimate}, \code{se},
\code{true_total}, \code{n}, \code{N}.
}
\description{
The "lottery" or cumulative-total method, drawing WITH replacement, so
the same unit can legitimately appear twice. Selection uses a pinned
Lehmer generator so the two language arms draw the SAME units.
Indices are one-based.
}
\details{
Formula: p_i = z_i / sum_j z_j; Yhat = (1/n) sum_i y_i / p_i;
v(Yhat) = sum_i (y_i/p_i - Yhat)^2 / (n (n - 1))
}
\examples{
Ppssamp(z = c(1, 2, 3, 4, 5, 6, 7, 8), y = c(1, 2, 3, 4, 5, 6, 7, 8), n = 5L)
}
\references{
Cochran (1977), Sampling Techniques, 3rd edition, Chapter
9A. Chapter 9A was NOT in the scanned excerpt available to this
batch, so the standard published form (Hansen & Hurwitz 1943, Annals
of Mathematical Statistics 14(4), 333-362) is used.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evt_native.R
\name{morie_evt_gpd_pwm}
\alias{morie_evt_gpd_pwm}
\title{PWM estimator of the generalised Pareto distribution}
\usage{
morie_evt_gpd_pwm(x, threshold = NULL)
}
\arguments{
\item{x}{raw sample, or excesses when \code{threshold} is \code{NULL}.}

\item{threshold}{when given, excesses are formed here.}
}
\value{
list: sigma, k_hosking, xi, n_excesses, threshold,
mean_excess, reliable, return_level_fn, method.
}
\description{
Hosking and Wallis (1987): \code{k = l1/l2 - 2}, \code{sigma = l1 (1 + k)}
on the excesses' L-moments; Hosking's k is minus the GPD shape xi.
PWM over ML because it has lower bias for -0.5 < k < 0.5 and none
of ML's convergence failures at exceedance sample sizes (their
Sec. 4). Reliable only for k > -0.5 (xi < 0.5, finite variance);
outside that the output carries a warning, not a silently
untrustworthy number.
}
\examples{
morie_evt_gpd_pwm(stats::rexp(200))$xi
}
\references{
Hosking and Wallis (1987), \emph{Technometrics} 29:339-349.
}

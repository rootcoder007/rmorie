% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fairness_metrics.R, R/frns_metrics.R
\name{morie_fairness_demographic_parity}
\alias{morie_fairness_demographic_parity}
\title{Demographic Parity Gap (difference in favourable-outcome rates)}
\usage{
morie_fairness_demographic_parity(
  y_pred,
  group,
  privileged = NULL,
  favorable = 1
)

morie_fairness_demographic_parity(
  y_pred,
  group,
  privileged = NULL,
  favorable = 1
)
}
\arguments{
\item{y_pred}{Vector of decisions/assignments, one per individual.}

\item{group}{Vector of protected-attribute values (e.g. race).}

\item{privileged}{The reference group. If \code{NULL} (default) the
highest-rate group is used and a warning is attached.}

\item{favorable}{The value of \code{y_pred} counted as the favourable
outcome (default \code{1}).}
}
\value{
A \code{morie_fairness_result}; headline value is the
largest absolute gap across groups.

A named list: \code{value} (largest absolute gap), \code{gaps}, \code{rates},
\code{privileged}, \code{warnings}, \code{interpretation}.
}
\description{
The gap is \code{rate(group) - rate(privileged)}. Demographic
parity holds when every group receives favourable outcomes at the
same rate, i.e. all gaps are zero. Unlike the disparate-impact
ratio this additive form is well defined even when the privileged
rate is zero.

The additive difference in favourable-outcome rates,
\code{rate(group) - rate(privileged)}. Demographic parity holds when every
group receives favourable outcomes at the same rate.
}
\examples{
pred <- c(1, 1, 1, 1, 0, 0, 0, 1, 0, 0)
race <- c("A","A","A","A","A","B","B","B","B","B")
morie_fairness_demographic_parity(pred, race, privileged = "A")$value
pred <- c(1, 1, 1, 1, 0, 0, 0, 1, 0, 0)
race <- c(rep("A", 5), rep("B", 5))
res <- morie_fairness_demographic_parity(pred, race, privileged = "A")
res$value # -0.6  (group B rate 0.2 minus group A rate 0.8)
}

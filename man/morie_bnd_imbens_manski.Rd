% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bnd_native.R
\name{morie_bnd_imbens_manski}
\alias{morie_bnd_imbens_manski}
\title{Imbens-Manski confidence interval for a partially identified parameter}
\usage{
morie_bnd_imbens_manski(
  lower_hat,
  upper_hat,
  se_lower,
  se_upper,
  n,
  alpha = 0.05
)
}
\arguments{
\item{lower_hat, upper_hat}{estimated bounds.}

\item{se_lower, se_upper}{their standard deviations on the
\code{sqrt(n)} scale, i.e. \code{sqrt(n) * se(bound)}.}

\item{n}{sample size.}

\item{alpha}{miss probability.}


}
\value{
list: ci, c, z_one_sided, z_two_sided, delta, covers,
stoye_caveat, n, method.
}
\description{
\verb{\[lower - c s_l/sqrt(n), upper + c s_u/sqrt(n)\]} with \code{c} solving
\verb{pnorm(c + sqrt(n) delta / max(s_l, s_u)) - pnorm(-c) = 1 - alpha}.
}
\details{
The construction exists because covering the identified SET and
covering the true PARAMETER are different targets. The parameter
can only be missed at one end at a time, so a wide identified set
needs only the one-sided \code{z}; as the set collapses to a point the
problem becomes point identification and the two-sided \code{z} is what
comes out. \code{c} interpolates and attains both limits exactly.
Stoye (2009) showed the interpolation needs \code{delta}'s own sampling
error negligible or the bound superefficient; the output says so.
}
\examples{
morie_bnd_imbens_manski(0.2, 0.8, 1, 1, 400)$c
}
\references{
Imbens and Manski (2004), \emph{Econometrica} 72:1845-1857,
Eq. (6); Stoye (2009), \emph{Econometrica} 77:1299-1315.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tps_csi.R
\name{morie_tps_analyze_csi_from_dataframes}
\alias{morie_tps_analyze_csi_from_dataframes}
\title{Toronto CSI per-year + per-ward from a named list of TPS data.frames}
\usage{
morie_tps_analyze_csi_from_dataframes(
  dfs,
  year_col = "OCC_YEAR",
  hood_col = "HOOD_158",
  variant = c("total", "violent")
)
}
\arguments{
\item{dfs}{Named list of TPS data.frames.  Keys outside of
\code{MORIE_TPS_CSI_CATEGORIES()} are ignored.}

\item{year_col}{Year column name (default "OCC_YEAR").}

\item{hood_col}{Neighbourhood column name (default "HOOD_158").}

\item{variant}{One of "total" or "violent" (default "total").}
}
\value{
A \code{morie_tps_result} named list carrying \code{by_year}
and \code{by_hood} data.frames in \code{payload}.
}
\description{
High-level orchestration: takes a named list \code{dfs} of TPS
open-data data.frames (one per CSI category) and returns both the
per-year and per-ward CSI as a single rich-result object.
}
\examples{
set.seed(1)
df <- data.frame(OCC_YEAR = rep(2014:2023, each = 30),
                 OCC_MONTH = sample(month.name, 300, TRUE),
                 HOOD_158 = sample(sprintf("\%03d", 1:20), 300, TRUE),
                 LAT_WGS84 = runif(300, 43.6, 43.8),
                 LONG_WGS84 = runif(300, -79.5, -79.2))
res <- try(morie_tps_analyze_csi_from_dataframes(list(Assault = df)))
if (!inherits(res, "try-error")) str(res, max.level = 1)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fzt51.R
\name{Kerngofeq}
\alias{Kerngofeq}
\alias{morie_fauzi_thm5_1_naive_kernel_equiv}
\title{Equivalence of the naive kernel and empirical goodness-of-fit statistics (Theorem 5.1)}
\usage{
Kerngofeq(ks_emp, ks_kernel, cvm_emp, cvm_kernel, tol = 0.05)

morie_fauzi_thm5_1_naive_kernel_equiv(
  ks_emp,
  ks_kernel,
  cvm_emp,
  cvm_kernel,
  tol = 0.05
)
}
\arguments{
\item{ks_emp, ks_kernel}{The empirical and naive-kernel KS statistics.}

\item{cvm_emp, cvm_kernel}{The empirical and naive-kernel CvM statistics.}

\item{tol}{Tolerance against which the differences are reported.}


}
\value{
Named list with \code{ksdiff}, \code{cvmdiff}, \code{close}, \code{tol}, \code{method}.
}
\description{
Theorem 5.1: under \code{H0: F_X = F},
\deqn{|KS_n - \widehat{KS}| \to_p 0\quad\text{and}\quad |CvM_n - \widehat{CvM}| \to_p 0,}{|KS_n - KShat| ->_p 0 and |CvM_n - CvMhat| ->_p 0,}
where the hatted statistics use the NAIVE kernel distribution function
estimator of (5.3)-(5.4).
}
\details{
The consequence Sec. 5.1 draws is practical: the smoothed statistics have
the very same Kolmogorov and Cramer-von Mises limiting distributions, so the
SAME critical values are used. Smoothing is not a new test, it is a
better-calibrated computation of the same test.

Reports the two differences against a caller-supplied tolerance. This is a
convergence DIAGNOSTIC, not a hypothesis test: "converges in probability to
zero" is a statement about a sequence, and no single sample can confirm or
refute it. Hence \code{close}, not a p-value.
}
\examples{
Kerngofeq(ks_emp = 0.20, ks_kernel = 0.21, cvm_emp = 0.30, cvm_kernel = 0.31)
}
\references{
Fauzi and Maesono (2023), Theorem 5.1.
}
\keyword{internal}

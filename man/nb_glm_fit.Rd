% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deseq2_native.R
\name{nb_glm_fit}
\alias{nb_glm_fit}
\title{Negative binomial GLM by ridge-penalised IRLS}
\usage{
nb_glm_fit(
  K,
  X,
  alpha,
  s = NULL,
  lam = NULL,
  max_iter = 100L,
  tol = 1e-08,
  beta0 = NULL
)
}
\arguments{
\item{K}{Sample-by-1 response vector.}

\item{X}{Design matrix.}

\item{alpha}{Dispersion.}

\item{s}{Optional size factors.}

\item{lam}{Optional ridge vector (one per coefficient).}

\item{max_iter}{Maximum iterations.}

\item{tol}{Convergence tolerance on the parameter step.}

\item{beta0}{Optional starting coefficients.}
}
\value{
A list with \code{beta}, \code{mu}, \code{sigma},
\code{converged}, \code{n_iter}.
}
\description{
Update is the paper's own:
\code{beta <- (X' W X + lambda I)^{-1} X' W z} with
\code{z_j = log(mu_j / s_j) + (K_j - mu_j) / mu_j} and
\code{w_jj = 1 / (1/mu_j + alpha)}.
}
\examples{
set.seed(1)
m <- 6
X <- cbind(1, rep(c(0, 1), each = 3))
K <- c(rpois(3, 50), rpois(3, 90))
r <- nb_glm_fit(K, X, alpha = 0.1)
str(r, max.level = 1)
}
\keyword{internal}

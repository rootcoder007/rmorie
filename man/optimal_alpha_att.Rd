% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tmlefp_native.R
\name{optimal_alpha_att}
\alias{optimal_alpha_att}
\title{Theorem 5.3: the one-sided threshold for the effect on the treated}
\usage{
optimal_alpha_att(pscore, treated, tol = 1e-12, max_iter = 200)
}
\arguments{
\item{pscore}{Propensity scores.}

\item{treated}{Treatment indicators.}

\item{tol}{Convergence tolerance.}

\item{max_iter}{Maximum iterations.}
}
\value{
A list with \code{alpha_t}, \code{keep}, \code{trim},
\code{no_trimming}.
}
\description{
A*_t = \{x : e(x) <= alpha_t\}, with alpha_t = 1 (no trimming) when
sup_x 1 / (1 - e(x)) <= 2 E[1 / (1 - e(X)) | W = 1], and otherwise
solving 1 / (1 - alpha_t) = 2 E[1 / (1 - e(X)) | W = 1, e(X) <= alpha_t].
Homoskedasticity only, as in the paper.
}
\examples{
set.seed(1)
e <- runif(100, 0.02, 0.98)
w <- rbinom(100, 1, e)
str(optimal_alpha_att(e, w), max.level = 1)
}
\references{
Crump, R. K. et al. (2009). Theorem 5.3.
}
\keyword{internal}

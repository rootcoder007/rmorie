% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/esl_native2.R
\name{morie_esl_svm_kernel}
\alias{morie_esl_svm_kernel}
\title{Kernel support vector machine}
\usage{
morie_esl_svm_kernel(
  X,
  y,
  C = 1,
  kernel = "rbf",
  gamma = NULL,
  degree = 3,
  coef0 = 1,
  newdata = NULL,
  tol = 0.001,
  max_passes = 50L,
  seed = 0L
)
}
\arguments{
\item{X}{Training predictors, n by p.}

\item{y}{Labels; any two distinct values, mapped internally to -1/+1.}

\item{C}{Box constraint, positive.}

\item{kernel}{"rbf", "linear", "poly" or "sigmoid".}

\item{gamma}{Kernel width for rbf/sigmoid; defaults to \code{1/p}.}

\item{degree, coef0}{Polynomial and sigmoid kernel parameters.}

\item{newdata}{Points to classify; defaults to \code{X}.}

\item{tol, max_passes, seed}{SMO controls.}



}
\value{
List with \code{alpha}, \code{b}, \code{support} (1-based indices), \code{decision},
\code{class}, \code{accuracy}, and \code{dual_gap_check} (which the equality constraint
forces to zero).
}
\description{
Fits the dual soft-margin SVM by sequential minimal optimisation. Only the
kernel enters, never the feature map. Observations with \code{alpha = 0} do not
appear in the decision function at all, which is what makes the solution
sparse in the training set; small \code{C} gives a wide margin and many support
vectors, large \code{C} a narrow one and few.
}
\examples{
set.seed(1)
X <- rbind(matrix(rnorm(120, -2), ncol = 2), matrix(rnorm(120, 2), ncol = 2))
y <- rep(c(-1, 1), each = 60)
fit <- morie_esl_svm_kernel(X, y, C = 1, kernel = "rbf", seed = 1L)
abs(fit$dual_gap_check) < 1e-9
}
\references{
Platt, J. (1998). Sequential minimal optimization. MSR-TR-98-14.
Hastie, T., et al. (2009). ESL (2nd ed.), Sec 12.3. Springer.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stlAn_native.R
\name{morie_stlAn}
\alias{morie_stlAn}
\title{STL decomposition with MAD outlier flags}
\usage{
morie_stlAn(
  x,
  period,
  s_window = 7L,
  k = 3,
  inner = 2L,
  outer = 0L,
  t_window = NULL,
  l_window = NULL
)
}
\arguments{
\item{x}{Numeric series.}

\item{period}{Number of observations per cycle, at least 2; the
series needs at least two full cycles.}

\item{s_window}{Seasonal loess span \eqn{n_s}, forced odd.}

\item{k}{Threshold in robust standard deviations, default 3.}

\item{inner}{Inner-loop iterations.}

\item{outer}{Robustness iterations; 0 gives the non-robust fit.
Both routes the paper defines are available.}

\item{t_window, l_window}{Trend and low-pass spans; \code{NULL} uses
the Sec. 3.4 defaults.}
}
\value{
A list with \code{seasonal}, \code{trend},
\code{remainder}, \code{outliers} (1-based indices),
\code{threshold}, \code{sigma_hat}, \code{s_window},
\code{t_window}, \code{l_window}, \code{estimate}, \code{n},
\code{method}.
}
\description{
Runs \code{\link{morie_stl_decompose}} and flags observations whose
remainder lies more than \code{k} robust standard deviations from
the remainder median, the scale being \eqn{1.4826\,\mathrm{MAD}}.
}
\references{
Cleveland, R. B. et al. (1990). STL. Journal of
Official Statistics, 6(1), 3-73.
}
\examples{
set.seed(1)
x <- sin(2 * pi * (1:60) / 12) + 0.02 * (1:60) + rnorm(60) * 0.1
morie_stlAn(x, period = 12)
}

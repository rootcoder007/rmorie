% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fzbfkf.R
\name{Bfkdf}
\alias{Bfkdf}
\alias{morie_fauzi_bdfree_kdfe_test}
\title{Boundary-free kernel distribution function estimator (Eq. 5.5)}
\usage{
Bfkdf(x, grid = NULL, ginv = NULL, h = NULL)

morie_fauzi_bdfree_kdfe_test(x, grid = NULL, ginv = NULL, h = NULL)
}
\arguments{
\item{x}{Sample, inside the support \code{Omega}.}

\item{grid}{Evaluation points; defaults to the sorted sample.}

\item{ginv}{The inverse transformation; defaults to \code{log}, i.e. \code{g = exp}
and \verb{Omega = (0, Inf)}.}

\item{h}{Bandwidth on the TRANSFORMED scale; defaults to the
distribution-function rule applied to \code{ginv(x)}.}
}
\value{
Named list with \code{estimate}, \code{grid}, \code{h}, \code{n}, \code{method}.
}
\description{
Eq. (5.5):
\deqn{\tilde F_X(x) = \frac{1}{n}\sum_i W\Big(\frac{g^{-1}(x) - g^{-1}(X_i)}{h}\Big),\quad x\in\Omega.}{Ftilde(x) = (1/n) sum_i W((g^-1(x) - g^-1(X_i))/h), x in Omega.}
}
\details{
It looks like nothing more than substituting \code{g^-1} into the naive
estimator, and that is what it is. The reason it WORKS is the
change-of-variable property of a distribution function: if \code{Y = g^-1(X)}
then \code{F_X(x) = F_Y(g^-1(x))} exactly, for increasing \code{g}. No Jacobian.

That is why the trick is available here and not to a density estimator,
where the same substitution needs the \verb{1/g'(g^-1(x))} factor of (5.9).
Sec. 5.2 puts it plainly: the property "cannot always be done to other
probability-related functions".

Estimating on the transformed scale, where the support is the whole line,
means a symmetric kernel puts no mass outside \code{Omega} after mapping back --
the \code{O(h)} boundary bias never arises, and Theorem 5.2 gets \code{O(h^2)}
everywhere including at the edge.
}
\examples{
Bfkdf(c(0.5, 1, 1.5, 2, 3), grid = 1.5)
}
\references{
Fauzi and Maesono (2023), Eq. (5.5).
}
\keyword{internal}

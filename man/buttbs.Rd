% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/signal.R
\name{buttbs}
\alias{buttbs}
\title{Butterworth bandstop (notch) filter}
\usage{
buttbs(x, fs, low = 59, high = 61, order = 4L)
}
\arguments{
\item{x}{Numeric vector.}

\item{fs}{Sampling frequency (Hz).}

\item{low}{Lower cutoff (Hz, default 59).}

\item{high}{Upper cutoff (Hz, default 61).}

\item{order}{Filter order (default 4).}
}
\value{
List with \code{filtered} (numeric vector), \code{fs}, \code{order}, \code{name}.
}
\description{
Zero-phase Butterworth bandstop filter. Default 59--61 Hz removes North-
American AC mains hum (60 Hz); use 49--51 Hz for European mains.
}
\examples{
\donttest{
if (requireNamespace("signal", quietly = TRUE)) {
  set.seed(1)
  t <- seq(0, 1, length.out = 1000)
  x <- sin(2 * pi * 10 * t) + sin(2 * pi * 60 * t)
  y <- buttbs(x, fs = 1000) # remove 60 Hz mains
  length(y$filtered)
}
}
}

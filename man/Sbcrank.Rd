% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sbcrk.R
\name{Sbcrank}
\alias{Sbcrank}
\title{Rank statistics for simulation-based calibration, and a uniformity test.}
\usage{
Sbcrank(prior_draw, post_draws, bins = NULL)
}
\arguments{
\item{prior_draw}{One prior draw per replication.}

\item{post_draws}{Matrix of posterior draws, one row per replication.}

\item{bins}{Number of histogram bins; must divide L + 1.}
}
\value{
List with \code{rank}, \code{histogram}, \code{expected},
\code{statistic}, \code{p_value}, \code{df}, \code{bins}, \code{J},
\code{L}.
}
\description{
The SHAPE of the departure names the fault: U means the posterior is
too narrow, a hump too wide, a slope biased. The bin count must divide
L + 1 exactly or the expected counts are unequal.
}
\details{
Formula: rank_j = #\{ l : theta^\{(l)\}_j < theta_j^prior \};
under calibration rank ~ Uniform\{0, ..., L\};
chi^2 = sum_b (O_b - E_b)^2 / E_b on (bins - 1) df
}
\references{
Talts, Betancourt, Simpson, Vehtari & Gelman (2018),
arXiv:1804.06788 -- the primary source. Bayesian Data Analysis, 3rd
edition, was fetched in full and searched; it predates
simulation-based calibration and does NOT contain it.
}
\examples{
V <- c(1, 2, 3, 4, 5, 6, 7, 8)
Sbcrank(V, V)
}

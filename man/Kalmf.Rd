% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Kalmf.R
\name{Kalmf}
\alias{Kalmf}
\title{Kalman filter for the linear Gaussian state space model}
\usage{
Kalmf(y, F, H, Q, R, x0 = NULL, P0 = NULL)
}
\arguments{
\item{y}{Observation matrix, one row per time point.}

\item{F}{State transition matrix.}

\item{H}{Observation matrix.}

\item{Q}{State noise covariance.}

\item{R}{Observation noise covariance.}

\item{x0}{Initial state mean; zero by default.}

\item{P0}{Initial state covariance; the identity by default.}
}
\value{
List with \code{estimate}, \code{state}, \code{cov},
\code{predicted}, \code{predicted_cov}, \code{loglik}, \code{n},
\code{method}.
}
\description{
The recursion alternates a prediction and an update, and the
prediction error decomposition gives the exact log-likelihood as a
by-product.  With Q = 0, F = I, H = I and P0 = R the filtered state
is exactly the running mean of the prior mean and the observations so
far, which is the closed form the tests check.
}
\details{
Formula: x_pred = F x; P_pred = F P F' + Q; S = H P_pred H' + R;
K = P_pred H' S^{-1}; x = x_pred + K (y - H x_pred);
P = (I - K H) P_pred.
}
\references{
Kalman (1960), A new approach to linear filtering and
prediction problems, Transactions of the ASME, Journal of Basic
Engineering 82(1):35-45. \doi{10.1115/1.3662552}
}

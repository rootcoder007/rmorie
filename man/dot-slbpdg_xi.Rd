% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/slbpdg_native.R
\name{.slbpdg_xi}
\alias{.slbpdg_xi}
\title{The deterministic bound xi_k = (1 - kappa) kappa^(k-1), built by}
\usage{
.slbpdg_xi(kappa, k)
}
\arguments{
\item{kappa}{Numeric; combined arithmetically in the body.}

\item{k}{A count; the body uses it as \code{seq_len(...)}.}
}
\value{
A numeric value.
}
\description{
repeated multiplication rather than kappa^(k-1): R\'s \code{^} on an
integer exponent is repeated squaring and Python\'s \verb{**} calls pow(),
and the two part company in the last bit exactly where the comparison
xi_k > u_i decides how many components to carry.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fzb4x.R
\name{Kdfb4}
\alias{Kdfb4}
\alias{morie_fauzi_b4_coefficient}
\title{The b_4 bias coefficient of the kernel distribution function estimator}
\usage{
Kdfb4(fppp, mu4 = 3)

morie_fauzi_b4_coefficient(fppp, mu4 = 3)
}
\arguments{
\item{fppp}{The third derivative \code{f^(3)(x)}.}

\item{mu4}{\verb{int w^4 K(w) dw}; 3 for the Gaussian kernel.}
}
\value{
Named list with \code{estimate}, \code{mu4}, \code{method}.
}
\description{
Eq. (2.8): \deqn{b_4(x) = \frac{f_X^{(3)}(x)}{24}\int w^4 K(w)\,dw.}{b4(x) = (f3(x)/24) int w^4 K(w) dw.}
}
\details{
The second bias coefficient in
\verb{J_h(x) = F(x)(1 + h^2 b2/F + h^4 b4/F) + o(h^4)}. It only matters once
\code{b2} has been eliminated -- which is exactly what the geometric
extrapolation of Theorem 2.1 does, leaving
\verb{h^4 a^2 (b2^2 - 2 b4 F) / (2 F)} as the whole bias.

So \code{b4} is not a refinement of the standard KDFE's error; it IS the error
of the bias-reduced one. Assumptions B2 (finite \code{mu4(K)}) and B4
(\code{f^(4)} exists) are needed for precisely this term.

\code{mu4} defaults to 3, the Gaussian value.
}
\examples{
Kdfb4(fppp = 24)
}
\references{
Fauzi and Maesono (2023), Eq. (2.8).
}
\keyword{internal}

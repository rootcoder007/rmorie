% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datasets_nyc_nypd.R
\name{morie_datasets_nyc_nypd_resolved}
\alias{morie_datasets_nyc_nypd_resolved}
\title{One-call NYPD data + borough + precinct join}
\usage{
morie_datasets_nyc_nypd_resolved(
  dataset_key,
  year = NULL,
  max_features = NULL,
  offline = TRUE,
  resource_id = NULL,
  mode = c("soda2", "soda3"),
  paginate = FALSE,
  page_size = 1000L,
  max_pages = 200L,
  app_token = NULL,
  resolvers = c("boro", "precinct", "offense", "law_code")
)
}
\arguments{
\item{dataset_key}{One of the keys in
\code{\link[=morie_datasets_nyc_nypd_layers]{morie_datasets_nyc_nypd_layers()}}.}

\item{year}{Optional year filter (server-side SoQL).}

\item{max_features}{Optional row cap. When \code{paginate = TRUE} this
is the total cap across walked pages.}

\item{offline}{If \code{TRUE} (default), read the bundled fixture.}

\item{resource_id}{Optional Socrata resource id override.}

\item{paginate}{Logical; if \code{TRUE} and \code{offline = FALSE}, walk
SODA2 \verb{$offset} in \code{page_size} chunks until exhausted or
\code{max_features} is reached. Default \code{FALSE} (single 1,000-row
request, matching the historical pre-3OO behaviour).}

\item{page_size}{Per-page row count when paginating (default 1,000,
the unauthenticated SODA2 ceiling).}

\item{max_pages}{Safety net on paginated walks (default 200 ->
up to 200,000 rows without an app_token).}

\item{resolvers}{Character subset of \code{c("boro", "precinct")} to
join. Default joins both.}
}
\value{
A wide \code{data.frame}: NYPD columns first, then prefixed
resolver columns.
}
\description{
Phase 3AAA. Pulls a slice of any
\code{\link[=morie_datasets_nyc_nypd_by_key]{morie_datasets_nyc_nypd_by_key()}}-resolvable dataset and
left-joins its borough + precinct foreign keys against the
bundled resolvers (\code{\link[=morie_datasets_nyc_boroughs]{morie_datasets_nyc_boroughs()}} +
\code{\link[=morie_datasets_nyc_police_precincts]{morie_datasets_nyc_police_precincts()}}).
}
\details{
Auto-detects the borough + precinct columns per dataset:

\tabular{lll}{
\strong{NYPD dataset}      \tab \strong{boro column}        \tab \strong{precinct column} \cr
nypd_arrests_historic     \tab \code{arrest_boro} (M/B/K/Q/S)    \tab \code{arrest_precinct}     \cr
nypd_arrests_ytd          \tab \code{arrest_boro}                \tab \code{arrest_precinct}     \cr
nypd_complaint_historic   \tab \code{boro_nm} (UPPER)            \tab \code{addr_pct_cd}         \cr
nypd_complaint_ytd        \tab \code{boro_nm} (UPPER)            \tab \code{addr_pct_cd}         \cr
nypd_hate_crimes          \tab \code{patrol_borough_name}        \tab \code{complaint_precinct_code} \cr
nypd_uof_incidents        \tab (none directly; precinct only)\tab \code{precinct}            \cr
}

Resolver columns prefixed \verb{boro_*} + \verb{precinct_*} to avoid
collisions. Left-join semantics (row count preserved).
}
\examples{
df <- morie_datasets_nyc_nypd_resolved("nypd_arrests_ytd",
                                         offline = TRUE)
names(df)
}

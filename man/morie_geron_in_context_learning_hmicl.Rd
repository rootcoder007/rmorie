% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geron_train_native.R, R/geron_w4b_native.R
\name{morie_geron_in_context_learning_hmicl}
\alias{morie_geron_in_context_learning_hmicl}
\title{In-context / few-shot prompt assembly (Geron Ch 16, morie.fn grinc)}
\usage{
morie_geron_in_context_learning_hmicl(
  model,
  examples,
  query,
  candidates = NULL,
  template = "{x} -> {y}",
  separator = "\\n"
)

morie_geron_in_context_learning_hmicl(
  model,
  examples,
  query,
  candidates = NULL,
  template = "{x} -> {y}",
  separator = "\\n"
)
}
\arguments{
\item{model}{function(prompt, candidate) -> log-probability. @param examples List of (x,y) pairs.}

\item{examples}{List of length-2 (input, output) pairs.}

\item{query}{Query input. @param candidates Optional label set. @param template,separator Prompt assembly.}

\item{template}{Must contain both \code{{input}} and \code{{output}}.}

\item{separator}{Joiner between blocks.}

\item{predict}{Optional function(prompt) -> answer.}
}
\value{
List with \code{prompt}, \code{k_shot}, \code{answer}, \code{prompt_chars},
\code{prompt_words}, \code{example_order}.

List with prediction, prompt, log_probs, posterior, n_shot, candidates, estimate, n, method.
}
\description{
In-context / few-shot prompt assembly (Geron Ch 16, morie.fn grinc)

In-context learning by prompt-conditioned scoring (Geron Ch 15, morie.fn hmicl)
}

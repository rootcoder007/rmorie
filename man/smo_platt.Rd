% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/smoopt_native.R
\name{smo_platt}
\alias{smo_platt}
\title{smo_platt}
\usage{
smo_platt(y, K, C = 1, tol = 0.001, eps = 1e-05, max_passes = 200, seed = 0)
}
\arguments{
\item{y}{Coerced to numeric by the body, with \code{as.numeric}.}

\item{K}{A matrix; indexed by row and column.}

\item{C}{Coerced to numeric by the body, with \code{as.numeric}. Defaults to \code{1}.}

\item{tol}{Passed to \code{violates_kkt}. Defaults to \code{0.001}.}

\item{eps}{Coerced to numeric by the body, with \code{as.numeric}. Defaults to \code{1e-05}.}

\item{max_passes}{Coerced to integer by the body, with \code{as.integer}. Defaults to \code{200}.}

\item{seed}{Passed to \code{.smoopt_make_rng}. Defaults to \code{0}.}
}
\value{
A list with \code{estimate}, \code{alpha}, \code{b}, \code{passes},
\code{full_passes}, \code{non_bound_passes}, \code{steps}, \code{support_vectors},
\code{n_sv}, \code{equality_residual}, \code{kkt_violations}, \code{objective},
\code{method}, \code{note}.
}
\description{
A step of the smoopt_native implementation. No other function in the package calls it.
See the file header for the source the module follows.
source it follows.
}
\examples{
set.seed(1)
X <- matrix(rnorm(20), 10, 2)
y <- sign(X[, 1])
K <- X \%*\% t(X)
smo_platt(y, K, C = 1.0)
}
\keyword{internal}

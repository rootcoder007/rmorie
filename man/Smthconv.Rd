% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fzt58.R
\name{Smthconv}
\alias{Smthconv}
\alias{morie_fauzi_thm5_8_smoothed_convergence}
\title{Mean-square equivalence of the smoothed and ordinary tests (Theorem 5.8)}
\usage{
Smthconv(d, c = 1, n = NULL, zstd = NULL, zsmooth = NULL)

morie_fauzi_thm5_8_smoothed_convergence(
  d,
  c = 1,
  n = NULL,
  zstd = NULL,
  zsmooth = NULL
)
}
\arguments{
\item{d}{The bandwidth exponent in \verb{h = c n^(-d)}.}

\item{c}{The bandwidth constant; must be positive.}

\item{n}{Sample size, used to report \code{h}.}

\item{zstd, zsmooth}{The two standardised statistics, for the finite-\code{n}
squared difference.}
}
\value{
Named list with \code{ok}, \code{d}, \code{h}, \code{sqdiff}, \code{lower}, \code{upper}, \code{method}.
}
\description{
Theorem 5.8: if \verb{f'} exists and is continuous near \code{-theta}, the bandwidth
is \verb{h = c n^(-d)} with \code{c > 0} and \verb{1/4 < d < 1/2}, the kernel is symmetric,
and two limiting moment conditions hold, then the standardised smoothed and
ordinary statistics agree in MEAN SQUARE:
\deqn{\lim_n E\Big\[\frac{S - E(S)}{\sqrt{V(S)}} - \frac{\tilde S - E(\tilde S)}{\sqrt{V(\tilde S)}}\Big\]^2 = 0,}{lim E\[(S - E S)/sqrt(V S) - (Stilde - E Stilde)/sqrt(V Stilde)\]^2 = 0,}
and likewise for \code{W} and \code{Wtilde}.
}
\details{
Mean-square convergence is stronger than convergence in probability and is
what licenses the conclusion that Pitman efficiencies coincide. Smoothing
costs nothing asymptotically.

The bandwidth window \verb{1/4 < d < 1/2} is the operative restriction and is
checked here. The lower end is the same \code{n^(-1/4)} undersmoothing threshold
as (3.8) and Theorem 5.7; the upper end, \code{d < 1/2}, says the bandwidth may
not shrink so fast that the smoothing does nothing -- \verb{n h -> Inf} in
disguise.
}
\examples{
Smthconv(d = 1/3, n = 1000)
}
\references{
Fauzi and Maesono (2023), Theorem 5.8.
}
\keyword{internal}

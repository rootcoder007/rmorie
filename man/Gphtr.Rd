% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Gphtr.R
\name{Gphtr}
\alias{Gphtr}
\title{Heteroscedastic Gaussian process regression}
\usage{
Gphtr(
  X,
  y,
  X_test = NULL,
  lengthscale = 1,
  variance = 1,
  noise0 = 0.1,
  noise_lengthscale = 1,
  iters = 3,
  floor = 1e-06
)
}
\arguments{
\item{X}{Training inputs.}

\item{y}{Training responses.}

\item{X_test}{Test inputs; the training inputs by default.}

\item{lengthscale, variance}{Mean-process hyperparameters.}

\item{noise0}{Starting noise variance.}

\item{noise_lengthscale}{Lengthscale of the log-variance process.}

\item{iters}{Alternations; zero gives the homoscedastic GP.}

\item{floor}{Lower bound on a squared residual before its logarithm.}

}
\value{
List with \code{estimate}, \code{mean}, \code{variance},
\code{noise}, \code{noise_test}, \code{loglik}, \code{n},
\code{method}.
}
\description{
Two processes: one for the mean, one for the LOG noise variance, so
the noise level is itself a smooth function of the input and cannot
go negative.  The alternation is the deterministic analogue of the
paper's sampler: fit the mean GP under the current noise, form the
log squared residuals, smooth them with the second GP, exponentiate,
refit.  Zero iterations leaves the plain homoscedastic GP exactly.
}
\details{
Formula: y(x) = f(x) + eps(x), f ~ GP, log var(eps(x)) ~ GP.
}
\examples{
V <- c(1, 2, 3, 4, 5, 6, 7, 8)
Gphtr(V, V)
}
\references{
Goldberg, Williams and Bishop (1998), Regression with
input-dependent noise: a Gaussian process treatment, NIPS 10.
}

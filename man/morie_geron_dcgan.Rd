% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geron_w4a_native.R
\name{morie_geron_dcgan}
\alias{morie_geron_dcgan}
\title{DCGAN generator/discriminator resolved to concrete shapes (hmdcg)}
\usage{
morie_geron_dcgan(
  X,
  z_dim = 100,
  filters = 64,
  epochs = 50,
  lr = 2e-04,
  seed_shape = c(4, 4),
  stride = 2
)
}
\arguments{
\item{X}{Real images (H,W) or (m,H,W).}

\item{z_dim, filters}{int.}

\item{epochs, lr}{Training config (recorded only; see hmgan for the loop).}

\item{seed_shape}{c(h0, w0).}

\item{stride}{Upsample stride (>=2).}


}
\value{
List with \code{generator_layers}, \code{discriminator_layers},
\code{generator_params}, \code{discriminator_params}, \code{total_params}, \code{n_layers}.
}
\description{
Architecture spec (like hmalex): layer-by-layer channel/param counts
for a square image reached from \code{seed_shape} by stride-2 transposed
convs. One generator forward pass on unit kernels is delegated to
\code{morie_geron_dcgan_generator} to demonstrate the resolved shapes.
}
\examples{
morie_geron_dcgan(array(0, dim = c(3, 16, 16)), z_dim = 8, filters = 4)
}

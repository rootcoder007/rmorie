% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geron_ml_native.R
\name{morie_geron_albert}
\alias{morie_geron_albert}
\title{ALBERT encoder: factorised embedding + cross-layer sharing (Geron Ch 15, hmalbt)}
\usage{
morie_geron_albert(
  X,
  n_layers = 4,
  n_heads = 2,
  d_model = 8,
  d_embed = 4,
  vocab_size = NULL,
  d_ff = NULL,
  seed = 0
)
}
\arguments{
\item{X}{0-based token ids, vector or (batch, T) matrix.}

\item{n_layers, n_heads, d_model, d_ff}{Encoder geometry.}

\item{d_embed}{Factorised width E in [1, d_model].}

\item{vocab_size}{Optional; defaults to max(X) + 1. @param seed LCG seed.}
}
\value{
List with \code{hidden} (array batch x T x d), \code{n_params},
\code{n_params_unshared}, \code{block_params}, \code{embedding_params},
\code{embedding_params_direct}, \code{shared}, \code{d_embed}.
}
\description{
The embedding goes V -> E -> H instead of V -> H, and ONE block object
is applied \code{n_layers} times, so depth costs no extra parameters --
which is why \code{n_params} is independent of \code{n_layers} while
\code{n_params_unshared} is not. Token ids are 0-based.
}

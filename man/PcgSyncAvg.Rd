% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aaa_rangayyan_corr.R
\name{PcgSyncAvg}
\alias{PcgSyncAvg}
\title{S_avg(f) = (1/M) sum |PCG_k(f)|^2.  The averaging is of POWER}
\usage{
PcgSyncAvg(cycles, fs = 1)
}
\arguments{
\item{cycles}{Iterated over elementwise, with \code{lapply}.}

\item{fs}{Coerced to numeric by the body, with \code{as.numeric}. Defaults to \code{1}.}
}
\value{
A list with \code{average_psd}, \code{psd_of_average}, \code{mean_waveform},
\code{freqs}, \code{m}, \code{n}, \code{power_retained}, \code{method}.
}
\description{
SPECTRA, not waveforms, and that is the point: successive PCG cycles
are not sample-aligned -- a murmur is turbulent, its phase differs
cycle to cycle -- so waveform averaging as in eq (3.96) would cancel
exactly the murmur being measured.  Both are computed so the
difference is visible.
}
\examples{
D <- data.frame(x = c(1, 2, 3, 4), y = c(2, 4, 5, 9))
PcgSyncAvg(D)
}
\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dp_native2.R
\name{morie_approx_dp}
\alias{morie_approx_dp}
\title{Empirical delta at a fixed epsilon}
\usage{
morie_approx_dp(
  mech,
  D,
  D_prime,
  epsilon = 1,
  n_samples = 20000,
  bins = 50,
  seed = NULL
)
}
\arguments{
\item{mech}{function of \code{(data)} returning one release; called
repeatedly.}

\item{D, D_prime}{neighbouring datasets.}

\item{epsilon}{the epsilon whose bound is being tested.}

\item{n_samples}{draws per dataset.}

\item{bins}{histogram bins.}

\item{seed}{optional integer seed.}
}
\value{
list with \code{delta_empirical}, \code{n_violating_bins}.
}
\description{
The probability mass on which the \eqn{e^\epsilon} bound fails.
}
\details{
Delta is not slack. At \eqn{\delta = 1/n} a mechanism may release one
record outright and still satisfy the definition, so delta belongs
well below 1/n -- cryptographically small, not merely small.
}
\examples{
set.seed(1)
m <- function(d) sum(d) + morie_dp_laplace_mechanism(0, 1, 1)$release
morie_approx_dp(m, rep(1, 10), rep(1, 9),
  epsilon = 1,
  n_samples = 2000
)$delta_empirical < 1
}
\references{
Dwork, C. and Roth, A. (2014). \emph{FnTTCS}, 9(3-4),
Sec. 2.3.
}

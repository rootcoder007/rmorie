% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geron_w4d_native.R
\name{morie_geron_semisupervised_cluster}
\alias{morie_geron_semisupervised_cluster}
\title{Semi-supervised learning via k-means representative labeling}
\usage{
morie_geron_semisupervised_cluster(
  X,
  X_labeled,
  y_labeled,
  n_clusters = 2,
  seed = 0,
  y_true = NULL
)
}
\arguments{
\item{X}{Unlabeled pool (n, d).}

\item{X_labeled}{Labeled instances (m, d).}

\item{y_labeled}{Their labels, length m.}

\item{n_clusters}{Labeling budget.}

\item{seed}{LCG seed.}

\item{y_true}{Optional gold labels to score against.}
}
\value{
list with labels, cluster, centers, representatives, representative_labels,
accuracy, estimate, n, method.
}
\description{
Clusters the unlabeled pool (Lloyd/k-means++ on a deterministic LCG
stream), finds each cluster's real-point representative closest to
its centroid, labels that representative from the nearest labeled
point, and propagates the label to every cluster member.
}
\examples{
morie_geron_semisupervised_cluster(X = c(1, 2, 3, 4, 5, 6, 7, 8),
  X_labeled = c(1, 2, 3, 4, 5, 6, 7, 8), y_labeled = c(1, 2, 3, 4, 5, 6, 7, 8))
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geron_ml_native.R
\name{morie_geron_autoencoder}
\alias{morie_geron_autoencoder}
\title{Linear undercomplete autoencoder solved by SVD (Geron Ch 18, hmaen)}
\usage{
morie_geron_autoencoder(X, bottleneck, center = TRUE)
}
\arguments{
\item{X}{Training data (n, d). @param bottleneck Code width in [1, d].}

\item{center}{Subtract feature means (this is what makes it PCA).}
}
\value{
List with \code{encoder}, \code{decoder}, \code{codes}, \code{reconstruction},
\code{recon_error}, \code{explained_variance_ratio}, \code{mean}, \code{encode}, \code{decode}.
}
\description{
For a linear autoencoder the optimum is spanned by the top principal
components, so the encoder is the leading right singular vectors and
the decoder their transpose. SVD sign is arbitrary and differs between
LAPACK builds, so \code{codes} and \code{encoder} carry a per-column sign
convention; \code{reconstruction}, \code{recon_error} and
\code{explained_variance_ratio} are sign-invariant and are what parity
tests should assert.
}

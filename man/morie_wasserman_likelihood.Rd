% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wasserman_native.R
\name{morie_wasserman_likelihood}
\alias{morie_wasserman_likelihood}
\title{Likelihood L(theta) = prod f(X_i; theta) (Ch 9, wsmlik)}
\usage{
morie_wasserman_likelihood(data, f, theta)
}
\arguments{
\item{data}{Numeric sample, non-empty.}

\item{f}{Density f(x, theta) vectorised in x; NULL = exponential(theta).}

\item{theta}{Parameter value.}
}
\value{
List with \code{estimate} (L), \code{log_likelihood}, \code{theta}, \code{n}, \code{method}.
}
\description{
Evaluated through the log domain so long samples cannot underflow
before the product finishes. A zero density anywhere makes L exactly
0 (log-likelihood -Inf), reported rather than signalled.
}
\examples{
round(morie_wasserman_likelihood(c(1, 2), NULL, 1)$log_likelihood, 12)
}

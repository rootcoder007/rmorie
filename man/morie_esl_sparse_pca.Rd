% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/esl_native2.R
\name{morie_esl_sparse_pca}
\alias{morie_esl_sparse_pca}
\title{Sparse principal components}
\usage{
morie_esl_sparse_pca(
  X,
  k = 2,
  lambda_ = 0.1,
  max_iter = 500L,
  tol = 1e-08,
  center = TRUE,
  scale = FALSE
)
}
\arguments{
\item{X}{Data, n by p.}

\item{k}{Number of components.}

\item{lambda_}{Soft-threshold level; zero recovers ordinary PCA.}

\item{max_iter, tol}{Power-iteration controls.}

\item{center, scale}{Centre columns, optionally scale to unit variance.}
}
\value{
List with \code{loadings}, \code{scores}, \code{sparsity}, \code{adjusted_variance},
\code{explained}, \code{n_iter}.
}
\description{
L1-penalised alternating maximisation: soft-threshold \code{Sigma \%*\% v}, then
renormalise, with later components on the deflated covariance. Ordinary PCA
loadings are almost never zero, so every component mixes all p variables;
the L1 penalty zeroes loadings outright. The cost, which must be stated:
sparse components are NOT orthogonal, so their variances do not sum to the
total and "percent variance explained" is not additive. \code{adjusted_variance}
applies the Zou-Hastie-Tibshirani correction.
}
\examples{
set.seed(1)
X <- matrix(rnorm(1200), ncol = 6) \%*\% diag(c(5, 3, 1, 1, 1, 1))
morie_esl_sparse_pca(X, k = 1, lambda_ = 2)$sparsity > 0
}
\references{
Zou, H., Hastie, T., & Tibshirani, R. (2006). Sparse principal
component analysis. JCGS 15(2), 265-286.
}

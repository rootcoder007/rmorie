% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dp_native3.R
\name{morie_dp_kmeans}
\alias{morie_dp_kmeans}
\title{Differentially private k-means}
\usage{
morie_dp_kmeans(X, k = 3, epsilon = 1, n_iter = 5, bounds = NULL, seed = NULL)
}
\arguments{
\item{X}{data matrix.}

\item{k}{number of clusters.}

\item{epsilon}{total budget across all iterations.}

\item{n_iter}{Lloyd iterations.}

\item{bounds}{\code{c(lo, hi)} for clipping, from outside the data.}

\item{seed}{optional integer seed.}
}
\value{
list with \code{centers}, \code{labels},
\code{epsilon_per_iteration}, \code{n_reinitialised},
\code{inertia}.
}
\description{
Lloyd iterations where each centre update uses a noisy count and a
noisy sum.
}
\details{
The budget splits across ITERATIONS as well as across the count and
sum queries, so each pass is cheaper and noisier than the last one
would like. More iterations therefore make the result WORSE, which
inverts the usual intuition: keep \code{n_iter} small. A noisy count
below 1 means the cluster cannot be located at all and the centre is
reinitialised, which is reported.
}
\examples{
set.seed(1)
r <- morie_dp_kmeans(matrix(rnorm(200), ncol = 2),
  k = 2, epsilon = 5,
  bounds = c(-4, 4)
)
dim(r$centers)
}
\references{
Su, D. et al. (2016). Differentially private k-means
clustering. \emph{CODASPY}, 26-37.
}

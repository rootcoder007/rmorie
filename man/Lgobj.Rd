% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lgobj.R
\name{Lgobj}
\alias{Lgobj}
\title{Logistic regression log-likelihood}
\usage{
Lgobj(y, X, beta)
}
\arguments{
\item{y}{binary response, entries 0 or 1.}

\item{X}{n-by-p design matrix; no intercept column is added.}

\item{beta}{coefficient vector of length p.}
}
\value{
list: estimate, loglik, p, gradient, n, method.
}
\description{
Montesinos Lopez, Montesinos Lopez and Crossa (2022), Multivariate
Statistical Machine Learning Methods for Genomic Prediction, Springer,
volume \link{Pages 71-108}, Chapter 3, Section 3.7, p. 99, read as a rendered
page.  The chapter models the response as Bernoulli with
p(x_i; beta) = exp(eta_i)/(1 + exp(eta_i)), eta_i = beta_0 + x_i' beta_0,
and writes
l(beta; y) = sum_i y_i eta_i - sum_i log(1 + exp(eta_i)),
with gradient X'\link{y - p(X; beta)} and Hessian -X' W X,
W = Diag(p(1-p)).
}
\details{
The sum-of-logs form and the chapter's form are the same function; the
chapter's is used because it does not lose precision when p underflows to
0 or overflows to 1, and the log1p guard keeps it finite for any eta.
}
\examples{
Lgobj(c(1, 0), matrix(1, 2, 1), 0)$loglik
}
\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datasets_ontario_tps.R
\name{morie_datasets_otis_c11_aggregate_lengths}
\alias{morie_datasets_otis_c11_aggregate_lengths}
\title{OTIS c11 -- Aggregate lengths}
\usage{
morie_datasets_otis_c11_aggregate_lengths(
  offline = TRUE,
  resource_id = NULL,
  source = NULL
)
}
\arguments{
\item{offline}{If \code{TRUE} (default), read the included synthetic
fixture. If \code{FALSE}, hit Ontario CKAN (\code{resource_id} required).}

\item{resource_id}{Optional CKAN resource id (required for live).}

\item{source}{One of \code{NULL} (default, ships an empty 0-row frame
with the documented schema when no real CKAN row is included),
\code{"real"} (force the real CKAN sample included in \verb{inst/extdata/},
error if absent), or \code{"synth"} (return a deterministic
\code{set.seed()} synthetic for didactic examples).}
}
\value{
A \code{data.frame} of the requested dataset (a 0-row typed frame when the data is unavailable offline).
}
\description{
OTIS c11 -- Aggregate lengths
}
\examples{
df <- morie_datasets_otis_c11_aggregate_lengths(offline = TRUE)
head(df)
}

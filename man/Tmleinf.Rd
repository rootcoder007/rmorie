% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tmlinf.R
\name{Tmleinf}
\alias{Tmleinf}
\title{Standard error, interval and score check from an influence curve.}
\usage{
Tmleinf(psi, ic, level = 0.95, null_value = 0)
}
\arguments{
\item{psi}{The targeted point estimate.}

\item{ic}{Influence-curve values, one per observation.}

\item{level}{Confidence level.}

\item{null_value}{Value tested against.}
}
\value{
List with \code{estimate}, \code{se}, \code{ci_lower},
\code{ci_upper}, \code{statistic}, \code{p_value}, \code{ic_mean},
\code{score_solved}, \code{n}.
}
\description{
An efficient influence curve must have empirical mean ZERO at the
targeted estimate; \code{score_solved} reports whether it does, because
if it does not the standard error is not trustworthy.
}
\details{
Formula: se = sqrt(var(IC)/n); CI = psi -+ z_\{alpha/2\} se;
the estimating equation is (1/n) sum IC_i = 0
}
\examples{
V <- c(1, 2, 3, 4, 5, 6, 7, 8)
Tmleinf(V, V)
}
\references{
Verified against the CRAN package tmle 2.1.1 (Gruber & van
der Laan), whose calcParameters computes var.psi <- var(IC)/n, the
qnorm-based interval and pvalue <- 2*pnorm(-abs(psi/sqrt(var.psi))).
}

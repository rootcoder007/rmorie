% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/did_modern_native.R
\name{morie_did_sun_abraham}
\alias{morie_did_sun_abraham}
\title{Sun & Abraham (2021) interaction-weighted event study}
\usage{
morie_did_sun_abraham(
  data,
  outcome,
  unit,
  time,
  treatment_time,
  leads = 4L,
  lags = 4L,
  alpha = 0.05
)
}
\arguments{
\item{data}{Long panel data frame.}

\item{outcome, unit, time}{Column names.}

\item{treatment_time}{First treated period per unit (NA = never).}

\item{leads, lags}{Relative-time window to report. Defaults -4..4.}

\item{alpha}{CI tail.}
}
\value{
data.frame: rel_time, estimate, std.error, conf.low,
conf.high, n; attribute "cells" holds the (cohort, rel) cell
coefficients.
}
\description{
Saturated cohort-by-relative-time event study: each (cohort, rel
time) cell gets its own coefficient via the native TWFE demeaner
(never-treated units as the clean control group), then the
cell coefficients are aggregated to relative-time effects with
cohort-share weights -- the IW estimator that is immune to the
negative-weighting contamination of a pooled event study.
}
\examples{
df <- expand.grid(id = 1:40, t = 1:8)
df$g <- ifelse(df$id <= 12, 4L, ifelse(df$id <= 24, 6L, NA))
df$y <- rnorm(nrow(df)) + ifelse(!is.na(df$g) & df$t >= df$g, 2, 0)
morie_did_sun_abraham(df, "y", "id", "t", "g")
}
\references{
Sun & Abraham (2021) J. Econometrics 225(2).
}

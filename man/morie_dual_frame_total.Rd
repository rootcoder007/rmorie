% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/median_frames_dose_native.R
\name{morie_dual_frame_total}
\alias{morie_dual_frame_total}
\title{Dual-frame total with sample overlap (Hartley 1962)}
\usage{
morie_dual_frame_total(
  frame_a,
  frame_b,
  overlap_a,
  overlap_b,
  weights_a = NULL,
  weights_b = NULL,
  theta = NULL,
  alpha = 0.05
)
}
\arguments{
\item{frame_a, frame_b}{Observed values in each frame's sample.}

\item{overlap_a, overlap_b}{1 if that unit is also in the other frame.}

\item{weights_a, weights_b}{Design weights; default 1.}

\item{theta}{Overlap weight; default the variance-minimising value.}

\item{alpha}{Two-sided level.}



}
\value{
A list with \code{estimate}, \code{theta}, \code{theta_optimal}, \code{se},
\code{naive_pooled_total}, \code{overlap_double_count}, domain totals.
}
\description{
Two frames together cover the population but neither alone does, and
they overlap. Splitting into domains a (frame A only), ab (both) and
b (frame B only),
\deqn{\hat Y = \hat Y_a + \theta \hat Y_{ab}^A +
  (1-\theta)\hat Y_{ab}^B + \hat Y_b .}
}
\details{
The failure this guards against is the one that looks like success:
POOLING THE TWO SAMPLES DOUBLE-COUNTS THE OVERLAP, and the inflated
total is stable across replications because it is bias, not noise.
More data makes it more precisely wrong. \code{naive_pooled_total} is
returned so the gap is visible rather than argued about.
}
\references{
Hartley HO (1962) \emph{Proc Soc Stat Sect ASA} 203-206.
}

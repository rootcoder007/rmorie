% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cox_native.R
\name{morie_cox_breslow_step}
\alias{morie_cox_breslow_step}
\title{Breslow estimator of the baseline cumulative hazard}
\usage{
morie_cox_breslow_step(time, event, X, beta = NULL, ties = "efron")
}
\arguments{
\item{time}{follow-up times, non-negative.}

\item{event}{1 for an event, 0 for right-censored.}

\item{X}{covariate matrix, one row per subject.}

\item{beta}{coefficient vector. Fitted internally when NULL.}

\item{ties}{tie correction used for the internal fit.}
}
\value{
list with \code{times}, \code{hazard}, \code{cumhazard},
\code{survival}.
}
\description{
The Cox partial likelihood throws the baseline away; this puts it
back, as a step function jumping at each event time by
\eqn{d_j / \sum_{i \in R_j} \exp(x_i'\beta)}.
}
\details{
"Baseline" means x = 0, which for uncentred covariates is a subject
who does not exist -- centre the covariates or the curve is not
interpretable. The curve is also undefined past the last event time;
it is drawn flat there by convention, not by evidence.
}
\examples{
set.seed(1)
X <- matrix(rnorm(60), ncol = 1)
tt <- rexp(60, exp(X * 0.5))
tail(morie_cox_breslow_step(tt, rep(1, 60), X)$cumhazard, 1)
}
\references{
Breslow, N. (1972). Discussion of Professor Cox's paper.
\emph{JRSS-B}, 34(2), 216-217.
}

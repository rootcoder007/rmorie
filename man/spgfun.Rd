% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spgfun.R
\name{spgfun}
\alias{spgfun}
\title{G-function: the nearest-neighbour distance distribution}
\usage{
spgfun(points, r = NULL, region = NULL)
}
\arguments{
\item{points}{Event coordinates (n by 2).}

\item{r}{Distances at which to evaluate the CDF.}

\item{region}{Used only for the CSR reference intensity.}
}
\value{
Named list: r, g, g_csr, nn_distances, mean_nn, lambda_est.
}
\description{
G_hat(y0) = #(y_i <= y0) / n, the empirical probability that an
event's nearest-neighbour distance is at most y0. Under CSR with
intensity lambda the theoretical form is G(y) = 1 - exp(-lambda pi y^2).
In a clustered pattern G_hat sits ABOVE this curve at small y; in a
regular pattern it sits below.
}
\examples{
spgfun(matrix(runif(400), 200, 2) * 10, region = c(0, 0, 10, 10))
}
\references{
Schabenberger & Gotway (2005), Sec 3.3.4, pp. 97-98.
}

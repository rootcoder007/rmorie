% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geron_train_native.R
\name{morie_geron_flamingo_cross_modal_attn}
\alias{morie_geron_flamingo_cross_modal_attn}
\title{Flamingo tanh-gated cross-attention (Geron Ch 17, morie.fn grflam)}
\usage{
morie_geron_flamingo_cross_modal_attn(
  h,
  visual_features,
  alpha,
  weights,
  mask = NULL
)
}
\arguments{
\item{h}{(T, d_model) language hidden states.}

\item{visual_features}{(S, d_v) visual tokens.}

\item{alpha}{Finite gate parameter; 0 leaves the LM untouched.}

\item{weights}{List or named list with WQ, WK, WV.}

\item{mask}{Optional attention mask.}
}
\value{
List with \code{h_new}, \code{gate}, \code{attention_output},
\code{attention_weights}, \code{delta_norm}, \code{is_identity}.
}
\description{
h_new = h + tanh(alpha) * crossattn(h, visual), the cross-attention
coming from \code{\link[=morie_geron_cross_attention]{morie_geron_cross_attention()}}.
}
\examples{
set.seed(1)
h <- matrix(rnorm(12), 3, 4)
vf <- matrix(rnorm(8), 2, 4)
W <- diag(4)
r <- morie_geron_flamingo_cross_modal_attn(h, vf, alpha = 0.5,
                                           weights = list(WQ = W, WK = W,
                                                          WV = W))
str(r, max.level = 1)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datasets.R
\name{morie_datasets_chicago_neighborhoods}
\alias{morie_datasets_chicago_neighborhoods}
\title{Chicago Neighborhoods boundary (Office of Tourism)}
\usage{
morie_datasets_chicago_neighborhoods(
  offline = TRUE,
  geometry = FALSE,
  max_features = NULL,
  resource_id = NULL,
  mode = c("soda2", "soda3"),
  paginate = FALSE,
  page_size = 1000L,
  max_pages = 200L,
  app_token = NULL
)
}
\arguments{
\item{offline}{If \code{TRUE} (default), read the bundled attribute-only
fixture from \code{inst/extdata/chicago_neighborhoods.csv}.}

\item{geometry}{If \code{TRUE} and \code{offline = FALSE}, include the
\code{the_geom} MultiPolygon column in the live-mode result.}

\item{max_features}{Optional cap on returned rows.}

\item{resource_id}{Optional Socrata resource id override.}

\item{mode}{One of \code{"soda2"} (default JSON resource endpoint) or
\code{"soda3"} (SoQL \code{query} endpoint). 3VV+ dual-mode dispatch.}

\item{paginate}{Logical; if \code{TRUE} and \code{offline = FALSE}, walk
SODA2 \verb{$offset} in \code{page_size} chunks (3OO).}

\item{page_size}{Per-page row count when paginating (default 1000,
the unauthenticated Socrata ceiling).}

\item{max_pages}{Safety net on paginated walks (default 200).}

\item{app_token}{Optional Socrata API app token for higher rate
limits; passed as the \code{X-App-Token} header.}
}
\value{
A \code{data.frame} with 4 attribute columns (offline mode) or
5 cols including \code{the_geom} (live mode with \code{geometry = TRUE}).
}
\description{
Wraps the City of Chicago "Boundaries - Neighborhoods" open dataset
(Socrata resource id \code{y6yq-dbs2}; portal landing
\url{https://data.cityofchicago.org/Facilities-Geographic-Boundaries/Boundaries-Neighborhoods/bbvz-uum9}).
98 neighbourhoods, originally derived from Neighborhoods_2012b
(updated 2025-02-20). The City notes these boundaries are
approximate and the names are not official.
}
\details{
Offline mode reads a bundled 98-row attribute-only fixture
(\code{pri_neigh}, \code{sec_neigh}, \code{shape_area}, \code{shape_len}) -- the
\code{the_geom} MultiPolygon column is stripped to keep the bundled
size sane (full GeoJSON is ~800 KB). Live mode hits the SODA2
endpoint via \code{.morie_dataset_socrata_fetch()} (mockable).

To get the polygons, pass \code{geometry = TRUE} in live mode, which
includes the SODA2 \code{the_geom} column.
}
\examples{
df <- morie_datasets_chicago_neighborhoods(offline = TRUE)
head(df[, c("pri_neigh", "sec_neigh")])
}
\references{
City of Chicago Data Portal, "Boundaries -
Neighborhoods"; based on Neighborhoods_2012b.
}

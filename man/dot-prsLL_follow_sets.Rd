% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prsLL_native.R
\name{.prsLL_follow_sets}
\alias{.prsLL_follow_sets}
\title{.prsLL_follow_sets}
\usage{
.prsLL_follow_sets(g, first = NULL)
}
\arguments{
\item{g}{A list; the body reads \code{$rules}, \code{$start} from it.}

\item{first}{Optional; may be \code{NULL}. Passed to \code{is.null}.}
}
\value{
The value of \code{follow}, as built in the body.
}
\description{
A step of the prsLL_native implementation. Called by \code{.prsLL_ll1_table}.
See the file header for the source the module follows.
source it follows.
}
\examples{
# The expression grammar of Aho, Lam, Sethi & Ullman (2nd ed., 4.4.2):
#   E -> T E';  E' -> + T E' | eps;  T -> F T';  T' -> * F T' | eps;
#   F -> ( E ) | id
rules <- list(
  list("E",  c("T", "Ep")),
  list("Ep", c("+", "T", "Ep")),
  list("Ep", character(0)),
  list("T",  c("F", "Tp")),
  list("Tp", c("*", "F", "Tp")),
  list("Tp", character(0)),
  list("F",  c("(", "E", ")")),
  list("F",  "id")
)
g <- .prsLL_grammar(rules, start = "E")
follow <- .prsLL_follow_sets(g)
follow$E  # ")" and "$", the end-of-input marker
follow$F  # "+", "*", ")", "$"
}

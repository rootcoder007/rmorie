% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geron_train_native.R
\name{morie_geron_randomized_search_cv}
\alias{morie_geron_randomized_search_cv}
\title{Randomized search with K-fold CV (Geron Ch 2, morie.fn grrnd)}
\usage{
morie_geron_randomized_search_cv(
  X,
  y,
  param_dist,
  n_iter,
  K,
  fit_score = NULL,
  seed = 42
)
}
\arguments{
\item{X, y}{Data.}

\item{param_dist}{Named list of specs: a function(u), a length-2
numeric range, or a vector of discrete choices.}

\item{n_iter}{Configurations to draw.}

\item{K}{Folds in [2, m].}

\item{fit_score}{Required scoring function.}

\item{seed}{LCG seed.}
}
\value{
List with \code{best_params}, \code{best_score}, \code{results},
\code{fold_sizes}.
}
\description{
The LCG stream is consumed first for the fold permutation (m draws,
argsorted stably) and then one draw PER PARAMETER per configuration,
in the order of \code{param_dist}. \code{std_score} is the ddof = 0 deviation.
}
\examples{
set.seed(3)
X <- matrix(rnorm(60), 30, 2)
y <- as.numeric(X[, 1] > 0)
fs <- function(Xtr, ytr, Xte, yte, params) {
  b <- qr.solve(cbind(1, as.matrix(Xtr)), as.numeric(ytr))
  pred <- as.numeric(cbind(1, as.matrix(Xte)) \%*\% b)
  -mean((pred - yte)^2) * params$w
}
morie_geron_randomized_search_cv(X, y, param_dist = list(w = c(0.5, 2)),
                                 n_iter = 3, K = 3, fit_score = fs)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geron_ml_native.R
\name{morie_geron_anomaly_autoencoder}
\alias{morie_geron_anomaly_autoencoder}
\title{Autoencoder anomaly detection (Geron Ch 8, morie.fn hmanae)}
\usage{
morie_geron_anomaly_autoencoder(model, X, threshold = NULL, quantile = 0.99)
}
\arguments{
\item{model}{Function(X) -> reconstruction, or a list with
\code{reconstruct}, \code{predict}, or \code{encode} + \code{decode}.}

\item{X}{Data to score. @param threshold Optional squared-error cut.}

\item{quantile}{Quantile in (0, 1] used when \code{threshold} is NULL.}
}
\value{
List with \code{errors}, \code{is_anomaly}, \code{threshold},
\code{threshold_calibrated}, \code{n_anomalies}, \code{reconstruction}.
}
\description{
Flags rows whose squared reconstruction error exceeds \code{threshold};
with no threshold the empirical \code{quantile} of the errors is used,
which calibrates the detector on the data being scored.
}

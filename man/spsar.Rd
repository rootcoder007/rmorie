% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spsar.R
\name{spsar}
\alias{spsar}
\title{Spatial autoregressive (lag) model}
\usage{
spsar(x, y, w)
}
\arguments{
\item{x}{Covariates (n by p).}

\item{y}{Response, length n.}

\item{w}{Spatial weights (n by n).}
}
\value{
The result of \code{sarla()}.
}
\description{
Same estimator as \code{sarla()}, which fits the concentrated
log-likelihood in rho; this delegates rather than carrying a second
implementation.
}
\examples{
n <- 20
W <- matrix(0, n, n); W[cbind(1:(n-1), 2:n)] <- 1; W <- W + t(W)
W <- W / pmax(rowSums(W), 1)
spsar(cbind(1, runif(n)), rnorm(n), W)
}
\references{
Schabenberger & Gotway (2005), Sec 6.2.2.1
"Simultaneous Autoregressive (SAR) Models", pp. 335-341.
}

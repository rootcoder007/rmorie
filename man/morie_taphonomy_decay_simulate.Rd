% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/taphonomy.R
\name{morie_taphonomy_decay_simulate}
\alias{morie_taphonomy_decay_simulate}
\title{Simulate one decay path through a taphonomic Markov chain}
\usage{
morie_taphonomy_decay_simulate(
  chain,
  start = chain$transient[1],
  n_steps = 100L,
  seed = 42L
)
}
\arguments{
\item{chain}{A chain from \code{\link{morie_taphonomy_decay_chain}}.}

\item{start}{Starting transient stage (default the first).}

\item{n_steps}{Maximum steps to simulate (default 100).}

\item{seed}{RNG seed (default 42) for reproducibility.}
}
\value{
A character vector: the sequence of visited states, ending at an
  absorbing state if reached.
}
\description{
Draws a single realised trajectory from death through the transient stages
until an absorbing fate is reached (or \code{n_steps} elapses).
}
\examples{
morie_taphonomy_decay_simulate(morie_taphonomy_decay_chain(0.7), seed = 1)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/caus_native.R
\name{morie_caus_iv_dag}
\alias{morie_caus_iv_dag}
\title{The instrumental-variable estimand under a causal graph}
\usage{
morie_caus_iv_dag(y, D, Z, homogeneous = FALSE)
}
\arguments{
\item{y}{outcome.}

\item{D}{binary treatment.}

\item{Z}{binary instrument.}

\item{homogeneous}{assert a constant treatment effect.}
}
\value{
list: beta, se, estimand, homogeneous_asserted, relevance,
relevance_t, relevance_p, assumptions, testable, untestable, n,
method.
}
\description{
Arithmetically identical to \code{morie_caus_iv_late()} -- the two
agree to machine precision, and a discrepancy would mean one is
wrong. What differs is the assumption set and therefore what the
number is ABOUT: \code{Z -> D -> Y} with no \code{Z -> Y} arrow (exclusion),
no common cause of \code{Z} and \code{Y} (exchangeability), and \code{Z} moving
\code{D} (relevance). With a CONSTANT treatment effect that graph
identifies the average treatment effect; without it, the
compliers' effect.
}
\details{
\code{homogeneous} records which claim is being made, since the number
is the same either way. It defaults to \code{FALSE} so the weaker,
safer reading is what comes out unless constancy is asserted
deliberately. Only relevance is testable, and it is reported.
}
\examples{
Z <- stats::rbinom(400, 1, 0.5)
D <- Z * stats::rbinom(400, 1, 0.7)
morie_caus_iv_dag(2 * D + stats::rnorm(400), D, Z)$relevance
}
\references{
Imbens and Angrist (1994), \emph{Econometrica} 62:467-475;
Angrist, Imbens and Rubin (1996), \emph{JASA} 91:444-455.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geron_w4d_native.R
\name{morie_geron_sgd_classifier}
\alias{morie_geron_sgd_classifier}
\title{SGD classifier with hinge loss (linear SVM) trained by stochastic gradient descent}
\usage{
morie_geron_sgd_classifier(
  X,
  y,
  lr = 0.1,
  n_iter = 10,
  alpha = 1e-04,
  seed = 0,
  shuffle = TRUE
)
}
\arguments{
\item{X}{Design matrix (n, d).}

\item{y}{Binary labels (2 distinct values).}

\item{lr, n_iter, alpha, seed, shuffle}{As in the Python original.}
}
\value{
list with w, b, loss_curve, decision, predicted, accuracy, n_support,
classes, estimate, n, method.
}
\description{
Subgradient update: violated margin (y_i f(x_i) < 1) -> w -= lr*(alpha\emph{w - y_i x_i),
b += lr}y_i; satisfied -> w -= lr\emph{alpha}w. Sample order drawn from a
deterministic LCG Fisher-Yates shuffle.
}

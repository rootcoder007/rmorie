% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datasets_tps_arcgis_hub.R
\name{morie_datasets_tps_arcgis_hub_by_id}
\alias{morie_datasets_tps_arcgis_hub_by_id}
\title{Generic TPS ArcGIS Hub dataset loader by hub_id}
\usage{
morie_datasets_tps_arcgis_hub_by_id(
  hub_id,
  format = "json",
  where = "1=1",
  max_features = NULL,
  layer_idx = 0L,
  offline = TRUE,
  dest = NULL
)
}
\arguments{
\item{hub_id}{32-char hex GUID. See
\code{\link[=morie_datasets_tps_arcgis_hub_layers]{morie_datasets_tps_arcgis_hub_layers()}} for the canonical 71.}

\item{format}{One of \code{"json"} (default), \code{"geojson"}, \code{"csv"},
\code{"shapefile"}, \code{"fgdb"}.}

\item{where}{SoQL-style \code{WHERE} filter passed to FeatureServer
\verb{?where=} (default \code{"1=1"}).}

\item{max_features}{Optional cap on returned rows. Passed as
\code{resultRecordCount} to FeatureServer; ignored for binary formats.}

\item{layer_idx}{Integer index of the FeatureServer layer to pull
(default \code{0L}, the first layer).}

\item{offline}{Logical; if \code{TRUE}, the hub_id is resolved via the
bundled catalog (no network needed for the resolution step).
Default \code{TRUE} -- you can run this against the 71 catalog
entries without network. Live data fetches always hit the
network regardless of this argument; "offline" here only
affects the hub_id -> FeatureServer URL lookup.}

\item{dest}{Optional path for binary downloads
(\code{format \%in\% c("shapefile", "fgdb")}); defaults to \code{tempfile()}.}
}
\value{
A \code{data.frame} (json / csv), a parsed GeoJSON list, or a
file path (binary).
}
\description{
Single entry point for the 71 datasets listed by
\code{\link[=morie_datasets_tps_arcgis_hub_layers]{morie_datasets_tps_arcgis_hub_layers()}}. Supports five export
formats:
}
\details{
\describe{
\item{\code{"json"} (default)}{Hits the FeatureServer
\verb{/0/query?where=...&outFields=*&f=json} endpoint and parses
attributes into a tidy \code{data.frame}. Same path the existing
TPS PSDP loaders (3FF) use; honours an arbitrary \code{where}
clause and \code{max_features} cap.}
\item{\code{"geojson"}}{Hits \code{?f=geojson} and returns the raw GeoJSON
as a parsed R list. Caller can pass to \code{sf::st_read()}.}
\item{\code{"csv"}}{Hits the Hub CSV exporter at
\verb{hub.arcgis.com/api/v3/datasets/<hub_id>_0/downloads/data?format=csv}
and parses the returned CSV into a \code{data.frame}.}
\item{\code{"shapefile"} / \code{"fgdb"}}{Downloads the binary archive
(Esri Shapefile zip / File Geodatabase zip) to \code{dest}
(default \code{tempfile()}) and returns the file path. Caller can
\code{sf::st_read()} the unzipped contents.}
}

For boundary layers (Police Divisions, Patrol Zone, Facilities)
you'll typically want \code{format = "geojson"} to get the polygon
geometry. For tabular outputs (UoF tables, KSI counts, ASR
tables, budgets) \code{format = "json"} is sufficient and lightest.
}

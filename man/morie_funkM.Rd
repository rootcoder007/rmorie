% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/funkM_native.R
\name{morie_funkM}
\alias{morie_funkM}
\title{morie_funkM}
\usage{
morie_funkM(
  ratings,
  n_users,
  n_items,
  factors = 8,
  epochs = 60,
  lr = 0.005,
  reg = 0.02,
  seed = 0,
  incremental = FALSE,
  epochs_per_factor = 20
)
}
\arguments{
\item{ratings}{Passed to \code{.funkM_as_ratings}.}

\item{n_users}{Coerced to integer by the body, with \code{as.integer}.}

\item{n_items}{Coerced to integer by the body, with \code{as.integer}.}

\item{factors}{Coerced to integer by the body, with \code{as.integer}. Defaults to \code{8}.}

\item{epochs}{Coerced to integer by the body, with \code{as.integer}. Defaults to \code{60}.}

\item{lr}{Passed to \code{.funkM_sgd_epoch}. Defaults to \code{0.005}.}

\item{reg}{Passed to \code{.funkM_sgd_epoch}. Defaults to \code{0.02}.}

\item{seed}{Passed to \code{.ghc_rng}. Defaults to \code{0}.}

\item{incremental}{A flag; the body branches on it. Defaults to \code{FALSE}.}

\item{epochs_per_factor}{Coerced to integer by the body, with \code{as.integer}.
Defaults to \code{20}.}
}
\value{
The value of \code{result}, as built in the body.
}
\description{
A step of the funkM_native implementation. No other function in the package calls it.
See the file header for the source the module follows.
source it follows.
}
\examples{
set.seed(1)
n_users <- 20L; n_items <- 15L
u <- sample.int(n_users, 300, TRUE) - 1L
i <- sample.int(n_items, 300, TRUE) - 1L
r <- 3 + 0.4 * (u \%\% 3) - 0.3 * (i \%\% 4) + rnorm(300, 0, 0.1)
ratings <- data.frame(u = u, i = i, r = r)
fit <- morie_funkM(ratings, n_users, n_items, factors = 3,
                   epochs = 30, lr = 0.02, seed = 1)
fit$rmse
# the error falls as the epochs run
round(head(fit$rmse_history, 3), 4)
round(tail(fit$rmse_history, 3), 4)
}
\keyword{internal}

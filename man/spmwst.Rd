% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spmwst.R
\name{spmwst}
\alias{spmwst}
\title{Moving-window kriging with locally re-estimated semivariograms}
\usage{
spmwst(
  coords,
  z,
  window_size = NULL,
  targets = NULL,
  min_sites = 35L,
  step = 5L,
  n_lags = 10L,
  local_variogram = TRUE,
  local_mean = FALSE
)
}
\arguments{
\item{coords}{Site coordinates, (n, d).}

\item{z}{Observations, length n.}

\item{window_size}{Optional fixed radius overriding Haas's adaptive rule;
the sites actually captured per window are reported, and a window
holding fewer than 35 sites draws a warning.}

\item{targets}{Prediction locations; defaults to the observed sites.}

\item{min_sites, step}{Haas's rule parameters (35 and 5 in the book).}

\item{n_lags}{Lag classes for the empirical semivariogram.}

\item{local_variogram}{\code{TRUE} for Haas's method, \code{FALSE} for local
kriging with the global model.}

\item{local_mean}{Re-estimate the mean inside each window.}
}
\value{
A list with \code{local_variograms} (per-window sill and range),
\code{prediction}, \code{window_sizes}, \code{converged}, \code{global_sill},
\code{global_range}, \code{theta_is_global}, \code{caveats}, \code{targets}, and for a
fixed window \code{fixed_window_size}, \code{fixed_window_counts} and possibly
\code{warning}.
}
\description{
Local kriging restricts the solve to a neighbourhood but keeps ONE global
covariance model: "all n data points contribute to the estimation of
theta in local kriging" (p. 425). The moving-window method of Haas (1990,
1995) re-estimates the semivariogram \emph{inside each window}, so every
prediction location carries its own \eqn{\theta_i}. That distinction is
the content of the section and \code{local_variogram} selects between them.
}
\details{
Window size follows p. 426 literally: enlarge a circle about the
prediction site until at least \code{min_sites} sites are inside, then add
\code{step} at a time until every lag class holds at least one pair and the
local fit converges. The book's cautions are returned rather than hidden:
the local predictor "is no longer best", and changing neighbourhoods can
introduce "spurious discontinuities".
}
\references{
Schabenberger Ch 8, Sec 8.3.1, pp. 425-426. Haas (1990),
JASA 85:950-963; Haas (1995), JASA 90:1189-1199.
}

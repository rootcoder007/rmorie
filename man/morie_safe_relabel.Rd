% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/categorical_guard.R
\name{morie_safe_relabel}
\alias{morie_safe_relabel}
\title{Relabel a categorical variable by name, never by position}
\usage{
morie_safe_relabel(x, mapping, keep = character())
}
\arguments{
\item{x}{A factor or character vector.}

\item{mapping}{Named character vector, old label to new label. Every
observed level must appear as a name unless listed in \code{keep}.}

\item{keep}{Levels allowed to pass through unchanged.}
}
\value{
A factor with the new labels, levels in the order of \code{mapping},
carrying a \code{morie_recode_audit} attribute (see
\code{\link{morie_safe_recode}}).
}
\description{
A relabel is only safe when every old level is mapped BY NAME to its new
label. Assigning a vector of labels by position (the \verb{levels<-} idiom,
or \code{factor(x, labels = ...)} with labels in a different order from the
codes) is the mechanism behind the documented four-way rotation in the
OHRC 2023 correction, so this function refuses unnamed mappings outright.
}
\examples{
f <- factor(c("W", "B", "O", "W"))
morie_safe_relabel(f, c(W = "White", B = "Black", O = "Other"))
try(morie_safe_relabel(f, c("Black", "Other", "White")))
}
\seealso{
\code{\link{morie_safe_recode}},
\code{\link{morie_relabel_forensics}},
\code{\link{morie_decode_labelled}}
}

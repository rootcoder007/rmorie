% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/propinf_native.R
\name{.propinf_forward}
\alias{.propinf_forward}
\title{.propinf_forward}
\usage{
.propinf_forward(net, x)
}
\arguments{
\item{net}{A vector; its length is taken and its elements indexed.}

\item{x}{Coerced to numeric by the body, with \code{as.numeric}.}
}
\value{
A list with \code{acts}, \code{pre}.
}
\description{
A step of the propinf_native implementation. Called by \code{morie_propinf_fcnn_predict}, \code{morie_propinf_train_fcnn}.
See the file header for the source the module follows.
the source it follows.
}
\examples{
# a small fully connected net: three hidden units over two inputs,
# then a single logistic output
net <- list(
  list(W = matrix(c(2, 3,
                    1, 0,
                    4, 5), nrow = 3, byrow = TRUE), b = c(10, 20, 30)),
  list(W = matrix(c(0.5, -0.5, 0.25), nrow = 1), b = 7)
)
fp <- .propinf_forward(net, c(1, -2))
fp$pre[[1]]        # pre-activations of the hidden layer
fp$acts[[2]]       # after ReLU
fp$acts[[3]]       # the logistic output
}

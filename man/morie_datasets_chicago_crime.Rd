% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datasets.R
\name{morie_datasets_chicago_crime}
\alias{morie_datasets_chicago_crime}
\title{City of Chicago "Crimes -- 2001 to Present" feed (\code{ijzp-q8t2})}
\usage{
morie_datasets_chicago_crime(
  year = NULL,
  max_features = NULL,
  offline = TRUE,
  mode = c("soda2", "soda3"),
  paginate = FALSE,
  page_size = 1000L,
  max_pages = 200L,
  app_token = NULL
)
}
\arguments{
\item{year}{Integer or \code{NULL}; server-side year filter.}

\item{max_features}{Integer or \code{NULL}; cap on returned rows. When
\code{paginate = TRUE} this is the total cap across all walked pages.}

\item{offline}{Logical; if \code{TRUE}, return the bundled synthetic frame.}

\item{mode}{One of \code{"soda2"} (default) or \code{"soda3"}. Selects the
API path for live mode:
\itemize{
\item \code{"soda2"} -> \verb{/resource/<id>.json?$where=...} via
\code{\link[=.morie_dataset_socrata_fetch]{.morie_dataset_socrata_fetch()}} (URL-param SoQL grammar).
\item \code{"soda3"} -> \verb{/api/v3/views/<id>/query.json?query=SELECT ...}
via \code{\link[=.morie_dataset_soda3_query]{.morie_dataset_soda3_query()}} (full SoQL passthrough).
Both modes return the same 22-column schema; SODA3 is required
when a derived/map view is involved (none here, but available
for parity with \code{\link[=morie_datasets_chicago_crime_map]{morie_datasets_chicago_crime_map()}}) and for
the canonical "single SoQL string" experience.
}}

\item{paginate}{Logical; if \code{TRUE} and \code{offline = FALSE}, walk
pagination in \code{page_size} chunks. SODA2 uses \verb{$offset}; SODA3
uses \verb{LIMIT page_size OFFSET m} baked into the SoQL.}

\item{page_size}{Integer; per-page row count when \code{paginate = TRUE}.
Default 1,000 (the unauthenticated SODA2 ceiling).}

\item{max_pages}{Integer; safety net on \code{paginate = TRUE} walks
(default 200 -> up to 200,000 rows without an app_token).}

\item{app_token}{Optional Socrata app token (SODA3 only -- sent
as the \code{X-App-Token} header; ignored under \code{mode = "soda2"}).}
}
\value{
A \code{data.frame} with the documented Socrata schema.
}
\description{
Wraps the City of Chicago "Crimes -- 2001 to Present" open dataset
(Socrata resource id \code{ijzp-q8t2}; portal landing
\url{https://data.cityofchicago.org/Public-Safety/Crimes-2001-to-Present/ijzp-q8t2/about_data}).
22-column schema, one row per reported crime incident (except
murders, where one row per victim). Data are extracted from the
Chicago PD CLEAR system, refreshed daily with a seven-day lag,
and addresses are block-only redacted.
}
\details{
\strong{Scale warning.} As of 2026-05 the live feed carries ~8,557,071
rows (8.56M; last refreshed 2026-05-23) -- too large for
spreadsheet programs and slow even for programmatic pulls without
filtering. Always prefer narrowing the query first (\code{year = ...}
server-side filter) or paginating with \code{paginate = TRUE} + a
large \code{page_size} (and ideally an app_token). A full unfiltered
pull at the default \code{page_size = 1000} would issue ~8,560
requests; with \code{page_size = 50000} + an app_token it drops to
~172.

Socrata accepts both the numeric id (\verb{/resource/ijzp-q8t2.json})
and the publisher's \code{crimes} alias (\verb{/resource/crimes.json}).
SODA3 endpoints are also available
(\verb{/api/v3/views/crimes/query.json}), as are CSV variants
(\verb{/resource/crimes.csv}, \verb{/api/v3/views/crimes/query.csv}).
morie defaults to SODA2 JSON via the UUID for stability.

\strong{Cross-referenced datasets (Chicago Open Data).} The 22-col
schema carries geographic and crime-classification foreign keys
that other Chicago datasets resolve:

\describe{
\item{beat}{morie wraps via
\code{\link[=morie_datasets_chicago_police_beats]{morie_datasets_chicago_police_beats()}} (\code{n9it-hstw}).}
\item{district}{morie wraps via
\code{\link[=morie_datasets_chicago_police_districts]{morie_datasets_chicago_police_districts()}} (\verb{24zt-jpfn}).}
\item{ward}{morie wraps via
\code{\link[=morie_datasets_chicago_wards]{morie_datasets_chicago_wards()}} (\verb{sp34-6z76}, 3UU).}
\item{community_area}{morie wraps via
\code{\link[=morie_datasets_chicago_community_areas]{morie_datasets_chicago_community_areas()}} (\verb{cauq-8yn6}, 3UU).}
\item{iucr / fbi_code}{morie wraps via
\code{\link[=morie_datasets_chicago_iucr_codes]{morie_datasets_chicago_iucr_codes()}} (\verb{c7ck-438e}, 3UU).}
}
}

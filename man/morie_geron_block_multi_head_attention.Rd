% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geron_train_native.R
\name{morie_geron_block_multi_head_attention}
\alias{morie_geron_block_multi_head_attention}
\title{Per-head multi-head attention used by the transformer blocks}
\usage{
morie_geron_block_multi_head_attention(Q_in, KV_in, weights, mask = NULL)
}
\arguments{
\item{Q_in}{Query-side input.}

\item{KV_in}{Key/value-side input.}

\item{weights}{Named list with \code{WQ}, \code{WK}, \code{WV} (lists of per-head
matrices) and \code{WO}.}

\item{mask}{Optional attention mask.}
}
\value{
List with \code{output} and \code{weights} (one per head).
}
\description{
Per-head multi-head attention used by the transformer blocks
}
\examples{
set.seed(7)
Q <- matrix(rnorm(12), 3, 4)
KV <- matrix(rnorm(20), 5, 4)
h1 <- matrix(rnorm(8, 0, 0.5), 4, 2)
wts <- list(WQ = list(h1, h1), WK = list(h1, h1), WV = list(h1, h1),
            WO = matrix(rnorm(16, 0, 0.5), 4, 4))
r <- morie_geron_block_multi_head_attention(Q, KV, wts)
dim(r$output)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nprphet.R
\name{Nprphet}
\alias{Nprphet}
\title{Additive forecast decomposition: trend, seasonality, autoregression}
\usage{
Nprphet(ds, y, ar_layers = 0, n_changepoints = 3, seasonality = c(365.25, 3))
}
\arguments{
\item{ds}{Numeric increasing time index.}

\item{y}{Observed series.}

\item{ar_layers}{Number of autoregressive lags p.}

\item{n_changepoints}{Interior trend changepoints at equally spaced quantiles of ds.}

\item{seasonality}{Length-2 vector: period and number of Fourier terms.}
}
\value{
List with \code{estimate}, \code{coef}, \code{fitted}, \code{resid}, \code{rmse}, \code{n}.
}
\description{
NeuralProphet keeps the Prophet decomposition but fits it, and adds an
autoregression block. With the linear AR-Net the model collapses to a
single design matrix: piecewise-linear trend, Fourier seasonality and
p lags estimated in one least squares.
}
\details{
Formula: \code{y(t) = T(t) + S(t) + A(t)},
\code{T(t) = (d0 + G(t)'d) t + (r0 + G(t)'r)},
\code{S_p(t) = sum_j a_j cos(2 pi j t / p) + b_j sin(2 pi j t / p)},
linear AR \code{A(t) = sum_i w_i y(t - i)}.
}
\references{
Triebe, O. et al. (2021). NeuralProphet: explainable
forecasting at scale. arXiv:2111.15397.
}
\examples{
V <- c(1, 2, 3, 4, 5, 6, 7, 8)
Nprphet(V, V)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fzt21.R
\name{Kdfgeoext}
\alias{Kdfgeoext}
\alias{morie_fauzi_thm2_1_expected_kdfe}
\title{Geometric extrapolation of the expected KDFE (Theorem 2.1)}
\usage{
Kdfgeoext(jh, jah, a)

morie_fauzi_thm2_1_expected_kdfe(jh, jah, a)
}
\arguments{
\item{jh}{\verb{J_h(x) = E\[Fhat_h(x)\]}.}

\item{jah}{\code{J_ah(x)}, the same at bandwidth \verb{a h}.}

\item{a}{Second smoothing parameter; \code{a > 0}, \code{a != 1}.}
}
\value{
Named list with \code{estimate}, \code{t1}, \code{t2}, \code{a}, \code{method}.
}
\description{
Theorem 2.1: for any \code{a > 0}, \code{a != 1},
\deqn{\[J_h(x)\]^{t_1}\[J_{ah}(x)\]^{t_2} = F_X(x) + O(h^4),\quad t_1 = \frac{a^2}{a^2-1},\ t_2 = -\frac{1}{a^2-1},}{\[J_h(x)\]^t1 \[J_ah(x)\]^t2 = F(x) + O(h^4), t1 = a^2/(a^2-1), t2 = -1/(a^2-1),}
with \verb{J_h = E\[Fhat_h\]}. The exponents solve \code{t1 + t2 = 1} (keep \verb{log F})
and \verb{t1 + a^2 t2 = 0} (kill the \code{h^2} term).
}
\details{
Contrast Chapter 1, where the same device forced the bandwidth ratio to 4:
there the expansion ran in \code{sqrt(h)}, so the second condition was
\verb{t1 + 2 t2 = 0}, which pins the ratio. Here it runs in \code{h^2} and \code{a} stays
FREE -- a genuine second smoothing parameter which, per Remark 2.2, need
not depend on \code{n}. Large \code{a} returns the plain KDFE; \code{a} near 0 is unwise,
since \code{a} divides the argument of \code{W}.

The exponents come back so the caller can see they sum to 1 -- the
invariant that makes the result a distribution function value.
}
\examples{
Kdfgeoext(jh = 0.5, jah = 0.5, a = 2)
}
\references{
Fauzi and Maesono (2023), Theorem 2.1.
}
\keyword{internal}

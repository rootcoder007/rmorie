% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/randres.R
\name{Randres}
\alias{Randres}
\title{Recover a population rate from deliberately noised answers}
\usage{
Randres(y, truth = NULL, p = 0.7)
}
\arguments{
\item{y}{Observed yes/no answers.}

\item{truth}{True statuses when known; reported only.}

\item{p}{Probability the randomiser selected the sensitive question.}
}
\value{
List with \code{estimate}, \code{se}, \code{lambda},
\code{truth_rate}, \code{n}.
}
\description{
The respondent answers one of two questions chosen by a private
randomiser, so no individual answer reveals anything -- which is what
makes people answer honestly. The rate is still identified because the
noise mechanism is known. The estimator blows up as p nears one half,
where the answer carries no information at all.
}
\details{
Formula: \code{pi = (lambda - (1 - p))/(2p - 1)},
\code{Var = lambda(1 - lambda)/(n (2p - 1)^2)}.
}
\references{
Warner, S. L. (1965). JASA 60:63-69, equations (1), (3).
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/abundance_text_voting_native.R
\name{morie_face_smooth}
\alias{morie_face_smooth}
\title{FACE sandwich-smoothed covariance with FPCA}
\usage{
morie_face_smooth(
  Y,
  argvals = NULL,
  n_basis = 12L,
  degree = 3L,
  lambdas = NULL,
  pve = 0.99,
  penalty_order = 2L
)
}
\arguments{
\item{Y}{Curves, one row per subject, on a common grid.}

\item{argvals}{Grid points; defaults to an equally spaced grid.}

\item{n_basis, degree, penalty_order}{Spline controls.}

\item{lambdas}{Smoothing parameters to search.}

\item{pve}{Proportion of variance explained.}
}
\value{
A list with \code{covariance}, \code{eigenvalues}, \code{eigenfunctions},
\code{noise_variance}, \code{npc}, \code{lambda}, \code{mean_function}.
}
\description{
The raw covariance is smoothed by a sandwich \eqn{S \hat C S'}, which
keeps the result symmetric by construction and never assembles the
two-dimensional problem.
}
\details{
THE DIAGONAL MUST BE HELD OUT, and that is the whole method. With
white measurement error the raw diagonal sits a constant
\eqn{\sigma^2} above the surface while the off-diagonal is unbiased,
so smoothing through it drags that ridge outwards and inflates the
leading eigenvalue. The diagonal is instead imputed from the smoothed
surface and iterated to a fixed point. Filling it with the mean of
the rest of the row -- the obvious first choice -- is badly wrong: on
a two-component Karhunen-Loeve design with sigma^2 = 0.09 it returned
0.334 and left 13 per cent of the eigenvalue mass negative.
}
\examples{
set.seed(1)
tt <- seq(0, 1, length.out = 30)
Y <- t(replicate(8, sin(2 * pi * tt) + rnorm(30, 0, 0.2)))
r <- morie_face_smooth(Y, argvals = tt, n_basis = 8L)
str(r, max.level = 1)
}
\references{
Xiao L, Zipunnikov V, Ruppert D, Crainiceanu C (2016)
\emph{Stat Comput} 26:409-421, \doi{10.1007/s11222-014-9485-x}.
}

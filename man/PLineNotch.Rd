% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aaa_rangayyan_qrs.R
\name{PLineNotch}
\alias{PLineNotch}
\title{Eq (3.150): H(z) = 1 - 2 cos(wo) z^-1 + z^-2, divided by its DC gain}
\usage{
PLineNotch(x, fs, f0 = 60, harmonics = 1)
}
\arguments{
\item{x}{Passed to \code{.morie_qrs_check}.}

\item{fs}{Numeric; combined arithmetically in the body.}

\item{f0}{Numeric; combined arithmetically in the body. Defaults to \code{60}.}

\item{harmonics}{A count; the body uses it as \code{seq_len(...)}. Defaults to \code{1}.}
}
\value{
A list with \code{y}, \code{coeffs}, \code{notched}, \code{f0}, \code{fs},
\code{n}, \code{method}.
}
\description{
H(1) = 2 - 2 cos(wo) so the passband gain at DC is unity.  Extra
zeros at n fo / fs make it a comb for the harmonics of a
non-sinusoidal mains waveform.
}
\examples{
set.seed(7)
fs <- 360
tv <- seq(0, 2, by = 1 / fs)
x <- sin(2 * pi * 5 * tv) + 0.5 * sin(2 * pi * 60 * tv)
PLineNotch(x, fs = fs, f0 = 60)
}
\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survival.R
\name{morie_survival_cox}
\alias{morie_survival_cox}
\title{Cox proportional hazards model.}
\usage{
morie_survival_cox(
  data,
  duration_col,
  event_col,
  covariate_cols,
  ties = c("efron", "breslow"),
  confidence = 0.95,
  penalizer = 0
)
}
\arguments{
\item{data}{data.frame.}

\item{duration_col}{Name of the time column.}

\item{event_col}{Name of the 0/1 event column.}

\item{covariate_cols}{Character vector of covariate column names.}

\item{ties}{"efron" (default) or "breslow".}

\item{confidence}{Confidence level (default 0.95).}

\item{penalizer}{L2 penalty (passed via \code{ridge()} term in the formula).}
}
\value{
A named list with elements \code{coefficients},
\code{standard_errors}, \code{hazard_ratios}, \code{z_scores},
\code{p_values}, \code{ci_lower}, \code{ci_upper},
\code{covariate_names}, \code{concordance}, \code{log_likelihood},
\code{n_events}, \code{n_observations}, \code{method},
\code{baseline_hazard}, \code{.coxph}.
}
\description{
Wraps \code{survival::coxph()} with Efron (default) or Breslow tie handling
and returns a tidy list including hazard ratios, CIs, p-values, and the
Breslow baseline cumulative hazard.
}
\examples{
set.seed(1)
df <- data.frame(t = rexp(60), e = rbinom(60, 1, 0.7),
                 x1 = rnorm(60), x2 = rnorm(60))
fit <- morie_survival_cox(df, "t", "e", c("x1", "x2"))
fit$hazard_ratios
}

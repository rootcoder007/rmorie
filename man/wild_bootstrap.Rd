% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap_methods.R
\name{wild_bootstrap}
\alias{wild_bootstrap}
\title{Wild bootstrap for linear regression with heteroskedasticity}
\usage{
wild_bootstrap(
  y,
  X,
  statistic_idx = 2L,
  n_boot = 999L,
  ci_level = 0.95,
  weight_distribution = "rademacher",
  seed = 42L
)
}
\arguments{
\item{y}{Numeric response vector.}

\item{X}{Numeric design matrix (include an intercept column).}

\item{statistic_idx}{Column index of the coefficient of interest
(1-based).}

\item{n_boot}{Number of replicates.}

\item{ci_level}{Confidence level.}

\item{weight_distribution}{\code{"rademacher"} or \code{"mammen"}.}

\item{seed}{Random seed.}
}
\value{
A \code{morie_bootstrap_result}.
}
\description{
Multiplies the residuals by random weights (Rademacher or Mammen)
and refits OLS. \code{sandwich::vcovBS} implements the standard
wild bootstrap variance-covariance and
\code{fwildclusterboot::boottest} adds cluster-wild
\emph{p}-values; both are cross-referenced here. The inline
implementation is retained because rmorie's API returns the
resampled coefficient distribution (not just a vcov), which is
what downstream MRM analyses consume.
}
\examples{
set.seed(1)
X <- cbind(1, rnorm(50))
y <- drop(X \%*\% c(1, 2)) + rnorm(50)
str(wild_bootstrap(y, X, n_boot = 199L), max.level = 1)
}
\seealso{
\code{sandwich::vcovBS}, \code{fwildclusterboot::boottest},
\code{morie_did_wild_cluster_bootstrap()}.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/defdtr.R
\name{Defdtr}
\alias{Defdtr}
\title{Deformable DETR attention}
\usage{
Defdtr(x, queries, K = 4, offsets = NULL, weights = NULL, seed = 42)
}
\arguments{
\item{x}{An H x W feature map.}

\item{queries}{A Q x 2 matrix of reference points in \link{0, 1}.}

\item{K}{Sampling points per query.}

\item{offsets}{Q x K x 2 offsets in pixels, or NULL.}

\item{weights}{Q x K attention weights, or NULL for 1/K each.}

\item{seed}{Seed of the deterministic stream.}
}
\value{
List with \code{estimate}, \code{out}, \code{samples},
\code{ref_pixels}, \code{Q}, \code{K}, \code{method}.
}
\description{
Formula: K reference points per query; sparse attention
}
\details{
Each query attends to only K sampled locations around its reference
point instead of every position, so the cost is O(K) rather than
O(HW) and convergence no longer needs 500 epochs.  The samples are
read by bilinear interpolation, which is what makes the learned
offsets differentiable.  With zero offsets and equal weights the
output is exactly the feature at the reference point.
}
\references{
Zhu et al. (2021), Deformable DETR, ICLR 2021.
}

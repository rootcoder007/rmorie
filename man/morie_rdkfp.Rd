% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rdkfp_native.R
\name{morie_rdkfp}
\alias{morie_rdkfp}
\title{RDKit path-based topological fingerprint}
\usage{
morie_rdkfp(
  adjacency,
  atomnum,
  aromatic = NULL,
  nbits = 2048L,
  minpath = 1L,
  maxpath = 7L,
  branched = TRUE,
  use_bond_order = TRUE
)
}
\arguments{
\item{adjacency}{Square symmetric matrix of bond orders.}

\item{atomnum}{Atomic number per atom.}

\item{aromatic}{Optional 0/1 aromaticity flag per atom.}

\item{nbits}{Folded fingerprint length, default 2048.}

\item{minpath}{Smallest subgraph size in bonds, default 1.}

\item{maxpath}{Largest subgraph size in bonds, default 7.}

\item{branched}{\code{TRUE} (default) enumerates all connected
subgraphs; \code{FALSE} enumerates only linear paths.  Both
routes the reference implementation offers are kept, branched
being RDKit's default.}

\item{use_bond_order}{Hash bond orders (default) or treat every
bond as single.}
}
\value{
A list with \code{bits}, \code{count}, \code{nset},
\code{features} (sorted unique subgraph hashes),
\code{nfeature}, \code{nsubgraph}, \code{a}, \code{nbits},
\code{minpath}, \code{maxpath}, \code{method}.
}
\description{
Enumerates molecular subgraphs of between \code{minpath} and
\code{maxpath} bonds, hashes each subgraph from its bonds and the
invariants and degrees of their end atoms, and folds the hashes
into a bit vector -- the Daylight-style path fingerprint that
RDKit's \code{RDKFingerprint} computes.
}
\references{
Daylight Chemical Information Systems (2019). Daylight
Theory Manual, Chapter 6: Fingerprints.
}
\examples{
morie_rdkfp(adjacency = 5L, atomnum = 5L)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evtlmom.R
\name{Evtlmom}
\alias{Evtlmom}
\title{Trimmed L-moments TL(s,t) of a sample}
\usage{
Evtlmom(x, s = 0, t = 0, order = 2)
}
\arguments{
\item{x}{Sample.}

\item{s}{Left trimming.}

\item{t}{Right trimming.}

\item{order}{Highest order r to compute.}
}
\value{
List with \code{lambda}, \code{estimate}, \code{tau},
\code{n}, \code{method}.
}
\description{
Formula: lambda_r^(s,t) = (1/r) sum_k (-1)^k C(r-1,k) E\link{X_(r+s-k):(r+s+t)}
}
\details{
with the order statistic expectation estimated by the unbiased
combinatorial weights
E\link{X_(j:m)} = sum_i C(i-1,j-1) C(n-i,m-j) / C(n,m) x_(i).  Trimming s
observations from the left and t from the right makes the moments
exist for heavy tails where ordinary L-moments do not.  With
s = t = 0 they reduce to Hosking's L-moments, so lambda_1 is the
sample mean and lambda_2 the L-scale.
}
\references{
Elamir & Seheult (2003), Comput. Statist. Data Anal.
43(3):299-314.
}

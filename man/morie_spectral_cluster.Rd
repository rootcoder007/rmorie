% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extenders_spatial.R
\name{morie_spectral_cluster}
\alias{morie_spectral_cluster}
\title{Spectral clustering via \pkg{kernlab}}
\usage{
morie_spectral_cluster(x, centers, ...)
}
\arguments{
\item{x}{Numeric matrix or data frame of features (rows =
observations).}

\item{centers}{Integer; the number of clusters to extract.}

\item{...}{Further arguments forwarded to \code{kernlab::specc}
(e.g. \code{kernel}, \code{kpar}, \code{nystrom.red},
\code{iterations}).}
}
\value{
A list with \code{$method = "kernlab::specc"} and
\code{$raw} (a \code{specc} S4 object containing the cluster
assignments, centres and within-cluster sums of squares).
}
\description{
Thin extender over \code{kernlab::specc} that performs Ng / Jordan
/ Weiss spectral clustering on a feature matrix.
}
\examples{
\donttest{
if (requireNamespace("kernlab", quietly = TRUE)) {
  set.seed(1)
  x <- rbind(
    matrix(stats::rnorm(80, mean = -2), ncol = 2),
    matrix(stats::rnorm(80, mean = 2), ncol = 2)
  )
  morie_spectral_cluster(x, centers = 2)
}
}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spkfnn.R
\name{spkfnn}
\alias{spkfnn}
\title{Leave-one-out cross-validation of a kriging model}
\usage{
spkfnn(coords, z, cov_model = NULL, mu = NULL)
}
\arguments{
\item{coords}{Observation coordinates (n by d).}

\item{z}{Observed values, length n.}

\item{cov_model}{List with \code{model}, \code{nugget}, \code{sill}, \code{range}.}

\item{mu}{Known mean; the sample mean of the retained points when NULL.}
}
\value{
Named list: mspe, rmspe, me, residuals, standardised,
std_variance, n.
}
\description{
Kriging honours the data, so in-sample residuals are identically zero
and carry no information about model fit. Cross-validation removes
each observation in turn, predicts it from the rest, and reports the
mean squared prediction error. The standardised residuals should have
mean near zero and variance near one if the covariance model is right;
their variance is the diagnostic that catches a mis-specified sill.
}
\examples{
co <- matrix(runif(40), 20, 2) * 5
spkfnn(co, rnorm(20), list(model = "exponential", sill = 1, range = 2))
}
\references{
Schabenberger & Gotway (2005), Ch 5.
}

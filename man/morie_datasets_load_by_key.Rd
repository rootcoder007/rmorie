% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dataset_load_by_key.R
\name{morie_datasets_load_by_key}
\alias{morie_datasets_load_by_key}
\title{Load a dataset by its cross-portal catalog \code{dataset_key}}
\usage{
morie_datasets_load_by_key(
  dataset_key,
  offline = TRUE,
  max_features = NULL,
  mode = c("auto", "soda2", "soda3", "odata"),
  app_token = NULL,
  source = NULL
)
}
\arguments{
\item{dataset_key}{A \code{dataset_key} from
\code{\link[=morie_dataset_portal_catalog]{morie_dataset_portal_catalog()}}.}

\item{offline}{If \code{TRUE} (default), prefer bundled fixtures.
\code{FALSE} forces live mode for loaders that support it.}

\item{max_features}{Optional row cap forwarded to the underlying
loader.}

\item{mode}{One of \code{"auto"} (default), \code{"soda2"}, \code{"soda3"},
\code{"odata"}. Only honoured for Socrata-backed sources
(chicago / nyc_nypd / nyc_opendata) that have a per-wrapper
\code{mode} argument. \code{"auto"} lets each underlying loader pick
its native default (soda2 today). 3FFF2.}

\item{app_token}{Optional Socrata application token forwarded
to SODA3-capable loaders. 3FFF2.}

\item{source}{Optional portal disambiguator (e.g.,
\code{"vancouver_opendata"} vs \code{"toronto_opendata"}) used when the
\code{dataset_key} collides across portals. 3HHH5. Omit when the key
is unique (the common case); pass when an ambiguous-key error
is raised to pick the intended portal.}
}
\value{
A \code{data.frame} (or, for StatCan, the WDS metadata list).
}
\description{
Phase 3EEE4. Single entry point that dispatches to the right
loader function for any of the ~550 datasets in
\code{\link[=morie_dataset_portal_catalog]{morie_dataset_portal_catalog()}}. Lets callers say
\code{morie_datasets_load_by_key("vpd_crime")} or
\code{morie_datasets_load_by_key("hub:b4d0...assault")} without
remembering whether the relevant loader is
\code{morie_datasets_vpd_crime()} or
\code{morie_datasets_tps_arcgis_hub_by_id()}.
}
\details{
Resolution rules:

\describe{
\item{Bundled-fixture loaders}{If the catalog's \code{loader}
column names a function that takes no required arguments
beyond optionally \code{offline}/\code{max_features}, that function is
called directly with \code{offline = offline} + \code{max_features}.}
\item{Generic CKAN dispatchers}{For Ontario / Montreal /
Toronto CKAN catalog entries whose loader is the generic
\verb{morie_datasets_*_ckan_resource()}, the \code{id} (CKAN
package_name slug) is resolved to a primary resource via
\code{package_show} + first CSV resource, then fetched.}
\item{Generic ArcGIS Hub dispatcher}{For TPS Hub entries
(\code{source == "tps_arcgis_hub"}), the bare \code{hub_id} is passed
to \code{\link[=morie_datasets_tps_arcgis_hub_by_id]{morie_datasets_tps_arcgis_hub_by_id()}}.}
\item{StatCan WDS}{For statcan_ccjs entries, returns the cube
metadata via \code{\link[=morie_datasets_statcan_cube_metadata]{morie_datasets_statcan_cube_metadata()}}.}
\item{Vancouver Opendatasoft}{For vancouver_opendata entries
beyond the 9 bundled fixtures, dispatches to
\code{\link[=morie_datasets_vancouver_opendata_by_id]{morie_datasets_vancouver_opendata_by_id()}}.}
}

For datasets where the catalog only knows a key + portal
(no row-level fixture, no targeted wrapper), \code{live = FALSE}
raises a clear error pointing at the right live-mode dispatcher.
}
\examples{
# All three calls below resolve to bundled offline fixtures (no
# network). The first call warms the cross-portal catalog cache
# (~2.8s); subsequent calls reuse it (<0.1s each).
df1 <- morie_datasets_load_by_key("vpd_crime")           # 550 rows
df2 <- morie_datasets_load_by_key("nypd_arrests_ytd")    # 5 rows
df3 <- morie_datasets_load_by_key("assault")             # 5 rows
c(vpd = nrow(df1), nypd = nrow(df2), tps_assault = nrow(df3))
}

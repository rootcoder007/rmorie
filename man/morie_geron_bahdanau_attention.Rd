% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geron_ml_native.R
\name{morie_geron_bahdanau_attention}
\alias{morie_geron_bahdanau_attention}
\title{Bahdanau (additive) attention (Geron Ch 14, morie.fn hmbdn and grbah)}
\usage{
morie_geron_bahdanau_attention(h, s_prev, W, U, v, b = NULL)
}
\arguments{
\item{h}{Encoder states (T, d_h).}

\item{s_prev}{Decoder state (d_s).}

\item{W}{(d_a, d_h).}

\item{U}{(d_a, d_s).}

\item{v}{(d_a).}

\item{b}{Optional bias (d_a).}
}
\value{
List with \code{alpha}, \code{weights}, \code{scores}, \code{context}, \code{entropy}, \code{argmax}.
}
\description{
e_i = v^T tanh(W h_i + U s + b); alpha = softmax(e); context = alpha h.
ONE R function serves both Python modules, whose only difference is
argument order and payload naming: grbah's
\code{geron_bahdanau_attention(decoder_state, encoder_states, Wh, Ws, v)}
maps onto this call as \code{h = encoder_states}, \code{s_prev = decoder_state},
\code{W = Ws}, \code{U = Wh}. Both payload spellings are returned (\code{alpha} is
hmbdn's, \code{weights} grbah's), and \code{argmax} is 0-based in both.
}

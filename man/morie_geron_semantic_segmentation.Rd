% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geron_w4d_native.R
\name{morie_geron_semantic_segmentation}
\alias{morie_geron_semantic_segmentation}
\title{Semantic segmentation: per-pixel class labels}
\usage{
morie_geron_semantic_segmentation(image, model, y_true = NULL)
}
\arguments{
\item{image}{(H,W) or (H,W,C) array, passed to model unchanged.}

\item{model}{Function(image) -> array (H,W,K) scores.}

\item{y_true}{Optional (H,W) integer (0-based) ground-truth labels.}
}
\value{
list with labels, scores, class_counts, iou, mean_iou, pixel_accuracy,
confusion, n_classes, estimate, n, method.
}
\description{
Per-pixel argmax over model(image) -> (H,W,K) scores; when y_true is
given, per-class IoU = |P_k & G_k| / |P_k | G_k|, mean over classes
present, pixel accuracy, confusion matrix.
}
\examples{
set.seed(6)
model <- function(img) {
  s <- array(rnorm(4 * 4 * 3), c(4, 4, 3))
  s[1:2, , 1] <- 5
  s
}
r <- morie_geron_semantic_segmentation(array(0, c(4, 4, 1)), model)
str(r, max.level = 1)
}

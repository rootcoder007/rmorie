% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geron_train_native.R
\name{morie_geron_stacked_autoencoder}
\alias{morie_geron_stacked_autoencoder}
\title{Stacked (deep) autoencoder forward pass (morie.fn grstae)}
\usage{
morie_geron_stacked_autoencoder(
  x,
  layer_weights,
  activation = "relu",
  tied = TRUE,
  output_activation = "linear"
)
}
\arguments{
\item{x}{Input batch.}

\item{layer_weights}{List of weight matrices.}

\item{activation}{"relu", "sigmoid", "tanh" or "linear".}

\item{tied}{Reuse the transposed encoder weights as the decoder.}

\item{output_activation}{Activation of the final decoder layer.}
}
\value{
List with \code{reconstruction}, \code{code}, \code{activations},
\code{reconstruction_error}, \code{compression}, \code{tied}.
}
\description{
Encoder matrices in, tied transposes back out; the bottleneck is
enforced and the tower's symmetry checked. No biases.
}
\examples{
set.seed(6)
x <- matrix(rnorm(12), 3, 4)
W1 <- matrix(rnorm(8, 0, 0.3), 4, 2)
r <- morie_geron_stacked_autoencoder(x, list(W1), activation = "relu",
                                     tied = TRUE)
str(r, max.level = 1)
}

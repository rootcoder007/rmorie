% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ingest_bigquery.R
\name{morie_ingest_bigquery_table}
\alias{morie_ingest_bigquery_table}
\title{Pull a BigQuery table (or filtered slice) into a data.frame}
\usage{
morie_ingest_bigquery_table(
  project,
  dataset,
  table,
  where = NULL,
  limit = NULL,
  select = "*",
  billing_project = NULL,
  page_size = 10000L,
  max_rows = Inf,
  quiet = TRUE
)
}
\arguments{
\item{project, dataset, table}{Fully-qualified BigQuery table
reference, e.g. \code{project = "bigquery-public-data"},
\code{dataset = "chicago_crime"}, \code{table = "crime"}.}

\item{where}{Optional raw SQL \code{WHERE} clause.}

\item{limit}{Optional \code{LIMIT}.}

\item{select}{Projection list (default \code{"*"}).}

\item{billing_project}{Billing project; falls back to
\code{GCP_PROJECT}, then ADC.}

\item{page_size}{Rows per download page.}

\item{max_rows}{Optional cap on rows downloaded.}

\item{quiet}{Suppress \pkg{bigrquery} progress output.}
}
\value{
A base R \code{data.frame}.
}
\description{
Convenience wrapper around
\code{\link{morie_ingest_bigquery_build_sql}} +
\code{\link{morie_ingest_bigquery_query}}: builds a validated,
backtick-quoted \code{SELECT} against a fully-qualified table and
downloads the result.
}
\examples{
\dontshow{if (nzchar(Sys.getenv("GCP_PROJECT")) && requireNamespace("bigrquery", quietly = TRUE)) withAutoprint(\{ # examplesIf}
# Runs when the caller has bigrquery + a GCP_PROJECT billing project.
# Keyless alternative for the same data: morie_ingest_chicago_crime()
# (Socrata, no account) or the bundled samples in 'rmoriedata'.
df <- morie_ingest_bigquery_table(
  project = "bigquery-public-data",
  dataset = "chicago_crime",
  table   = "crime",
  where   = "year = 2024",
  limit   = 10000L
)
head(df)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link{morie_ingest_bigquery_query}}
}

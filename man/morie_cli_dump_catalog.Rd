% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cli_unified_catalog.R
\name{morie_cli_dump_catalog}
\alias{morie_cli_dump_catalog}
\title{Emit a unified catalog CSV for the rmorie-cli binary}
\usage{
morie_cli_dump_catalog(out_path = NULL)
}
\arguments{
\item{out_path}{Where to write. Defaults to
\verb{inst/extdata/_unified_catalog.csv} inside the installed package
(or the dev tree if running under \code{devtools::load_all()}).}
}
\value{
Invisibly, the path written.
}
\description{
Walks \code{morie_datasets_browse()} (9242 datasets across all portals)
and writes a single CSV with the columns the C++ \code{rmorie} binary's
catalog parser expects: \code{dataset_key}, \code{portal}, \code{title},
\code{description}, \code{url}, \code{license}, \code{formats}.
}
\details{
The CLI binary prefers this unified file when present; otherwise it
falls back to scanning every \verb{inst/extdata/*_catalog.csv} (which
have heterogeneous per-portal schemas and don't parse cleanly).

Idempotent: regenerate after any change to the catalog registry.
}
\examples{
out <- morie_cli_dump_catalog(out_path = tempfile(fileext = ".csv"))
file.exists(out)
}

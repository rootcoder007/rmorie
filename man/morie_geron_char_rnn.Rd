% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geron_ml2_native.R
\name{morie_geron_char_rnn}
\alias{morie_geron_char_rnn}
\title{Character-level RNN language model (Geron Ch 16, morie.fn hmchrn)}
\usage{
morie_geron_char_rnn(
  text,
  hidden = 8,
  epochs = 50,
  lr = 0.1,
  seed = 0,
  generate = 0
)
}
\arguments{
\item{text}{Character scalar of at least 2 characters.}

\item{hidden}{Hidden width.}

\item{epochs}{Training epochs.}

\item{lr}{Learning rate.}

\item{seed}{LCG seed.}

\item{generate}{Number of characters to sample greedily.}
}
\value{
List with \code{loss_history}, \code{final_loss}, \code{chance_loss},
\code{perplexity}, \code{vocab}, \code{vocab_size}, \code{weights}, \code{hidden},
\code{generated}, \code{estimate}, \code{n}.
}
\description{
A vanilla RNN trained with FULL backpropagation through time on the
single sequence \code{text}. The vocabulary is the sorted unique
characters in codepoint order (\code{method = "radix"}, matching Python's
\code{sorted(..., key=str)}; R's locale collation would NOT match).
Weights come from one continuing LCG stream: Wxh (V x H) then Whh
(H x H), each reshaped row-major.
}
\details{
A uniform model scores log V, so \code{final_loss} below that is
structure the RNN has captured.
}
\examples{
D <- data.frame(x = c(1, 2, 3, 4), y = c(2, 4, 5, 9))
morie_geron_char_rnn(D)
}

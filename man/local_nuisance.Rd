% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/orfgrf_native.R
\name{local_nuisance}
\alias{local_nuisance}
\title{Weighted linear fit of \code{target} on W near a point. weights are the}
\usage{
local_nuisance(target, W, weights, exclude = NULL, ridge = 1e-08)
}
\arguments{
\item{target}{A matrix; passed to \code{crossprod}.}

\item{W}{A matrix; passed to \code{as.matrix}.}

\item{weights}{Coerced to numeric by the body, with \code{as.numeric}.}

\item{exclude}{Optional; may be \code{NULL}. Coerced to integer by the body, with
\code{as.integer}.}

\item{ridge}{Numeric; combined arithmetically in the body. Defaults to \code{1e-08}.}
}
\value{
A list with \code{fit}, \code{coef}.
}
\description{
forest kernel weights alpha_i(x), so the fit is local by
construction. exclude drops one index for the leave-one-out residual.
}
\examples{
local_nuisance(target = c(1, 2, 3, 4, 5, 6, 7, 8), W = c(1, 2, 3, 4, 5, 6, 7, 8),
  weights = c(1, 2, 3, 4, 5, 6, 7, 8))
}
\keyword{internal}

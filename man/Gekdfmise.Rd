% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fzt24.R
\name{Gekdfmise}
\alias{Gekdfmise}
\alias{morie_fauzi_thm2_4_mise_brdkdfe}
\title{MISE of the bias-reduced KDFE (Theorem 2.4)}
\usage{
Gekdfmise(n, h, a, biasint, varint, r1 = NULL, r2 = NULL)

morie_fauzi_thm2_4_mise_brdkdfe(n, h, a, biasint, varint, r1 = NULL, r2 = NULL)
}
\arguments{
\item{n}{Sample size.}

\item{h}{Bandwidth.}

\item{a}{Second smoothing parameter.}

\item{biasint}{\verb{int \[(b2^2 - 2 b4 F)/(2 F)\]^2 dx}.}

\item{varint}{\verb{int F(x)(1 - F(x)) dx}.}

\item{r1, r2}{Kernel constants; default to Gaussian \code{r1} and \code{r2(a)}.}

}
\value{
Named list with \code{mise}, \code{biasterm}, \code{varterm}, \code{smoothgain}, \code{h}, \code{a}, \code{method}.
}
\description{
Theorem 2.4:
\deqn{\mathrm{MISE}(\tilde F_X) = h^8 a^4\int\Big\[\frac{b_2^2 - 2b_4F}{2F}\Big\]^2 dx + \frac1n\int F(1-F)dx - \frac hn\Big\[\frac{2(a^4+1)}{(a^2-1)^2}r_1 + r_2\Big\] + o(h^8 + h/n).}{MISE(Ftilde) = h^8 a^4 int \[(b2^2 - 2 b4 F)/(2F)\]^2 dx + (1/n) int F(1-F) dx - (h/n)\[2(a^4+1)/(a^2-1)^2 r1 + r2\] + o(h^8 + h/n).}
}
\details{
Compare the plain KDFE's MISE from Sec. 2.1, which leads with
\verb{h^4 mu2^2 R(f')/4}: the bias term has gone from \code{h^4} to \code{h^8} while the
variance terms are unchanged in order. That is the claim in one line.

The third term has lost its \code{f(x)} factor relative to (2.3): integrating
\code{f} over the line gives 1. So the variance gain from smoothing is a pure
constant, independent of \code{F} -- which is why Sec. 2.1 can assert dominance
over the empirical df for EVERY \code{F_X}.

The integrals are the caller's to supply: they depend on the unknown \code{F_X},
and estimating them here would silently turn an exact theoretical quantity
into a plug-in with its own error.
}
\examples{
Gekdfmise(n = 100, h = 0.2, a = 2, biasint = 1, varint = 0.5)
}
\references{
Fauzi and Maesono (2023), Theorem 2.4.
}
\keyword{internal}

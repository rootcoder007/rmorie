% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bayes_mcmc.R
\name{morie_bayes_lm}
\alias{morie_bayes_lm}
\title{Fit a Bayesian linear regression by random-walk Metropolis}
\usage{
morie_bayes_lm(
  formula,
  data,
  prior_sd = 10,
  chains = 4L,
  iter = 2000L,
  warmup = 1000L,
  seed = 42L,
  starting_values = NULL,
  step = 0.1,
  check_convergence = TRUE,
  converge_threshold = 1.1,
  stop_on_convergence = FALSE,
  quiet = FALSE,
  verbose = FALSE
)
}
\arguments{
\item{formula}{A model formula.}

\item{data}{A data.frame.}

\item{prior_sd}{Prior standard deviation on the regression
coefficients (a single value, or a vector of length \code{p}). The
meaning of this hyperparameter is the scale of the zero-mean Normal
coefficient priors.}

\item{chains}{Number of independent chains.}

\item{iter}{Iterations per chain (post-warmup).}

\item{warmup}{Warmup (burn-in) iterations.}

\item{seed}{Base RNG seed; each chain uses \code{seed + chain_index} so
chains differ by default.}

\item{starting_values}{Optional numeric vector (length \code{p + 1}, the
coefficients plus log-sigma) used as the starting state of every
chain; or a list of such vectors, one per chain.}

\item{step}{Metropolis proposal scale.}

\item{check_convergence}{Whether to compute convergence diagnostics.}

\item{converge_threshold}{R-hat threshold for declaring convergence.}

\item{stop_on_convergence}{Stop early once converged.}

\item{quiet}{Suppress warnings/progress.}

\item{verbose}{Emit per-chain progress messages.}
}
\value{
A \code{morie_bayes_fit} object.
}
\description{
A self-contained MCMC sampler (no external backend). Runs multiple
seeded chains, supports explicit starting values and run continuation,
assesses convergence with several checkers, and returns the posterior
draws with diagnostics.
}
\examples{
d <- data.frame(x = rnorm(50)); d$y <- 1 + 2 * d$x + rnorm(50)
fit <- morie_bayes_lm(y ~ x, d, chains = 2, iter = 500, warmup = 200)
}

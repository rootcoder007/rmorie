% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extenders_stats.R
\name{morie_desc_cramers_v}
\alias{morie_desc_cramers_v}
\title{Cramer's V via \pkg{DescTools}}
\usage{
morie_desc_cramers_v(x, y = NULL, ...)
}
\arguments{
\item{x}{A factor, character vector, or contingency table.}

\item{y}{Optional second categorical vector when \code{x} is not
already a contingency table.}

\item{...}{Further arguments forwarded to
\code{DescTools::CramerV} (e.g. \code{conf.level}).}
}
\value{
A list with \code{$method = "DescTools::CramerV"} and
\code{$raw} (the upstream numeric or matrix).
}
\description{
Thin extender over \code{DescTools::CramerV} for the symmetric
association statistic between two categorical variables.
}
\examples{
if (requireNamespace("DescTools", quietly = TRUE)) {
  set.seed(1)
  x <- sample(c("a", "b"), 60, TRUE)
  y <- sample(c("u", "v"), 60, TRUE)
  morie_desc_cramers_v(table(x, y))
}
}

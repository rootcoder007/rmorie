% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bounds_native.R
\name{morie_permutation_attention_masks}
\alias{morie_permutation_attention_masks}
\title{Two-stream attention masks induced by a factorization order}
\usage{
morie_permutation_attention_masks(permutation)
}
\arguments{
\item{permutation}{Integer permutation of \code{0:(T-1)}, where
\verb{permutation\[t\]} is the sequence position generated \code{t}-th.}
}
\value{
A list with \code{content}, \code{query} and \code{rank}.
}
\description{
Returns the pair of logical matrices that make XLNet's permutation
objective computable in a single pass over an unpermuted sequence.
\verb{content\[i, j\]} is TRUE when position \code{i} may attend to position \code{j}.
}
\details{
The content stream sees \eqn{z_{\le t}} -- itself included -- and the
query stream sees only \eqn{z_{<t}}. The single difference is the
diagonal, and that diagonal is the whole reason two streams exist: a
query able to see its own token would make predicting that token
trivial, while a content stream unable to see it could not encode it
for later positions.
}
\examples{
morie_permutation_attention_masks(c(2L, 0L, 3L, 1L))
}
\references{
Yang Z, Dai Z, Yang Y, Carbonell J, Salakhutdinov R,
Le QV (2019) XLNet. arXiv:1906.08237, eq (3).
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wasserman_native.R
\name{morie_wasserman_white_huber}
\alias{morie_wasserman_white_huber}
\title{White-Huber sandwich covariance (Ch 9, wsmwhz)}
\usage{
morie_wasserman_white_huber(X, y, f = NULL)
}
\arguments{
\item{X}{Design matrix (n x p), n > p; add your own intercept.}

\item{y}{Response of length n.}

\item{f}{Optional mean function f(X, beta); NULL = linear.}
}
\value{
List with \code{estimate} (robust se of the first coefficient),
\code{beta}, \code{robust_se}, \code{covariance}, \code{bread}, \code{meat}, \code{n}, \code{p}, \code{method}.
}
\description{
V = A^-1 B A^-1 / n with A = X'X/n and B = X' diag(e^2) X / n, the
HC0 form. Matrices come back ROW-MAJOR to match the Python payload.
}
\examples{
X <- cbind(1, rep(c(-1, 1), 4)); y <- X \%*\% c(1, 2) + rep(c(0.5, -0.5, -0.5, 0.5), 2)
round(morie_wasserman_white_huber(X, y)$beta, 6)
}

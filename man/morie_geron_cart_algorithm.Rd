% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geron_ml2_native.R
\name{morie_geron_cart_algorithm}
\alias{morie_geron_cart_algorithm}
\title{CART algorithm (Geron Ch 5, morie.fn hmcart)}
\usage{
morie_geron_cart_algorithm(
  X,
  y,
  criterion = "gini",
  max_depth = NULL,
  min_samples_split = 2,
  min_samples_leaf = 1,
  min_impurity_decrease = 0
)
}
\arguments{
\item{X}{Feature matrix.}

\item{y}{Labels or targets.}

\item{criterion}{"gini", "entropy" or "mse".}

\item{max_depth}{Optional depth cap.}

\item{min_samples_split}{Minimum
node size to consider a split.}

\item{min_samples_leaf}{Minimum leaf
size.}

\item{min_impurity_decrease}{Minimum gain to accept a split.}
}
\value{
List with \code{tree}, \code{predictions}, \code{n_leaves}, \code{depth},
\code{n_splits}, \code{criterion}, \code{feature_importances}, \code{train_accuracy}
or \code{train_mse}, \code{estimate}, \code{n}.
}
\description{
Greedy binary splits minimising J(k, t) = (m_L/m) G_L + (m_R/m) G_R.
The split COST is delegated to \code{morie_geron_cart_split_cost} exactly
as the Python module delegates to grcart, so the impurity arithmetic
is never re-derived here. \code{feature} indices in the returned tree are
0-based, matching Python.
}
\details{
This one function serves the whole tree family: hmcdt
(\code{morie_geron_classification_tree}) is the only wrapper ported here,
but hmdtr, hmdthv, hmdtst, hmext, hmgbrt, hmrdt and hmrfc all route
their growth through this same core in Python.
}

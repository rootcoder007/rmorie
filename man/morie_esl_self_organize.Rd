% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/esl_native2.R
\name{morie_esl_self_organize}
\alias{morie_esl_self_organize}
\title{Self-organizing map}
\usage{
morie_esl_self_organize(
  X,
  grid = c(5L, 5L),
  eta = 0.5,
  n_epochs = 50L,
  sigma0 = NULL,
  seed = 0L
)
}
\arguments{
\item{X}{Data, n by p.}

\item{grid}{Lattice shape \code{c(rows, cols)}.}

\item{eta}{Initial learning rate in (0, 1].}

\item{n_epochs}{Passes over the data.}

\item{sigma0}{Initial neighbourhood width; defaults to \code{max(grid) / 2}.}

\item{seed}{Seed.}
}
\value{
List with \code{prototypes}, \code{lattice}, \code{assignment},
\code{quantization_error}, \code{topographic_error}, \code{counts}.
}
\description{
Prototypes live on a fixed lattice; the winner and its LATTICE neighbours
move toward each observation. Dragging the neighbours along is what
separates a SOM from k-means -- it forces prototypes adjacent on the grid to
be adjacent in data space, which is what makes the map usable as a display.
If the neighbourhood width decays too fast the SOM degenerates into online
k-means, which is the usual cause of a map showing no topological ordering.
}
\examples{
set.seed(1)
X <- matrix(runif(600), ncol = 2)
morie_esl_self_organize(X, grid = c(5, 5), seed = 1L)$topographic_error < 0.25
}
\references{
Kohonen, T. (1990). The self-organizing map. Proc. IEEE 78(9),
1464-1480.
}

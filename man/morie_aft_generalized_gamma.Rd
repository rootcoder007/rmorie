% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aft_native.R
\name{morie_aft_generalized_gamma}
\alias{morie_aft_generalized_gamma}
\title{Log-normal accelerated-failure-time model}
\usage{
morie_aft_generalized_gamma(time, event, X, ...)
}
\arguments{
\item{time}{follow-up times, strictly positive.}

\item{event}{1 for an event, 0 for right-censored.}

\item{X}{covariate matrix, one row per subject. An intercept is added.}

\item{...}{passed to the fitter (\code{max_iter}, \code{tol},
\code{add_intercept}).}
}
\value{
as \code{\link{morie_aft_weibull}}.
}
\description{
\eqn{\log T = x'\beta + \sigma W} with W standard normal -- the
log-normal member of the generalized-gamma family, which is what this
module provides. Like the log-logistic it gives a hazard that rises
and then falls; unlike it, the survival function has no closed form
and is evaluated through the normal.
}
\examples{
set.seed(2)
X <- matrix(rnorm(200), ncol = 2)
tt <- exp(0.5 + X \%*\% c(0.8, -0.4) + 0.6 * rnorm(100))
round(morie_aft_generalized_gamma(tt, rep(1, 100), X)$beta, 3)
}
\references{
Lawless, J. F. (2003). \emph{Statistical Models and
Methods for Lifetime Data}, 2nd ed., Sec. 5.3.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dp_native3.R
\name{morie_dp_logistic}
\alias{morie_dp_logistic}
\title{Differentially private logistic regression}
\usage{
morie_dp_logistic(
  X,
  y,
  epsilon = 1,
  method = c("objective", "output"),
  lam = 0.01,
  C = 1,
  n_iter = 100,
  lr = 0.1,
  seed = NULL
)
}
\arguments{
\item{X}{design matrix.}

\item{y}{0/1 response.}

\item{epsilon}{privacy budget.}

\item{method}{\code{"objective"} or \code{"output"}.}

\item{lam}{L2 regularisation; must be positive for objective
perturbation.}

\item{C}{row-norm clipping bound.}

\item{n_iter, lr}{gradient-descent controls.}

\item{seed}{optional integer seed.}
}
\value{
list with \code{beta}, \code{prob}, \code{accuracy},
\code{clipped_fraction}.
}
\description{
Objective perturbation (a random linear term added to the penalised
objective) or output perturbation (noise on the fitted coefficients).
}
\details{
Objective perturbation is the better mechanism at equal epsilon: the
noise enters before the optimisation, so the fit adapts to it instead
of carrying it. Note what the regulariser buys -- strong convexity is
what BOUNDS the sensitivity in the first place, so \code{lam} is not
a tuning knob here but part of the guarantee, and lam = 0 has no
guarantee at all.
}
\examples{
set.seed(1)
X <- matrix(rnorm(400), ncol = 2)
y <- as.numeric(runif(200) < 1 / (1 + exp(-(X \%*\% c(1, -1)))))
morie_dp_logistic(X, y, epsilon = 4)$accuracy > 0.4
}
\references{
Chaudhuri, K., Monteleoni, C. and Sarwate, A. D. (2011).
Differentially private empirical risk minimization. \emph{JMLR},
12, 1069-1109.
}

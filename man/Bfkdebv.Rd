% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fzt55.R
\name{Bfkdebv}
\alias{Bfkdebv}
\alias{morie_fauzi_thm5_5_bdfree_kde_bv}
\title{Bias and variance of the boundary-free KDE (Theorem 5.5)}
\usage{
Bfkdebv(n, h, density, c2, dg, mu2 = 1, rk = NULL)

morie_fauzi_thm5_5_bdfree_kde_bv(n, h, density, c2, dg, mu2 = 1, rk = NULL)
}
\arguments{
\item{n}{Sample size.}

\item{h}{Bandwidth.}

\item{density}{\code{f_X(x)}.}

\item{c2}{The Theorem 5.5 coefficient.}

\item{dg}{\verb{g'(g^-1(x))}, strictly positive.}

\item{mu2}{\verb{int v^2 K(v) dv}.}

\item{rk}{\verb{int K^2(v) dv}; defaults to the Gaussian \verb{1/(2 sqrt(pi))}.}
}
\value{
Named list with \code{bias}, \code{variance}, \code{se}, \code{mse}, \code{hopt}, \code{h}, \code{n}, \code{method}.
}
\description{
Theorem 5.5, Eqs. (5.10)-(5.11):
\deqn{\mathrm{Bias}[\tilde f_X(x)] = \frac{h^2 c_2(x)}{2g'(g^{-1}(x))}\mu_2(K) + o(h^2),\quad \mathrm{Var}[\tilde f_X(x)] = \frac{f_X(x)}{nhg'(g^{-1}(x))}\int K^2(v)dv + o(1/(nh)).}{Bias[ftilde(x)] = h^2 c2(x) mu2(K) / (2 g'(g^-1(x))) + o(h^2), Var[ftilde(x)] = f(x) int K^2 / (n h g'(g^-1(x))) + o(1/(nh)).}
}
\details{
Both carry \verb{1/g'}, and the SAME power in both -- which is why the
transformation does not change the shape of the bias-variance tradeoff, only
its constants. The MSE-optimal bandwidth is still \code{O(n^(-1/5))} and the
optimal MSE still \code{O(n^(-4/5))}.

The contrast with Theorem 5.2 is the point. There the variance was
\code{O(1/n) - O(h/n)} and smoothing HELPED; here it is \code{O(1/(nh))} and smoothing
hurts, so bandwidth must be traded against bias in the usual way. A
distribution function is estimable at the parametric rate; a density is not.

\code{rk} defaults to the Gaussian \verb{1/(2 sqrt(pi))}.
}
\examples{
Bfkdebv(n = 100, h = 0.3, density = 0.4, c2 = -0.1, dg = 1)
}
\references{
Fauzi and Maesono (2023), Theorem 5.5, Eqs. (5.10)-(5.11).
}
\keyword{internal}

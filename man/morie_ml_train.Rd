% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ml_pipeline.R
\name{morie_ml_train}
\alias{morie_ml_train}
\title{Train a specified model}
\usage{
morie_ml_train(model, x, y, verbose = FALSE)
}
\arguments{
\item{model}{A \code{morie_ml_model} specification.}

\item{x}{Predictor matrix or data.frame (numeric columns used).}

\item{y}{Response vector (0/1 for logistic).}

\item{verbose}{Print per-epoch progress (default FALSE). Progress can
thus be suppressed while warnings/errors are retained.}
}
\value{
A \code{morie_ml_fit} object (or the untrained spec if
\code{model$nofit}), carrying \code{weights}, \code{loss_path}, \code{grad_norm},
\code{converged}, and the input metadata.
}
\description{
The single unified training entry point. Runs the iterative optimiser
declared in the model spec, recording the loss at each epoch and the
gradient norm at each step, and returns a trained-model object.
}
\examples{
m <- morie_ml_model("linear", optimizer = "gd", epochs = 100)
fit <- morie_ml_train(m, mtcars[c("hp", "wt")], mtcars$mpg)
}

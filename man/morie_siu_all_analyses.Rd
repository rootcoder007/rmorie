% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/siu_analyze.R
\name{morie_siu_all_analyses}
\alias{morie_siu_all_analyses}
\title{Run every SIU descriptive analysis in this module}
\usage{
morie_siu_all_analyses(data = NULL, out_dir = NULL)
}
\arguments{
\item{data}{Either a data frame (e.g. the output of
\code{morie_fetch_siu()} read in) or a path to
\code{SIU_by_case.csv}. \code{NULL} (the default) looks in
\code{file.path(tempdir(), "morie", "siu", "SIU_by_case.csv")}.}

\item{out_dir}{Optional directory; if non-\code{NULL}, each result
is written as \code{siu_analysis_<name>.txt}.}
}
\value{
A named list of \code{morie_rich_result} objects.
}
\description{
Convenience wrapper that runs each of the analysis surfaces and
optionally writes a \code{.txt} dump (and \code{.json} payload if
\pkg{jsonlite} is available) per result. Failures are captured
per-analysis: one bad surface does not stop the rest.
}
\examples{
df <- data.frame(
  police_service = rep(c("Toronto Police Service", "Ontario Provincial Police"), 4),
  charges_recommended = rep(c(TRUE, FALSE), each = 4),
  date_of_incident_iso = rep(c("2022-01-05", "2023-02-18", "2024-03-01", "2024-11-11"), 2),
  date_siu_notified_iso = rep(c("2022-01-06", "2023-02-19", "2024-03-02", "2024-11-12"), 2),
  date_of_director_decision_iso = rep(c("2022-08-01", "2023-09-10", "2024-09-01", "2025-02-14"), 2),
  sex_gender_affected = rep(c("Male", "Female"), 4),
  age_affected = c("34", "27", "55", "19", "42", "31", "48", "29"),
  mental_health_or_race_indications = rep(c("mental health; Indigenous", ""), 4))
res <- morie_siu_all_analyses(df)
names(res)
}

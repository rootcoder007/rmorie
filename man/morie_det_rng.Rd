% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aaa_helpers_det_rng.R
\name{morie_det_rng}
\alias{morie_det_rng}
\title{SHA-keyed deterministic RNG for Py<->R parity}
\usage{
morie_det_rng(name, seed)
}
\arguments{
\item{name}{Character scalar; stable callable / fixture name.
Must be identical to the string the Python side passes.}

\item{seed}{Integer; user-supplied seed.}
}
\value{
Integer seed installed via \code{\link[=set.seed]{set.seed()}} (invisibly).
}
\description{
Given a callable / fixture name and an integer seed, derive a stable
R-side seed value via SHA-256, install it with \code{\link[=set.seed]{set.seed()}}, and
return it invisibly.  The matched Python helper
\code{morie._det_rng.from_seed(name, seed)} builds a \code{numpy.random.Generator}
from the same SHA digest so bootstrap / MCMC draws on the two sides
agree to Monte-Carlo tolerance (and bit-identical when a
deterministic-pseudo-bootstrap mode is plumbed).
}
\details{
Mechanism: \code{SHA-256(paste0(name, ":", seed))} is truncated to 32 bytes;
bytes \verb{[9:12]} (1-indexed, i.e. hex chars 17..24) form a 32-bit value
reduced modulo \code{2^31 - 1} and passed to \code{\link[=set.seed]{set.seed()}}.  Bytes \verb{[1:8]}
are reserved for the Python Philox key.  See \code{inst/python-stub/det_rng.py}
(or the parent \verb{morie/_det_rng.py}) for the Python counterpart.

Requires either the \code{digest} or \code{openssl} package for SHA-256.  Both
are widely available on CRAN; we try \code{digest} first, then \code{openssl},
and finally fall back to an internal pure-R SHA-256 implementation
loaded only when neither is available.  In practice CRAN reverse
dependencies of \code{morie} ship with at least one of the two.
}
\examples{
morie_det_rng("ksr07_bootstrap", 42L)
rnorm(5) # reproducible draws keyed by ("ksr07_bootstrap", 42)
}

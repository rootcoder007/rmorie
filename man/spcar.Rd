% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spcar.R
\name{spcar}
\alias{spcar}
\title{Conditional autoregressive model: the conditional specification.}
\usage{
spcar(z, w, covariates = NULL, parameterization = "weighted")
}
\arguments{
\item{z}{Response, length n.}

\item{w}{Symmetric adjacency weights (n by n).}

\item{covariates}{Covariates (n by p); an intercept when NULL.}

\item{parameterization}{"weighted" (conditional variance sigma^2/d_i)
or "identity" (constant). These are different models; forwarded to
\code{\link[=sgcar]{sgcar()}}.}
}
\value{
The result of \code{sgcar()}.
}
\description{
Rather than one multivariate model, the CAR approach models each
conditional distribution f(Z(s_i) | Z(s_j), s_j in N_i). By
Hammersley-Clifford these generate a valid joint Gaussian with
Sigma_CAR = (I - C)^-1 Sigma_c.
}
\details{
Same estimator as \code{\link[=sgcar]{sgcar()}}; this delegates rather than carrying a
second implementation.
}
\examples{
n <- 20
W <- matrix(0, n, n); W[cbind(1:(n - 1), 2:n)] <- 1; W <- W + t(W)
spcar(sin(seq_len(n) * 0.7), W)
}
\references{
Schabenberger & Gotway (2005), Sec 6.2.2.2, eqs
(6.43)-(6.45), pp. 338-339.
}

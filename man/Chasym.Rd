% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chasym.R
\name{Chasym}
\alias{Chasym}
\title{Asymptotic-variance check for an IPW marginal structural model}
\usage{
Chasym(y, A = NULL, H = NULL, B = 200, seed = 42)
}
\arguments{
\item{y}{Outcome.}

\item{A}{Treatment indicator, or NULL.}

\item{H}{IPW weights, or NULL for unit weights.}

\item{B}{Bootstrap replicates.}

\item{seed}{Seed of the deterministic bootstrap stream.}
}
\value{
List with \code{estimate}, \code{theta}, \code{var_if},
\code{var_boot}, \code{ratio}, \code{ess}, \code{agree}, \code{n},
\code{B}, \code{method}.
}
\description{
Formula: compare E\link{psi^2}/n to the bootstrap
}
\details{
The influence function of the IPW mean is psi_i = w_i (Y_i - theta),
so the sandwich variance is
sum w_i^2 (Y_i - theta)^2 / (sum w_i)^2.  If the estimator really is
asymptotically linear that number matches the bootstrap variance; a
systematic gap is the signal that the weights are too unstable for
the normal approximation.  With unit weights the sandwich reduces to
the plain sample variance over n.
}
\references{
van der Laan & Rose (2011), Targeted Learning, Springer,
ch. 5; Funk et al. (2011), Am. J. Epidemiology 173(7):761-767.
}
\examples{
V <- c(1, 2, 3, 4, 5, 6, 7, 8)
Chasym(V)
}

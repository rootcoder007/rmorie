% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spiso.R
\name{spiso}
\alias{spiso}
\title{Isotropy check by comparing directional semivariograms.}
\usage{
spiso(coords, z, n_dir = 4, n_bins = 10, max_dist = NULL, tol = 0.25)
}
\arguments{
\item{coords}{Coordinates (n by 2).}

\item{z}{Numeric vector of length n.}

\item{n_dir}{Angular sectors on the half circle; at least 2.}

\item{n_bins}{Lag bins within each sector.}

\item{max_dist}{Largest lag retained.}

\item{tol}{Relative spread above which isotropy is rejected.}
}
\value{
Named list: is_isotropic, relative_spread, directional_gamma,
angles, omnidirectional, tol.
}
\description{
A second-order stationary field is ISOTROPIC when the covariance
depends only on the LENGTH of the lag, C(h) = C(||h||), so no
direction is special. Under geometric anisotropy the iso-correlation
contours are ellipses rather than circles and the fix is the affine
correction of Sec 4.3.7.
}
\details{
Pairs are split into \code{n_dir} angular sectors on the HALF circle -- a
lag and its negation are the same direction -- a semivariogram is
fitted in each, and the spread across directions relative to the
overall level is the statistic.
}
\examples{
co <- matrix(runif(400), 200, 2) * 10
spiso(co, sin(co[, 1]) + cos(co[, 2]))$relative_spread
}
\references{
Schabenberger & Gotway (2005), Sec 2.2; correction Sec 4.3.7.
}

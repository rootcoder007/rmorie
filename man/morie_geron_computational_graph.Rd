% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geron_ml2_native.R
\name{morie_geron_computational_graph}
\alias{morie_geron_computational_graph}
\title{Computational graph with reverse-mode differentiation (Geron Ch 12, morie.fn hmcgrf)}
\usage{
morie_geron_computational_graph(expr, values = NULL)
}
\arguments{
\item{expr}{Expression tree.}

\item{values}{Named list of variable values.}
}
\value{
List with \code{value}, \code{grad}, \code{gradient}, \code{node_grads},
\code{nodes}, \code{n_nodes}, \code{topo_order}, \code{fd_check}, \code{estimate}, \code{n}.
}
\description{
Builds the expression DAG, evaluates it forwards, then makes ONE
reverse adjoint sweep that yields every partial derivative at once
-- whatever the number of variables. A central-difference check at
h = 1e-5 is returned alongside, so the analytic gradient is verified
by an independent route rather than asserted.
}
\details{
Expressions are nested lists: \code{list("mul", "x", list("add", "y", 2))},
with bare strings for variables and bare numbers for constants.
Supported ops: add, sub, mul, div, pow, neg, exp, log, sin, cos,
tanh, sqrt, square.
}
\examples{
morie_geron_computational_graph(expr = 5L)
}

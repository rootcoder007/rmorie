% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geron_train_native.R
\name{morie_geron_ddpm_reverse_step}
\alias{morie_geron_ddpm_reverse_step}
\title{DDPM reverse denoising step (Geron Ch 18, morie.fn grdpmr)}
\usage{
morie_geron_ddpm_reverse_step(
  x_t,
  t,
  eps_pred,
  alpha,
  alpha_bar,
  sigma,
  z = NULL,
  seed = 0
)
}
\arguments{
\item{x_t, eps_pred}{Numeric objects of the same shape.}

\item{t}{0-based timestep.}

\item{alpha, alpha_bar}{Schedules indexed by \code{t}.}

\item{sigma}{Non-negative noise scale; 0 gives the DDIM step.}

\item{z}{Optional noise; otherwise LCG normals.}

\item{seed}{LCG seed.}


}
\value{
List with \code{x_prev}, \code{mean}, \code{noise_term}, \code{eps_coef},
\code{x0_estimate}, \code{alpha_t}, \code{alpha_bar_t}.
}
\description{
\verb{x_\{t-1\}} = (x_t - ((1-a_t)/sqrt(1-ab_t)) eps) / sqrt(a_t) + sigma z.
}
\examples{
alpha <- rep(0.98, 10)
alpha_bar <- cumprod(alpha)
morie_geron_ddpm_reverse_step(c(0.4, -0.2, 0.9), t = 3,
                              eps_pred = c(0.1, -0.05, 0.2),
                              alpha = alpha, alpha_bar = alpha_bar,
                              sigma = sqrt(1 - alpha[4]),
                              z = c(0.3, -0.1, 0.2))
}

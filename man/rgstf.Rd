% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rgstf.R
\name{rgstf}
\alias{rgstf}
\alias{morie_rangayyan_stft}
\title{Short-time Fourier transform -- Rangayyan Ch 4}
\usage{
rgstf(x, fs = 1, nperseg = 256L, noverlap = NULL, window = "hann")

morie_rangayyan_stft(
  x,
  fs = 1,
  nperseg = 256L,
  noverlap = NULL,
  window = "hann"
)
}
\arguments{
\item{x}{Numeric vector.}

\item{fs}{Sampling rate (Hz).}

\item{nperseg}{Window length in samples (default 256).}

\item{noverlap}{Overlap in samples (default \code{nperseg/2}).}

\item{window}{\code{"hann"} (default), \code{"hamming"}, \code{"boxcar"}.}
}
\value{
Named list \code{freqs}, \code{times}, \code{Sxx} (rows=freqs, cols=times),
\code{nperseg}, \code{noverlap}, \code{fs}.
}
\description{
Sliding-window FFT spectrogram (one-sided, PSD-scaled). Mirrors
\code{scipy.signal.spectrogram} with \code{scaling="density"}.
}
\examples{
\donttest{
t <- seq(0, 10, length.out = 1024)
x <- sin(2 * pi * 10 * t)
r <- rgstf(x, fs = 100, nperseg = 128)
dim(r$Sxx)
}
}
\references{
Rangayyan Ch 4.
}
\keyword{internal}

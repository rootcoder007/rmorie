% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/signal.R
\name{pcgseg}
\alias{pcgseg}
\title{PCG S1/S2 heart-sound segmentation}
\usage{
pcgseg(envelope, fs = 2000, min_gap_ms = 100)
}
\arguments{
\item{envelope}{Numeric vector (Shannon-energy envelope).}

\item{fs}{Sampling frequency in Hz (default 2000).}

\item{min_gap_ms}{Minimum gap between peaks in ms (default 100).}
}
\value{
List with \code{value} (cycle count), \code{name}, and \code{extra}
(\code{s1_indices}, \code{s2_indices}, \code{n_cycles}, \code{n_peaks}).
}
\description{
Segments a PCG Shannon-energy envelope into S1 (systolic) and S2
(diastolic) heart-sound events: threshold, find above-threshold runs,
merge close peaks, label alternating events.
}
\details{
Reference: Liang, Lukkarinen & Hartimo (1997), \emph{Comput. Cardiol.},
pp. 105--108.
}
\examples{
\donttest{
set.seed(1)
env <- abs(sin(seq(0, 20, length.out = 4000))) + 0.05 * rnorm(4000)
env[env < 0] <- 0
res <- pcgseg(env, fs = 2000)
res$extra$n_cycles
}
}

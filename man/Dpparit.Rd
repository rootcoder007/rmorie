% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dpparit.R
\name{Dpparit}
\alias{Dpparit}
\title{Pitman-Yor two-parameter process}
\usage{
Dpparit(n = 100, alpha = 1, sigma = 0.5, seed = 42)
}
\arguments{
\item{n}{Number of customers to seat.}

\item{alpha}{Concentration; must exceed -sigma.}

\item{sigma}{Discount in [0, 1).}

\item{seed}{Seed of the deterministic stream.}
}
\value{
List with \code{estimate} (number of blocks), \code{K},
\code{counts}, \code{p_new}, \code{n}, \code{method}.
}
\description{
Formula: P(new) = (alpha + sigma K) / (n + alpha)
}
\details{
An occupied table takes (n_k - sigma)/(n + alpha), so the discount
sigma moves mass from large tables to new ones and the number of
blocks grows like n^sigma instead of log n.  Setting sigma = 0
recovers the Dirichlet process exactly.
}
\references{
Pitman & Yor (1997), Ann. Probab. 25(2):855-900.
}
\examples{
Dpparit()
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datasets_tps_arcgis_hub_wrappers.R
\name{morie_datasets_tps_use_of_force_call_sources_by_month}
\alias{morie_datasets_tps_use_of_force_call_sources_by_month}
\title{Use of Force: Call Sources by Month (RBDC-UOF-TBL-001)}
\usage{
morie_datasets_tps_use_of_force_call_sources_by_month(
  format = "json",
  where = "1=1",
  max_features = NULL,
  layer_idx = 0L,
  offline = TRUE,
  dest = NULL
)
}
\arguments{
\item{format}{One of \code{"json"} (default), \code{"geojson"}, \code{"csv"},
\code{"shapefile"}, \code{"fgdb"}.}

\item{where}{SoQL-style \code{WHERE} filter passed to FeatureServer
\verb{?where=} (default \code{"1=1"}).}

\item{max_features}{Optional cap on returned rows. Passed as
\code{resultRecordCount} to FeatureServer; ignored for binary formats.}

\item{layer_idx}{Integer index of the FeatureServer layer to pull
(default \code{0L}, the first layer).}

\item{offline}{Logical; if \code{TRUE}, the hub_id is resolved via the
bundled catalog (no network needed for the resolution step).
Default \code{TRUE} -- you can run this against the 71 catalog
entries without network. Live data fetches always hit the
network regardless of this argument; "offline" here only
affects the hub_id -> FeatureServer URL lookup.}

\item{dest}{Optional path for binary downloads
(\code{format \%in\% c("shapefile", "fgdb")}); defaults to \code{tempfile()}.}
}
\value{
A data.frame / GeoJSON list / file path; see
\code{\link[=morie_datasets_tps_arcgis_hub_by_id]{morie_datasets_tps_arcgis_hub_by_id()}} for per-format semantics.
}
\description{
Toronto PS ArcGIS Hub dataset wrapper. Thin dispatch to
\code{\link[=morie_datasets_tps_arcgis_hub_by_id]{morie_datasets_tps_arcgis_hub_by_id()}} with the canonical
hub item_id \verb{98d88b18c0364c8d86e6a7c690037b85}.
}
\details{
This table provides monthly counts of incidents which officers
responded to from different call sources and which resulted in
an enforcement action and/or reported use of force.

Tags: RBDC; race based data; race
}

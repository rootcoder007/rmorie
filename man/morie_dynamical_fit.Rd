% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scvelo_native.R
\name{morie_dynamical_fit}
\alias{morie_dynamical_fit}
\title{EM over the rates and the latent variables. E step is the closest}
\usage{
morie_dynamical_fit(
  u,
  s,
  alpha0 = NULL,
  beta0 = 1,
  gamma0 = 0.5,
  t_switch0 = NULL,
  n_iter = 25,
  grid = 120
)
}
\arguments{
\item{u}{A vector; its length is taken and its elements indexed.}

\item{s}{A vector; its length is taken and its elements indexed.}

\item{alpha0}{Optional; may be \code{NULL}. Coerced to numeric by the body, with
\code{as.numeric}.}

\item{beta0}{Numeric; combined arithmetically in the body. Defaults to \code{1}.}

\item{gamma0}{Coerced to numeric by the body, with \code{as.numeric}. Defaults to \code{0.5}.}

\item{t_switch0}{Optional; may be \code{NULL}. Coerced to numeric by the body, with
\code{as.numeric}.}

\item{n_iter}{Coerced to integer by the body, with \code{as.integer}. Defaults to \code{25}.}

\item{grid}{Passed to \code{.scvelo_residual}. Defaults to \code{120}.}
}
\value{
A list with \code{estimate}, \code{alpha}, \code{beta}, \code{gamma},
\code{t_switch}, \code{rss}, \code{rss_history}, \code{latent}, \code{velocity},
\code{steady_on}, \code{method}.
}
\description{
point on the trajectory; M step is a one-at-a-time multiplicative
grid search on alpha, beta, gamma, t_switch. The loop stops as soon
as no candidate improves the residual, so the rss_history is the
visible monotone sequence.
}
\examples{
V <- c(1, 2, 3, 4, 5, 6, 7, 8)
morie_dynamical_fit(V, V)
}
\keyword{internal}

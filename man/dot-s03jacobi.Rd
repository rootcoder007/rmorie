% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aaa_helpers_s03.R
\name{.s03jacobi}
\alias{.s03jacobi}
\title{Symmetric eigenproblem by cyclic Jacobi.  Values ascending; each
vector}
\usage{
.s03jacobi(A, sweeps = 60L)
}
\arguments{
\item{A}{A matrix; passed to \code{nrow}.}

\item{sweeps}{A count; the body uses it as \code{seq_len(...)}. Defaults to \code{60L}.}
}
\value{
A list with \code{values}, \code{vectors}.
}
\description{
sign-fixed so its largest-magnitude entry is positive, because the
eigenproblem does not determine the sign and the arms must agree.
}
\examples{
A <- matrix(c(4, 1, 0.5, 1, 3, 0.8, 0.5, 0.8, 2), nrow = 3)
res <- .s03jacobi(A = A)
res
}

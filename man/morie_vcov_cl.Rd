% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vcov_native.R
\name{morie_vcov_cl}
\alias{morie_vcov_cl}
\title{One-way clustered covariance (native, CR0 / CR1)}
\usage{
morie_vcov_cl(model, cluster, type = "HC1")
}
\arguments{
\item{model}{A fitted \code{lm} / \code{glm}.}

\item{cluster}{A grouping vector (length n).}

\item{type}{\code{"HC1"} (CR1, default) or \code{"HC0"} (CR0).}
}
\value{
The coefficient covariance matrix.
}
\description{
Reproduces \code{sandwich::vcovCL} for one-way clustering: the meat
sums the outer products of cluster-summed score contributions,
with the CR1 small-sample factor
\eqn{\frac{G}{G-1}\cdot\frac{n-1}{n-k}} by default.
}
\examples{
if (requireNamespace("sandwich", quietly = TRUE)) {
  set.seed(1)
  df <- data.frame(y = rnorm(60), x = rnorm(60),
                   cl = rep(1:6, each = 10))
  fit <- stats::lm(y ~ x, data = df)
  morie_vcov_cl(fit, cluster = df$cl)
}
}
\references{
Cameron, A. C., & Miller, D. L. (2015). A practitioner's
guide to cluster-robust inference. \emph{Journal of Human
Resources}, 50(2), 317-372.
}

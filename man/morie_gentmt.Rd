% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gentmt_native.R
\name{morie_gentmt}
\alias{morie_gentmt}
\title{morie_gentmt}
\usage{
morie_gentmt(
  y,
  A,
  H,
  method = "weight",
  degree = 1,
  n_strata = 5,
  doses = NULL,
  trim = NULL
)
}
\arguments{
\item{y}{Passed to \code{.gentmt_vec}.}

\item{A}{Passed to \code{.gentmt_vec}.}

\item{H}{Passed to \code{.gentmt_ip_weights}.}

\item{method}{One of \code{"subclassify"}, \code{"weight"}. Defaults to \code{"weight"}.}

\item{degree}{Coerced to integer by the body, with \code{as.integer}. Defaults to \code{1}.}

\item{n_strata}{Passed to \code{.gentmt_gps_subclassify}. Defaults to \code{5}.}

\item{doses}{Passed to \code{.gentmt_dose_response_curve}.}

\item{trim}{Passed to \code{.gentmt_ip_weights}.}
}
\value{
The value of \code{out}, as built in the body.
}
\description{
A step of the gentmt_native implementation. No other function in the package calls it.
See the file header for the source the module follows.
source it follows.
}
\examples{
# A continuous dose that depends on the covariate but is not a
# deterministic function of it: the treatment model must retain some
# residual variance, or f(A | X) is a point mass and no
# inverse-probability weight exists.
set.seed(1)
H <- stats::rnorm(50)
A <- 0.8 * H + stats::rnorm(50)
y <- 0.5 * A + H + stats::rnorm(50)
morie_gentmt(y = y, A = A, H = H)$estimate
}
\keyword{internal}

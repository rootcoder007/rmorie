% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fzc2x.R
\name{Bfc2}
\alias{Bfc2}
\alias{morie_fauzi_c2_coefficient}
\title{The c_2 bias coefficient of the boundary-free KDE (Theorem 5.5)}
\usage{
Bfc2(dg, d2g, d3g, density, fp, fpp)

morie_fauzi_c2_coefficient(dg, d2g, d3g, density, fp, fpp)
}
\arguments{
\item{dg, d2g, d3g}{\verb{g'}, \verb{g''} and \code{g3} evaluated at \code{g^-1(x)}.}

\item{density}{\code{f_X(x)}.}

\item{fp, fpp}{\verb{f'(x)} and \verb{f''(x)}.}
}
\value{
Named list with \code{estimate}, \code{scaled}, \code{method}.
}
\description{
From Theorem 5.5:
\deqn{c_2(x) = g^{(3)}(g^{-1}(x))f_X(x) + 3g''(g^{-1}(x))g'(g^{-1}(x))f_X'(x) + \[g'(g^{-1}(x))\]^3 f_X''(x).}{c2(x) = g3(g^-1(x)) f(x) + 3 g''(g^-1(x)) g'(g^-1(x)) f'(x) + \[g'(g^-1(x))\]^3 f''(x).}
}
\details{
The coefficients 1, 3, 1 and the derivative orders are the Faa di Bruno
pattern for the second derivative of a composition -- which is what this is,
since the estimator lives on the transformed scale and the bias is read back
on the original one.

It enters the bias of the boundary-free DENSITY estimator divided by \verb{g'}:
\verb{Bias\[ftilde(x)\] = h^2 c2(x) mu2(K) / (2 g'(g^-1(x))) + o(h^2)}. That
division is the Jacobian which the DISTRIBUTION estimator of (5.5) does not
need -- the clearest statement in the book of why the transformation trick
is cheaper for distribution functions.

With \code{g} the identity this collapses to \verb{f''(x)}, the classical
kernel-density bias coefficient.
}
\examples{
Bfc2(dg = 1, d2g = 0, d3g = 0, density = 0.3, fp = 0.2, fpp = -0.1)
}
\references{
Fauzi and Maesono (2023), Theorem 5.5.
}
\keyword{internal}

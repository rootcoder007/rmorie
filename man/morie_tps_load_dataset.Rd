% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tps_datasets.R
\name{morie_tps_load_dataset}
\alias{morie_tps_load_dataset}
\title{Load one TPS dataset by category name (CSV thin path).}
\usage{
morie_tps_load_dataset(name, path = NULL, csv_filename = NULL, nrows = NULL)
}
\arguments{
\item{name}{Character scalar. One of \code{names(MORIE_TPS_REGISTRY)},
case-insensitive.}

\item{path}{Optional character scalar. Override the CSV file or
directory to load from. If a directory, the first \verb{*.csv} inside
is picked. If \code{NULL}, the loader walks \code{morie_tps_data_dir()}.}

\item{csv_filename}{Optional filename inside the category's \verb{CSV/}
directory.}

\item{nrows}{Optional integer. Cap on rows to load.}
}
\value{
A \code{data.frame} (the CSV contents) with tolerant
OCCURRENCE_* / REPORTED_* column renaming applied.
}
\description{
\code{name} is case-insensitive. Pass \code{nrows = N} for a quick sample
while developing against the largest tables.
}
\details{
For non-CSV sibling formats (Excel, GeoJSON, KML, GeoPackage,
Shapefile, etc.), use \code{\link[=morie_tps_load]{morie_tps_load()}} from \code{tps_io.R} instead.
}
\examples{
\dontrun{
df <- morie_tps_load_dataset("Assault", nrows = 1000L)
}

}

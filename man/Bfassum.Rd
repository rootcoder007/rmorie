% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fzd1d5.R
\name{Bfassum}
\alias{Bfassum}
\alias{morie_fauzi_conditions_d1_d5}
\title{Check conditions D1-D5 of the boundary-free kernel estimators}
\usage{
Bfassum(
  kernel = NULL,
  g = NULL,
  h = NULL,
  n = NULL,
  smooth = NA,
  tol = 1e-06,
  lo = -8,
  hi = 8,
  ngrid = 4001L
)

morie_fauzi_conditions_d1_d5(
  kernel = NULL,
  g = NULL,
  h = NULL,
  n = NULL,
  smooth = NA,
  tol = 1e-06,
  lo = -8,
  hi = 8,
  ngrid = 4001L
)
}
\arguments{
\item{kernel}{\code{K(v)}; defaults to the Gaussian density.}

\item{g}{The transformation, for D4.}

\item{h}{Bandwidth, for D3.}

\item{n}{Sample size, for D3.}

\item{smooth}{The caller's assertion of D5.}

\item{tol}{Tolerance for the D1/D2 checks.}

\item{lo, hi, ngrid}{Fixed quadrature window and node count.}


}
\value{
Named list with \code{d1}, \code{d2}, \code{d3}, \code{d4}, \code{d5}, \code{mass}, \code{mu2}, \code{monotone}, \code{method}.
}
\description{
The five conditions of Sec. 5.2, checked rather than asserted:
}
\details{
\itemize{
\item D1: \code{K} non-negative, continuous, symmetric at 0.
\item D2: \verb{int v^2 K(v) dv} finite and \verb{int K(v) dv = 1}.
\item D3: \code{h > 0}, \code{h -> 0}, \verb{n h -> Inf}; checked as \verb{0 < h < 1}, \verb{n h > 1}.
\item D4: \code{g} an INCREASING bijection from the real line onto the support
\code{Omega}; checked by monotonicity on a fixed grid.
\item D5: \code{f_X} and \code{g} twice differentiable -- a smoothness claim, reported as
\code{NA} unless the caller asserts it.
}

The book says something worth repeating: it is SUFFICIENT for \code{g} to be
bijective, and the increasing property in D4 is imposed only to make the
proofs simpler. A decreasing bijection is not wrong, merely outside what is
proved -- which is why a decreasing \code{g} returns \code{d4 = FALSE} with the
direction in \code{monotone}, rather than an error.

D2 is checked as a pair: a kernel can integrate to 1 and still have an
infinite second moment (Cauchy), so mass and \code{mu2} are reported separately.
}
\examples{
Bfassum(h = 0.1, n = 100, g = exp)
}
\references{
Fauzi and Maesono (2023), conditions D1-D5 of Sec. 5.2.
}
\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/caus_native.R
\name{morie_caus_dml_partial_lin}
\alias{morie_caus_dml_partial_lin}
\title{Double machine learning for the partially linear model}
\usage{
morie_caus_dml_partial_lin(y, D, X, n_folds = 5, learner = NULL, seed = 0)
}
\arguments{
\item{y}{outcome.}

\item{D}{treatment.}

\item{X}{controls.}

\item{n_folds}{cross-fitting folds, at least 2.}

\item{learner}{optional \verb{function(Xtr, ytr)} returning a predict
function; a ridge fit when \code{NULL}.}

\item{seed}{fold-assignment seed.}
}
\value{
list: theta, se, ci, y_residual, d_residual,
theta_in_sample, cross_fitted, n_folds, first_stage_r2, n, p,
method.
}
\description{
\verb{Y = theta D + g(X) + eps}, \code{D = m(X) + nu}, estimated by
\verb{theta = (Dtilde'Dtilde)^-1 Dtilde'Ytilde} with both residuals
CROSS-FITTED: each observation's nuisance prediction comes from a
model fitted without it.
}
\details{
Two ingredients, neither optional. Residualising BOTH \code{Y} and \code{D}
on \code{X} makes the score Neyman-orthogonal, so first-order errors in
the nuisance estimates do not contaminate \code{theta}; residualising
only \code{Y} and regressing on raw \code{D} is not orthogonal and the bias
returns. And cross-fitting removes a regularisation bias that
otherwise does not vanish at root-n. \code{theta_in_sample} is computed
alongside precisely so the gap is visible.
}
\examples{
X <- matrix(stats::rnorm(600), 200)
D <- X[, 1] + stats::rnorm(200)
morie_caus_dml_partial_lin(1.5 * D + X[, 1], D, X, n_folds = 3)$theta
}
\references{
Chernozhukov, Chetverikov, Demirer, Duflo, Hansen,
Newey and Robins (2018), \emph{Econometrics Journal} 21:C1-C68.
}

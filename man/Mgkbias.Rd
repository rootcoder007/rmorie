% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fzt13.R
\name{Mgkbias}
\alias{Mgkbias}
\alias{morie_fauzi_thm1_3_mise_mgkde}
\title{Bias of the modified gamma kernel density estimator (Theorem 1.3)}
\usage{
Mgkbias(x, h, f, fp, fpp, fppp, book = TRUE)

morie_fauzi_thm1_3_mise_mgkde(x, h, f, fp, fpp, fppp, book = TRUE)
}
\arguments{
\item{x}{Evaluation point, \code{x >= 0}.}

\item{h}{Bandwidth.}

\item{f, fp, fpp, fppp}{\code{f(x)} and its first three derivatives at \code{x}.}

\item{book}{Logical; reproduce (1.17) as printed (default) or use the
derived form.}
}
\value{
Named list with \code{bias}, \code{a}, \code{b}, \code{h}, \code{book}, \code{method}.
}
\description{
Theorem 1.3, Eq. (1.15):
\deqn{\mathrm{Bias}\[\tilde f_X(x)\] = -2(b(x) - a^2(x)/(2 f_X(x)))h + o(h) + O(n^{-1}h^{-1/4}),}{Bias\[ftilde(x)\] = -2(b(x) - a^2(x)/(2 f(x))) h + o(h) + O(n^-1 h^-1/4),}
with \code{a} and \code{b} from (1.16) and (1.17). The order is back to \code{h} -- the
same as Chen's -- while the variance keeps the smaller order of
Theorem 1.1, which is the point of the chapter.
}
\details{
Two notes on (1.15)-(1.17), stated plainly.

(i) In the extracted text of (1.15) the superscript on \code{a} is lost; the
proof of Theorem 1.2 writes \verb{a^2(x)/(2 f(x))} three times, and Eq. (2.8) of
Fauzi's doctoral thesis prints the square, so it is certain.

(ii) (1.17) prints \verb{b(x) = x + f''(x)/2 + x^2(x/3 + 1/2) f3(x)}, with a bare
ADDITIVE \code{x}. That reading is confirmed against the primary source, where
the prime marks are legible: Fauzi, R. R. (2020), "Bias Reduction of
Kernel-Type Estimators without Boundary Problems", doctoral thesis, Kyushu
University (institutional repository, \code{math0257}), Eq. (2.10). It is what
both sources print, not a transcription artefact.

It is nevertheless dimensionally impossible. \code{b(x)} multiplies \code{h} to give a
density, so \code{b} carries the units of \verb{f''}; a bare \code{x} carries units of
length and cannot be added to it. Carrying the book's own Taylor argument
through gives the \code{h} coefficient as
\verb{(x + 1/2) f''(x) + x^2(x/3 + 1/2) f3(x)} -- the \code{x} MULTIPLYING \verb{f''},
dimensionally consistent and differing from the printed form only by a
missing pair of brackets.

\code{book = TRUE} (default) reproduces what both published sources print;
\code{book = FALSE} uses the derived form. They agree only when \verb{f''(x) = 1}.
}
\examples{
Mgkbias(x = 1, h = 0.01, f = 0.5, fp = 0, fpp = 0, fppp = 0)
}
\references{
Fauzi and Maesono (2023), Theorem 1.3, Eqs. (1.15)-(1.17).
}
\keyword{internal}

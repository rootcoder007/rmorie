% Generated from the package source by gen_rd_tree.R -- roxygen2
% hangs on this package. Usage comes from args(), so it tracks
% the formals.
\name{.wrd2v_rng}
\alias{.wrd2v_rng}
\title{SplitMix64 via .ghc_rng: the Python arm is}
\usage{
.wrd2v_rng(seed)
}
\arguments{
\item{seed}{Passed to \code{.ghc_rng}.}
}
\description{
np.random.default_rng(seed) (_array_core _SplitMix64), so the draws here are bit-identical to it
}
\value{
The value of \code{e}, as built in the body.
}
\keyword{internal}
\examples{
.wrd2v_rng(seed = 5L)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geron_ml_native.R
\name{morie_geron_1cycle_schedule}
\alias{morie_geron_1cycle_schedule}
\title{1cycle learning-rate and momentum schedule (Geron Ch 11, morie.fn gr1cy)}
\usage{
morie_geron_1cycle_schedule(
  eta_min,
  eta_max,
  t,
  T,
  mom_max = 0.95,
  mom_min = 0.85
)
}
\arguments{
\item{eta_min, eta_max}{Learning-rate bounds, 0 < eta_min < eta_max.}

\item{t}{Reporting step, 0-based, 0 <= t < T.}

\item{T}{Total steps, at least 2.}

\item{mom_max, mom_min}{Momentum bounds, 0 <= mom_min < mom_max < 1.}
}
\value{
List with \code{lr_schedule}, \code{momentum_schedule}, \code{peak_step},
\code{lr_at_t}, \code{momentum_at_t}.
}
\description{
Peak step is \code{(T - 1) \%/\% 2}; the LR is a linear interpolation over
knots (0, peak, T-1) with values (eta_min, eta_max, eta_min) and the
momentum mirrors it. T = 2 collapses the peak onto step 0, which is
the two-knot branch.
}
\examples{
morie_geron_1cycle_schedule(0.1, 0.5, t = 1, T = 7)
}

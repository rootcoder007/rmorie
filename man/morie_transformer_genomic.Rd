% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trfge.R
\name{morie_transformer_genomic}
\alias{morie_transformer_genomic}
\title{Transformer (1-head self-attention) genomic predictor (base R)}
\usage{
morie_transformer_genomic(
  x,
  y,
  markers,
  d_model = 8,
  lam = 1,
  seed = 0,
  deterministic_seed = NULL
)
}
\arguments{
\item{x}{Optional fixed-effect features.}

\item{y}{Numeric response.}

\item{markers}{(n x L) marker sequence.}

\item{d_model}{Model dimension.}

\item{lam}{Ridge regulariser for the linear head.}

\item{seed}{Seed.}

\item{deterministic_seed}{Optional integer; if supplied, RNG state is
derived via \code{\link[=morie_det_rng]{morie_det_rng()}} keyed on ("trfge", deterministic_seed)
so Py<->R streams agree on the canonical fixture.  When \code{NULL}
(default) behaviour is unchanged.}
}
\value{
list(estimate, y_hat, beta, attention, context, se, n, method).
}
\description{
Random fixed projections + ridge head on the mean-pooled context vector.
}
\examples{
morie_transformer_genomic(
  x = rnorm(50), y = rnorm(50),
  markers = matrix(sample(0:2, 200, TRUE), 50, 4)
)
}
\references{
Vaswani et al. (2017). Montesinos Lopez Ch 15.
}

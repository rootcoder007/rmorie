% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Gradds.R
\name{Gradds}
\alias{Gradds}
\title{Gradient descent}
\usage{
Gradds(f, grad_f, x0, lr = 0.1, steps = 100, tol = 1e-12)
}
\arguments{
\item{f}{Objective function of a numeric vector.}

\item{grad_f}{Gradient function returning a vector of the same length.}

\item{x0}{Starting point.}

\item{lr}{Positive step size.}

\item{steps}{Number of iterations.}

\item{tol}{Stop when the gradient norm falls below this.}
}
\value{
List with \code{estimate} (final objective), \code{x},
\code{f_path}, \code{grad_norm}, \code{steps_used},
\code{converged}, \code{n}, \code{method}.
}
\description{
On the quadratic f(x) = x^2 the iteration has the exact closed form
x_t = x_0 (1 - 2 lr)^t, so it converges for lr < 1, oscillates at
lr = 1 and diverges beyond -- the behaviour the tests pin down.
}
\details{
Formula: x <- x - lr * grad f(x).
}
\references{
Cauchy (1847), Methode generale pour la resolution des
systemes d'equations simultanees, C. R. Acad. Sci. Paris 25:536-538.
}
\examples{
Gradds(function(x) sum(x^2), function(x) 2 * x, x0 = c(1, 1))
}

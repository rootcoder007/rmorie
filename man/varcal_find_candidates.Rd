% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/varcal_native.R
\name{varcal_find_candidates}
\alias{varcal_find_candidates}
\title{varcal_find_candidates}
\usage{
varcal_find_candidates(
  reads,
  reference,
  min_alt_count = 2,
  min_alt_fraction = 0.05,
  min_bq = 10
)
}
\arguments{
\item{reads}{Passed to \code{.varcal_norm_reads}.}

\item{reference}{A vector; its length is taken.}

\item{min_alt_count}{The body requires: varcal: min_alt_count must be at least 1.
Defaults to \code{2}.}

\item{min_alt_fraction}{The body requires: varcal: min_alt_fraction must lie in [0,
1]. Defaults to \code{0.05}.}

\item{min_bq}{Passed to \code{>=}. Defaults to \code{10}.}
}
\value{
The value of \code{out}, as built in the body.
}
\description{
A step of the varcal_native implementation. No other function in the package calls it.
See the file header for the source the module follows.
source it follows.
}
\examples{
reads <- c(
  lapply(1:3, function(i) list(pos = 0L, seq = "ACGTTCGT")),
  lapply(1:3, function(i) list(pos = 0L, seq = "ACGTACGT")))
r <- varcal_find_candidates(reads, reference = "ACGTACGTAC")
str(r, max.level = 1)
}
\keyword{internal}

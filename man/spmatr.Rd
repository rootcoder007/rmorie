% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spmatr.R
\name{spmatr}
\alias{spmatr}
\title{Matern covariance function class}
\usage{
spmatr(h, sigma2 = 1, nu = 0.5, a = 1)
}
\arguments{
\item{h}{Numeric vector of non-negative lag distances.}

\item{sigma2}{Process variance, positive.}

\item{nu}{Smoothness, positive.}

\item{a}{Scale theta, positive.}
}
\value{
Named list: covariance, semivariogram, sigma2, nu, theta.
}
\description{
C(h) = sigma^2 (1/gamma(nu)) (theta h / 2)^nu 2 K_nu(theta h), for
nu > 0 and theta > 0. Smoothness increases with nu. Because
K_nu(t) is approximately gamma(nu)/2 (t/2)^-nu as t approaches 0, the
process variance is exactly sigma^2, so C(0) = sigma2.
}
\details{
The book names three members: nu = 1/2 is the exponential model
(eq 4.11), nu = 1 is Whittle's model (eq 4.12), and nu approaching
infinity is the gaussian model (eq 4.10).

Note the parameterisation: \code{a} is the book's theta from eq (4.9), NOT
a practical range. The practical range of a Matern model is itself a
function of nu.
}
\examples{
spmatr(h = c(0, 0.5, 1, 3), sigma2 = 2, nu = 1.5, a = 1)
}
\references{
Schabenberger & Gotway (2005), Sec 4.3.2, eq (4.9), p. 143.
}

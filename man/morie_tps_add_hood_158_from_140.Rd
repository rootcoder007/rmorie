% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/toronto_neighbourhoods.R
\name{morie_tps_add_hood_158_from_140}
\alias{morie_tps_add_hood_158_from_140}
\title{Add an equivalent HOOD_158 column to a HOOD_140-keyed data.frame}
\usage{
morie_tps_add_hood_158_from_140(
  df,
  col_in = NULL,
  col_out = "HOOD_158_equiv",
  crosswalk = NULL
)
}
\arguments{
\item{df}{A TPS crime \code{data.frame}.}

\item{col_in}{Name of the input HOOD_140 column. By default the
first match from \code{c("HOOD_140", "hood_140", "NEIGHBOURHOOD_140", "neighbourhood_140")} present in \code{df}.}

\item{col_out}{Name of the new column to add. Default
\code{"HOOD_158_equiv"}.}

\item{crosswalk}{Optional pre-loaded crosswalk; defaults to
\code{morie_to_hood_crosswalk()}.}
}
\value{
\code{df} with the equivalent-code column appended.
}
\description{
Looks up each row's \code{HOOD_140} (or \code{hood_140} / \code{NEIGHBOURHOOD_140}
/ \code{neighbourhood_140}) in the bundled crosswalk and writes the
PRIMARY-overlap 158 hood code into a new column (default name
\code{HOOD_158_equiv}).
}
\details{
For 1:1 mappings the result is exact. For splits (1 historical
hood -> 2--4 current hoods) the 158 hood with the largest area
overlap wins; this is \strong{lossy} -- analyses at the 158-level
should ideally re-aggregate from the per-incident lat/lon rather
than relying on the primary-overlap join.
}
\examples{
df <- data.frame(EVENT_ID = 1:3, HOOD_140 = c("082", "001", "075"))
morie_tps_add_hood_158_from_140(df)
}

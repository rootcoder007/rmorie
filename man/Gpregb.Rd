% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Gpregb.R
\name{Gpregb}
\alias{Gpregb}
\title{Gaussian process regression with the hyperparameters integrated out}
\usage{
Gpregb(
  X,
  y,
  kernel = NULL,
  X_test = NULL,
  lengthscales = NULL,
  noises = NULL,
  variance = 1
)
}
\arguments{
\item{X}{Input matrix, one row per training point.}

\item{y}{Training responses.}

\item{kernel}{Unused placeholder kept for the interface.}

\item{X_test}{Test inputs; the training inputs by default.}

\item{lengthscales}{Grid of positive lengthscales.}

\item{noises}{Grid of positive noise variances.}

\item{variance}{Signal variance.}
}
\value{
List with \code{estimate}, \code{mean}, \code{variance},
\code{weights}, \code{loglik}, \code{n}, \code{method}.
}
\description{
Conditional on the lengthscale and noise the predictive moments and
the marginal likelihood are closed form; the hyperparameter average
is taken by DETERMINISTIC quadrature over a grid weighted by that
marginal likelihood, not by slice sampling, so both language arms
land on the same numbers.  A one-point grid collapses to the plain
point-estimate GP, which is the identity the tests check.
}
\details{
Formula: mu* = k*'(K + s2 I)^{-1} y;
var* = k** - k*'(K + s2 I)^{-1} k*;
log p(y) = -0.5 y'(K + s2 I)^{-1} y - 0.5 log|K + s2 I| - (n/2) log 2 pi.
}
\examples{
V <- c(1, 2, 3, 4, 5, 6, 7, 8)
Gpregb(V, V)
}
\references{
Rasmussen and Williams (2006), Gaussian Processes for
Machine Learning, MIT Press, eqs. (2.23)-(2.26) and (5.8); Murray
and Adams (2010), Slice sampling covariance hyperparameters of
latent Gaussian models, NIPS 23, arXiv:1006.0868.
}

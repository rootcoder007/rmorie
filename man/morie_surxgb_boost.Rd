% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/surxgb_native.R
\name{morie_surxgb_boost}
\alias{morie_surxgb_boost}
\alias{morie_surxgb}
\title{morie_surxgb_boost}
\usage{
morie_surxgb_boost(
  X,
  y_lower,
  y_upper,
  n_rounds = 50,
  eta = 0.1,
  max_depth = 3,
  lam = 1,
  gamma = 0,
  min_child = 5,
  sigma = 1,
  dist = "normal",
  base_score = NULL,
  derivatives = "analytic"
)

morie_surxgb(
  X,
  y_lower,
  y_upper,
  n_rounds = 50,
  eta = 0.1,
  max_depth = 3,
  lam = 1,
  gamma = 0,
  min_child = 5,
  sigma = 1,
  dist = "normal",
  base_score = NULL,
  derivatives = "analytic"
)
}
\arguments{
\item{X}{A matrix; indexed by row and column.}

\item{y_lower}{A vector; its length is taken.}

\item{y_upper}{A vector; its length is taken.}

\item{n_rounds}{Coerced to integer by the body, with \code{as.integer}. Defaults to \code{50}.}

\item{eta}{Numeric; combined arithmetically in the body. Defaults to \code{0.1}.}

\item{max_depth}{Coerced to integer by the body, with \code{as.integer}. Defaults to \code{3}.}

\item{lam}{Coerced to numeric by the body, with \code{as.numeric}. Defaults to \code{1}.}

\item{gamma}{Coerced to numeric by the body, with \code{as.numeric}. Defaults to \code{0}.}

\item{min_child}{Coerced to integer by the body, with \code{as.integer}. Defaults to \code{5}.}

\item{sigma}{Passed to \code{morie_surxgb_aft_gradient_hessian}. Defaults to \code{1}.}

\item{dist}{Passed to \code{.surxgb_check_dist}. Defaults to \code{"normal"}.}

\item{base_score}{Optional; may be \code{NULL}. Coerced to numeric by the body, with
\code{as.numeric}.}

\item{derivatives}{Passed to \code{morie_surxgb_aft_gradient_hessian}. Defaults to
\code{"analytic"}.}
}
\value{
A list with \code{estimate}, \code{trees}, \code{eta}, \code{lam},
\code{gamma}, \code{sigma}, \code{dist}, \code{base_score}, \code{derivatives},
\code{loss_history}, \code{prediction}, \code{n_rounds}, \code{max_depth},
\code{method}.
}
\description{
A step of the surxgb_native implementation. No other function in the package calls it.
See the file header for the source the module follows.
source it follows.
}
\examples{
morie_surxgb_boost(X = c(1, 2, 3, 4, 5, 6, 7, 8), y_lower = c(1, 2, 3, 4, 5, 6, 7, 8),
  y_upper = c(1, 2, 3, 4, 5, 6, 7, 8))
}
\keyword{internal}

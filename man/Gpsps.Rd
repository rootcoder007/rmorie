% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Gpsps.R
\name{Gpsps}
\alias{Gpsps}
\title{Spectral mixture kernel GP}
\usage{
Gpsps(
  X,
  y,
  X_test = NULL,
  Q = 1,
  weights = NULL,
  variances = NULL,
  means = NULL,
  noise = 0.01
)
}
\arguments{
\item{X}{One-dimensional training inputs.}

\item{y}{Training responses.}

\item{X_test}{Test inputs; the training inputs by default.}

\item{Q}{Number of mixture components.}

\item{weights, variances, means}{Length-Q mixture parameters.}

\item{noise}{Observation noise variance.}
}
\value{
List with \code{estimate}, \code{mean}, \code{variance},
\code{k_zero}, \code{loglik}, \code{n}, \code{method}.
}
\description{
Modelling the spectral density as a mixture of Gaussians and
inverting Bochner's theorem gives the kernel below.  The stub this
function replaces printed the exponent as exp(-tau^2 v), dropping the
2 pi^2; the arXiv PDF was checked and the factor is there.  It
matters: without it the mu = 0 case is not a squared exponential with
lengthscale 1/(2 pi sqrt(v)), and that mapping is the anchor the
tests use.
}
\details{
Formula: k(tau) = sum_q w_q exp(-2 pi^2 tau^2 v_q) cos(2 pi tau mu_q).
}
\examples{
V <- c(1, 2, 3, 4, 5, 6, 7, 8)
Gpsps(V, V)
}
\references{
Wilson and Adams (2013), Gaussian process kernels for
pattern discovery and extrapolation, ICML 2013, PMLR
28(3):1067-1075. arXiv:1302.4245
}

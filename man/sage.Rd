% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sage.R
\name{sage}
\alias{sage}
\alias{morie_graphsage}
\title{GraphSAGE embedding generation (forward propagation)}
\usage{
sage(G, X, W = NULL, aggregator = "mean", K = 1L, convolutional = FALSE)

morie_graphsage(
  G,
  X,
  W = NULL,
  aggregator = "mean",
  K = 1L,
  convolutional = FALSE
)
}
\arguments{
\item{G}{square adjacency matrix.}

\item{X}{node feature matrix, one row per node.}

\item{W}{optional weight matrix; defaults to averaging the self and
neighbourhood halves.}

\item{aggregator}{one of mean, sum, max.}

\item{K}{search depth (number of layers).}

\item{convolutional}{use equation (2) instead of Algorithm 1 line 5.}
}
\value{
list: estimate, Z, frob, n, dim, method.
}
\description{
Algorithm 1 of the paper: aggregate the neighbourhood, concatenate with the
previous representation, apply W and a non-linearity, then L2-normalise;
equation (2) gives the convolutional variant.  Full neighbourhoods are used,
so the pass is deterministic.  Source consulted: Hamilton, Ying and Leskovec
(2017), Inductive Representation Learning on Large Graphs, arXiv:1706.02216.
}
\examples{
sage(matrix(c(0, 1, 1, 0), 2, 2), matrix(c(1, 0, 0, 1), 2, 2, byrow = TRUE))
}
\keyword{internal}

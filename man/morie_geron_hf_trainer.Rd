% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geron_w4b_native.R
\name{morie_geron_hf_trainer}
\alias{morie_geron_hf_trainer}
\title{Trainer loop: mini-batch SGD with per-epoch evaluation (Geron Ch 14, morie.fn hmhftn)}
\usage{
morie_geron_hf_trainer(model, args = list(), train_ds, eval_ds = NULL)
}
\arguments{
\item{model}{List with params, loss_and_grad = function(params,X,y) -> list(loss, grad).}

\item{args}{List with epochs, batch_size, learning_rate, seed.}

\item{train_ds, eval_ds}{List(X, y).}
}
\value{
List with params, best_params, train_loss, eval_loss, history, best_epoch, estimate, n, method.
}
\description{
Shuffle order comes from the shared LCG stream, not numpy's PCG64
permutation; with batch_size >= nrow(X) there is one batch per epoch so
the result is exact and shuffle-independent.
}

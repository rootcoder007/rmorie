% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/psymet.R
\name{morie_psymet_alpha}
\alias{morie_psymet_alpha}
\title{Cronbach's coefficient alpha with Feldt CI}
\usage{
morie_psymet_alpha(data, ci = 0.95)
}
\arguments{
\item{data}{Numeric matrix or data.frame: items as columns, respondents as rows.}

\item{ci}{Confidence level (default 0.95).}
}
\value{
A list with components \code{raw}, \code{std}, \code{avgr}, \code{k}, \code{n}, \code{ci_lo}, \code{ci_hi}.
}
\description{
Hand-rolled base-R implementation. When the \code{psych} package is installed,
results agree with \code{psych::alpha()$total} to numerical precision.
}
\examples{
set.seed(1)
f <- rnorm(50)
X <- sapply(1:5, function(j) sqrt(0.6) * f + sqrt(0.4) * rnorm(50))
colnames(X) <- paste0("i", 1:5)
res <- morie_psymet_alpha(X)
res$raw
}

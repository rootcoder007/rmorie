% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/snpest_native.R
\name{morie_snpest}
\alias{morie_snpest}
\title{On-line DP-mixture filter over a stream of observations}
\usage{
morie_snpest(
  y_stream,
  alpha = 1,
  n_particles = 100L,
  proposal = "optimal",
  resampler = "systematic",
  ess_threshold = 0.5,
  m0 = NULL,
  kappa0 = 0.01,
  a0 = 2,
  b0 = NULL,
  seed = 1,
  grid = NULL,
  seed_stats = TRUE
)
}
\arguments{
\item{y_stream}{The observations, in arrival order. The order MATTERS:
this is a filter, not a batch method, and the answer after t points
is a function of the first t.}

\item{alpha}{Dirichlet-process concentration.}

\item{n_particles}{Particles carried.}

\item{proposal}{"optimal" or "prior".}

\item{resampler}{"systematic", "stratified" or "multinomial".}

\item{ess_threshold}{Resample when the effective sample size falls
below this fraction of the particle count.}

\item{m0}{Prior mean; the stream mean by default.}

\item{kappa0}{Prior precision scaling on the mean.}

\item{a0}{Inverse-gamma shape.}

\item{b0}{Inverse-gamma scale; from the stream variance by default.}

\item{seed}{Seed for the generator shared with the Python arm.}

\item{grid}{Points at which the filtered predictive density is
recorded after the last observation.}

\item{seed_stats}{Take m0 and b0 from the whole stream when not given.
FALSE uses 0 and 1 instead and keeps the run strictly causal.}
}
\value{
A list with the running log marginal likelihood, the expected
number of clusters after each point, the ESS trace, the resampling
times, and the filtered predictive density on the grid.
}
\description{
On-line DP-mixture filter over a stream of observations
}
\examples{
V <- c(1, 2, 3, 4, 5, 6, 7, 8)
morie_snpest(V)
}
\keyword{internal}

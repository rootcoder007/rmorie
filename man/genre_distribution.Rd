% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/caltbR_native.R
\name{genre_distribution}
\alias{genre_distribution}
\alias{morie_caltbR}
\title{Weighted average of \eqn{p(g \mid i)} over a set of items}
\usage{
genre_distribution(items, p_g_given_i, weights = NULL)

morie_caltbR(items, p_g_given_i, weights = NULL)
}
\arguments{
\item{items}{Integer vector of item indices (0-based in Python,
1-based in R -- shifted internally).}

\item{p_g_given_i}{Numeric matrix or list of rows with one row per
item and one column per genre, holding \eqn{p(g \mid i)}.}

\item{weights}{Optional numeric vector of per-item weights; if
\code{NULL} each item contributes 1.}
}
\value{
Numeric vector \eqn{q(g)} of length G, the weighted
average.
}
\description{
Implements eqs. (2)-(3): a per-genre average of the genre
memberships, weighted either by user-supplied weights (recency of
the play, say) or by rank (when called from the reranker with a
rank_weights argument).
}
\examples{
genre_distribution(items = list(a = 1, b = 2), p_g_given_i = matrix(c(1, 2, 3, 4, 5, 6), nrow = 2))
}
\references{
Steck (2018), RecSys '18, eqs. (2)-(3).
}
\keyword{internal}

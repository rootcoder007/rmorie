% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bridgs_native.R
\name{morie_bridgs}
\alias{morie_bridgs}
\title{Bridge sampling estimate of a ratio of normalising constants}
\usage{
morie_bridgs(draws1, draws2, log_q1, log_q2, tol = 1e-12, max_iter = 1000L)
}
\arguments{
\item{draws1, draws2}{Lists of draws from the first and second
density.}

\item{log_q1, log_q2}{Functions giving the unnormalised log
densities.}

\item{tol}{Relative convergence tolerance on \eqn{\log r}.}

\item{max_iter}{Maximum iterations.}
}
\value{
A list with \code{ratio}, \code{log_ratio},
\code{iterations}, \code{converged}, \code{n1}, \code{n2},
\code{method}.
}
\description{
Estimates \eqn{r = c_1 / c_2} from draws under each unnormalised
density using the optimal bridge of Meng and Wong (1996), eq.
(3.5), evaluated by their Sec. 4 fixed-point iteration.  The
estimator uses BOTH sets of draws, which is what makes it far more
stable than a one-sided importance-sampling ratio when the two
densities overlap only partially.
}
\references{
Meng, X.-L. and Wong, W. H. (1996). Simulating ratios
of normalizing constants via a simple identity. Statistica
Sinica, 6, 831-860.
}
\examples{
set.seed(1)
morie_bridgs(draws1 = as.list(rnorm(100)), draws2 = as.list(rnorm(100, 0.5)),
             log_q1 = function(x) -0.5 * x^2,
             log_q2 = function(x) -0.5 * (x - 0.5)^2)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ml_pipeline.R
\name{morie_ml_resample}
\alias{morie_ml_resample}
\title{Train + assess across resampling folds and combine the results}
\usage{
morie_ml_resample(
  model,
  x,
  y,
  n_folds = 5L,
  metric = if (model$type == "logistic") "roc_auc" else "rmse",
  seed = 42L
)
}
\arguments{
\item{model}{A \code{morie_ml_model}.}

\item{x}{Predictors.}

\item{y}{Response.}

\item{n_folds}{Number of folds (default 5). Data is partitioned so
each observation is used for testing exactly once (ML4.8).}

\item{metric}{Metric to report per fold.}

\item{seed}{RNG seed.}
}
\value{
A list with per-fold \code{scores} and their \code{mean}/\code{sd}.
}
\description{
Train + assess across resampling folds and combine the results
}
\examples{
morie_ml_resample(morie_ml_model("linear", "gd", epochs = 50),
                  mtcars[c("hp", "wt")], mtcars$mpg, n_folds = 3)$mean
}

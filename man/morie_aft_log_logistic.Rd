% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aft_native.R
\name{morie_aft_log_logistic}
\alias{morie_aft_log_logistic}
\title{Log-logistic accelerated-failure-time model}
\usage{
morie_aft_log_logistic(time, event, X, ...)
}
\arguments{
\item{time}{follow-up times, strictly positive.}

\item{event}{1 for an event, 0 for right-censored.}

\item{X}{covariate matrix, one row per subject. An intercept is added.}

\item{...}{passed to the fitter (\code{max_iter}, \code{tol},
\code{add_intercept}).}
}
\value{
as \code{\link{morie_aft_weibull}}.
}
\description{
\eqn{\log T = x'\beta + \sigma W} with W standard logistic. The
hazard rises then falls, which is what recommends it when the risk
peaks early and then recedes -- a shape no proportional-hazards
Weibull can produce.
}
\details{
As for every AFT model, \code{exp(beta)} is a time ratio: positive
means longer survival.
}
\examples{
set.seed(2)
X <- matrix(rnorm(200), ncol = 2)
tt <- exp(0.5 + X \%*\% c(0.8, -0.4) + 0.6 * rlogis(100))
round(morie_aft_log_logistic(tt, rep(1, 100), X)$sigma, 3)
}
\references{
Bennett, S. (1983). Log-logistic regression models for
survival data. \emph{Applied Statistics}, 32(2), 165-171.
}

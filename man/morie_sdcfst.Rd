% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sdcfst_native.R
\name{morie_sdcfst}
\alias{morie_sdcfst}
\title{Cross-fitted treatment effect with honest-forest nuisances}
\usage{
morie_sdcfst(
  y,
  D,
  X,
  K_fold = 5L,
  score = "aipw",
  learner = "forest",
  n_trees = 20L,
  mtry = NULL,
  min_leaf = 5L,
  max_depth = 6L,
  trim = 0.02,
  seed = 1,
  ridge = 1e-06
)
}
\arguments{
\item{y}{Outcome.}

\item{D}{Binary treatment, 0 or 1.}

\item{X}{Covariates.}

\item{K_fold}{Number of cross-fitting folds. K = 1 means no
cross-fitting at all, offered so the bias it removes can be
measured.}

\item{score}{"aipw", "partialling_out", "ipw" or "plugin".}

\item{learner}{"forest" or "linear".}

\item{n_trees}{Trees per forest.}

\item{mtry}{Features tried per split.}

\item{min_leaf}{Minimum rows in a leaf.}

\item{max_depth}{Maximum tree depth.}

\item{trim}{Propensities are clipped into this range from each end,
and the number clipped is reported.}

\item{seed}{Seed for the generator shared with the Python arm.}

\item{ridge}{Ridge penalty for the logistic propensity.}
}
\value{
A list with the estimate, its standard error from the
influence function, the per-fold estimates, the trimming count and
the fitted propensity summary.
}
\description{
Cross-fitted treatment effect with honest-forest nuisances
}
\examples{
set.seed(1)
r <- morie_sdcfst(y = rnorm(10), D = rbinom(10, 1, 0.5), X = rnorm(10)); TRUE
}
\keyword{internal}

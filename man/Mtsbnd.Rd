% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mtsbnd.R
\name{Mtsbnd}
\alias{Mtsbnd}
\title{Monotone treatment selection bounds (alias of Mtsbound)}
\usage{
Mtsbnd(Y, X, monotone = TRUE, d = NULL, ymin = NULL, ymax = NULL)
}
\arguments{
\item{Y}{Observed outcomes.}

\item{X}{Observed (selected) treatment levels.}

\item{monotone}{TRUE if higher selection implies weakly higher outcomes.}

\item{d}{Level whose mean counterfactual is bounded; defaults to
\code{max(X)}.}

\item{ymin, ymax}{A priori outcome support; default to the observed range.}
}
\value{
List with lower, upper, width, condmean, pbelow, pat, pabove,
n, d.
}
\description{
This module is an ALIAS. The bound is implemented once, in
\code{Mtsbound} (module \code{bdmnsl}); this entry point only supplies
the argument spelling used by the selection-bounds literature and the
direction switch, then delegates.
}
\details{
Under monotone treatment selection,
\code{E\[Y(d)\] <= P(X<=d) E\[Y|X=d\] + P(X>d) ymax} and
\code{E\[Y(d)\] >= P(X<d) ymin + P(X>=d) E\[Y|X=d\]}.
\code{monotone = FALSE} reverses the selection inequality, which is the
same bound on \code{-Y} over the reflected support with the sides
swapped back.
}
\examples{
V <- c(1, 2, 3, 4, 5, 6, 7, 8)
Mtsbnd(V, V)
}
\references{
Manski and Pepper (2000), Econometrica 68(4), 997-1010.
Standard published form; see \code{bdmnsl} for the availability note.
}

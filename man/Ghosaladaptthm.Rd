% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gh_c10_1.R
\name{Ghosaladaptthm}
\alias{Ghosaladaptthm}
\title{Adaptive hierarchical model prior}
\usage{
Ghosaladaptthm(y = NULL, n = 200, K_true = 3, lam = 1, K_max = 12, seed = 42)
}
\arguments{
\item{y}{Numeric vector of observed coordinates; simulated when NULL.}

\item{n}{Precision (sample size) of each coordinate.}

\item{K_true}{Number of nonzero coordinates in the simulated truth.}

\item{lam}{Complexity-penalty scale of the model prior.}

\item{K_max}{Largest model dimension considered.}

\item{seed}{Seed for the deterministic draw of \code{y}.}
}
\value{
List with \code{estimate} (posterior mode of K),
\code{model_posterior}, \code{K_true}, \code{method}.
}
\description{
Pi = sum_k pi_k Pi_k with pi_k proportional to exp(-lam k log n): the
posterior over the model index k concentrates, so the contraction rate
adapts to the truth without the truth being known.  The evidence of
each sub-model is available in closed form here (conjugate normal
means), so the model posterior is exact rather than sampled.
}
\details{
Formula: log m_K(y) = sum_k [-log(2 pi s_k^2)/2 - y_k^2/(2 s_k^2)],
s_k^2 = 1/n + tau2 1{k < K};  pi_K propto m_K(y) exp(-lam K log n).
}
\references{
Ghosal & van der Vaart (2017), Fundamentals of
Nonparametric Bayesian Inference, CUP, section 10.1.
}
\examples{
Ghosaladaptthm()
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recur_native.R
\name{Pwpgt}
\alias{Pwpgt}
\title{Prentice-Williams-Peterson conditional gap-time model}
\usage{
Pwpgt(start, stop, event, X, occurrence, max_iter = 50L, tol = 1e-09)
}
\arguments{
\item{start, stop}{Interval endpoints on the total-time scale.}

\item{event}{1 if an event ends the interval, 0 if censored.}

\item{X}{Covariate matrix, one row per interval.}

\item{occurrence}{Occurrence number of each interval (stratum).}

\item{max_iter, tol}{Newton-Raphson controls.}
}
\value{
List as in \code{\link{Agrec}}.
}
\description{
Stratified Cox fit on gap = stop - start with stratum = occurrence
number and a common beta: lambda_k(t | H) =
lambda_0k(t - t_(k-1)) exp(beta' X).
}
\examples{
set.seed(1)
occ <- rep(1:3, times = 20)
start <- (occ - 1) * 10
stop <- occ * 10 - runif(60, 0, 3)
event <- rbinom(60, 1, 0.6)
X <- matrix(rnorm(60 * 2), 60, 2)
fit <- Pwpgt(start, stop, event, X, occurrence = occ)
length(fit$estimate) == 2L
}
\references{
Prentice, R. L., Williams, B. J. and Peterson, A. V.
(1981), Biometrika 68(2), 373-379.
}

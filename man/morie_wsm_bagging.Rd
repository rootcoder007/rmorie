% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wsm_native.R
\name{morie_wsm_bagging}
\alias{morie_wsm_bagging}
\title{Bagging}
\usage{
morie_wsm_bagging(X, y, model = NULL, B = 100, newdata = NULL, seed = 0)
}
\arguments{
\item{X}{predictors.}

\item{y}{response.}

\item{model}{optional \verb{function(Xtr, ytr)} returning a predict
function; least squares when \code{NULL}, which is precisely the case
bagging cannot help and is the default only to make that visible.}

\item{B}{replicates.}

\item{newdata}{optional points to predict.}

\item{seed}{resampling seed.}
}
\value{
list: prediction, single_fit, oob_prediction, oob_mse,
replicate_spread, bagged_spread, max_shift_from_single_fit,
n_oob_missing, B, n, method.
}
\description{
ESL Sec. 8.7, \verb{f_bag(x) = (1/B) sum_b f*b(x)}: the average of the
fits over \code{B} bootstrap samples.
}
\details{
Bagging moves VARIANCE and leaves bias alone -- the replicates are
identically distributed, so the average has the same expectation
as any one of them. A sharp consequence follows: for a procedure
that is LINEAR in \code{y}, least squares above all, the bootstrap
average converges back to the fit on the original data, so bagging
does essentially nothing. It pays off where that argument fails,
on high-variance low-bias procedures that are wildly nonlinear in
the data -- a deep regression tree being the standard case.
}
\examples{
X <- matrix(stats::rnorm(150), 50)
morie_wsm_bagging(X, stats::rnorm(50), B = 20)$max_shift_from_single_fit
}
\references{
Hastie, Tibshirani and Friedman (2009), Sec. 8.7;
Breiman, L. (1996), \emph{Machine Learning} 24:123-140.
}

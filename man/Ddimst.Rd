% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ddimst.R
\name{Ddimst}
\alias{Ddimst}
\title{One DDIM reverse step}
\usage{
Ddimst(
  x_t,
  t,
  eps_theta,
  eta = 0,
  T = 1000,
  alpha_bar_t = NULL,
  alpha_bar_prev = NULL
)
}
\arguments{
\item{x_t}{Current latent.}

\item{t}{Current timestep, at least 1.}

\item{eps_theta}{Predicted noise, same length as x_t.}

\item{eta}{Stochasticity in [0, 1].}

\item{T}{Total number of steps for the cosine schedule.}

\item{alpha_bar_t, alpha_bar_prev}{Optional explicit cumulative alphas.}
}
\value{
List with \code{estimate}, \code{x_prev}, \code{x0_pred},
\code{sigma}, \code{alpha_bar_t}, \code{alpha_bar_prev}, \code{n},
\code{method}.
}
\description{
Formula: non-Markovian deterministic reverse
}
\details{
x_{t-1} = sqrt(a_{t-1}) x0_hat + sqrt(1 - a_{t-1} - sigma^2) eps
\itemize{
\item sigma z, with x0_hat = (x_t - sqrt(1 - a_t) eps) / sqrt(a_t) and
sigma = eta sqrt((1 - a_{t-1})/(1 - a_t)) sqrt(1 - a_t/a_{t-1}).  At
eta = 0 the update is fully deterministic (that is DDIM); at eta = 1
it reproduces the DDPM ancestral step.  The noise z is taken as zero
so the map is a function of its arguments alone.
}
}
\references{
Song, Meng & Ermon (2021), Denoising Diffusion Implicit
Models, ICLR 2021.
}
\examples{
Ddimst(x_t = c(1, 2, 3, 4, 5, 6, 7, 8), t = 5L, eps_theta = c(1, 2, 3, 4, 5, 6, 7, 8))
}

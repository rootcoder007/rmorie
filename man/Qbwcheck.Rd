% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fzbwc.R
\name{Qbwcheck}
\alias{Qbwcheck}
\alias{morie_fauzi_quantile_bw_condition}
\title{Bandwidth condition for the kernel quantile Edgeworth expansion}
\usage{
Qbwcheck(h, n, eps = 0.05)

morie_fauzi_quantile_bw_condition(h, n, eps = 0.05)
}
\arguments{
\item{h}{Bandwidth.}

\item{n}{Sample size.}

\item{eps}{The margin at which the lower-side condition is probed.}
}
\value{
Named list with \code{ok}, \code{upper}, \code{lower}, \code{hbook}, \code{ratio}, \code{method}.
}
\description{
Eq. (3.8): the expansion is proved under
\deqn{h = o(n^{-1/4})\quad\text{and}\quad \lim_n (n^{1/4}h)^{-k}n^{-\beta} = 0}{h = o(n^-1/4) and lim (n^1/4 h)^-k n^-beta = 0}
for every \code{beta > 0} and integer \code{k}.
}
\details{
The first half is a rate. The second is not -- it says \code{h} may not shrink
FASTER than any negative power of \code{n}, so a bandwidth like \code{exp(-n)} is
excluded even though it satisfies the first condition comfortably. The
window is genuinely two-sided, which is why the book's working choice is
\code{h = n^(-1/4) / log(n)}: it beats \code{n^(-1/4)} by a logarithm, and only by one.

Checked as \code{h < n^(-1/4)} for the upper side and \verb{h n^(1/4 + eps)} large for
the lower, both finite-sample proxies for limit statements, reported
separately with the book's reference bandwidth so a caller can see which
side a bandwidth fails on.
}
\examples{
Qbwcheck(h = 0.05, n = 1000)
}
\references{
Fauzi and Maesono (2023), Eq. (3.8).
}
\keyword{internal}

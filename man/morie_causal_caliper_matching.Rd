% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causal_match_native.R
\name{morie_causal_caliper_matching}
\alias{morie_causal_caliper_matching}
\title{Caliper matching on the propensity score}
\usage{
morie_causal_caliper_matching(
  ps,
  treat,
  caliper = NULL,
  k = 1,
  replace = TRUE,
  on_logit = TRUE
)
}
\arguments{
\item{ps}{propensity scores, strictly inside (0, 1).}

\item{treat}{0/1 treatment indicator.}

\item{caliper}{maximum distance. Defaults to 0.2 standard deviations
of the (logit) score, the Austin recommendation.}

\item{k}{matches per treated unit.}

\item{replace}{allow reuse of controls.}

\item{on_logit}{match on the logit scale.}
}
\value{
list with \code{matches} (0-based), \code{distances},
\code{n_unmatched}, \code{caliper_used}, \code{match_rate},
\code{estimand}.
}
\description{
Nearest-neighbour matching with a hard maximum distance, by default
on the LOGIT of the propensity score.
}
\details{
The logit scale matters: the raw propensity score is compressed near
0 and 1, so a fixed caliper there is far stricter in the middle of
the distribution than at the ends. On the logit the caliper means the
same thing everywhere.

Treated units with no control inside the caliper are DROPPED, and
that changes the estimand: what remains is the ATT among matchable
units, not the ATT. The returned \code{estimand} field says which one
you actually have.
}
\examples{
set.seed(1)
ps <- plogis(rnorm(200))
tr <- rbinom(200, 1, ps)
morie_causal_caliper_matching(ps, tr)$match_rate
}
\references{
Austin, P. C. (2011). Optimal caliper widths for
propensity-score matching. \emph{Pharmaceutical Statistics}, 10(2),
150-161.
}

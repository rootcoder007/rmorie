% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/did.R
\name{morie_did_sensitivity_analysis}
\alias{morie_did_sensitivity_analysis}
\title{Sensitivity of DiD estimate to parallel-trends violations}
\usage{
morie_did_sensitivity_analysis(
  data,
  outcome,
  treatment,
  post,
  covariates = NULL,
  delta_range = NULL,
  cluster = NULL,
  alpha = 0.05
)
}
\arguments{
\item{data}{A data frame containing the outcome, treatment, post and
any covariate columns.}

\item{outcome}{Name of the outcome column.}

\item{treatment}{Name of the binary (0/1) treatment-group column.}

\item{post}{Name of the binary (0/1) post-period column.}

\item{covariates}{Optional character vector of covariate column names.}

\item{delta_range}{Numeric vector of \eqn{\delta}{delta} values to evaluate
(default \code{seq(0, 2, 0.25)}).}

\item{cluster}{Optional cluster ID column for CR1 standard errors.}

\item{alpha}{Significance level for confidence intervals (default 0.05).}
}
\value{
A data frame with columns \code{delta}, \code{ci_lower},
\code{ci_upper}, \code{covers_zero}.
}
\description{
For each \eqn{\delta}{delta}, computes a bias-adjusted confidence
set under the bound
\eqn{|\mathrm{bias}| \le \delta \hat\sigma}{|bias| <= delta hatsigma}
(Rambachan & Roth, 2023, conservative version).
}
\details{
For a relative-magnitudes bound anchored on observed event-study
pre-trends (Rambachan & Roth's \eqn{\bar M}{M-bar}
parameterization, conservative version) see
\code{\link{morie_did_honest_sensitivity}}.
}
\examples{
set.seed(7)
n <- 300
d <- rbinom(n, 1, 0.5); p <- rbinom(n, 1, 0.5)
y <- 1 + 0.3 * d + 0.4 * p + 0.5 * d * p + rnorm(n, sd = 0.5)
df <- data.frame(y = y, d = d, post = p)
out <- morie_did_sensitivity_analysis(df, "y", "d", "post")
str(out, max.level = 1)
}
\references{
Rambachan, A., & Roth, J. (2023). A more credible approach
to parallel trends. \emph{Review of Economic Studies}, 90(5),
2555--2591.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fzt22.R
\name{Gekdfbias}
\alias{Gekdfbias}
\alias{morie_fauzi_thm2_2_bias_brdkdfe}
\title{Bias of the bias-reduced KDFE (Theorem 2.2)}
\usage{
Gekdfbias(h, a, b2, b4, fx)

morie_fauzi_thm2_2_bias_brdkdfe(h, a, b2, b4, fx)
}
\arguments{
\item{h}{Bandwidth.}

\item{a}{Second smoothing parameter; \code{a > 0}, \code{a != 1}.}

\item{b2, b4}{The coefficients (2.7) and (2.8) at \code{x}.}

\item{fx}{\code{F_X(x)}, strictly between 0 and 1.}
}
\value{
Named list with \code{bias}, \code{leading}, \code{h}, \code{a}, \code{method}.
}
\description{
Theorem 2.2, Eq. (2.6):
\deqn{\mathrm{Bias}[\tilde F_X(x)] = h^4 a^2 \frac{b_2^2(x) - 2 b_4(x) F_X(x)}{2 F_X(x)} + o(h^4) + O(n^{-1}),}{Bias[Ftilde(x)] = h^4 a^2 (b2(x)^2 - 2 b4(x) F(x)) / (2 F(x)) + o(h^4) + O(1/n),}
with \code{b2} from (2.7) and \code{b4} from (2.8).
}
\details{
Two things worth naming. The \code{O(1/n)} term is not a smoothing error but the
price of the estimator being a NONLINEAR function of two linear statistics,
from the \code{O(p^2)} remainder in \code{(1+p)^q = 1 + pq + O(p^2)}. There is a floor
on the bias that no bandwidth can cross.

And the \code{a^2} factor: the bias GROWS with the second smoothing parameter,
while Remark 2.2 says \code{Ftilde -> Fhat_h} as \code{a -> Inf}. Both are true -- the
\verb{h^4 a^2} form is asymptotic in \code{h} for FIXED \code{a}, and Table 2.1 duly shows
the smallest AISE at \code{a = 0.01}.
}
\examples{
Gekdfbias(h = 0.1, a = 2, b2 = 0, b4 = 0.5, fx = 0.5)
}
\references{
Fauzi and Maesono (2023), Theorem 2.2, Eqs. (2.6)-(2.8).
}
\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/siu_panel.R
\name{morie_siu_panel}
\alias{morie_siu_panel}
\title{Run the Mixture-of-Agents reading panel on SIU reports}
\usage{
morie_siu_panel(
  html,
  mode = 4L,
  readers = NULL,
  auditors = NULL,
  reader_concurrency = 3L,
  granularity = c("all_fields", "per_field"),
  host = Sys.getenv("OLLAMA_HOST"),
  timeout = 300
)
}
\arguments{
\item{html}{Report HTML (character), or a file path, or a drid (numeric)
fetched via the bricklayer engine.}

\item{mode}{Panel mode 1-4 (see above). Default 4.}

\item{readers}{Optional character vector of reader model names.}

\item{auditors}{Optional character vector of auditor model names
(sequential chain).}

\item{reader_concurrency}{Max readers running at once (default 3).}

\item{granularity}{\code{"all_fields"} (one pass per reader) or \code{"per_field"}
(one focused read per schema field). Applies to readers and auditors.}

\item{host}{Ollama server; default \code{Sys.getenv("OLLAMA_HOST")}.}

\item{timeout}{Per-call timeout in seconds (default 300).}
}
\value{
A list: \code{fields} (named character vector, the auditor's final
values), \code{readers} (each reader's raw answers), \code{audit_chain} (each
auditor's verdicts), \code{models} (who served).
}
\description{
The model tier of the SIU mining pipeline, for reports that are not yet
in the panel-reviewed corpus (see \code{\link[=morie_siu_reports]{morie_siu_reports()}}; reviewed reports
never need this -- their answers are already verified). Modes mirror the
original panel that built the corpus:
}
\details{
\itemize{
\item \code{mode = 1} -- one reader, no auditor (quick pass)
\item \code{mode = 2} -- one reader + one auditor
\item \code{mode = 3} -- two readers + one auditor
\item \code{mode = 4} -- three readers + one auditor (highest confidence, default)
}

Explicit \code{readers} / \code{auditors} vectors override the mode's counts, and
extra auditors form a sequential review chain (each sees its
predecessors' verdicts). Readers run concurrently up to
\code{reader_concurrency} (cloud tiers cap requests; local servers cap VRAM);
auditors are always sequential. The auditor never starts before every
reader has answered.

The context prompt forbids lazy \code{"None"} answers: count-type fields must
be counted (0 is a real answer only for a witness-official-only case),
and every answer needs a verbatim supporting quote. With
\code{granularity = "per_field"} the model is asked one field at a time and
re-reads the whole report for each -- slower, but it stops a model
skimming once and hallucinating 60 answers.
}
\examples{
\dontshow{if (morie_llm_probe_ollama()) withAutoprint(\{ # examplesIf}
# Runs when a local Ollama server is reachable (free default).
res <- morie_siu_panel(5161, mode = 2)
res$fields\["number_of_subject_officers"\]
\dontshow{\}) # examplesIf}
}

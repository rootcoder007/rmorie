% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sptgk.R
\name{sptgk}
\alias{sptgk}
\title{Trans-Gaussian kriging}
\usage{
sptgk(coords, z, target, phi, dphi, d2phi, semivariogram_fn)
}
\arguments{
\item{coords}{Matrix of sampling locations.}

\item{z}{Observed values on the Y (Gaussian) scale.}

\item{target}{Prediction location.}

\item{phi, dphi, d2phi}{The transformation and its first two derivatives.}

\item{semivariogram_fn}{gamma(h) for the Y scale.}


}
\value{
A list with \code{prediction}, \code{naive_prediction}, \code{correction},
\code{mspe}, \code{kriging_variance}, \code{lagrange} and \code{mu_y}.
}
\description{
With \eqn{Z(s) = \phi(Y(s))} and Y Gaussian, the natural predictor
\eqn{\phi(p_{ok}(Y;s_0))} is biased. Expanding to second order about
\eqn{\mu_Y} and matching expectations gives eq (5.58),
\eqn{p_{tg} = \phi(p_{ok}) + \phi''(\mu_Y)/2\,(\sigma^2_{ok} - 2 m_Y)},
with mean squared prediction error eq (5.59),
\eqn{\[\phi'(\mu_Y)\]^2 \sigma^2_{ok}}.
}
\details{
\eqn{m_Y} enters with the sign convention of eq (5.20); a flipped
multiplier would move every prediction with no other symptom.
}
\references{
Schabenberger Ch 5, Sec 5.6.2
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rfgen.R
\name{morie_random_forest_genomic}
\alias{morie_random_forest_genomic}
\title{Random-forest genomic predictor}
\usage{
morie_random_forest_genomic(
  x,
  y,
  markers,
  n_trees = 100,
  max_depth = 10,
  min_samples = 2,
  mtry = NULL,
  seed = 0
)
}
\arguments{
\item{x}{Optional fixed features.}

\item{y}{Numeric response.}

\item{markers}{Genotype matrix (n x m).}

\item{n_trees}{Number of trees.}

\item{max_depth}{Max depth (fallback only).}

\item{min_samples}{Min samples per node.}

\item{mtry}{Features sampled per split (default sqrt(p)).}

\item{seed}{Seed.}
}
\value{
list(estimate, y_hat, oob_score, feature_importance, se, n, method).
}
\description{
Uses randomForest if available; otherwise a base-R bagged-tree
fallback (regression CART approximation).
}
\examples{
morie_random_forest_genomic(
  x = rnorm(50), y = rnorm(50),
  markers = matrix(sample(0:2, 200, TRUE), 50, 4)
)
}
\references{
Breiman (2001); Montesinos Lopez Ch 8.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gblpq.R
\name{Gblupeq}
\alias{Gblupeq}
\title{Cholesky re-parameterization that makes GBLUP an ordinary mixed model.}
\usage{
Gblupeq(Z, G, sigma2_g)
}
\arguments{
\item{Z}{Design matrix of lines.}

\item{G}{Genomic relationship matrix; must be positive definite.}

\item{sigma2_g}{Genomic variance component.}
}
\value{
List with \code{Zstar}, \code{L}, \code{V_original}, \code{V_reparameterized}, \code{max_gap}, \code{n}, \code{q}.
}
\description{
Formula: G = L L';  Zstar = Z L;  then Y = X beta + Zstar ustar + e with ustar ~ N(0, sigma2_g I) has the same marginal variance as u ~ N(0, sigma2_g G)
}
\references{
Montesinos Lopez, Montesinos Lopez and Crossa (2022), Multivariate Statistical Machine Learning Methods for Genomic Prediction, Springer, doi:10.1007/978-3-030-89010-0.  Chapter 2, p. 46: the GBLUP model can be expressed equivalently as Y = X beta + Z* u + e with Z* = Z L', G = L'L the Cholesky decomposition of G, and u ~ N(0, sigma2_g I_q).  The book's L is the upper factor returned by R's chol(); this implementation uses the lower factor L with G = L L' and Z* = Z L in BOTH language arms, which gives the identical marginal variance sigma2_g Z G Z' -- \code{max_gap} reports the largest entry-wise difference between the two marginal variances, and is zero up to rounding.  Read from the chapter PDF, not recalled.
}
\examples{
set.seed(1)
Z <- matrix(rbinom(50, 1, 0.5), 10, 5)
G <- diag(5)
Gblupeq(Z, G, sigma2_g = 1)
}

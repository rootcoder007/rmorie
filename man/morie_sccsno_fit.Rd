% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sccsno_native.R
\name{morie_sccsno_fit}
\alias{morie_sccsno_fit}
\alias{morie_sccsno}
\title{morie_sccsno_fit}
\usage{
morie_sccsno_fit(
  cases,
  risk_periods,
  age_breaks = c(),
  iters = 100,
  tol = 1e-10,
  ridge = 1e-10
)

morie_sccsno(
  cases,
  risk_periods,
  age_breaks = c(),
  iters = 100,
  tol = 1e-10,
  ridge = 1e-10
)
}
\arguments{
\item{cases}{See Usage.}

\item{risk_periods}{Passed to \code{.sccsno_rp}.}

\item{age_breaks}{Coerced to numeric by the body, with \code{as.numeric}. Defaults to \code{c()}.}

\item{iters}{Coerced to integer by the body, with \code{as.integer}. Defaults to \code{100}.}

\item{tol}{Passed to \code{<}. Defaults to \code{1e-10}.}

\item{ridge}{A matrix; passed to \code{diag}. Defaults to \code{1e-10}.}
}
\value{
A list with \code{estimate}, \code{relative_incidence}, \code{log_ri},
\code{se_log_ri}, \code{age_effects}, \code{se_age}, \code{coef}, \code{se},
\code{loglik}, \code{n_cases}, \code{converged}, \code{iterations},
\code{n_risk_periods}, \code{n_age_bands}, \code{method}, \code{conditions_out}.
}
\description{
A step of the sccsno_native implementation. No other function in the package calls it.
See the file header for the source the module follows.
source it follows.
}
\examples{
set.seed(1)
cases <- lapply(1:12, function(i) {
  expo <- runif(1, 60, 300)
  n_ev <- 1 + rbinom(1, 2, 0.5)
  ev <- sort(c(runif(max(n_ev - 1, 0), 0, 365),
               expo + runif(1, 0, 30)))
  list(start = 0, end = 365, exposure = expo, events = ev)
})
r <- morie_sccsno_fit(cases, risk_periods = list(c(0, 30)))
str(r, max.level = 1)
}
\keyword{internal}

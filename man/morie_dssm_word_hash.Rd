% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dssm_native.R
\name{morie_dssm_word_hash}
\alias{morie_dssm_word_hash}
\title{Word hashing: replace the bag-of-words with a bag of letter n-grams}
\usage{
morie_dssm_word_hash(words, n = 3L, vocabulary = NULL)
}
\arguments{
\item{words}{character vector. The token stream.}

\item{n}{integer(1). Letter n-gram size. Default \code{3}.}

\item{vocabulary}{character vector or \code{NULL}. If \code{NULL}
(default) the n-grams present in \code{words} are themselves the
vocabulary; otherwise only n-grams that appear in this list count
toward the output dimension, and any others contribute to
\code{unseen_ngrams}.}
}
\value{
A named list with \code{vector} (numeric, the bag-of-ngrams
counts in the canonical order), \code{dimension}, \code{ngrams}
(raw counts keyed by n-gram string), \code{unseen_ngrams} (number
of n-grams that were not in the supplied vocabulary), and
\code{note}.
}
\description{
The input layer's width becomes the number of n-grams, not the
size of the vocabulary -- and an unseen word still has a vector.
}
\examples{
V <- c(1, 2, 3, 4, 5, 6, 7, 8)
morie_dssm_word_hash(V)
}
\keyword{internal}

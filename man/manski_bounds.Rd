% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensitivity.R
\name{manski_bounds}
\alias{manski_bounds}
\title{Manski worst-case bounds for the ATE}
\usage{
manski_bounds(
  outcome_treated,
  outcome_control,
  p_treated,
  outcome_range = NULL
)
}
\arguments{
\item{outcome_treated}{Outcomes for treated units.}

\item{outcome_control}{Outcomes for control units.}

\item{p_treated}{Proportion treated.}

\item{outcome_range}{c(min, max) on the outcome. Default c(0, 1).}
}
\value{
Named list.
}
\description{
Under no assumptions about selection, the ATE is only partially
identified (Manski 1990): with the outcome in \eqn{[a, b]},
\eqn{E[Y_1] \in [p \bar y_1 + a(1-p), p \bar y_1 + b(1-p)]} and
\eqn{E[Y_0] \in [(1-p) \bar y_0 + a p, (1-p) \bar y_0 + b p]}, so the
bounds always have width \eqn{b - a}.  Returns a named list with
\code{lower_bound}, \code{upper_bound}, \code{point_estimate}, \code{width}.
}
\examples{
set.seed(1)
res <- manski_bounds(runif(50), runif(50), p_treated = 0.5)
c(res$lower_bound, res$upper_bound)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vcov_native.R
\name{morie_vcov_hac}
\alias{morie_vcov_hac}
\title{HAC (Newey-West) covariance (native)}
\usage{
morie_vcov_hac(model, lag = NULL, prewhite = FALSE, adjust = TRUE)
}
\arguments{
\item{model}{A fitted \code{lm} / \code{glm}.}

\item{lag}{Number of lags (default the Newey-West rule
\eqn{\lfloor 4 (n/100)^{2/9} \rfloor}).}

\item{prewhite}{Currently ignored (kept for signature parity).}

\item{adjust}{Apply the \eqn{n/(n-k)} finite-sample factor
(default TRUE, matching \code{sandwich::NeweyWest}).}
}
\value{
The coefficient covariance matrix.
}
\description{
Reproduces \code{sandwich::vcovHAC} / \code{sandwich::NeweyWest}
with the Bartlett kernel and a fixed lag. The meat accumulates the
weighted lag-l autocovariances of the score contributions.
}
\examples{
if (requireNamespace("sandwich", quietly = TRUE)) {
  set.seed(1)
  df <- data.frame(y = rnorm(60), x = rnorm(60),
                   cl = rep(1:6, each = 10))
  fit <- stats::lm(y ~ x, data = df)
  morie_vcov_hac(fit, lag = 2L)
}
}
\references{
Newey, W. K., & West, K. D. (1987). A simple, positive
semi-definite, heteroskedasticity and autocorrelation consistent
covariance matrix. \emph{Econometrica}, 55(3), 703-708.
}

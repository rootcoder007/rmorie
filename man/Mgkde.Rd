% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fzmgkd.R
\name{Mgkde}
\alias{Mgkde}
\alias{morie_fauzi_modified_gamma_kde}
\title{Modified gamma kernel density estimator (Eq. 1.14)}
\usage{
Mgkde(x, grid, h)

morie_fauzi_modified_gamma_kde(x, grid, h)
}
\arguments{
\item{x}{Sample on \verb{[0, infinity)}.}

\item{grid}{Points at which to evaluate the density.}

\item{h}{Bandwidth, \code{h > 0}.}
}
\value{
Named list with \code{estimate}, \code{ah}, \code{a4h}, \code{grid}, \code{h}, \code{n}, \code{method}.
}
\description{
Eq. (1.14): \eqn{\tilde f_X(x) = [A_h(x)]^2 [A_{4h}(x)]^{-1}}{ftilde(x) = A_h(x)^2 / A_4h(x)},
with \code{A_h} the raw gamma-kernel function (1.9) -- a sample mean of
Gamma(\code{h^(-1/2)}, \verb{x sqrt(h) + h}) densities.
}
\details{
The exponents 2 and -1 are the \verb{(t1, t2)} of Theorem 1.2, and the factor 4
on the bandwidth is fixed by that theorem too; neither is tunable, which is
why neither is a parameter. The estimator is non-negative by construction,
unlike the order-4 kernel alternative.

Chapter 1 fixes the bandwidth ratio at 4 because its expansion is in
\code{sqrt(h)}; the free ratio \code{a} belongs to the Chapter 2 DISTRIBUTION-function
construction (2.5), whose expansion is in \code{h^2}.
}
\examples{
Mgkde(c(0.5, 1, 1.5, 2, 2.5), grid = 1, h = 0.2)
}
\references{
Fauzi and Maesono (2023), Eqs. (1.9) and (1.14).
}
\keyword{internal}

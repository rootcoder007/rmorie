% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/caus_native.R
\name{morie_caus_iv_2sls}
\alias{morie_caus_iv_2sls}
\title{Two-stage least squares}
\usage{
morie_caus_iv_2sls(y, X, Z, cluster = NULL)
}
\arguments{
\item{y}{outcome.}

\item{X}{regressors, endogenous and exogenous together.}

\item{Z}{instruments; exogenous regressors instrument for
themselves and belong in both.}

\item{cluster}{optional cluster identifiers.}
}
\value{
list: beta, se, t, residuals, fitted, first_stage_F,
overidentified, n_overid_restrictions, sargan, sargan_p,
vcov_type, n, k, m, method.
}
\description{
\verb{beta = (X'P_Z X)^-1 X'P_Z y}. Identification needs at least as
many instruments as regressors (the order condition, arithmetic
and checked here) and \verb{rank(Z'X) = dim(beta)} (the rank condition,
which for floating-point data is a matter of degree rather than a
decidable property -- the degree is what \code{first_stage_F} reports).
}
\details{
Residuals are \verb{y - X beta} with the ORIGINAL \code{X}, not the
first-stage fitted values. Using the fitted values there is a
common and silent error that produces a smaller number which is
not a standard error of anything.
}
\examples{
Z <- matrix(stats::rnorm(200), 100)
D <- Z \%*\% c(1, 0.5) + stats::rnorm(100)
morie_caus_iv_2sls(2 * D + stats::rnorm(100), D, Z)$beta
}
\references{
Wooldridge (2010), \emph{Econometric Analysis of Cross
Section and Panel Data}, 2nd ed., Ch. 5; Sargan (1958),
\emph{Econometrica} 26:393-415.
}

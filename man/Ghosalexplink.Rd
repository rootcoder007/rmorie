% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gh_c2_4.R
\name{Ghosalexplink}
\alias{Ghosalexplink}
\title{Exponential-link density prior}
\usage{
Ghosalexplink(x, psi = NULL)
}
\arguments{
\item{x}{Grid points; sorted internally, at least two of them.}

\item{psi}{Log-density shape as a function; sin(3 t) when NULL.}
}
\value{
List with \code{estimate} (density at the middle grid point),
\code{density}, \code{normalizer}, \code{method}.
}
\description{
p(x) = exp(psi(x)) / int exp(psi) turns ANY bounded function psi into a
positive normalised density.  The two constraints that make density
estimation awkward -- positivity and unit mass -- are both discharged
by the link, so the prior on psi can be completely unconstrained.
}
\details{
Formula: Z = int exp(psi) by the trapezoid rule on the sorted grid;
density = exp(psi) / Z.
}
\references{
Ghosal & van der Vaart (2017), Fundamentals of
Nonparametric Bayesian Inference, CUP, section 2.3.1.
}
\examples{
V <- c(1, 2, 3, 4, 5, 6, 7, 8)
Ghosalexplink(V)
}

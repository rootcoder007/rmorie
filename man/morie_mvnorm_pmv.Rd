% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extenders_spatial.R
\name{morie_mvnorm_pmv}
\alias{morie_mvnorm_pmv}
\title{Multivariate normal rectangle probability via \pkg{mvtnorm}}
\usage{
morie_mvnorm_pmv(lower, upper, mean = rep(0, length(lower)), sigma, ...)
}
\arguments{
\item{lower}{Numeric vector of lower integration limits
(\code{-Inf} permitted).}

\item{upper}{Numeric vector of upper integration limits
(\code{Inf} permitted).}

\item{mean}{Numeric mean vector of the same length as
\code{lower} (defaults to a zero vector).}

\item{sigma}{Numeric positive-(semi)definite covariance matrix.}

\item{...}{Further arguments forwarded to
\code{mvtnorm::pmvnorm} (e.g. \code{corr}, \code{algorithm},
\code{keepAttr}).}
}
\value{
A list with \code{$method = "mvtnorm::pmvnorm"} and
\code{$raw} (a numeric scalar with the estimated probability
and Monte-Carlo error attributes attached).
}
\description{
Thin extender over \code{mvtnorm::pmvnorm} that evaluates the
multivariate normal CDF over a hyper-rectangle
\eqn{[lower, upper]}.
}
\examples{
\donttest{
  if (requireNamespace("mvtnorm", quietly = TRUE)) {
    set.seed(1)
    S <- matrix(c(1, 0.4, 0.4, 1), 2, 2)
    morie_mvnorm_pmv(
      lower = c(-1, -1), upper = c(1, 1),
      mean = c(0, 0), sigma = S
    )
  }
}
}

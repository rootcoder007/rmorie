% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cssant_native.R
\name{morie_cssant}
\alias{morie_cssant}
\title{Callaway-Sant'Anna staggered difference-in-differences}
\usage{
morie_cssant(y, D, unit, time, cohort = NULL, control = "notyet")
}
\arguments{
\item{y}{Outcome, long format.}

\item{D}{Binary 0/1 absorbing treatment, long format.}

\item{unit, time}{Panel identifiers.}

\item{cohort}{Optional per-unit adoption period, constant within
unit, as an alternative to deriving it from \code{D}.}

\item{control}{\code{"notyet"} (default) or \code{"never"}.}
}
\value{
A list with \code{estimate} (overall ATT), \code{se},
\code{ci}, \code{att_gt}, \code{n_by_cell}, \code{event},
\code{cohort_att}, \code{calendar}, \code{pretrend},
\code{pretrend_max_abs}, \code{pretrend_note},
\code{control_group}, \code{cohorts}, \code{n_cells},
\code{n_units}, \code{n_periods}, \code{clean_controls},
\code{method}.
}
\description{
Estimates every group-time average treatment effect ATT(g, t)
against a clean comparison group and aggregates them (Callaway and
Sant'Anna 2021).  Because an already-treated unit is never used as
a control, the negative-weight comparisons that contaminate a
two-way fixed-effects regression cannot arise.
}
\references{
Callaway, B. and Sant'Anna, P. H. C. (2021).
Difference-in-differences with multiple time periods. Journal of
Econometrics, 225(2), 200-230.
}
\examples{
set.seed(1)
nu <- 6; T <- 5
unit <- rep(1:nu, each = T)
time <- rep(1:T, nu)
ft <- rep(c(Inf, Inf, Inf, 4, 4, 3), each = T)
D <- as.integer(time >= ft)
y <- rnorm(nu * T) + D * 1.5
morie_cssant(y, D, unit, time)
}

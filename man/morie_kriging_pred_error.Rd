% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schaben_native.R
\name{morie_kriging_pred_error}
\alias{morie_kriging_pred_error}
\title{Ordinary kriging with a Prasad-Rao corrected prediction error}
\usage{
morie_kriging_pred_error(
  coords,
  z,
  target,
  model = "exponential",
  nugget = NULL,
  psill = NULL,
  rng = NULL,
  jitter = 0.05,
  n_jitter = 24L,
  seed = 0L
)
}
\arguments{
\item{coords, z}{As in \code{morie_cressie_hawkins()}.}

\item{target}{Matrix of prediction locations.}

\item{model}{Covariance model name.}

\item{nugget, psill, rng}{Covariance parameters; estimated by weighted
least squares when any is NULL.}

\item{jitter, n_jitter, seed}{Controls for approximating \eqn{B}.}





}
\value{
A list with \code{prediction}, \code{mse}, \code{mse_plugin}, \code{correction},
\code{se}, \code{parameters}, \code{parameters_estimated}.
}
\description{
The plug-in kriging variance is not the prediction error of the
predictor actually in use. Substituting an estimated \eqn{\theta} into
the predictor gives an EBLUP, which is no longer best, while
substituting into the variance formula gives the prediction error of a
DIFFERENT predictor - the one that would apply had \eqn{\theta} been
known. Schabenberger & Gotway are blunt that this "can be
substantially biased", and always downward.
}
\details{
Kackar & Harville (1984) supply the missing term
\eqn{\mathrm{tr}\{A(\theta)B(\theta)\}}; evaluating it at
\eqn{\hat\theta} removes only half the bias, so Prasad & Rao's
approximately unbiased form, equation (5.53), doubles the correction.
}
\examples{
morie_kriging_pred_error(coords = c(1, 2, 3, 4, 5, 6, 7, 8), z = c(1, 2, 3, 4, 5, 6, 7, 8),
  target = c(1, 2, 3, 4, 5, 6, 7, 8))
}
\references{
Schabenberger O, Gotway CA (2005), Sec 5.5.4, eq
(5.51)-(5.53), pp. 263-266. Kackar RN, Harville DA (1984).
Prasad NGN, Rao JNK (1990).
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geron_ml2_native.R
\name{morie_geron_minibatch_gd}
\alias{morie_geron_minibatch_gd}
\title{Mini-batch gradient descent (Geron Ch 4, morie.fn hmmbgd)}
\usage{
morie_geron_minibatch_gd(X, y, theta, eta, b, order = NULL, n_steps = 1)
}
\arguments{
\item{X, y, theta}{Design, targets, coefficients.}

\item{eta}{Positive rate.}

\item{b}{Batch size in 1..m.}

\item{order}{Optional 0-based permutation; NULL means identity.}

\item{n_steps}{Number of updates.}
}
\value{
List with \code{theta}, \code{gradient}, \code{full_gradient},
\code{batch_indices} (0-based), \code{mse}, \code{batch_size}, \code{estimate}, \code{n}.
}
\description{
b = m is batch GD, b = 1 is SGD. IMPORTANT: the Python module draws
its shuffle from \code{numpy.random.default_rng} (PCG64), which R cannot
reproduce, so this port takes the batch order explicitly. Pass
\code{order} as a 0-based permutation of 0..m-1 (the parity tests read
the permutation out of Python and pass it in); with \code{order = NULL}
the identity order is used. This is the one honest divergence in
this file.
}
\examples{
set.seed(4)
X <- cbind(1, rnorm(30))
y <- X \%*\% c(1, 2) + rnorm(30, 0, 0.2)
r <- morie_geron_minibatch_gd(X, as.numeric(y), theta = c(0, 0),
                              eta = 0.05, b = 10, n_steps = 20)
str(r, max.level = 1)
}

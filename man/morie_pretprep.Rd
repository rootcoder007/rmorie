% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ml_geron.R
\name{morie_pretprep}
\alias{morie_pretprep}
\title{Preprocess an image for a pretrained model (pp. 458-459)}
\usage{
morie_pretprep(image, size, mean, sd, logits = NULL, topk = 1L)
}
\arguments{
\item{image}{height by width by channels array}

\item{size}{the square edge the model expects}

\item{mean, sd}{per-channel standardization constants}

\item{logits}{optional class logits, for the p. 459 argmax read-out}

\item{topk}{how many classes to report}

}
\value{
list(pixels, size, channels, cropside, pixelmean, pixelmax,
and pred/topk/topprob when logits are supplied)
}
\description{
The section's substance is the preprocessing contract, not the
download: the image must be brought to the size the pretrained model
expects (224 x 224 for ConvNeXt) and the pixel intensities
standardized per colour channel using ImageNet's means and standard
deviations. Those constants are NOT printed in the extracted text,
so mean and sd are required arguments rather than baked-in defaults.
}
\details{
ponytail: centre crop to a square, then nearest-neighbour subsample.
weights.transforms() uses bilinear resizing; swap the sampler if you
need to match torchvision to the last decimal.
}
\examples{
set.seed(1)
img <- array(runif(8 * 8 * 3), dim = c(8, 8, 3))
out <- morie_pretprep(img, size = 4, mean = c(0.5, 0.5, 0.5),
                      sd = c(0.25, 0.25, 0.25))
dim(out$image)
}

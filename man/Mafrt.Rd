% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mafrt.R
\name{Mafrt}
\alias{Mafrt}
\title{Variance-stabilising double arcsine transform of a proportion}
\usage{
Mafrt(x, n)
}
\arguments{
\item{x}{Event counts.}

\item{n}{Sample sizes, elementwise against x.}
}
\value{
List with \code{ft}, \code{var}, \code{se}, \code{k}.
}
\description{
A single arcsine blows up at zero and one; the double arcsine
averages two neighbouring single arcsines so a study with no events
still contributes, which is the case that matters in rare-event
synthesis. The variance depends only on n, so the weights are fixed
rather than estimated.
}
\details{
Formula: \code{FT = asin sqrt(x/(n+1)) + asin sqrt((x+1)/(n+1))},
\code{Var(FT) = 1/(n + 1/2)}.
}
\examples{
V <- c(1, 2, 3, 4, 5, 6, 7, 8)
Mafrt(V, V)
}
\references{
Freeman, M. F. & Tukey, J. W. (1950). Ann Math Statist
21:607-611; Miller, J. J. (1978). Amer Statist 32:138.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fzecdf.R
\name{Edf}
\alias{Edf}
\alias{morie_fauzi_ecdf}
\title{Empirical distribution function}
\usage{
Edf(x, grid = NULL)

morie_fauzi_ecdf(x, grid = NULL)
}
\arguments{
\item{x}{Sample.}

\item{grid}{Evaluation points; defaults to the sorted sample.}
}
\value{
Named list with \code{estimate}, \code{se}, \code{variance}, \code{grid}, \code{n}, \code{method}.
}
\description{
\deqn{F_n(x) = n^{-1}\sum_i I(X_i \le x).}{F_n(x) = (1/n) sum_i I(X_i <= x).}
}
\details{
The book's baseline, and the thing every kernel estimator in it is trying
to beat. Its bias is exactly zero and its variance exactly
\code{F(x)(1 - F(x))/n} -- no expansion, no remainder -- which is why Sec. 2.1
can say flatly that a kernel with \code{r1 > 0} beats it for ANY \code{F_X}: the
kernel estimator's variance is that same quantity minus
\verb{2 h r1 f(x) / n}.

Right-continuous, as the definition requires: ties at \code{x} count. \code{se} is
the exact binomial standard error, not an asymptotic one.
}
\examples{
Edf(c(1, 2, 3, 4), grid = 2)
}
\references{
Fauzi and Maesono (2023), Sec. 2.1, and the bias/variance display preceding (2.3).
}
\keyword{internal}

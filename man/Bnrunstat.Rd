% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/agbtnm.R
\name{Bnrunstat}
\alias{Bnrunstat}
\title{Batch-normalisation running statistics and inference transform.}
\usage{
Bnrunstat(
  x,
  runmean = 0,
  runvar = 1,
  momentum = 0.1,
  eps = 1e-05,
  gamma = 1,
  beta = 0
)
}
\arguments{
\item{x}{Mini-batch of activations, length m >= 2.}

\item{runmean, runvar}{Running population moments.}

\item{momentum}{Weight given to the new batch, in [0, 1].}

\item{eps}{Numerical floor inside the square root.}

\item{gamma, beta}{Learned scale and shift.}
}
\value{
List with runmean, runvar, batchmean, batchvar, batchvarunb,
normalized, trainnorm, m.
}
\description{
runmean <- (1-momentum) runmean + momentum mean(x);
runvar  <- (1-momentum) runvar + momentum (m/(m-1)) biased_var(x);
y = gamma (x - runmean)/sqrt(runvar + eps) + beta.
}
\examples{
V <- c(1, 2, 3, 4, 5, 6, 7, 8)
Bnrunstat(V)
}
\references{
Ioffe and Szegedy (2015), arXiv:1502.03167, Sect. 3.1 and
Algorithm 2.  Read from the ar5iv rendering; the same paper is in the
local corpus.
}

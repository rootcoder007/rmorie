% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geron_train_native.R
\name{morie_geron_swin_window_attention}
\alias{morie_geron_swin_window_attention}
\title{Swin windowed self-attention (morie.fn grswin)}
\usage{
morie_geron_swin_window_attention(X, window_size, WQ, WK, WV, shift = 0)
}
\arguments{
\item{X}{(H, W, d_model) array.}

\item{window_size}{M dividing both H and W.}

\item{WQ, WK, WV}{Projections with d_model rows.}

\item{shift}{Cyclic shift in [0, M-1].}


}
\value{
List with \code{output}, \code{window_weights}, \code{n_windows},
\code{tokens_per_window}, \code{shift}.
}
\description{
Attention inside M x M windows, O(HW M^2) rather than O((HW)^2);
a non-zero \code{shift} rolls the partition by -shift on both spatial axes
before windowing and rolls the output back.
}
\examples{
set.seed(7)
X <- array(rnorm(4 * 4 * 3), c(4, 4, 3))
W <- diag(3)
r <- morie_geron_swin_window_attention(X, window_size = 2,
                                       WQ = W, WK = W, WV = W)
str(r, max.level = 1)
}

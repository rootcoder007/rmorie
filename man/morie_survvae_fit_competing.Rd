% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survvae_native.R
\name{morie_survvae_fit_competing}
\alias{morie_survvae_fit_competing}
\title{One fit per risk, other causes treated as censoring}
\usage{
morie_survvae_fit_competing(
  X,
  times,
  causes,
  K = 3L,
  primitive = "weibull",
  alpha = 1,
  prior = 0,
  seed = 0
)
}
\arguments{
\item{X}{List of covariate vectors, length \code{n}.}

\item{times}{Numeric vector of survival times, length \code{n}.}

\item{causes}{Integer vector of cause labels (0 = censored,
\code{>0} = competing events).}

\item{K}{Number of experts.}

\item{primitive}{One of \code{"weibull"} or \code{"lognormal"}.}

\item{alpha}{Discount on the censored term, in \code{[0, 1]}.}

\item{prior}{Strength of the log-parameter L2 penalty.}

\item{seed}{Seed for the shared generator (see
\code{.ghc_rng}).}
}
\value{
Named list with \code{estimate} (number of risks),
\code{risks} (sorted unique non-zero labels), \code{fits}
(one fit per risk keyed by the label as a string) and
\code{method}.
}
\description{
For each non-zero cause label \code{lab}, refits the model
treating \code{lab} as the event of interest and every other
non-zero cause as independent censoring (cause 0 is already
censored).
}
\examples{
\donttest{
morie_survvae_fit_competing(X = c(1, 2, 3, 4, 5, 6, 7, 8), times = c(1, 2, 3, 4, 5, 6, 7, 8),
  causes = c(1, 2, 3, 4, 5, 6, 7, 8))
}
}
\references{
Nagpal et al. (2021), Sec. III-D.
}
\keyword{internal}

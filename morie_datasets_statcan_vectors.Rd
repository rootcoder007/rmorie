% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datasets_statcan.R
\name{morie_datasets_statcan_vectors}
\alias{morie_datasets_statcan_vectors}
\title{Fetch the latest N periods for a set of StatCan vectors via WDS}
\usage{
morie_datasets_statcan_vectors(vector_ids, n_periods = 5L, timeout_s = 60L)
}
\arguments{
\item{vector_ids}{Integer vector of StatCan vector IDs (no \code{v}
prefix; the API takes raw integers).}

\item{n_periods}{Number of latest periods per vector (default 5).}

\item{timeout_s}{HTTP timeout.}
}
\value{
A \code{data.frame} with one row per (vector, period)
observation: \code{vector_id}, \code{coordinate}, \code{ref_period}, \code{value},
\code{decimals}, \code{status}, \code{symbol}, \code{scalar_factor}.
}
\description{
Phase 3DDD3. Wraps the \code{getDataFromVectorsAndLatestNPeriods}
POST endpoint. A "vector" is StatCan's atomic time series ID
(e.g., \code{v109502878} for "Canada -- Total, all violations").
}
\examples{
\dontrun{
df <- morie_datasets_statcan_vectors(c(109502878L, 109502879L),
                                        n_periods = 3)
nrow(df)  # ~6
}
}

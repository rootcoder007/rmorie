% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mrm_diagnostics.R
\name{mrm_check_balancing}
\alias{mrm_check_balancing}
\title{Composite balance verdict using the Imbens-Rubin \%SMD criterion}
\usage{
mrm_check_balancing(data, treatment_col, covariates, threshold_pct = 10)
}
\arguments{
\item{data}{data.frame.}

\item{treatment_col}{Binary 0/1 treatment column.}

\item{covariates}{Character vector of covariate columns.}

\item{threshold_pct}{\%SMD imbalance threshold (default 10).}
}
\value{
Named list with table, threshold_pct, n_imbalanced,
overall_balanced, interpretation.
}
\description{
A design is "balanced on X" if every |SMD(X_i)| <= threshold.
}
\examples{
set.seed(2026)
n <- 200L
df <- data.frame(
  D   = rbinom(n, 1, 0.4),
  age = rnorm(n, 50, 10),
  bmi = rnorm(n, 27, 4)
)
df$age[df$D == 1] <- df$age[df$D == 1] + 3 # imbalance on age
bal <- mrm_check_balancing(df,
  treatment_col = "D",
  covariates = c("age", "bmi")
)
bal$overall_balanced
bal$interpretation
}

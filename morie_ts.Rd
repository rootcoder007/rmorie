% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ts_forecast.R
\name{morie_ts}
\alias{morie_ts}
\title{Construct a validated time series}
\usage{
morie_ts(
  x,
  frequency = 1,
  start = 1,
  calendar = NA_character_,
  na_action = c("error", "interpolate", "omit"),
  units = attr(x, "units")
)
}
\arguments{
\item{x}{A numeric vector (or an object coercible to one, including a
\code{units}-typed vector, which is coerced via \code{\link[=as.numeric]{as.numeric()}}), or an
existing \code{ts}.}

\item{frequency}{Number of observations per unit of time (e.g. 12 for
monthly-in-years). Default 1.}

\item{start}{Time of the first observation. Default 1.}

\item{calendar}{Character tag documenting the calendar system assumed
(e.g. "monthly"); recorded as an attribute, not interpreted.}

\item{na_action}{How to handle missing values (regular series require
an explicit decision): \code{"error"} (default) rejects implicit missing
values; \code{"interpolate"} fills them by linear interpolation
(imputation); \code{"omit"} drops them with a warning (the series is then
treated as irregular).}

\item{units}{Optional character unit label carried as an attribute and
propagated to return values.}
}
\value{
A \code{morie_ts} object (a \code{ts} with validated ordering).
}
\description{
Wraps \code{\link[stats:ts]{stats::ts()}} with explicit validation: the input must be a
strictly ordered, regular numeric series with no implicit missing
periods. This is the single pre-processing routine through which all
subsequent functions receive their data, so time attributes are
maintained uniformly.
}
\examples{
morie_ts(cumsum(rnorm(24)), frequency = 12, start = 2000)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/signal.R
\name{pburg}
\alias{pburg}
\title{Burg autoregressive power spectral density}
\usage{
pburg(x, fs, order = 16L, nfft = 256L)
}
\arguments{
\item{x}{Numeric vector (1-D signal).}

\item{fs}{Sampling frequency in Hz.}

\item{order}{AR model order (default 16).}

\item{nfft}{FFT length for PSD evaluation (default 256).}
}
\value{
List with \code{filtered} (PSD), \code{name}, \code{fs}, \code{n_samples}, and
\code{extra} (\code{freqs}, \code{order}, \code{ar_coefficients}).
}
\description{
Burg AR-spectrum estimation: parametric PSD via the Burg algorithm
for AR-coefficient estimation. Well-suited to short HRV windows
where Welch suffers from low spectral resolution.
}
\details{
Reference: Marple, S.L. (1987) \emph{Digital Spectral Analysis with
Applications}, Prentice-Hall, on the Burg algorithm.
}
\examples{
\donttest{
set.seed(1)
t <- seq(0, 1, length.out = 512)
x <- sin(2 * pi * 10 * t) + 0.5 * rnorm(length(t))
res <- pburg(x, fs = 512)
length(res$filtered)
}
}

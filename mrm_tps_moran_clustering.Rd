% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mrm_tps.R
\name{mrm_tps_moran_clustering}
\alias{mrm_tps_moran_clustering}
\title{Global Moran's I + DBSCAN summary on TPS lat/long event data}
\usage{
mrm_tps_moran_clustering(
  data,
  lat_col = "LAT_WGS84",
  lon_col = "LONG_WGS84",
  grid_resolution = 40L,
  dbscan_eps = 0.3,
  dbscan_minpts = 5L
)
}
\arguments{
\item{data}{A data.frame with \code{lat_col} and \code{lon_col}.}

\item{lat_col}{Column name of WGS84 latitude.}

\item{lon_col}{Column name of WGS84 longitude.}

\item{grid_resolution}{Number of cells per axis (default \code{40L}).}

\item{dbscan_eps}{DBSCAN radius in km (default \code{0.3}).}

\item{dbscan_minpts}{DBSCAN minimum points per core (default \code{5L}).}
}
\value{
A list with \code{morans_I}, \code{morans_z}, \code{dbscan_n_clusters},
\code{dbscan_n_noise}, \code{dbscan_largest}.
}
\description{
Grids the lat/long extent of \code{data} into a coarse raster of
\code{grid_resolution} cells, counts events per cell, and computes the
global Moran's I via a rook contiguity matrix. Also runs DBSCAN on
the raw lat/long points (rescaled to km) and reports cluster
counts.
}
\details{
This function is a thin computational wrapper. For high-precision
computations on full-sized TPS files use the \code{morie} Python
\code{tps_spatial_advanced} pipeline; the R version is for quick
interactive auditing.
}
\examples{
if (FALSE) {
  tps <- read.csv("Assault_Open_Data.csv")
  mrm_tps_moran_clustering(tps)
}
}

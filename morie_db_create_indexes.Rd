% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db_indexes.R
\name{morie_db_create_indexes}
\alias{morie_db_create_indexes}
\title{Create the recommended B-tree indexes for a morie cache table}
\usage{
morie_db_create_indexes(con, table_name)
}
\arguments{
\item{con}{A DBI connection.}

\item{table_name}{The cache table name (case-sensitive). Common
short names: \code{"SIU"}, \code{"b01"}, \code{"c01"}, \code{"d01"},
\code{"uof_main_records"}, \code{"assault"}, \code{"homicide"}, etc.}
}
\value{
Invisibly returns the integer count of \verb{CREATE INDEX}
statements executed.
}
\description{
Looks up \code{table_name} in the per-dataset index registry (see
\code{\link[=.morie_db_index_registry]{.morie_db_index_registry()}} for the full list) and creates each
\verb{CREATE INDEX IF NOT EXISTS} against \code{con}. Specs whose columns
aren't present in the actual table are silently skipped, so this is
safe to call on any morie cache table — including subsets that drop
some columns. Returns the number of \verb{CREATE INDEX} statements that
actually ran (not the number registered).
}
\details{
Cardinality-based selection: every indexed column has > 30 distinct
values in the real published data; low-cardinality columns
(\code{Gender}, \code{Yes}/\code{No} alerts, \code{Measure}) are intentionally skipped
because the index overhead exceeds the lookup benefit.
}

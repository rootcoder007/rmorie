% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/signal.R
\name{buttlp}
\alias{buttlp}
\title{Butterworth lowpass filter}
\usage{
buttlp(x, fs, cutoff, order = 4L)
}
\arguments{
\item{x}{Numeric vector.}

\item{fs}{Sampling frequency (Hz).}

\item{cutoff}{Cutoff frequency (Hz).}

\item{order}{Filter order (default 4).}
}
\value{
List with \code{filtered} (numeric vector), \code{fs}, \code{order}, \code{name}.
}
\description{
Zero-phase Butterworth lowpass filter via the suggested \code{signal} package's
\code{butter()} + \code{filtfilt()}. Useful for removing high-frequency noise from
biological or geophysical time series.
}
\examples{
\donttest{
if (requireNamespace("signal", quietly = TRUE)) {
  set.seed(1)
  t <- seq(0, 1, length.out = 500)
  x <- sin(2 * pi * 5 * t) + 0.5 * sin(2 * pi * 60 * t) # 5 Hz + 60 Hz
  y <- buttlp(x, fs = 500, cutoff = 20)
  length(y$filtered) # 500
}
}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lm_model.R
\name{morie_lm}
\alias{morie_lm}
\title{Fit a regression model with a full model-object contract}
\usage{
morie_lm(
  formula,
  data,
  family = c("gaussian", "binomial"),
  center = FALSE,
  scale = FALSE,
  na_predictor = c("omit", "fail"),
  na_response = c("omit", "keep"),
  nofit = FALSE
)
}
\arguments{
\item{formula}{A model formula.}

\item{data}{A data.frame.}

\item{family}{"gaussian" (linear) or "binomial" (logistic).}

\item{center, scale}{Logical; center predictors to zero mean / scale to
unit SD before fitting (the fit is back-transformed so coefficients
remain on the original scale). Documented and tested (RE2.3).}

\item{na_predictor}{How to handle missing predictor values: "omit"
(drop incomplete predictor rows) or "fail" (error). Controlled
separately from the response (RE2.2).}

\item{na_response}{How to handle missing response values: "omit" or
"keep" (retain rows with missing response so fitted values can still
be generated for their predictors) (RE2.2).}

\item{nofit}{If TRUE, return an unfitted model specification (RE4.1).}
}
\value{
A \code{morie_lm} object, or an unfitted \code{morie_lm_spec} when
\code{nofit = TRUE}.
}
\description{
A formula-interface linear (\code{gaussian}) or logistic (\code{binomial})
regression returning a model object that exposes coefficients,
variance-covariance, fitted values, residuals, response and predictor
metadata, convergence information, and supports prediction with
confidence / prediction intervals and default \code{plot} / \code{summary}
methods.
}
\examples{
morie_lm(mpg ~ hp + wt, mtcars)
}

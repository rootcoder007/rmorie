% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extenders_spatial.R
\name{morie_mvnorm_sample}
\alias{morie_mvnorm_sample}
\title{Sample from the multivariate normal via \pkg{mvtnorm}}
\usage{
morie_mvnorm_sample(n, mean = rep(0, ncol(sigma)), sigma, ...)
}
\arguments{
\item{n}{Integer; the number of multivariate observations to
draw.}

\item{mean}{Numeric vector of length \code{ncol(sigma)} giving
the mean (defaults to a zero vector).}

\item{sigma}{Numeric positive-(semi)definite covariance matrix.}

\item{...}{Further arguments forwarded to
\code{mvtnorm::rmvnorm} (e.g. \code{method}, \code{pre0.9_9994},
\code{checkSymmetry}).}
}
\value{
A list with \code{$method = "mvtnorm::rmvnorm"} and
\code{$raw} (a numeric matrix of dimension
\eqn{n \times \mathrm{ncol}(\Sigma)}).
}
\description{
Thin extender over \code{mvtnorm::rmvnorm} that draws \eqn{n}
observations from the multivariate normal distribution with a
given mean vector and covariance matrix.
}
\examples{
\dontrun{
  if (requireNamespace("mvtnorm", quietly = TRUE)) {
    set.seed(1)
    S <- matrix(c(1, 0.4, 0.4, 1), 2, 2)
    morie_mvnorm_sample(100, mean = c(0, 0), sigma = S)
  }
}
}
